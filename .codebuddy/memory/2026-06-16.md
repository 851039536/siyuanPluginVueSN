# 2026-06-16

- 将「Vue 实例常驻模式」（Persistent Modal + CustomEvent + setInterval）文档化到 CLAUDE_RULES.md，新增完整章节：三层架构图、四步实现代码、关键点速查表、dataBackup 参考实现。其他 feature 如需后台常驻可参考此模式。
- 密码箱安全加固 — 导出二次确认：`exportAllData()` 改为先弹出 warning 弹窗（橙色警告图标 + 明文 JSON 风险提示 + 条目数量），用户点"确认导出"后才执行下载。修改文件：index.vue（+showExportConfirm 状态、openExportConfirm/confirmExport 函数、Teleport 模态框模板）和 index.scss（+.export-warning 样式）。
- 密码箱安全加固 — 导出密码验证：确认导出后增加密码验证步骤（两阶段弹窗：警告 → 输入主密码 → hashMasterPassword 验证 → 通过后执行导出）。新增 showExportPassword/exportPassword/exportPasswordError 状态、verifyAndExport/cancelExportPassword 函数。index.scss 新增 .export-verify 样式。
