## 2026-06-22：新增 skillLearning（技能学习）功能模块

### 功能概述
代码片段练习库 + 闪卡式记忆，聚焦 C#/JS/TS/Vue 技术栈。Dock 右侧栏面板（宽度 420px），快捷键 `Ctrl+Alt+L`。

### 核心设计
- **三种视图**：列表视图（2列网格+筛选搜索）、闪卡视图（3D CSS翻转动画+暗色代码块）、复习模式（加权随机队列+自评按钮）
- **数据存储**：PluginStorage 封装，存储键 `skill-learning-cards`，带 `incrementPracticeCount` 增量更新
- **复习算法**：复用 useTypingQueue 的 computeWeights → weightedRandomIndex → buildQueue 加权随机逻辑
- **内置预设**：C#/JS/TS/Vue 各 5 张入门卡片（共 20 张），首次启动自动导入
- **Markdown 导入**：支持 `## 标题` + 代码块格式批量导入，` ```language ``` ` 自动映射语言

### 新增文件（18个）
- `src/features/skillLearning/index.ts` — registerSkillLearning(plugin) Dock 注册
- `src/features/skillLearning/index.vue` — 主面板组件（三标签页+工具栏+统计栏）
- `src/features/skillLearning/types/index.ts` — SkillCard / CreateSkillDTO 等类型定义
- `src/features/skillLearning/types/storage.ts` — SkillStorage（CRUD + 预设数据加载）
- `src/features/skillLearning/data/presetData.ts` — 20 张预设卡片
- `src/features/skillLearning/composables/useSkillStorage.ts` — 存储响应式封装
- `src/features/skillLearning/composables/useReviewQueue.ts` — 加权随机复习队列
- `src/features/skillLearning/composables/useI18n.ts` — 国际化 composable
- `src/features/skillLearning/components/` — 7 个子组件（SkillListView、FlashcardView、ReviewMode、SkillDialog、ImportDialog、CategoryFilter、DifficultyBadge）
- `src/i18n/zh_CN/skillLearning.json` / `en_US/skillLearning.json`

### 修改文件（6个）
- `src/features/config.ts` — FEATURE_CONFIG 添加 skillLearning
- `src/features/index.ts` — _Registered + export
- `src/config/settings.ts` — enableSkillLearning + 默认值
- `src/config/icons.ts` — 图标 mdi:code-braces-box #6366f1
- `src/i18n/zh_CN.json` / `en_US.json` — 主翻译条目
- `src/index.ts` — import + 条件注册

### 2026-06-22 下午更新：闪卡视图重构为选择题模式
- 将 FlashcardView 从单张翻转模式改为**全部展示 + 每题 4 选项**的选择题模式
- 每道题自动生成选项：1 个正确答案（卡片自身的 answer）+ 3 个干扰项（从其他卡片中随机选取，优先同语言）
- 选项随机排列（Fisher-Yates），用 A/B/C/D 字母标注
- 选择后立即反馈：正确绿色边框、错误红色边框 + 自动高亮正确答案
- 顶部显示进度（X/Y answered）和正确率
- 支持"重来"按钮重置所有答题状态
- 答题后可展开查看代码片段
- 同步更新 index.vue 去掉 cardIndex ref（不再需要前后翻页）
- 新增 i18n 键：correct/incorrect/retry/viewCode/hideCode

### 2026-06-22 下午更新2：支持手动指定干扰项
- SkillCard / CreateSkillDTO / UpdateSkillDTO 新增 `distractors: string[]` 字段
- SkillDialog 新增 3 个干扰项输入框，编辑时回填已有数据
- ImportDialog Markdown 解析：以 `- ` 开头的行自动识别为干扰项（最多 3 个）
- 导入提示更新为含干扰项的 Markdown 格式示例
- FlashcardView buildQuiz：优先使用 card.distractors（手动指定），回退到自动从其他卡片抽取
- 20 张预设卡片全部补了高质量干扰项
- storage 层批量创建/单条创建/读取全部适配 distractors 字段
- 新增 i18n 键：distractors/distractor1/distractor2/distractor3
