# 2026-06-24

## UI 风格优化与 HTML 测试页
- 更新 `CLAUDE_RULES.md` 的「UI 风格：Codex」章节，吸收参考代码的 sidebar、header、hero-card、section-header、tabs、connector-card、user-profile、badge 变体等布局/组件模式，全部映射到现有 SCSS Token（边框优先、琥珀/主色强调、无阴影）
- 更新 `docs/codex-ui-reference.html`：新增对应可视化区块与导航锚点、明暗主题切换、响应式断点、SVG 图标可访问性（aria-label/aria-hidden）、移除 emoji
- 通过 `web-design-guidelines` skill 审核并修复焦点与可访问性细节

## gitPush Codex 样式修复
- 审查报告识别 78 处违规，三阶段修复全部完成
- 阶段一：删除 4 处 box-shadow（gp-add-popover/gp-ide-popover/gp-dialog/wt-diff-dialog），拆分 9 处 transition:all → 具体属性
- 阶段二：在 10 个 SCSS 文件中批量替换 Token：border-radius → $radius-sm/$vp-radius，font-size → $font-size-xs/$font-size-sm，font-weight → $font-weight-bold/$font-weight-semibold，spacing → $spacing-1~$spacing-4
- 阶段三：buttons.scss→Buttons.scss、shared.scss→Shared.scss（PascalCase），更新 2 处 Vue + 1 处 SCSS @use 引用，消除 3 处 !important
- 验证：build 通过（✓ 1103 modules, ✓ 20.41s），read_lints 无新增错误
- 保留例外：品牌色（gitee/gitea/CNB）、diff 标准色、10px 大写标签

## gitPush 点击项目防抖


- 为 `handleCardClick` 添加时间冷却防抖：`lastRefreshTime` Map 记录每个项目最后刷新时间，500ms 冷却期内跳过重复点击
- 为 `handleRefreshAll` 添加全局刷新防抖：`allRefreshLastTime` 变量，500ms 冷却期
- 原有 `refreshing.value === projectId` 检查在刷新进行中有效，但刷新完成后立即再次点击仍有漏洞，时间戳方案弥补此缺口
- tsc + lint 均无新增错误
