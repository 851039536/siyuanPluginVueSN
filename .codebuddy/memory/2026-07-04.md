# 2026-07-04

## OutputPanel — 图标审查 + 背景移除（09:11-09:13）

### 图标审查
| 修复 | 变更 |
|------|------|
| L30 状态字符 | `✓`/`—`/`✗` Unicode → `<Icon icon="mdi:check/minus/close" height="14" />` |

### 背景移除
| 修复 | 文件 | 变更 |
|------|------|------|
| `.gp-output` 面板背景 | `styles/index.scss` | `@include output-base` → 显式样式（移除 background，保留 border/padding/overflow/pre） |
| `.gp-output-item:hover` | `styles/index.scss` | 移除 `background: var(--b3-theme-surface-lighter)` + 移除 `transition: background` |
| `.gp-output-stdout` | `styles/index.scss` | 移除 `background: var(--b3-theme-surface)` |
| `.gp-output-status` | `styles/index.scss` | `text-align: center` → `display: flex + align-items/justify-content: center`（适配 Icon 组件） |

**保留的背景**：`.gp-output-stderr`（红色错误语义）+ `.gp-output-copy` 按钮背景（交互必需）

### 验证
- ESLint 0 error ✅
- tsc 零新增 ✅

## OutputPanel — 控制台风格重构（09:18-09:21）

### 需求
移除复制按钮，完全重构样式为控制台输出风格，固定高度可滚动，原生鼠标选中复制。

### 变更 4 个文件

| 文件 | 变更 |
|------|------|
| `OutputPanel.vue` | 移除复制按钮 + `details`/`summary`；改为等宽文本行布局（`[✓] label │ durationms │ summary`）；移除 `copyText` prop 和 `copy` emit |
| `styles/index.scss` | 替换所有 `.gp-output-*` 样式块：`$vp-mono` 等宽字体、200px 固定高度、`overflow-y: auto`、`user-select: text`、行内流水布局 |
| `ProjectCard.vue` | 移除 `entriesToText` prop、`copyOutput` emit、`outputPanels` 中的 `copyText` |
| `index.vue` | 移除 `entriesToText` 导入/传递、`@copy-output` 事件、`handleCopyOutput` 函数 |

### 验证
- ESLint 0 error（4 文件均通过）✅
- tsc 零新增（gitPush 模块无任何类型错误）✅

## 移除 git 操作进度条（09:32-09:35）

从 gitPush/index.vue 移除了 gp-progress-bar（模板 + activeGitOps ref + opsPoller 轮询定时器 + onUnmounted clearInterval），并从 styles/index.scss 移除了 .gp-progress-bar 样式和 @keyframes gp-shimmer。GitPushManager.ts 的 activeGitOps getter 保留不变。ESLint + tsc 验证通过。

## 批量进度条重构为 BatchProgressBar 组件（09:43-09:50）

### 需求
将 gitPush/index.vue 中内联的批量加载进度条重构为独立组件，审查冗余，新增可缩放日志输出框。

### 新增文件（4个）
| 文件 | 说明 |
|------|------|
| `types/batchProgress.ts` | LogEntry + LoadProgress 接口 |
| `composables/useBatchProgress.ts` | 进度状态管理（start/advance/end/addLog） |
| `components/BatchProgressBar.vue` | 进度条 + 可缩放日志区 UI |
| `styles/BatchProgressBar.scss` | 独立样式（gp-batch-progress-* + gp-batch-log-*） |

### 修改文件（3个）
| 文件 | 变更 |
|------|------|
| `index.vue` | 移除内联模板/接口/ref/progressTimer/3个函数，接入 useBatchProgress + BatchProgressBar；runBatchWithProgress 增加 per-item try-catch + addLog |
| `styles/index.scss` | 移除 gp-load-progress-* 样式块（~45行） |
| `styles/_mixins.scss` | 新增 btn-reset mixin |

### 新功能
- 进度条右侧展开/折叠按钮（有日志时显示），展开后显示历史执行日志
- 日志区 `resize: vertical` 可拖拽调整高度，新批次自动清空，自动滚动到底部
- per-item 异常隔离：单项目失败不影响后续，标记 fail + 记录错误

### 验证
- ESLint 0 error ✅
- tsc 零新增 ✅
- Codex UI 审查通过
