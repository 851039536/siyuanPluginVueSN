# 2026-07-07

## textDiff SCSS 审查与重构
- 将 `src/features/textDiff/index.vue` 的内联 `<style scoped>`（原 469~835 行）整体提取到 `src/features/textDiff/styles/TextDiff.scss`，vue 改为 `@use './styles/TextDiff.scss';`（保留 scoped，视觉零变化）。
- 合并冗余：新增 `@mixin codex-btn-base`（toggle-btn/action-btn/file-btn 共用静态基础样式）、`@mixin flex-row($gap)`（header-left/right 共用）；`$codex-mono` → 全局 `$vp-mono`。
- Token 替换：硬编码圆角 `6px→$vp-radius`、`4px→$radius-sm`；可精确映射的间距 `16px→$spacing-4`、`8px→$spacing-2`、`4px→$spacing-1`；`12px→$font-size-xs`、`font-weight 500→$font-weight-medium`、`line-height 1.5→$line-height-normal`。
- 性能/精简：`.diff-viewer` 中 `:deep(.vue-diff *)` 已覆盖全部后代，删除其后冗余的 4 个 `.d2h-*` 具体选择器。
- 故意保留（无精确 Token 且为保持视觉不变）：`10px/14px` padding、`6px` padding、`5px` 圆角、`29px` 定位、`24px` 图标按钮尺寸、`11px/13px` 字体。
- 验证：`eslint src/features/textDiff` = 0 errors（仅脚本层既有 warning）；`tsc --noEmit` 无 textDiff 新增错误。codex-ui-style-guide 合规校验通过。
- 注意：`$vp-radius`/`$vp-mono` 已在 `@/variables.scss` 全局定义，按 CLAUDE_RULES 直接继承，无需在模块内重复声明（与 codex-ui-style-guide skill 的 Rule 5 说法冲突，以 CLAUDE_RULES + 实际变量定义为准）。
