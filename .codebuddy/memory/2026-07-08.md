## 2026-07-08

### Git 全局配置查询功能
- 在 gitPush 模块的 PanelHeader 中新增了"本机 Git 配置查询"按钮（`mdi:information-outline` 图标）
- 新增 `GitPushManager.getGitGlobalConfig()` 方法，执行 `git config --global --list` 获取全局配置
- 新增 `GitConfigDialog.vue` 弹窗组件（参照 MarkdownPreviewDialog 模式），支持加载/错误/空/正常四种状态展示
- 新增 `GitConfigDialog.scss` 样式，等宽字体展示 key=value 配置项
- 底部操作栏支持"复制全部"到剪贴板功能
- 中英文 i18n 已同步（gitConfigTitle/gitConfigLoading/gitConfigEmpty/gitConfigCopyAll）
- lint 通过，0 error
- 后续新增：为 GitConfigDialog 新增 80+ 个 Git 配置键的中文说明映射（GIT_CONFIG_DESC），支持通配匹配（alias.*/remote.*.url/filter.* 等），中文说明以半透明括号形式显示在 key 和 value 之间
- 后续新增：GitConfigDialog 底部增加"打开配置文件"按钮（`shell.openPath` 打开 `~/.gitconfig`）
- 后续新增：ProjectCard 操作栏增加项目 Git 配置查看按钮（`mdi:file-document-outline`），复用 GitConfigDialog 弹窗展示项目级 `git config --local --list` 输出，GitPushManager 新增 `getProjectGitConfig()` 和 `getProjectGitConfigFilePath()` 方法
