# 2026-07-09

## statusBar：监控项与功能快捷入口视觉分隔

- 在 `index.vue` 模板中，于最后一个监控项（monitor-uptime）之后、动态任务/快捷入口之前插入 `<span class="status-bar-separator">` 分隔线
- 新增 `showSeparator` computed：仅当 `visibleMonitors.size > 0` 且（`statusBarShortcuts.length > 0` 或 `activeTasks.length > 0`）时显示
- 在 `styles/index.scss` 新增 `.status-bar-separator` 样式：1px 宽、18px 高竖线，颜色 `--b3-theme-border`，`align-self: center`

## statusBar：FeatureDrawer "全部"Tab 隐藏监控项

- 在 `FeatureDrawer.vue` 的 `displayItems` computed 中，"全部"Tab（`activeGroup === "__all__"`）过滤掉 `group === "监控"` 的条目
- 搜索模式不受影响，"监控"独立 Tab 仍正常显示所有监控项
