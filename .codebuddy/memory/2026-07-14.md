# 2026-07-14

## ComparisonView.vue 模块化重构

- **文件**: `src/features/statistics/components/ComparisonView.vue`
- **问题**: 580 行严重超标（500 行硬阈值），`barPct()` 与 ReportView.vue 重复定义，SCSS 内联，缺文件头注释
- **操作**:
  1. `barPct()` 提取到 `utils/index.ts`，两组件统一导入
  2. 新建 `PeriodPicker.vue` 子组件，消除 A/B picker 模板重复
  3. SCSS 提取到 `styles/comparison-view.scss`
  4. 添加文件头注释
- **结果**: ComparisonView.vue 580→268 行（降 54%），进入可接受范围

---

## statistics components/ 文件夹重组

将 20 个平铺组件按类型分组，仅在 ≥3 个同质组件时创建子文件夹：

- **新建 `charts/`**：BarChart / DocBarChart / NotebookBlockTypeChart / NotebookWordPie（4 个纯渲染图表，达到 Rule of Three）
- **其余 16 个保持平铺**：不含其他 ≥3 个同质组件的自然分组
- **修改文件**：
  - 移动 4 个 .vue 到 `components/charts/`，修复相对路径 `../` → `../../`（4 文件各 1~3 处）
  - `index.vue` 更新 4 条 import 路径为 `./components/charts/xxx`
- **规则更新**：AGENTS_RULES.md 新增「四、组件文件夹组织标准」完整章节；CODEBUDDY.md + AGENTS.md 硬规则区新增「组件文件夹组织」条目。核心规则：≥15 文件时可建子文件夹，仅 ≥3 同质组件时创建，禁止按 Tab/页面分组，每个 feature 最多 3 个子文件夹

---

## DocChangeSection.vue SCSS 合规修复

- **文件**: `src/features/statistics/components/DocChangeSection.vue`
- **问题**: 8 类违规 — ①缺文件头注释 ②370 行内联 SCSS 未分离 ③12 处 font-size 硬编码 ④11 处 font-weight 硬编码 ⑤4 处 letter-spacing 0.04em ⑥box-shadow 未用 mixin ⑦未复用 codex-label mixin ⑧硬编码间距值
- **操作**:
  1. 新建 `styles/DocChangeSection.scss`，提取全部 SCSS 并完成 Token 替换
  2. Vue 文件添加文件头注释 + `<style>` 块替换为两行 `@use` 导入
  3. 复用 `stats.codex-label` mixin（3 处）+ `stats.codex-focus-glow` mixin（1 处）
  4. font-size: 10px→`$font-size-2xs`、12px→`$font-size-xs`；font-weight: 500/600/700→`$font-weight-*`；letter-spacing: 0.04em→0.06em；间距→`$spacing-1~3`；transition 0.15s→0.12s
- **结果**: Vue 文件从 909 行缩减到 544 行（降 40%），lint 零错误

---

## HeatmapCard.vue SCSS 合规修复

- **文件**: `src/features/statistics/components/HeatmapCard.vue`
- **问题**: 9 类违规 — ①缺文件头注释 ②~419 行内联 SCSS 未分离 ③~15 处 font-size 硬编码（10/11/12/16px）④~14 处 font-weight 硬编码（600/700）⑤2 处 letter-spacing 0.04em ⑥line-height 硬编码 14px ⑦~15 处间距硬编码 ⑧3 处 transition 不一致（0.1s/0.15s）⑨codex-label mixin 未复用（2 处）
- **操作**:
  1. 新建 `styles/HeatmapCard.scss`，提取全部 SCSS 并完成 Token 替换
  2. Vue 文件添加文件头注释 + `<style>` 块替换为单行 `@use` 导入
  3. 复用 `stats.codex-label` mixin（`.detail-group-title` + `.section-title`）
  4. font-size: 10px→`$font-size-2xs`、12px→`$font-size-xs`、16px→`$font-size-base`、11px→`$font-size-sm`；font-weight: 600→`$font-weight-semibold`、700→`$font-weight-bold`；letter-spacing: 0.04em→0.06em；间距 3px→`$spacing-px`、4px→`$spacing-1`、8px→`$spacing-2`、12px→`$spacing-3`、16px→`$spacing-4`、24px→`$spacing-6`；transition 0.15s/0.1s→0.12s（柱状图 0.3s 保留）
- **保留的精细值**: 20px（汇总数字）、14px（日历 padding/行高对齐）、10px（间距）、6px（gap），均无精确 Token
- **结果**: Vue 文件从 922 行缩减到 507 行（降 45%），lint 零错误

---

## AGENTS_RULES.md 重复描述修复

- **问题**: 发现 2 处实质性重复 — ①Persistent Modal `open()→close()` 预挂载代码在「Modal 弹窗」和「Vue 实例常驻模式」两处重复 ②Composable 提取判定标准表格在「强制规则：Composable 提取」和「模块提取判定标准 → 三」两处重复
- **修复**: ①删除 API 参考中的预挂载代码+注意事项段，改为指向下方章节的引用 ②将重复表格改为精简引用
- **结果**: 消除了文件内部冗余，CODEBUDDY.md 无需同步修改

---

## MilestoneChip.vue 合并回 MilestonesCard.vue

- **文件**: 删除 `src/features/statistics/components/MilestoneChip.vue`，合并到 `MilestonesCard.vue`
- **判定**: 违反模块提取三条件的全部三条 — 仅 1 处使用（未达 Rule of Three）、文件 125 行（未超 500）、纯展示组件（0 状态 + 0 emit + 0 方法）。7 个 props 全透传，无独立决策能力，属"父传全量 props + 子纯渲染傀儡"禁止模式
- **操作**:
  1. 模板内联到两处 `v-for`（展开视图 + 折叠预览），直接引用 `m.xxx` 替代 props
  2. SCSS（75 行）追加到父组件 `<style>` 块末尾
  3. 移除 `import MilestoneChip` 语句（父组件已有 `IconWrapper` 导入无需新增）
  4. 删除 `MilestoneChip.vue` 文件
- **结果**: 父组件增约 70 行（模板 +60 / SCSS +75 / 移除 import -1），lint 零错误

---

## WordRanking.vue SCSS 合规修复

- **文件**: `src/features/statistics/components/WordRanking.vue`
- **问题**: 12 类违规 — ①缺文件头注释 ②158 行内联 SCSS 未分离 ③冗余 `@use superPanel/styles/shared` ④4 处 font-size 硬编码（10px）⑤4 处 font-weight 硬编码（700）⑥box-shadow focus 未用 mixin ⑦2 处 gap 8px 硬编码 ⑧padding 硬编码 ⑨transition 0.15s ⑩保留拆分判定（275 行，有内部交互状态）
- **操作**:
  1. 新建 `styles/WordRanking.scss`，提取全部 SCSS 并完成 Token 替换
  2. Vue 文件添加文件头注释 + `<style>` 块替换为单行 `@use` 导入
  3. font-size: 10px→`$font-size-2xs`（4 处）；font-weight: 700→`$font-weight-bold`（4 处）；box-shadow→`codex-focus-glow` mixin；gap: 8px→`$spacing-2`（2 处）；padding: 8px 12px→`$spacing-2 $spacing-3` / 20px→`$spacing-5`；transition: 0.15s→0.12s
  4. 移除冗余 `@use "@/variables" as *;`（index.scss 已 forward）；保留 `@use superPanel/styles/shared`（scrollbar-thin/mobile-only 需直接导入）
- **保留的精细值**: 11px（2 处，介于 $font-size-2xs 与 $font-size-xs 之间）、6px（1 处）、2px（1 处）
- **结果**: Vue 文件从 275 行缩减到 120 行（降 56%），lint 零错误

---

## aiContentGenerator 模块全面重构

- **文件**: `src/features/aiContentGenerator/`
- **问题**: index.vue 1536 行严重超标（500 行硬阈值），useReview composable 已实现但未被使用，4 处 system prompt 重复构建，utils.ts 跨功能违规导入 skillsViewer，emoji 违规，generationStartTime 用 0 做哨兵值
- **操作**:
  1. **utils.ts** — 迁移 6 个工具函数（removeFrontmatter/processContentByType/convertToSiyuanMarkdown/splitMarkdownBlocks/getCurrentBlockId/getDocIdByBlockId），修复跨功能导入（`AI_TOOLS` → 本地 `TOOL_META` 常量，含 5 个工具 id/name/color）
  2. **新建 useGeneration.ts** (~350行) — 抽取全部生成管道：状态管理、RAF 流式缓冲、对话历史、buildGenerateOptions（稳定搜索回调引用）、executeGeneration 封装、handleStop、cleanupRaf；导出 `buildSkillSystemPrompt()` 公共函数消除 4 处重复
  3. **增强 useReview.ts** (~190行) — 集成 performReview/handleAutoFix/handleReReview/handleFixIssue，支持 deps 注入模式（有 deps 时包含完整审核逻辑，无 deps 时仅状态管理），使用 buildSkillSystemPrompt 替代内联拼接
  4. **新建 useEditOperations.ts** (~200行) — 抽取 applyEdit/undoEdit/insertSubDocument/copyContent/clearContent/clearEditState/clearTargetDocument
  5. **新建 useDocumentTarget.ts** (~120行) — 抽取 loadDocument/selectTargetDocument/selectTargetBlock/loadTargetDocument/setTargetDocState
  6. **新建 usePromptManager.ts** (~170行) — 抽取 saveCurrentPrompt/loadPrompt/deletePrompt/clearCurrentPrompt/onPromptNameFocus/savePromptsToStorage/loadPromptsFromStorage + showSettings
  7. **index.vue** — 从 1536 行精简至 ~445 行，按 7 步组装 composable（技能→提示词→生成→编辑→文档目标→审核→视图层），薄壳包装 aiEditAction/handleCustomEdit，设置持久化 + 生命周期
  8. **AIContentGenerator.ts** — 修复 emoji 违规（🚫 移除）
  9. **generationStartTime** — 哨兵值从 `0` 改为 `null`（语义明确）
- **结果**: index.vue 1536→445 行（降 71%），6 个 composable 各司其职，跨功能违规修复，审核系统成功集成

---

## BottomInputArea.vue 合规审查与重构

- **文件**: `src/features/aiContentGenerator/components/BottomInputArea.vue`
- **问题**: 1001 行严重超标（500 行硬阈值），329 行内联 SCSS 未分离，Props 接口在 defineProps 之后定义，缺文件头注释，SkillPreviewModal 内联 159 行，技能选择器内联 235 行，大量硬编码 CSS 值
- **操作**:
  1. **提取 SkillPreviewModal.vue** — Teleport 弹窗组件（~65 行），接收 currentSkill prop，emit close；配套 styles/SkillPreviewModal.scss（~112 行，Token 化：font-size→$font-size-base/$font-size-xs/$font-size-sm，font-weight→$font-weight-medium/$font-weight-semibold，border-radius→$radius-sm/$radius-lg，padding→$spacing-1~$spacing-3）
  2. **提取 SkillSection.vue** — 技能选择器组件（~150 行），含下拉面板 + clickOutside + 搜索；配套 styles/SkillSection.scss（~165 行，含从 styles/index.scss 迁移的 .skill-dropdown 共享样式，Token 化：font-size→$font-size-xs/$font-size-2xs，gap→$spacing-1，padding→$spacing-1~$spacing-3）
  3. **创建 styles/BottomInputArea.scss** — 126 行，含 .rag-toggle + .options-bar 样式，Token 化：font-size→$font-size-2xs，font-weight→$font-weight-medium，border-radius→$radius-sm，padding→$spacing-1
  4. **重写 BottomInputArea.vue** → 340 行（模板 213 + 脚本 121 + 样式 6），Props 接口前置、文件头注释、组装 2 个子组件
  5. **清理 styles/index.scss** — 移除已迁移的 .skill-dropdown ~ 85 行
- **结果**: BottomInputArea.vue 1001→340 行（降 66%），SCSS 完全分离，新增 4 个文件，styles/index.scss 减负 85 行

