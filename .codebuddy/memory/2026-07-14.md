# 2026-07-14

## ComparisonView.vue 模块化重构

- **文件**: `src/features/statistics/components/ComparisonView.vue`
- **问题**: 580 行严重超标（500 行硬阈值），`barPct()` 与 ReportView.vue 重复定义，SCSS 内联，缺文件头注释
- **操作**:
  1. `barPct()` 提取到 `utils/index.ts`，两组件统一导入
  2. 新建 `PeriodPicker.vue` 子组件，消除 A/B picker 模板重复
  3. SCSS 提取到 `styles/comparison-view.scss`
  4. 添加文件头注释
- **结果**: ComparisonView.vue 580→268 行（降 54%），进入可接受范围

---

## statistics components/ 文件夹重组

将 20 个平铺组件按类型分组，仅在 ≥3 个同质组件时创建子文件夹：

- **新建 `charts/`**：BarChart / DocBarChart / NotebookBlockTypeChart / NotebookWordPie（4 个纯渲染图表，达到 Rule of Three）
- **其余 16 个保持平铺**：不含其他 ≥3 个同质组件的自然分组
- **修改文件**：
  - 移动 4 个 .vue 到 `components/charts/`，修复相对路径 `../` → `../../`（4 文件各 1~3 处）
  - `index.vue` 更新 4 条 import 路径为 `./components/charts/xxx`
- **规则更新**：AGENTS_RULES.md 新增「四、组件文件夹组织标准」完整章节；CODEBUDDY.md + AGENTS.md 硬规则区新增「组件文件夹组织」条目。核心规则：≥15 文件时可建子文件夹，仅 ≥3 同质组件时创建，禁止按 Tab/页面分组，每个 feature 最多 3 个子文件夹
