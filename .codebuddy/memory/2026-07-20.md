# 2026-07-20

## generalSettings 模块冗余清理

完成了 `src/features/generalSettings` 模块的全面冗余代码清理，涉及 7 个步骤：

1. **死代码清理**：删除 `handleSettingsChange` 中的 `"font"` 和 `"list"` 废弃分支（无 UI tab 触发）
2. **函数去重**：将 `applyDocumentFontStyles` 和 `generateLevelDisplayCss` 提取到 `utils/styles.ts`，消除 GeneralSettings.ts / DocumentFontSettings.vue / HeadingSettings.vue 三处重复定义
3. **SCSS 分离**：将 HeadingSettings.vue（~600行）、EncryptionSettings.vue（~127行）、HighlightSettings.vue（~133行）的内联样式提取到独立 SCSS 文件
4. **接口去重**：删除 DocumentFontSettings.vue 中重复的 `DocumentFontSettingsData` 接口和 `DEFAULT_SETTINGS` 常量，改用 storage.ts 导入
5. **index.scss 精简**：从 1548 行精简到 ~100 行，只保留面板布局样式，移除所有重复组件样式和其他模块样式
6. **入口统一**：DocCountManager 改用 `@/api` 的 `sql` 函数，删除 `fetchSyncPost` 私有方法（~30行）

**关键变更文件**：
- `GeneralSettings.ts`：删除私有方法 + 更新调用
- `utils/styles.ts`：新增 `applyDocumentFontStyles` / `generateLevelDisplayCss` / `DocumentFontSettingsData`
- `components/HeadingSettings.vue`：内联 SCSS → 独立文件，删除重复函数
- `components/DocumentFontSettings.vue`：删除重复接口/常量/函数
- `components/EncryptionSettings.vue`：内联 SCSS → 独立文件
- `components/HighlightSettings.vue`：内联 SCSS → 独立文件
- `modules/DocCountManager.ts`：改用 @/api sql
- `styles/index.scss`：1548 行 → ~100 行
- 新增：`styles/HeadingSettings.scss`、`styles/EncryptionSettings.scss`、`styles/HighlightSettings.scss`

lint 检查全部通过。
