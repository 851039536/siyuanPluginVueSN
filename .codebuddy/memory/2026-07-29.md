# 2026-07-29

## CommitMsgGenerator.ts 冗余与逻辑修复
- 合并 3 条 import 语句（从 `../types/storage` 合并为 1 条值 import + 1 条 type import）
- `fullDiff` 子进程从 `apiKey` 检查之前移到之后，无 API Key 时不再浪费一次 `git diff --cached`
- AI 返回的正则表达式从硬编码改为 `COMMIT_TYPE_VALUES.join("|")` 动态构建（单一数据源）
- AI 返回的 type 加 `.toLowerCase()` 归一化，修复 `Fix:` → 应为 `fix:` 的问题
- stash 描述降级文案提取 `desc` 变量，无 API Key / AI 为空 / AI 异常三条路径统一返回
- `heuristicCommitMessage` 增加 `files.length === 0` 空数组守卫，防止产出 `"chore: "` 尾随空描述

## GitExecutor.ts 并发缺陷修复（6 项）
1. 队列项类型新增 `reject` 字段；onAbort 过滤时对被移除项 reject；destroy 清空队列前对所有排队项 reject（修僵尸 Promise）
2. 提取 `scheduleNext(isNetwork)` 方法循环 shift 队列跳过已中止项；`run()` 三个早退路径（signal.aborted、!cp、signal aborted after onAbort）均调用 `scheduleNext`；完成回调由 `next.run()` 改为 `scheduleNext`（修池饥饿/死锁）
3. `execFile` options 增加 `maxBuffer: 10 * 1024 * 1024` 常量 + 中文注释
4. 完成回调中 `signal.removeEventListener("abort", onAbort)` 防止监听器泄漏
5. `getCommandName(args)` 跳过前导 `-c`/`-C` 参数对，再查 `NETWORK_COMMANDS`
6. `import type { ChildProcess }`（type-only）；`Set<any>` → `Set<ChildProcess>`；`error: any` → `Error & { code?: number } | null`；`d: any` → `Buffer | string`
- 外部接口不变，lint 零错误，所有调用方无需改动

## ProjectStore.ts 缓存与 ID 修复（3 项）

### #1 缓存引用泄漏 + save 失败脏缓存
- 新增 `getProjectsForWrite()` 私有方法（`getProjects()` 返回的浅克隆副本）
- 10 个写方法全部改用 `getProjectsForWrite()`：addProject/removeProject/updateProjectMeta/toggleStar/appendTag/removeTag/recordLastActivity/refreshRemotes/deleteCategory/moveProject
- `syncGlobalTags` 保持用 `getProjects()`（只读遍历）
- save 失败时因未触碰 `projectsCache`，缓存自动保持一致

### #2 moveProject 目标分类校验
- 新增目标分类存在性检查（`UNGROUPED_ID` 恒有效）
- 顺带幂等短路：`p.categoryId === categoryId` 不写
- 使用 `getProjectsForWrite()` 替代 `getProjects()`

### #4 分类 ID 同毫秒碰撞
- `addCategory` 的 ID 从裸 `Date.now().toString()` 改为 `Date.now().toString(36)-${idCounter}`（复用项目 ID 防碰撞方案）

## RemoteOps.ts 缓存与类型修复（4 项）

### #1 push 成功后失效智能跳过缓存
- `remoteOpAll`：在 `withAbortController` 回调 return 前 `action === "push"` 时调用 `invalidatePushStatusCache(id)`
- `remoteOpSingle`：`tryRemoteOp` 成功后 `action === "push"` 时调用 `invalidatePushStatusCache(id)`
- 仅 push 失效，pull 不涉及推送状态

### #2 rev-list --left-right 解析加注释
- `checkPushStatus` 中 `${remoteName}/${status.branch}...HEAD` 上方补注释说明 left=behind(right=remote)、right=ahead(left=HEAD) 的顺序依赖，防止未来误改颠倒

### #4 消除 as any
- 新增 `type SettledEntry = { key: PlatformKey } & RemoteOpResult`
- `Promise.allSettled` map 回调标注返回类型 `Promise<SettledEntry>`
- 解构 `const { key, ...rest } = r.value` 替代 `const val = r.value as any`

### #5 fetch 失败告警
- `checkPushStatus` 的 `fetchFirst` 分支接收 `fetchAllForProject` 返回的 `errors`，非空时 `console.warn`
- 不改变控制流，仍继续用跟踪分支状态

## WorktreeOps 与 RepoOps 解析修复（3 项）

### #1 WorktreeOps 重命名文件 porcelain 解析修复 + #3 局限注释
- **核心**：原先先对整行去引号再按 ` -> ` 拆分，重命名含特殊字符时 `"old" -> "new"` 被整体 unquote 导致引号错位
- 修复：新增 `unquote()` 辅助函数，先拆分 ` -> ` 再分别对 old/new 路径去引号；非重命名路径统一走 `unquote(rawPath)`
- 对 `"` 开头结尾的字符串才 `slice(1,-1)`，处理 `"` 内嵌 `"` 场景（如 """"）
- 补注释：基于 `core.quotepath=false` + 文本解析，路径含换行等极端字符仍有局限，未用 `-z` 是权衡

### #5 WorktreeOps commit log 格式依赖注释
- `getCommitLog` 的 `%h%n%s%n%an%n%ar%n%aI` 上方补注释："依赖 %s(subject) 单行，勿加入 %b(body) 等多行字段，否则 5 行固定切分错位"

### #7 RepoOps scanForGitRepos 同步递归扫描加边界
- 队列项从裸 `string` 改为 `{ dir, depth }`，新增 `MAX_DEPTH=8`、`MAX_RESULTS=500` 常量
- 入队子目录时检查 `depth < MAX_DEPTH`，找到结果后检查 `results.length >= MAX_RESULTS` break
- 顶部注释说明边界值用途（防止扫描大目录树同步阻塞渲染进程）

## gitPush 操作日志视图（log view）实现
按照 `.qoder/plans/gitPush_操作日志视图_f03a8599.md` 完整实现，新增/修改 12 个文件：

### 新增文件（4 个）
- `src/features/gitPush/composables/useOpLog.ts` — 日志 composable（惰性加载 + 防抖 1s 落盘 + 环形 300 上限 + flush）
- `src/features/gitPush/components/LogPanel.vue` — 操作日志面板（空状态、筛选、分页 50 条、平台明细展开、commit message 展示）
- `src/features/gitPush/styles/LogPanel.scss` — Codex 风格样式（两级字号制、等宽字体、边框卡片）

### 修改文件（8 个）
- `types/storage.ts` — 新增 GitOpAction/GitOpLogPlatform/GitOpLogEntry 类型 + MAX_OP_LOG_COUNT 常量 + GitPushStorage.opLogs 槽位
- `types/meta.ts` — 新增 PanelView = "list" | "stats" | "log" 类型
- `types/index.ts` — 重导出新类型
- `composables/useRemoteProgress.ts` — opts 增加 appendOpLog 回调，remoteOpAll/remoteOpSingle 双漏斗埋点（成功+失败分支）
- `composables/useGitOps.ts` — 实例化 useOpLog，doCommit 增加 try/catch 记录 commit 日志，透出 opLogs/ensureOpLogsLoaded/clearOpLogs
- `composables/useGitPush.ts` — return 块透出 opLogs/ensureOpLogsLoaded/clearOpLogs/flushOpLogs
- `components/PanelHeader.vue` — defineModel 改为 PanelView，新增第三个 "log" 按钮（mdi:history 图标）
- `index.vue` — currentView 改为 PanelView，挂载 LogPanel，新增 confirmClearOpLogs 二次确认，watch 惰性加载日志
- `i18n/zh_CN/gitPush.json` + `i18n/en_US/gitPush.json` — 各新增 16 个键

### 架构要点
- 埋点位于 composable 漏斗层（useRemoteProgress + useGitOps），共 5 个插入点（remoteOpAll 成功/失败 + remoteOpSingle 成功/失败 + doCommit）
- 日志写入 fire-and-forget + try/catch 仅 console.warn，不影响 git 主流程
- appendOpLog 内部先 ensureLoaded 防覆盖历史
- 清空需二次确认（复用通用 ConfirmDialog）
