# 2026-07-29

## CommitMsgGenerator.ts 冗余与逻辑修复
- 合并 3 条 import 语句（从 `../types/storage` 合并为 1 条值 import + 1 条 type import）
- `fullDiff` 子进程从 `apiKey` 检查之前移到之后，无 API Key 时不再浪费一次 `git diff --cached`
- AI 返回的正则表达式从硬编码改为 `COMMIT_TYPE_VALUES.join("|")` 动态构建（单一数据源）
- AI 返回的 type 加 `.toLowerCase()` 归一化，修复 `Fix:` → 应为 `fix:` 的问题
- stash 描述降级文案提取 `desc` 变量，无 API Key / AI 为空 / AI 异常三条路径统一返回
- `heuristicCommitMessage` 增加 `files.length === 0` 空数组守卫，防止产出 `"chore: "` 尾随空描述

## GitExecutor.ts 并发缺陷修复（6 项）
1. 队列项类型新增 `reject` 字段；onAbort 过滤时对被移除项 reject；destroy 清空队列前对所有排队项 reject（修僵尸 Promise）
2. 提取 `scheduleNext(isNetwork)` 方法循环 shift 队列跳过已中止项；`run()` 三个早退路径（signal.aborted、!cp、signal aborted after onAbort）均调用 `scheduleNext`；完成回调由 `next.run()` 改为 `scheduleNext`（修池饥饿/死锁）
3. `execFile` options 增加 `maxBuffer: 10 * 1024 * 1024` 常量 + 中文注释
4. 完成回调中 `signal.removeEventListener("abort", onAbort)` 防止监听器泄漏
5. `getCommandName(args)` 跳过前导 `-c`/`-C` 参数对，再查 `NETWORK_COMMANDS`
6. `import type { ChildProcess }`（type-only）；`Set<any>` → `Set<ChildProcess>`；`error: any` → `Error & { code?: number } | null`；`d: any` → `Buffer | string`
- 外部接口不变，lint 零错误，所有调用方无需改动

## ProjectStore.ts 缓存与 ID 修复（3 项）

### #1 缓存引用泄漏 + save 失败脏缓存
- 新增 `getProjectsForWrite()` 私有方法（`getProjects()` 返回的浅克隆副本）
- 10 个写方法全部改用 `getProjectsForWrite()`：addProject/removeProject/updateProjectMeta/toggleStar/appendTag/removeTag/recordLastActivity/refreshRemotes/deleteCategory/moveProject
- `syncGlobalTags` 保持用 `getProjects()`（只读遍历）
- save 失败时因未触碰 `projectsCache`，缓存自动保持一致

### #2 moveProject 目标分类校验
- 新增目标分类存在性检查（`UNGROUPED_ID` 恒有效）
- 顺带幂等短路：`p.categoryId === categoryId` 不写
- 使用 `getProjectsForWrite()` 替代 `getProjects()`

### #4 分类 ID 同毫秒碰撞
- `addCategory` 的 ID 从裸 `Date.now().toString()` 改为 `Date.now().toString(36)-${idCounter}`（复用项目 ID 防碰撞方案）
