2026-07-29：执行了 AutoBackupCard 冗余与逻辑修复计划（.qoder/plans/AutoBackupCard_冗余与逻辑修复_9db3245e.md）。修改 2 个文件：

**AutoBackupCard.vue**：
- 删除 13 处 i18n 硬编码兜底（`|| "中文"`），模板 8 处 + 脚本 5 处
- defineModel 重构：4 个双向 prop 改用 defineModel，删除 defineEmits 块
- 新增 sanitizeKeepCount() 函数：Number() → Math.floor → 非有限/<1 钳制为 1
- backupTime Input 增加 placeholder="03:00"
- 添加中文 HTML 注释标注每处 i18n 渲染点
- 修正保留份数 Input 块多余缩进

**s3Backup/index.vue L155-165**：
- AutoBackupCard 绑定从 `:prop`+`@update:prop="赋值;保存"` 改为 `v-model:prop`+独立 `@update:prop="save()"`

验证：ESLint 0 error，tsc --noEmit 无类型错误。

---

随后执行 BackupListCard 类型与冗余修复计划（.qoder/plans/BackupListCard_类型与冗余修复_9db3245e.md）。修改 3 个文件：

**types/index.ts**：
- 新增 BackupListDisplayItem 接口（name/size + 可选 time/lastModified），供 BackupListCard 泛型化

**BackupListCard.vue**：
- 泛型化：`<script setup lang="ts" generic="T extends BackupListDisplayItem">`，items: T[]，父组件插槽 item 获得精确类型
- 删除 timeKey prop 与 withDefaults（唯一默认值），时间列改为 `formatTime(item.time ?? item.lastModified ?? "")`
- 删除 `|| "刷新"` i18n 兜底
- `:key="index"` 改为 `:key="item.name"`（v-for index 变量同步移除）
- `hostMap && hostMap[item.name]` 简化为 `hostMap?.[item.name]`
- 添加中文 HTML 注释（刷新按钮、条目列表、空状态）

**index.vue**：
- S3 列表移除 `time-key="lastModified"` 绑定
- 删除 7 处 i18n 兜底

验证：ESLint 0 error（3文件），tsc --noEmit 无类型错误。
