# 2026-08-01 工作记录

## CommitAnalysisPanel 代码冗余重构

审查并消除了 `src/features/gitPush/components/analysis/CommitAnalysisPanel.vue` 中的 4 类代码冗余：

### 新增 4 个公共件
1. **`components/common/EmptyState.vue`** — 通用空状态占位组件（icon + text + spin + iconSize props），复用 `index.scss` 的 `.gp-empty` 基座样式
2. **`composables/usePagedList.ts`** — 本地分页 composable（visibleCount / paged / hasMore / loadMore / reset），泛型支持任意数组类型
3. **`components/common/LoadMoreButton.vue`** — 通用加载更多按钮薄壳（i18n / visible / total props + loadMore emit）
4. **`utils.ts` 新增 `withBarPct` 泛型函数** — 为排行条目预计算条形宽度百分比，支持 `zeroAsEmpty` 参数处理零值场景；`barPct` 从 CommitAnalysisPanel 迁移到 utils.ts

### 修改 9 个文件
- **CommitAnalysisPanel.vue**（-36 行）：3 处空状态→EmptyState；分页→usePagedList+LoadMoreButton；4 个同构 computed→withBarPct 一行；移除本地 barPct 函数
- **LogPanel.vue**（-15 行）：2 处空状态→EmptyState；分页→usePagedList+LoadMoreButton；移除本地 visibleCount/slice 逻辑
- **StatsPanel.vue**：空状态→EmptyState
- **index.vue**：空状态→EmptyState；移除不再使用的 Icon import
- **ScanImportDialog.vue**：空状态→EmptyState（icon-size="36"）
- **CommitAnalysisPanel.scss**：移除 `.gpa-load-more`（改用统一 `.gp-load-more`）
- **LogPanel.scss**：移除 `.gp-log-load-more`（改用统一 `.gp-load-more`）
- **index.scss**：新增统一 `.gp-load-more` 样式；移除不再引用的 `.gp-scan-empty`

### 验证结果
- ESLint/tsc：0 errors, 0 warnings
- 净减少约 60 行重复代码

## toolCollection 新增 deepSeekCost 工具

新增 DeepSeek 成本工具（集成于工具合集底部面板，仅需 registry.ts 注册，无需 8 步清单）：

### 新增文件（src/features/toolCollection/tools/deepSeekCost/）
1. **index.vue** — Tab 外壳（成本换算 / 余额查询）
2. **utils/pricing.ts** — DeepSeek 官方价格常量（v4-flash: 命中¥0.02/未命中¥1/输出¥2；v4-pro: 命中¥0.025/未命中¥3/输出¥6，单位元/百万tokens）+ calcCost 纯计算函数 + 高峰时段判断（北京时间 9-12/14-18 时 ×2）+ formatCost/formatTokens/formatPercent
3. **components/CostCalculator.vue** — 模型选择 + 命中/未命中/输出 token 输入 + 峰谷开关 + 示例填充（7830144/222757/142987）+ 实时费用/命中率/节省展示
4. **components/BalanceQuery.vue** — 读取 plugin.settings.aiApiKeys.deepseek 密钥 → GET https://api.deepseek.com/user/balance（10s 超时 AbortController）→ 展示 is_available + balance_infos（total/granted/topped_up_balance）
5. **styles/index.scss + CostCalculator.scss + BalanceQuery.scss** — Codex 风格，共享类提为顶层（.dc-label/.dc-input/.dc-hint/.dc-check）供子组件 @use 复用，transition 0.12s
6. **README.md**

### 修改文件
- tools/registry.ts：import + TOOL_REGISTRY 添加 deepSeekCost（mdi:chart-line）+ TOOL_LABEL_KEYS
- toolCollection/README.md：工具表新增一行
- i18n 分片：新建 zh_CN/en_US/deepSeekCost.json（各 27 键）

### 验证
- ESLint 0 error、tsc 无新增错误（既有错误为 video/vueAppHelper/formatAssistant 等历史问题）、pnpm i18n:verify 通过（3262 键对齐）

### 关键设计
- 密钥直接从 plugin.settings.aiApiKeys.deepseek 读取（与当前 AI provider 无关，不走 getApiConfigFromPlugin）
- 成本换算为纯本地计算，余额查询走独立 fetch GET（不走 callAI 的 POST chat/completions）

## deepSeekCost 增加官网链接栏

- index.vue 底部新增 `.dc-footer` 官网链接栏（OFFICIAL_LINKS 常量数组 + v-for 渲染）
- 3 个链接：价格参考（api-docs.deepseek.com/zh-cn/quick_start/pricing）/ API 文档（api-docs.deepseek.com/zh-cn/api/）/ 控制台（platform.deepseek.com），target="_blank" rel="noreferrer"
- index.scss 新增 .dc-footer/.dc-footer-label/.dc-link 样式（border-top 分隔、主色链接、hover underline、transition 0.12s）
- i18n 新增 4 键（officialLinks/linkPricing/linkDocs/linkConsole），中英分片同步
- 验证：ESLint 0 error、pnpm i18n:verify 通过（3289 键对齐）

## DeepSeek 官网 API 盘点（2026-08 审查）

| API | 端点 | 状态 |
|-----|------|------|
| Chat Completions | POST /chat/completions | 已有 callAI，无需 |
| 查询余额 | GET /user/balance | 已接入 |
| **获取模型列表** | **GET /models** | **未接入，最值得加**（鉴权同余额，响应 { object:"list", data:[{id,object,owned_by}] }） |
| FIM 补全（Beta） | POST /fim/completions | 未接入，价值一般 |
| Responses API / Anthropic 兼容 | 兼容端点 | 未接入，仅调用方式差异 |
| 思考模式/JSON Output/Tool Calls/前缀续写 | Chat 参数 | 参数级能力，走 callAI |

- 模型：deepseek-v4-flash（已更新为 DeepSeek-V4-Flash-0731）/ deepseek-v4-pro
- base_url：https://api.deepseek.com（OpenAI 兼容）、https://api.deepseek.com/anthropic（Anthropic 兼容）

## deepSeekCost 余额 Tab 接入 GET /models + 每百万 token 中美元价格

- **utils/pricing.ts** 新增：`CNY_PER_USD = 7.15`（人民币兑美元参考汇率常量）、`toUSD(cny)` 美元换算、`findPriceForModel(modelId)` 模型 ID 匹配价格表（精确匹配 → 包含匹配，返回 ModelPrice | null）、`formatUSD(value)`（保留 4 位去尾零）
- **BalanceQuery.vue** 重构 `queryBalance()`：`Promise.all` 并行请求 `/user/balance` + `/models`（同一 AbortController 10s 超时，任一失败单独提示互不影响）；新增 `ModelsResponse/ModelItem/ModelRow` 类型与 `modelRows` computed（自动匹配价格表并换算美元）
- 模板新增 `.dc-models` 模型列表区块：每张卡片显示模型 ID + 每百万 tokens 的命中/未命中/输出单价（`¥ x / $ x`），未收录模型显示"价格表未收录该模型"提示，区块头部显示汇率提示（1 USD ≈ ¥7.15）
- i18n 新增 9 键（models/rateHint/perMillion/cacheHitPrice/cacheMissPrice/outputPrice/priceUnknown/modelsFailed），中英分片同步
- 验证：ESLint 0 error、pnpm i18n:verify 通过（3331 键对齐）

## base64Image 模块漏洞修复与冗余清理（toolCollection）

审查并修复 `src/features/toolCollection/tools/base64Image/` 的 21 项问题：

### index.vue 重写（833→917 行，修复 3 大类）
1. **内存泄露**：
   - 新增 `releasePreviewUrl()` 统一释放 `imagePreviewUrl` 的 ObjectURL（handleFile 换图前 / clearAll / onUnmounted 均调用）
   - `processImage` 改为复用 `imagePreviewUrl` 作为 img.src（不再重复创建 ObjectURL），并增加 `img.onerror` 处理
   - `decodeTimer` / `qrcodeTimer` 在 onUnmounted 与 switchMode 中清理
   - `fetchUrlToBase64` 增加 FileReader onerror + response.ok 检查
2. **逻辑漏洞**：
   - `processImage` 增加 `processRequestId` 请求序列号丢弃过期异步结果（滑块快速拖动竞态）
   - `decodeBase64` 重构：移除无效 try-catch，isDecoding 在 img.onload/onerror 后置 false（loading 真实有效），失败清空预览，`decodeRequestId` 防旧覆盖新
   - `watch(qrcodeInput)` 加 300ms 防抖（与 base64Input 的 500ms 一致）
   - `copyFormatted` css 模式且 withCss=false 时提示"当前模式不支持CSS格式"而非复制空内容
   - 压缩率单位修复：新增 `calcDataUrlBytes`（去 MIME 前缀按 base64 解码估算实际字节），`base64OutputBytes`/`urlBase64OutputBytes` 传入 StatsSection
   - URL 模式 fetch 失败提示 CORS 可能原因
3. **规范**：i18n 用 `computed(() => props.i18n)` 恢复响应式；移除全部 40+ 处 `i18n.xxx || '中文'` 硬编码兜底；`buildFilterString` 公共函数消除滤镜字符串重复；`copyOptions`/`urlCopyOptions` 由 `copyOptionBase` 单一源派生；文件头注释；模板补 i18n 中文注释

### 子组件
- CopyDropdown.vue：新增 rootRef + document click 监听实现点击外部自动关闭，onUnmounted 移除
- UploadArea.vue：handleFileSelect emit 后重置 `target.value = ""`（允许重复选同一文件）
- 全部 6 个子组件补充文件头注释

### styles/index.scss
- 删除 `.panel-header` 死代码
- 硬编码值全部替换为设计 Token：font-size → $font-size-xs/2xs/sm（统计数值 sm 加注释），font-weight → $font-weight-medium/semibold，border-radius → $radius-sm/base/md，间距 → $spacing-*，响应式断点用 $mobile-breakpoint；顶部 `@use "@/variables.scss" as *;`

### i18n
- base64Image.json（zh_CN/en_US）新增 2 键：base64Image_fetchCorsFailed / base64Image_cssUnavailable

### 验证
- ESLint 0 error、pnpm i18n:verify 通过（3337 键对齐）

## 全局基准字号规则（gitPush 两级字号制提升为全局）

按用户需求，以 `src/features/gitPush` 两级字号制为参考，全局更新 `_variables.scss` / `index.scss`。用户选择「加全局基准字号规则」+「两者都做」（全局兜底规则 + Modal mask 硬编码 12px 改 Token）。

### 改动 6 个文件
1. **`src/_variables.scss`** — 修正字号 Token 注释为两级语义：`$font-size-xs`=12px 标题与正文内容基准；`$font-size-2xs`=10px 辅助文字（标签/提示/描述/状态/输出/路径/徽章）；`$font-size-sm`=14px 仅限阅读区正文与数据突出展示
2. **`src/index.scss`** — 新增全局规则：
   - `:root { --vp-font-size-xs: #{$font-size-xs}; }` CSS 变量（桥接 SCSS Token 供 TS 内联样式引用）
   - `.vp-dock-root, .vp-modal-mask { font-size: $font-size-xs; }` 基准字号规则
3. **`src/utils/vueAppHelper.ts`** — Dock 容器加 `vp-dock-root` 类；Modal 遮罩加 `vp-modal-mask` 类，`font-size: 12px` 硬编码改为 `var(--vp-font-size-xs, 12px)`
4. **`src/main.ts`** — 全局 UI 宿主容器 `.siyuan-plugin-vite-vue-sn-app` 追加 `vp-dock-root` 类
5. **`src/features/floatingBox/index.ts`** — `#floating-box-container` 追加 `vp-dock-root` 类
6. **`src/features/toolCollection/index.ts`** — `#tool-collection-root` 追加 `vp-dock-root` 类

### 不补类的判定
- **docNavigation**：文档流内嵌 UI（12px 为设计意图，protyle 正文区上下文）→ 不加
- **pageLock**：纯 DOM 锁屏遮罩，有自有字号体系（标题 lg/正文 sm 为刻意放大）→ 不加
- **statusBar**：思源官方 addStatusBar 区域，根 `.status__resUsage` 已有 `$font-size-xs` → 不加

### 说明
- 各 feature 根容器已有的重复 `font-size: $font-size-xs` 声明未强制移除（渐进式，冗余但无害）
- 既有 lint 错误 `vueAppHelper.ts:77`（`init` 签名与 siyuan `addDock` 类型不匹配）为修改前已存在，非本次引入
- 验证（lint / vite build / tsc）由用户自行执行

## 演示案例：修复 shortcut 面板根容器硬编码字号

以 `src/features/shortcut/styles/index.scss` 为示例演示全局基准字号的差异效果。修复前该面板是"全局规则失效"的典型案例：

### 修复前（差异点）
- `.shortcut-panel` 根容器**硬编码 `font-size: 13px`**（非 Token），恰好覆盖了全局 `vp-dock-root` 的 12px 基准 → 全局兜底规则对该面板无效
- `.storage-path-toggle`：`font-size: 10px` + `font-weight: 600` 硬编码
- `.storage-file`：`font-size: 10px` + `font-family: monospace` 硬编码

### 修复内容（3 处，对齐 gitPush 两级字号制）
1. 顶部补 `@use "@/variables.scss" as *;`
2. `.shortcut-panel`：`font-size: 13px` → `$font-size-xs`（12px），与全局 `vp-dock-root` 基准一致
3. `.storage-path-toggle`：`font-size: 10px` → `$font-size-2xs`，`font-weight: 600` → `$font-weight-semibold`
4. `.storage-file`：`font-size: 10px` → `$font-size-2xs`，`font-family: monospace` → `$vp-mono`

### 修复后差异
- 根容器 13px → 12px，与全局基准统一；辅助文字统一 10px + 语义 Token；等宽字体用 `$vp-mono`
- 该面板其余子组件样式（ShortcutCard 9px/11px、PanelHeader、Dialog 14px 等）仍有多处硬编码字号，属 Codex 规范审查范畴，未在本次范围处理
- lint 0 error，验证由用户执行

## 全局基准字号规则写入强制规则文档

将「字号层级与全局基准字号」写入 3 个规则文件，供后续审查直接按此修改：

1. **AGENTS_RULES.md** — 新增章节「## 强制规则：字号层级与全局基准字号」（第 629 行起），包含：
   - 两级字号制表格（2xs=10px 辅助 / xs=12px 内容 / sm+ 仅阅读区与数据突出）
   - 全局基准机制（`src/index.scss` 的 `--vp-font-size-xs` + `.vp-dock-root, .vp-modal-mask` 规则、`vp-dock-root`/`vp-modal-mask` 自动挂载点）
   - 兜底语义说明（全局规则仅对未显式声明字号元素生效，硬编码会覆盖全局 → 根容器必须主动声明）
   - 5 条审查检查点（根容器显式 `$font-size-xs` / 禁止硬编码字号/字重/行高/字体族 / 自建挂载补类 / 优先复用挂载工具）
   - 参考实现（gitPush / index.scss / vueAppHelper）
   - 「禁止事项」表新增一行：根容器缺基准字号 / JS 硬编码 12px
2. **CODEBUDDY.md + AGENTS.md**（行 219）— 「字号层级规范」硬规则条目扩展：补充全局基准字号机制说明 + 新增锚点链接 `./AGENTS_RULES.md#强制规则字号层级与全局基准字号`，两文件保持同步一致

后续审查 SCSS 时，可直接依据 AGENTS_RULES.md 的 5 条检查点修复硬编码字号（如 shortcut 面板其余子组件的 9px/11px/14px）。
