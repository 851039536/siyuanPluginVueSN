# 2026-08-01

## gitPush components 子文件夹分类重构

将 `src/features/gitPush/components/` 下 31 个平铺组件按 Tab/视图重组成 5 个子文件夹：

- `common/` (14 个)：面板常驻 + 全局弹窗 + 弹窗子组件
- `list/` (10 个)：列表视图专属
- `stats/` (2 个)：统计视图专属
- `log/` (1 个)：操作日志视图专属
- `analysis/` (4 个)：提交分析视图专属

### 变更文件数
- 31 个组件文件移动
- 1 个 `index.vue` — 16 处 import 路径更新
- 28 个组件文件 — 内部 `../` → `../../` 路径更新（TypeScript import + SCSS @use）
- 1 个 `README.md` — 目录结构更新

### 规则依据
AGENTS_RULES.md § 四、组件文件夹组织标准：≥3 个 Tab 必须按 Tab 建子文件夹。
