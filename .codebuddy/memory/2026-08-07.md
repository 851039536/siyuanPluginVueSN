# 2026-08-07

## gitPush 技术债务分区 UI 修复（TechDebtSection）
修复了 `src/features/gitPush/components/report/TechDebtSection.vue` 的 3 处 UI 显示问题：

1. **多分组表格边框重复割裂** → 将 3 个严重度分组（severe/high/medium）的独立 `.gpr-table-wrap` 合并为单个连续表格，表头提到外层共用，分组标题降级为表内跨行分隔行（`.gpr-row--group`，浅色底 `var(--b3-list-hover)` 区分）
2. **说明文案行背景与悬停高亮冲突** → `.gpr-row--desc` 移除固定 `background: var(--b3-list-hover)`，改为透明背景 + 左竖线（`border-left: 2px`）+ 缩进强调，悬停文件块时与指标行背景连续
3. **空分组仍显示标题** → `groups` computed 追加 `.filter((g) => g.rows.length > 0)`，空分组不渲染

涉及文件：
- `components/report/TechDebtSection.vue`（模板重构 + computed 过滤）
- `styles/TechDebtSection.scss`（删除 `.gpr-debt-group`/`.gpr-debt-group-title`，新增 `.gpr-row--group`/`.gpr-cell--group`，改 `.gpr-row--desc`）

共享样式 `index.scss` 的 `.gpr-*` 基座零改动。lint 0 error，无残留旧类名引用。验证项（用户自行执行）：`npx tsc --noEmit` / `pnpm i18n:verify`（无新增 i18n 键）/ `pnpm validate:icons`。

## gitPush 统计报告幽灵文件修复（reportMetrics）
修复统计报告显示已删除文件（LOC 为空 `-`）的问题。根因：`aggregateFileStats` 基于 `git log --numstat` 聚合历史所有被触碰过的文件，git 历史不因文件删除而消失，已删除文件进入 `fileMap`；`countFileLines` 对已删除文件 `statSync` 抛 ENOENT 返回 null，UI 显示 `-`。

改动（`src/features/gitPush/reportMetrics.ts`）：
1. 新增 `fileExistsInRepo(project, filePath)` 导出函数 —— `statSync().isFile()` 做当前磁盘存在性检查
2. `buildReportData` 的 `rankedFiles` 构造处追加 `.filter(([path]: [string, FileAgg]) => fileExistsInRepo(project, path))` 剔除幽灵文件

效果：技术债务表/热点榜单/analyzedFiles 统计只含当前存在的文件；作者贡献度 `filesTouched` 不变（历史事实）。lint 0 error。

## gitPush 技术债务统计算法重写 + 冗余清理（reportMetrics）
用户反馈：所有建议雷同（类型列全是"高频修改"）、修改 1 次也算债务。完成算法重构（思路参考 code-maat churn 分析，GitHub 本身无基于 git 统计的债务功能）：

**算法修复**（`src/features/gitPush/reportMetrics.ts`）：
- 新增门槛 `DEBT_MIN_MOD_COUNT = 3`：修改 < 3 次视为正常迭代，不进债务表（但仍在热点榜）
- `riskScore` 重写为 `clamp100(modCount*2.5 + authorCount*6)`，聚焦 churn + 多人触碰
- `debtSeverity(riskScore)` 基于统一风险分分档：severe≥50 / high≥30 / medium
- 删除 `classifyDebt`/`stabilityScore`/`complexityEstimate`/`buildDebtDescription`/`rangeDurationKey` 5 个冗余函数
- `buildReportData` 签名删除 `range` 参数（仅 description 用），`useCodeReport.ts` 同步更新

**删除冗余**：
- 类型：`DebtType`/`DEBT_TYPE_META`、`FileStatRow.complexity`/`stability`、`DebtFileRow.debtType`/`description`
- UI（TechDebtSection.vue）：类型/复杂度/稳定性 3 列 + 说明文案行 → 5 列（文件/修改次数/参与人数/代码行数/评分）
- SCSS：`.gpr-cell--type`/`.gpr-cell--desc`/`.gpr-type-chip`/`.gpr-row--desc` 全删
- i18n：4 文件各删 12 键（3 类型 + 3 说明 + 4 时长词 + 3 表头）

验证：gitPush 模块及全 src 零残留引用，lint 0 error。用户验证：`npx tsc --noEmit` / `pnpm i18n:verify` / `pnpm lint`。

## gitPush 技术债务 P0+P1 改进（grill-me 质询产物）
用 grill-me 技能对上一轮算法重构进行无情质询，产出 P0（bug 修正）+ P1（公式改进）共 6 项，用户确认执行。完成改动：

**P0（修正 bug/不一致）**：
1. `debtRows.sort` 组内排序从升序改降序（`b.riskScore - a.riskScore`），高分（更紧急）排前；同步 4 处注释（report.ts:155 / TechDebtSection.vue 模板+script）
2. `debtSeverity` 加 `low` 第四档（medium≥15 / low<15），消除 medium 无下限兜底；同步 `DebtSeverity` 类型、`DEBT_SEVERITY_META`（low 色 #9ca3af）、`SEVERITY_ORDER`、`DEBT_SEVERITY_ORDER`、两处 `debtSummary` 初始化（buildReportData + buildEmptyReport）；TechDebtSection 用 `DEBT_SEVERITY_ORDER` 遍历自动多一组，零模板改动
3. `hotspotSummary` 口径注释澄清：分母=全部现存文件（含低于门槛的常态文件），与债务表（仅 ≥ 门槛的风险子集）口径不同；`CodeReportData.analyzedFiles` 注释同步

**P1（公式改进）**：
4. `debtRiskScore` 改为 `clamp100(Math.sqrt(modCount)*10 + authorCount*6 + recencyBonus(lastModified))`——sqrt 使 churn 边际收益递减，避免线性公式在 modCount≥35 时过早饱和（25/40/100 仍可区分）
5. 新增共用 `recencyBonus(lastModified)`（3天+8/7天+5/30天+2），`heatScore` 和 `debtRiskScore` 复用，消除内联重复；债务评分获得"恶化趋势"信号
6. 门槛可配置（仅数据层，UI 控件留后续）：`CodeReportPrefs.debtMinModCount?: number`（可选兼容旧存储），`buildReportData` 加 `debtMinModCount: number = DEBT_MIN_MOD_COUNT` 参数，`useCodeReport` 接线（ref + loadPrefs 钳位 [1,99] + savePrefs + 传参 + return）

i18n：4 文件各加 1 键 `reportDebtLow`（zh"低"/en"Low"）。修复了崩溃导致的 useCodeReport 3 处接线丢失和分片 i18n 重复键问题。

公式校准：modCount=3,1人,久远=23→medium；+近期=31→high；modCount=25,1人=56→severe；100=100→severe。
验证：gitPush lint 0 error，`reportDebtLow` 每文件恰好 1 次，`debtRiskScore`/`buildReportData` 调用点签名一致。用户验证：`npx tsc --noEmit` / `pnpm i18n:verify` / `pnpm lint`。

## gitPush 技术债务分区展示层扩展（grill-me 确认后实施）
基于 CodeScene 行为代码方法论，对 TechDebtSection 做展示层扩展（不改风险公式与数据模型类型，用户确认）。新增 5 文件 + 改 4 文件：

**新增文件**：
- `composables/useDebtInsights.ts` — 纯函数计算引擎：`inferDebtTrend`（复用 recencyBonus 的 7d/30d 阈值，单快照推断 rising/steady/settling/calm 四向）、`buildCoupledMap`（lastModified 日期聚类 → 近期共变代理信号）、`buildSeverityDist`、`buildTopDebts`（Top3 治理）
- `components/report/DebtSummaryBar.vue` + SCSS — 严重度分布堆叠条 + 占比图例 + Top3 优先治理清单
- `components/report/DebtFileDetail.vue` + SCSS — 展开行详情面板：LOC 懒加载（onMounted 调 countFileLines）、趋势推断解释条、近期共变文件列表

**修改文件**：
- `components/report/TechDebtSection.vue` — 集成汇总条 + 手风琴可展开行 + 趋势徽章（箭头+方向色）+ 补全最后修改列（原 LOC 列移入详情懒加载）
- `components/report/CodeReportPanel.vue` — 新增 `currentProject` computed（选中优先/回退首个，与 useCodeReport 同逻辑），传 `project` prop 给 TechDebtSection
- `styles/TechDebtSection.scss` — 新增趋势徽章/展开箭头/日期列/展开态样式
- `src/config/icons.ts` — 注册 `linkVariant`（mdi:link-variant）
- i18n zh_CN/en_US 各加 12 键（趋势 4+1、分布/治理 3、共变 3、展开提示 1）

**设计决策**：趋势为单快照推断（非双周期对比，透明标注）；耦合为日期聚类代理（非 commit 级共现矩阵，UI 标注启发式）；手风琴同时只展开一个；DebtSummaryBar 用 computed 确保报告重生成后更新。

**验证**：`pnpm i18n:verify` 通过（3456 键对齐）。注意：本机 shell 中 `tsc`/`validate:icons`/ts-plugin 均因 node_modules Junction 符号链接无法穿透而报环境错误（TS2688/找不到模块 vue），非代码问题；未改动的 TeamOverviewSection/HotspotSection 显示 0 错误佐证。用户需自行验证：`npx tsc --noEmit` / `pnpm lint` / `pnpm validate:icons`。

## gitPush 代码统计报告排除 .md 文档
用户需求：`.md` 格式文件不属于代码，应在代码统计中过滤。改动 `src/features/gitPush/reportMetrics.ts`（2 处）：
1. 新增导出 `isCodeFile(filePath)` —— `!filePath.toLowerCase().endsWith(".md")`（不区分大小写）
2. `buildReportData` 的 `rankedFiles` 过滤链追加 `&& isCodeFile(path)`（与 `fileExistsInRepo` 幽灵文件过滤合并）

影响：债务表/热点榜/analyzedFiles/热点分布分母全部排除 `.md`（rankedFiles 是单一数据源）。作者 `filesTouched` 保持历史事实不变。零 i18n/类型/UI 改动。lint 0 error。

## gitPush HotspotSection 修复（grill-me 审查后确认 5 项）
基于对 `HotspotSection.vue` 的 grill-me 审查，用户确认修复 5 项：

1. **问题2（i18n 烤入数据层）**：`buildHeatAdvice(level, i18n)` → `heatAdviceKey(level)` 只返回键名（reportHeatAdviceHot/Warm/Cool）；`HotspotFileRow.advice` → `adviceKey`，`CodeReportData.suggestion` → `suggestionKey`；`buildReportData` 删除 i18n 参数（5 参→4 参），`useCodeReport.ts:91` 同步去参。数据层不再持久化语言快照，切语言无需重新分析。i18n 文本（reportHeatAdvice*/reportSug*）仍在 UI 层 `i18n[key]` 解析。
2. **问题1（LOC 读而不用）**：`HotspotSection.vue` 指标行新增 `LOC: {{ h.loc ?? "-" }}`，top12 预读的 `countFileLines` 开销现在有渲染消费点。
3. **问题3（缺行级 i18n 注释）**：模板每个 i18n 键渲染处补中文行级注释（含"热门/温热/冷却/冷门"、建议文案示例、表头等）。
4. **问题5（opacity 过低）**：`.gpr-hot-meta` opacity 0.5 / `.gpr-hot-advice` opacity 0.6 → `color: var(--b3-theme-on-surface-light)`（语义色弱化，保留可读性）。
5. **问题4（hover 误导）**：`.gpr-hot-item` 移除 hover 边框变色 + transition（热点卡片不可点击，去除可交互暗示）。

涉及 4 文件：`types/report.ts`、`reportMetrics.ts`、`composables/useCodeReport.ts`、`components/report/HotspotSection.vue`、`styles/HotspotSection.scss`。全部 lint 0 error，无 `buildHeatAdvice`/`.advice`/`.suggestion` 残留引用。用户验证：`npx tsc --noEmit` / `pnpm lint`。

## gitPush HotspotSection 剩余 3 项修复（问题 7/8/9）
补齐上一轮未选的 3 项（HotspotSection.vue + SCSS + i18n）：

1. **问题7（徽章三重冗余）**：热度徽章 `{{ h.heat }}/100 [等级名]` → 仅 `{{ h.heat }}`。颜色已编码等级，去掉 `/100` 与等级文字；等级名移入 `:title="i18n[HOTSPOT_LEVEL_META[h.level].labelKey]"` 悬浮提示。`.gpr-heat-chip` SCSS 注释同步更新。
2. **问题8（analyzedFiles 口径提示）**：标题旁文件数徽章加 `:title="i18n.reportAnalyzedFilesTip"`，新增 i18n 键 `reportAnalyzedFilesTip`（zh"分析范围内涉及的文件数（不含 .md 文档）"/en"Number of files analyzed in range (excluding .md docs)"）。
3. **问题9（路径 basename 高亮）**：文件路径拆为 `.gpr-path-dir`（弱色 `--b3-theme-on-surface-light`）+ `.gpr-path-base`（`$font-weight-medium` 强调）。新增 `splitPath(path)` 纯函数（`lastIndexOf("/")` 拆 dirname/basename），外层 `.gpr-hot-path`（flex:1 + text-ellipsis mixin）替代原 `.gpr-cell--file`（index.scss 基座类仍被 TechDebtSection 使用，未删）。

涉及文件：`components/report/HotspotSection.vue`（模板+script）、`styles/HotspotSection.scss`（新增 3 类）、`zh_CN/gitPush.json` + `en_US/gitPush.json`（各 +1 键）。零类型/数据层改动。lint 0 error。用户验证：`npx tsc --noEmit` / `pnpm i18n:verify` / `pnpm lint`。

## gitPush HotspotSection 逻辑冗余修复 + 扩展（grill-me 二轮）
对 HotspotSection.vue 做第二轮审查后确认执行 5 项：

1. **LOC 计算时序 bug（reportMetrics.ts）**：`countFileLines` 原按 modCount 榜前 12 预读，热度重排后维度不同导致部分热点文件 loc=null。修复：`hotspotRows.sort(heat)` 后追加补齐循环 `hotspotRows.slice(0, HOTSPOT_LIMIT).forEach(row => { if (row.loc === null) row.loc = countFileLines(...) })`。debtRows 与 hotspotRows 是独立 spread 对象，互不影响。
2. **splitPath 双调**：模板 `splitPath(h.path).dir/.base` 每项调用 2 次 → 新增 `preparedHotspots` computed 预映射（`{ ...h, dir, base }`），模板改 `v-for="h in preparedHotspots"` + `h.dir`/`h.base`。
3. **LOC 硬编码**：`LOC: {{ h.loc ?? "-" }}` → `{{ i18n.reportLinesCol }}: {{ h.loc ?? "-" }}`（与 DebtFileDetail/AuthorContributionSection 一致）。
4. **截断提示**：新增 `truncatedText` computed（`analyzedFiles > hotspots.length` 时生成），模板在热点列表后显示「仅展示热度前 {0} 个文件，共分析 {1} 个文件」。新增 i18n 键 `reportHotspotTruncated`（zh/en）。
5. **文件路径可点击**：`.gpr-hot-path` 加 `role="button"` + `@click="openFile(h.path)"`；`openFile` 用 `getNodeFsPathOs()?.path.join(resolveValidPath(project), path)` 拼绝对路径后走 `openLocalPath`（shell.openPath）。`CodeReportPanel.vue` 透传 `:project="currentProject"`（与 TechDebtSection 同）。新增 i18n 键 `reportOpenFileTitle`（`{0}（点击打开文件）`）。SCSS：hover 主题色 + cursor pointer + `.gpr-hot-truncated` 样式。

涉及 5 文件：`reportMetrics.ts`、`components/report/HotspotSection.vue`、`components/report/CodeReportPanel.vue`、`styles/HotspotSection.scss`、`zh_CN/en_US/gitPush.json`（各 +2 键）。lint 0 error。用户验证：`npx tsc --noEmit` / `pnpm i18n:verify` / `pnpm lint`。

## gitPush HotspotSection 排版与冗余修复（UI 审查第三轮）
对 `HotspotSection.vue` + `HotspotSection.scss` 做 UI 排版与冗余审查后修复 6 项（用户确认"开始吧"）：
1. **`.gpr-hot-truncated` 重复定义**：SCSS 中 52-60 行与 114-122 行两组完全相同的规则，删除后者。
2. **hover 路径 dir 不变色 bug**：`.gpr-path-dir` 有显式 `color`，父 `.gpr-hot-path:hover` 变色无法传递到子元素。修复：hover 内嵌套 `.gpr-path-dir { color: inherit; }` 强制继承。
3. **transition 位置错误**：原写在 `.gpr-hot-path`（自身无 color 声明，继承色不经过子元素 transition），移至实际承载颜色变化的 `.gpr-path-base` 上。
4. **`role="button"` 无键盘支持**：`<span role="button">` 补 `tabindex="0"` + `@keydown.enter` / `@keydown.space.prevent` 触发 `openFile(h.path)`。
5. **`.gpr-heat-dot` 死代码**：`flex-shrink: 0` 在 inline 上下文无效（`.gpr-cell--name` 非 flex 容器），删除。
6. **8px 硬编码**：`width/height: 8px` → `$spacing-2`（Token 化，附注释说明）。

涉及 2 文件：`components/report/HotspotSection.vue`、`styles/HotspotSection.scss`。lint 0 error。数据层/类型/i18n 零改动。用户验证：`npx tsc --noEmit` / `pnpm lint`。

## gitPush HotspotSection UI 布局重构：卡片→表格+右侧面板
用户要求 "调整UI布局 — 表格化,多栏布局 — 下方不能有空隙"。完成改动：

**模板重构**（`components/report/HotspotSection.vue`）：
- 卡片列表 `.gpr-hot-item` → HTML `<table>` 6 列表格（等级/文件/修改次数/参与人数/代码行数/最后修改）
- 去掉每行独立建议文案（`.gpr-hot-advice`），热度徽章颜色 + 整体建议已足够
- 汇总表 + 优化建议 → 右侧定宽面板（`.gpr-hot-right`，w=200px）
- 两栏 `flex` 容器 `align-items: flex-start`，右侧不拉伸，表格行少时不留空

**SCSS 完全重写**（`styles/HotspotSection.scss`）：
- 新增 `.gpr-hot-grid`/`.gpr-hot-left`/`.gpr-hot-right` 两栏布局
- 新增 `.gpr-hot-table`/`.gpr-hot-th`/`.gpr-hot-row`/`.gpr-hot-cell` 表格体系
- 表格行 hover 高亮（`var(--b3-list-hover)`）+ 底部细线分隔
- 数字列等宽字体右对齐（`$vp-mono` + `text-align: right`）
- 删除卡片类：`.gpr-hot-list`/`.gpr-hot-item`/`.gpr-hot-head`/`.gpr-hot-meta`/`.gpr-hot-advice`
- 保留：`.gpr-heat-chip` 热度徽章、`.gpr-hot-path` 路径、`.gpr-subsection` 汇总、`.gpr-suggestion` 建议

**LOC 时序根治**（`reportMetrics.ts`）：
- `forEach` 内全部 `loc: null`，不再按 modCount 排序预读
- heat 排序后用 `for` 循环仅对最终前 `HOTSPOT_LIMIT` 条计算 LOC
- 删除旧的"补齐"兜底循环（373-379 行）

**i18n 新增 2 键**：`reportLevelCol`（"等级"/"Level"）、`reportFilePathCol`（"文件"/"File"），位于 `zh_CN/en_US/gitPush.json`。

**零改动**：`CodeReportPanel.vue` 已透传 `:project="currentProject"`（上轮已做）。

涉及 4 文件：`HotspotSection.vue`（模板全重写）、`HotspotSection.scss`（全重写）、`reportMetrics.ts`（LOC 时序根治）、`zh_CN/en_US/gitPush.json`（各 +2 键）。所有文件 lint 0 error。用户验证：`npx tsc --noEmit` / `pnpm i18n:verify` / `pnpm lint`。

## gitPush HotspotSection 布局微调：统计摘要移到第二行
用户反馈"把统计摘要放到第二行"，将上一轮的「左表格 + 右汇总面板」两栏布局改为纵向两行：
- **模板**（`HotspotSection.vue`）：注释更新为"第一行热点表格 + 第二行统计摘要"；结构不变（`.gpr-hot-left` 表格、`.gpr-hot-right` 汇总面板仍是 grid 容器子元素）
- **SCSS**（`HotspotSection.scss`）：`.gpr-hot-grid` 由 `flex + align-items: flex-start` 改为 `flex-direction: column`；`.gpr-hot-left` 去掉 `flex:1` 改 `width: 100%`；`.gpr-hot-right` 去掉 `flex-shrink:0; width:200px` 改 `width: 100%`（汇总表为弹性布局 `gpr-cell--name flex:1`，全宽显示正常）

零数据层/i18n/类型改动，lint 0 error。用户验证：`npx tsc --noEmit` / `pnpm lint`。

## gitPush 报告页右侧间距修复（gpr-panel）
用户反馈："三个功能页面右侧间距跟侧边栏贴合了"。三个页面（TeamOverviewSection/TechDebtSection/HotspotSection）共用的父容器 `.gpr-panel` 原 padding 为 `$spacing-1 0`（左右 0），内容紧贴侧边栏。
改动：`CodeReportPanel.scss` 中 `.gpr-panel` 的 `padding` 改为 `$spacing-1 $spacing-3 $spacing-1 0`（上4/右12/下4/左0），一处修改覆盖三个页面，同时为 overflow-y 滚动条留出距离。左侧保持 0。lint 0 error。

## gitPush 报告页右侧间距调整：12px→8px + 新增侧边栏间距规则
用户反馈"右侧间距小一号并更新规则，后续注意侧边栏间距问题"。两处改动：
1. **SCSS**：`CodeReportPanel.scss` `.gpr-panel` 的 `padding` 从 `$spacing-1 $spacing-3 $spacing-1 0`（右12px）改为 `$spacing-1 $spacing-2 $spacing-1 0`（右8px，小一号），注释同步更新
2. **规则固化**：新增「强制规则：Dock 面板侧边栏间距」——面板根容器必须设 `padding-right`（至少 `$spacing-2`=8px）为滚动条/侧边栏留出呼吸空间；禁止 `padding: xxx 0` 省略左右；多个 Tab 共用根容器时统一加右侧间距一次覆盖所有页面。已同步写入 AGENTS_RULES.md（新章节）+ AGENTS.md + CODEBUDDY.md（硬规则清单行）。lint 0 error。

## gitPush 作者贡献度「提交」列宽度 42px→66px
用户要求 `AuthorContributionSection.vue:24` 的提交列宽度改为 66px。`.gpr-cell--num` 是共享基座类（42px，TechDebtSection/HotspotSection 也在用），不能直接改。做法：新增专属类 `.gpr-cell--commits { width: 66px; }` 放 `AuthorContributionSection.scss`，表头（24行）+ 数据行（42行）同时加类对齐。零数据层/i18n/类型改动，lint 0 error。

## gitPush 作者贡献度数字列改为自适应宽度
用户反馈 `AuthorContributionSection.vue:24-31` 的字段宽度被挤压。原固定宽度：`.gpr-cell--num` 42px（共享基座 index.scss）、`.gpr-cell--wider` 78px、`.gpr-cell--commits` 66px，窄容器下表头/数值挤压换行。由于 `.gpr-cell--num` 被 TechDebt/Hotspot 分区共用不能改基座，改法：`AuthorContributionSection.scss` 中用更高特异性 `.gpr-row .gpr-cell--num { width: auto; white-space: nowrap; }`（0,2,0）覆盖基座规则，删除 `.gpr-cell--commits` 固定 66px 规则（commits 列同时带 --num 类，自动继承自适应；wider 列同理）。只影响本表格，TechDebt/Hotspot 保持 42px。lint 0 error。

## gitPush 作者贡献度分区「表格化」重构（flex 模拟表 → HTML table）
用户反馈 flex 模拟表格排版乱（flex 各行列宽独立计算，表头与数据行同列宽度不对齐）。重构 `AuthorContributionSection`：
- **模板**（`AuthorContributionSection.vue`）：`.gpr-row/.gpr-cell` flex 行 → `<table class="gpr-author-table">` + `<thead><th>` + `<tbody><tr><td>` 9 列（作者/提交/新增/净增/平均大小/频率/文件/质量/活跃天数）。质量列沿用 `gpr-grade-chip`+`gpr-stars`；净增正负色复用 index.scss 的 `.gpr-cell--pos/--neg` 颜色类
- **SCSS**（`AuthorContributionSection.scss` 全重写）：`.gpr-author-table`（width:100% + border-collapse:separate + border-spacing:0 + table-layout:auto 数字列按内容自适应）、`.gpr-author-th`（sticky 表头 + uppercase 2xs + 底部 2px 分隔）、`.gpr-author-row`（底部细线）、`.gpr-author-cell--name`（text-ellipsis 省略）/`--num`（$vp-mono 等宽右对齐 nowrap）。删除上一轮的 `.gpr-row .gpr-cell--num` 覆盖规则
- **死代码清理**（`index.scss`）：删除 `.gpr-cell--wider`（width:78px，已无引用）
lint 0 error，无 `.gpr-cell--commits/--wider/--net` 残留引用。用户验证：`npx tsc --noEmit`。
