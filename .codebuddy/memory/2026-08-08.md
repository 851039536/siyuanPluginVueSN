# 2026-08-08

## gitPush 报告移除"质量评分"（方案 C：参考 git-fame，不做综合评分）

用户选择方案 C——移除代码贡献度表的单一"质量评分"列及启发式公式，仅保留原始多维指标（提交次数/代码行数/净增/平均提交大小/提交频率/文件数/活跃天数），让用户自行判断。

### 改动文件
1. `src/features/gitPush/types/report.ts` — 移除 `QualityGrade` 类型、`GRADE_META` 常量；`AuthorReportRow` 移除 `quality`/`grade` 字段；`CodeReportData.teamOverview` 移除 `avgQuality`
2. `src/features/gitPush/types/index.ts` — 移除 `GRADE_META`、`QualityGrade` 重导出
3. `src/features/gitPush/reportMetrics.ts` — 移除 `qualityScore()`/`qualityGrade()` 函数及 import；`aggregateAuthorStats()` 移除 quality/grade 计算；`buildReportData()` 移除 avgQuality 聚合与赋值；`buildEmptyReport()` 同步移除
4. `src/features/gitPush/components/report/AuthorContributionSection.vue` — 移除质量评分表头列与 `<td>` 渲染；展开行 `colspan` 10→9（列数从 10 减为 9）；移除 `GRADE_META` import 及 gradeLabel/gradeColor/gradeBg/stars 派生值
5. `src/features/gitPush/components/report/TeamOverviewSection.vue` — 移除"平均代码质量"卡片（overviewCards 第 4 项）
6. `src/features/gitPush/styles/AuthorContributionSection.scss` — 移除 `.gpr-grade-chip`、`.gpr-stars` 样式；`TeamOverviewSection.scss` 移除孤立的 `.gpr-card--warn` 样式
7. i18n 4 个文件（`zh_CN/gitPush.json`、`en_US/gitPush.json`、`zh_CN.json`、`en_US.json`）— 移除 `reportAvgQuality`/`reportQualityCol`/`reportGradeS~D` 共 8 个键

### 经验教训
并行执行多个 `replace_in_file` 编辑**同一文件**的多个不同区块时，发生了部分编辑被覆盖/丢失（尤其 i18n JSON 与 report.ts 的多个独立替换），导致中间状态不一致（`qualityScore` 引用残留、`GRADE_META` 重复）。最终靠 search_content 全量扫描 + 逐文件串行重读重改才收敛。**教训：同一文件的多个编辑务必串行执行，或一次 replace 覆盖完整连续区块。**

### 验证
- `search_content` 全库扫描 `qualityScore|qualityGrade|GRADE_META|QualityGrade|avgQuality|reportQualityCol|reportGrade|gpr-grade-chip|gpr-stars|gpr-card--warn` → 0 匹配
- `read_lints` gitPush 目录 → 0 error
- 待用户自行执行：`pnpm i18n:verify`（键对齐）、`npx tsc --noEmit`、`pnpm lint`

## gitPush 报告清理收尾 + 新增"删除行数"列（2026-08-08）

质量评分移除后的收尾计划（分 3 阶段执行完毕）：

### 阶段 1：清理过期注释 + 代码组织
1. `AuthorContributionSection.scss` L1 — 删除已移除的"等级徽章"字样
2. `reportMetrics.ts` L1 — "启发式评分"→"债务/热点评分"（更准确）
3. `reportMetrics.ts` — 新增 `// ── 文件工具函数 ──` 区块，将 `LOC_READ_MAX_BYTES`/`countFileLines`/`fileExistsInRepo`/`isCodeFile` 从"启发式评分公式"区块提取到独立区块（评分公式区块仅保留 `clamp100`/`round2`）
4. `useCodeReport.ts` L1 — "启发式聚合"→"报告聚合"

### 阶段 2：新增"删除行数"列表格列（功能扩展）
`AuthorContributionSection.vue`：在"新增行数"与"净增"之间新增"删除行数"列（复用 `.gpr-author-cell--num` 样式，无需新增 SCSS），展开行 `colspan` 9→10，文件头注释同步更新。数据源为已有的 `AuthorReportRow.linesDeleted`（此前仅作展开详情中流失率分母）。列顺序：排名/作者/提交/新增/删除/净增/平均/频率/文件/活跃。

### 阶段 3：i18n 新增 1 键 ×4
`reportDeletedLinesCol`：中文"删除行数" / 英文"Deleted Lines"，4 个文件（分片 + 合并）同步。

### 验证
- `read_lints` gitPush → 0 error
- `search_content` 确认 `reportDeletedLinesCol` 在 4 个 i18n 文件中对齐 + vue 正确引用
- 待用户自行执行：`pnpm i18n:verify`、`npx tsc --noEmit`、`pnpm lint`

## gitPush 报告清理收尾 — 最终执行（2026-08-08）

上一轮遗漏了 `reportMetrics.ts` 的 3 处注释（L1 "启发式评分"、L3 "启发式公式说明"、L183 区块标题）和 `colspan` 9→10。本轮补齐：

- `reportMetrics.ts` L1-L3："启发式评分"→"债务/热点评分"，"启发式公式说明"→"评分公式说明"
- `reportMetrics.ts` L183-L196："启发式评分公式"→"通用工具"，"文件工具函数"→"文件读取工具"
- `AuthorContributionSection.vue` L116：colspan 9→10

### 验证
- `read_lints` gitPush → 0 error
- 待用户自行执行：`pnpm i18n:verify`、`npx tsc --noEmit`、`pnpm lint`

## gitPush 报告「文件详情弹窗」功能（2026-08-08）

用户需求：作者贡献度展开行"主要修改文件"Top3 列表 → 点击文件项弹出居中 Modal 展示基础详情（路径/修改次数/LOC/参与作者数/最后修改时间）。已确认方案：路线 B（报告级查找表）+ 点击弹出 Modal。

### 改动文件
1. `types/report.ts` — `CodeReportData` 新增 `fileDetailsMap: Record<string, FileStatRow>`（必填字段）
2. `reportMetrics.ts` — `buildReportData` 内构建 `fileDetailsMap`（遍历各作者 topFiles 去重 → 从 `aggregateFileStats` 的 FileAgg 补全 modCount/authorCount/lastModified + 预读 LOC，fs 读取量受 作者数×3 约束）；`buildEmptyReport` 补 `fileDetailsMap: {}`
3. `utils.ts` — 新增 `formatIsoDate(iso)` 纯函数（ISO → YYYY-MM-DD，无值返回 —），替代 AuthorContributionSection 本地 formatDate（两个组件共用，按分层规则提取）
4. `components/report/FileDetailModal.vue` — **新建**，Teleport to body + 遮罩/ESC/关闭按钮关闭，头部（图标+标题）+ 文件路径 + 2×2 指标网格
5. `styles/FileDetailModal.scss` — **新建**，Codex 风格（固定遮罩 + 420px 边框卡片，基准字号 $font-size-xs）
6. `components/report/AuthorContributionSection.vue` — props 新增 `fileDetailsMap`；selectedFile ref + openFileDetail()；文件项 `@click.stop` 触发；模板 formatDate → formatIsoDate；渲染 FileDetailModal
7. `styles/AuthorContributionSection.scss` — `.gpr-detail-file` 加 cursor:pointer + hover 底色 + 原 hover 路径变色改为嵌套写法
8. `components/report/CodeReportPanel.vue` — 传递 `:file-details-map="report.fileDetailsMap"`
9. i18n ×4 — 新增 7 键：`reportFileDetailTitle`/`reportFileDetailClickHint`/`reportFileDetailPath`/`reportFileDetailMods`/`reportFileDetailLoc`/`reportFileDetailAuthors`/`reportFileDetailLastModified`

### 关键设计
- LOC 仅在构建报告时对 topFiles 去重后的文件预读（最多 作者数×3 次 fs read，与 hotspot 的 HOTSPOT_LIMIT 预读策略互补）
- 弹窗为组件内受控 Modal（非 createModalVueApp），ESC 在 document 层监听（Teleport 到 body 后）
- `fileDetailsMap[path]` 理论上必存在（从 topFiles 构建），openFileDetail 取不到时降级 null（弹窗不显示）

### 验证
- `read_lints` gitPush → 0 error
- `CodeReportData` 构造点确认仅 `buildReportData`/`buildEmptyReport`（useCodeReport.ts 引用），均已覆盖
- 待用户自行执行：`pnpm i18n:verify`、`npx tsc --noEmit`、`pnpm lint`

## i18n 规则修正（2026-08-08）

用户指出：顶层合并 JSON（`src/i18n/zh_CN.json` / `en_US.json`）禁止手动修改，由 `pnpm i18n:merge` 脚本自动生成。已撤销本轮对顶层 JSON 手动添加的 `reportFileDetail*` 7 键（恢复原状），分片文件（`zh_CN/gitPush.json` / `en_US/gitPush.json`）保留新键。规则已写入 MEMORY.md 编码规范。用户运行 `pnpm i18n:merge`（构建时自动执行）后顶层 JSON 会自动补全。

## 「i18n 只改分片文件」规则补入 AGENTS_RULES.md（2026-08-08）

用户要求将该规则正式更新到规则文件。检查发现 `AGENTS.md` 与 `CODEBUDDY.md` 的「i18n 国际化」章节已包含此硬规则（第 317-328 行），但 `AGENTS_RULES.md`（规则明细文件）缺少独立的强制规则章节。已在 `AGENTS_RULES.md` 的「强制规则：禁止 i18n 硬编码兜底值」之前新增「**强制规则：i18n 只改分片文件**」章节，内容含：规则声明（只改分片文件、顶层 JSON 由 merge 自动生成禁止手动修改）、原因（顶层 JSON 是构建产物会被覆盖）、操作规范（grep 定位分片 / 新建分片 / 中英同步 + i18n:verify / i18n:merge 重新生成）。AGENTS.md 与 CODEBUDDY.md 无需改动（已一致）。

## 文件详情弹窗：LOC → 变更行数修正（2026-08-08）

用户反馈：弹窗显示的是文件"总代码行数"（`countFileLines` 从磁盘读取的 LOC），但应该展示 git 变更数据（numstat 的新增/删除行数）。

### 改动文件

1. `types/report.ts` — `FileStatRow` 新增 `added: number` / `deleted: number` 字段
2. `reportMetrics.ts` — `FileAgg` 新增 `added`/`deleted` 字段；`aggregateFileStats` 循环中 `agg.added += f.added; agg.deleted += f.deleted` 累加；`buildReportData` 两处 `FileStatRow` 构造点（fileDetailsMap + base）补 `added: agg.added` / `deleted: agg.deleted`
3. `FileDetailModal.vue` — 网格从 2×2 扩展为 3×2（修改次数/新增行数/删除行数 + 参与作者数/净增/最后修改）；新增 `netLines` computed（added - deleted）；移除 `reportFileDetailLoc` 引用
4. `FileDetailModal.scss` — `grid-template-columns: repeat(3, ...)`；新增 `.gpr-fm-added`（绿 #28a745）/ `.gpr-fm-deleted`（红 #dc3545）颜色
5. i18n 分片 — `reportFileDetailLoc`（"代码行数"）→ `reportFileDetailAdded`（"新增行数"）+ `reportFileDetailDeleted`（"删除行数"）；净增复用已有键 `reportNetCol`

### 验证
- `read_lints` gitPush → 0 error
- `FileStatRow` 构造点仅 reportMetrics.ts 中两处 + FileDetailModal.vue 类型注解，均已更新
- 待用户自行执行：`pnpm i18n:merge`（补全顶层 JSON 中的新键）、`pnpm i18n:verify`、`npx tsc --noEmit`、`pnpm lint`

## 文件详情弹窗：新增 git diff 代码显示（2026-08-08）

用户反馈：保留当前指标显示的基础上，额外展示文件的 git diff 代码变更内容。

### 改动文件
1. `types/report.ts` — `FileStatRow` 新增 `diffContent: string | null` 字段（null=获取失败/不适用）
2. `reportMetrics.ts` — 新增 `fetchFileDiff(project, filePath)` 函数：使用 `getNodeProcessModules().child_process.execFileSync` 同步执行 `git log -p --max-count=5 -- <file>`，截断上限 5000 字符；`buildReportData` 中 `fileDetailsMap` 每条记录调用 `fetchFileDiff` 填充 `diffContent`；`base` 构造点补 `diffContent: null`
3. `FileDetailModal.vue` — 修复误删的 `Icon` 导入；指标网格下方新增 `<!-- 变更详情 -->` 区域：`v-if="fileStat.diffContent"` 条件渲染，`<pre class="gpr-fm-diff">` 展示原始 diff
4. `FileDetailModal.scss` — 卡片加宽（420→680px）；新增 `.gpr-fm-diff-section` / `.gpr-fm-diff` 样式：等宽 10px、暗色底边框、最大高度 280px 可滚动
5. i18n 分片 — 新增 `reportFileDetailDiff` = "变更详情" / "Code Changes"

### 性能考量
- `fetchFileDiff` 为同步 `execFileSync`，每个文件跑一次 `git log -p --max-count=5`（与 `countFileLines` 的同步 fs 读策略一致）
- 文件数上限为 作者数×3（通常 ≤30），单文件 diff 提取约 10-50ms，总量 < 1s
- 截断上限 5000 字符 + 2MB maxBuffer 防止巨型文件 OOM
- diff 仅对 `fileDetailsMap` 中的 topFiles 条目执行（债务/热点行 `diffContent: null`）

### 验证
- `read_lints` gitPush → 0 error
- 待用户自行执行：`pnpm i18n:merge`、`pnpm i18n:verify`、`npx tsc --noEmit`、`pnpm lint`

## 文件详情弹窗：diff 高亮显示（2026-08-08）

用户需求：变更详情的 diff 代码要高亮显示。复用项目已有的 diff 解析 + 着色方案（WorkingTreeDiffDialog 同款），未引入新依赖。

### 改动文件
1. `FileDetailModal.vue` — 将 `<pre>` 原始 diff 改为行级渲染：
   - 引入 `parseDiffLines` / `DiffLineType`（来自 `utils.ts`，与 WorkingTreeDiffDialog 共用）
   - 新增 `DIFF_SIGN` 常量（diff 行类型 → 行首符号）+ `diffLines` computed（`parseDiffLines(diffContent)`）
   - 模板循环渲染行：旧/新行号双列 + 符号列 + 文本列，行容器 `:class="gpr-fm-dl-${line.type}"`
2. `FileDetailModal.scss` — `.gpr-fm-overlay` 注入 `@include gp-diff-color-vars`（Teleport 到 body 取不到面板变量，须重注入）；`.gpr-fm-diff` 从 pre 改为容器；新增行样式：
   - `.gpr-fm-dl-add`（绿色背景/符号）、`.gpr-fm-dl-del`（红色背景/符号）、`.gpr-fm-dl-hunk`（蓝色粗体）、`.gpr-fm-dl-ctx`（0.6 透明度）、`.gpr-fm-dl-meta`（0.35 透明度）
   - 行号列 `.gpr-fm-dl-no`（28px 右对齐）、符号列 `.gpr-fm-dl-sign`（16px 居中）、文本列 `.gpr-fm-dl-text`（pre-wrap）

### 关键设计
- 完全复用 `parseDiffLines`（含行号计算 + meta 前缀识别 + 词级分段），无重复实现
- 复用 `gp-diff-color-vars` mixin（浅色 GitHub Light / 暗色 GitHub Dark 自动切换）
- 词级差异分段（segments）暂未渲染（历史 diff 行内对比意义有限），仅整行着色

### 验证
- `read_lints`：FileDetailModal.vue 无报错（其余文件报错为 IDE 环境噪音：找不到 vue 模块，与本次改动无关）
- 待用户自行执行：`npx tsc --noEmit`、`pnpm lint`

## docNavigation 子文档改为下拉树形面板（2026-08-08）

用户需求：docNavigation 现有基础上增加"显示列表（下拉跨显示）"，参考思源 API 显示下级文档。将原内联 "+N" 展开模式改造为下拉树形面板，支持逐级懒加载钻取。

### 改动文件
1. `types/index.ts` — `Block` 新增 `path?`（存储路径去 .sy 后缀，供 listDocsByPath 查询）与 `subFileCount?`（子文档数，0 隐藏展开箭头）
2. `types/storage.ts` — `iFileToBlock` 改为 `export`，保留 `path`/`subFileCount` 字段
3. `composables/useDocNavigation.ts` — 移除 `isExpanded`/`visibleChildren`/`hiddenChildren`/`toggleExpand` 及 `settings`/`settingsStorage`；新增 `notebook` ref + `childCount` computed；更新 `UseDocNavigationReturn`
4. `components/ChildDocDropdown.vue` — **新建**：触发按钮（图标 + "下级文档 (N)" + 三角）+ 下拉面板 + 点击外部关闭（document click + rootRef.contains）
5. `components/TreeNode.vue` — **新建**：递归树节点（`defineOptions({ name: "TreeNode" })` 自引用），有 `subFileCount > 0` 显示展开箭头，点击经 `listDocsByPath` 懒加载子文档，加载中/空状态占位，当前文档 `.doc-nav-tree-current`（font-weight-semibold + border-left 2px 色条）
6. `components/DocNavContainer.vue` — 子文档区域（原 122-183 行）替换为 `<ChildDocDropdown v-if="childCount > 0">`，i18n 取 `plugin.i18n.docNavigation`
7. `styles/ChildDocDropdown.scss` / `styles/TreeNode.scss` — **新建**：面板绝对定位、Transition 150ms 动画、每层缩进用 `.doc-nav-tree-children { margin-left: 12px }` 纯 CSS 累进（无需 depth prop）、箭头旋转 90 度动画、无 box-shadow
8. `styles/index.scss` — 移除 `.doc-nav-children-list`/`.doc-nav-link-hidden`/`#doc-nav-hidden-children`/`.doc-nav-expand`，`.doc-nav-parent, .doc-nav-children` → `.doc-nav-parent`
9. i18n 分片 ×2 — 新增 5 键：`docNavShowChildren`/`docNavNoChildren`/`docNavLoading`/`docNavExpand`/`docNavCollapse`
10. `README.md` — 补充"下级文档下拉树形面板"功能特性

### 技术要点
- 懒加载：初次仅直接子文档，展开节点时按需 `listDocsByPath(notebook, node.path || node.hpath, 0)`，组件内 children ref 天然缓存（展开后不重复请求）
- 递归组件名通过 `defineOptions({ name: "TreeNode" })` 声明（项目首个递归组件，antfu ESLint 未开启 no-undef-components 故安全）
- `DocNavSettings.maxVisibleChildren` 字段保留未删（无 UI 使用，防破坏）

### 验证
- `read_lints` docNavigation 目录 → 0 error
- 待用户自行执行：`pnpm i18n:merge`、`pnpm i18n:verify`、`npx tsc --noEmit`、`pnpm lint`

### 修复：新 SCSS 缺 Token 导入（同轮）
用户反馈 `Undefined variable: $spacing-1`。根因：`ChildDocDropdown.scss`/`TreeNode.scss` 使用了设计 Token（`$spacing-*`/`$font-size-*`/`$vp-radius` 等）但未导入全局变量文件。修复：两个文件顶部补 `@use "@/variables" as *;`（项目标准写法，见 gitPush/s3Backup 等模块）。同时清理 `useDocNavigation` 移除 plugin 参数后遗留的 unused 参数 warning（`useDocNavigation(plugin: Plugin)` → `useDocNavigation()`，DocNavContainer 调用同步改为无参）。**教训：docNavigation 模块此前 SCSS 只用思源 CSS 变量（`var(--b3-theme-*)`），首次引入设计 Token 时须记得 `@use "@/variables" as *;`。**

### 下拉面板新增标题"子文档"（同轮）
用户反馈："非常好，列表增加文字显示：子文档"。在下拉面板顶部新增标题栏。改动：`ChildDocDropdown.vue` 面板内新增 `.doc-nav-dropdown-header`（Codex 风格大写小字标签，10px 辅助字号 + 底部边框分隔 + text-transform: uppercase）；`ChildDocDropdown.scss` 新增对应样式；i18n 分片 ×2 新增键 `docNavPanelTitle` = "子文档"/"Child Documents"（触发按钮仍用 `docNavShowChildren` = "下级文档 (N)"）。

### 修复：docNavigation i18n 访问路径错误（同轮）
用户反馈"只看到图标（5）"——触发按钮文字与面板标题均为空白。根因：docNavigation 分片文件键是**平铺**的（`enableDocNavigation`/`docNavShowChildren` 等不带模块前缀），合并后位于 `plugin.i18n` **顶层**；但组件错误地用 `(plugin.i18n as any)?.docNavigation` 嵌套访问，取到空对象。修复：`DocNavContainer.vue` 改为 `const i18n = props.plugin.i18n as Record<string, string>` 直接访问顶层（与 `tableOfContents`/`pageLock` 一致）。**教训：分片 i18n 键是否平铺取决于分片文件自身的结构——docNavigation 分片是平铺键，而 apiDebugger 等分片是嵌套 `{ apiDebugger: {...} }` 结构，运行时访问路径必须与分片结构匹配。**

## docNavigation 新增「参考」下拉框（2026-08-08）

用户需求：在「下级文档」下拉框之后新增「参考」下拉框，内容同为下级文档，仅当**当前文档标题（文档名）包含"参考"**（包含匹配，"参考资料"等也触发）且存在子文档（childCount > 0）时显示。

### 设计方案（DRY 复用）
- **ChildDocDropdown.vue 通用化**：新增 3 个 props——`triggerText`（按钮文字）、`panelTitle`（面板标题）、`triggerIcon?`（图标，默认 docNavChildren）。图标需用 `computed<IconKey>` + `(props.triggerIcon || "docNavChildren") as IconKey` 转换（IconWrapper 的 name 是 IconKey 联合类型，直接绑定 string 报 TS2322）。模板 `{{ i18n.docNavShowChildren }}` → `{{ triggerText }}` 等。一个组件供「下级文档」「参考」两处复用，零复制。
- **标题检测**：`storage.ts` 新增 `fetchDocTitle(box, docId, cache)`——`api.getBlockByID(docId)` 取文档根块 `content`（即标题，可能含内联 HTML，`stripHtml` 在 composable 判断处统一处理，fetchDocTitle 仅返回原始 content），独立 try-catch 失败返回空串。`DocNavigationCache` 新增 `titleCache` + `getCachedTitle`/`setCachedTitle`，`clearAll` 同步清理（switch-protyle 事件会重复 loadHierarchy，缓存避免重复 SQL）。
- **hasReference computed**：`childDocs.length > 0 && docTitle.includes("参考")`，放 `useDocNavigation.ts`，返回 `docTitle`/`hasReference`。
- **独立容错**：`loadHierarchy` 中标题获取用 `void fetchDocTitle(...).then(...).catch(...)` 与主 `Promise.all`（hierarchy/breadcrumb/siblings）并行且不阻塞——失败仅「参考」框不显示。
- **渲染**：`DocNavContainer.vue` 解构 `hasReference`，现有实例补 `trigger-text`/`panel-title`，参考实例 `v-if="hasReference"` 且 `trigger-icon="docNavReference"`。`.doc-navigation` 已有 `gap: 8px`，无需新样式。
- **i18n 分片**：新增 `docNavReference`（"参考"/"Reference"）、`docNavReferencePanelTitle`（"参考"/"References"）。
- **icons.ts**：注册 `docNavReference: mdi:book-open-variant`（与 docNavChildren 同色系 #8b5cf6）。

### 教训
同文件**多个** replace_in_file 并行执行时再次发生部分编辑被覆盖丢失（import 与 defineProps 修改丢失、接口新增丢失，导致 TS2339/TS2353/TS2304）。**后续凡同一文件的多个独立编辑，必须串行提交或一次 replace 覆盖完整连续区块，绝不并行。**（今日 gitPush 任务已踩过同样坑）

### 验证
- `read_lints` docNavigation 目录 → 0 error（仅剩预存在 HINT：storage.ts fetchBreadcrumb map 回调可转 async）
- 待用户自行执行：`pnpm i18n:merge`、`pnpm i18n:verify`、`pnpm validate:icons`、`npx tsc --noEmit`、`pnpm lint`

## 修复：参考下拉框未显示 + stripHtml 遗漏（2026-08-08）

用户反馈"下级文档旁边没有看到新的下拉框"。排查结论：代码链路本身正确（`getBlockByID` 经 `sql`→`request` 返回 `response.data`，`data[0]` 即文档根块，`content` 即标题），最可能原因是**测试文档标题不含"参考"**（触发条件是标题包含"参考"且 childCount>0，这是需求定义）。

同时修复两处健壮性问题：
1. **stripHtml 遗漏（计划要求但首版漏掉）**：`fetchDocTitle` 现在返回前调用 `cache.stripHtml(block?.content || "")` 清洗为纯文本再缓存/判断，防止根块 content 中的内联 HTML 标记干扰 `includes("参考")` 匹配。
2. **失败日志可诊断**：`getBlockByID` 在 `data[0]` 为 null/undefined 时抛错（`data[0]` 未判空），`fetchDocTitle` 的 catch 由 `console.error` 改为 `console.warn("获取文档标题失败（「参考」下拉框不可用）", error)`，用户可通过控制台警告快速定位。

**排查指引**：若用户测试文档标题不含"参考"则参考框不显示属预期；需用标题含"参考"（如"参考资料"）且有子文档的文档验证。控制台无警告 + 标题含"参考"仍不显示时才需进一步查 hasReference 链路。

## 修正：参考下拉框改为过滤子文档列表（2026-08-08）

用户纠偏：需求不是根据当前文档标题含"参考"来显示/隐藏，而是"参考"下拉框显示标题含"参考"的**子文档**（过滤后的子集）。修正内容：

### 移除（错误实现）
1. `types/index.ts` — 移除 `TitleCacheItem` 接口
2. `types/storage.ts` — 移除 `titleCache` 字段、`getCachedTitle`/`setCachedTitle` 方法、`fetchDocTitle` 函数、`TitleCacheItem` 导入
3. `useDocNavigation.ts` — 移除 `fetchDocTitle` 导入、`docTitle` ref、`hasReference` computed（基于当前文档标题判断）、`loadHierarchy` 中 `void fetchDocTitle(...)` 并行调用、`resetState` 中 `docTitle` 清理、接口中 `docTitle`/`hasReference` 字段

### 新增（正确实现）
4. `useDocNavigation.ts` — 新增 `filteredChildDocs` computed（`childDocs.filter(doc => stripHtml(doc.content).includes("参考"))`）+ `filteredChildCount` computed，加入接口与 return
5. `DocNavContainer.vue` — `hasReference`→`filteredChildDocs`/`filteredChildCount`；参考实例 `:child-docs="filteredChildDocs"`、`:child-count="filteredChildCount"`、`v-if="filteredChildCount > 0"`

### 不变
- `ChildDocDropdown.vue` 通用化 props（triggerText/panelTitle/triggerIcon）保留
- i18n 分片、图标注册、README 保持不变

### 验证
- `read_lints` docNavigation → 0 error（仅预置 HINT）
- 待用户自行执行：`pnpm i18n:verify`、`pnpm validate:icons`、`npx tsc --noEmit`、`pnpm lint`

## docNavigation 全量代码审查修复（12 问题，2026-08-08）

对 docNavigation 功能做完整代码审查，发现并修复 12 个问题（4 内存泄露 / 2 死代码 / 2 冗余 / 1 重复类型 / 2 逻辑漏洞 / 1 样式违规）：

### 内存泄露修复（4 个）
1. **index.ts 重构为 destroy() 自挂载模式**（参考 pageLock）：`registerDocNavigation` 返回 `{ destroy() }` 挂载到 `(plugin as any).__docNavigation`，已加入 `src/index.ts` 的 `DESTROYABLE_KEYS`。destroy() 内按序：(1) `observer.disconnect()` 释放 IntersectionObserver；(2) `plugin.eventBus.off()` 解绑 switch-protyle / loaded-protyle-dynamic / loaded-protyle-static 三个事件（消除重载累积）；(3) 遍历模块级 `mountedProtyles: Set<ProtyleLike>`（在 debounce 入口即 add，覆盖未完成的 timer）清 timer + `__docNavApp.unmount()` + `__docNavContainer.remove()`；(4) `disposeCache()` 清空数据缓存
2. **storage.ts `get()` 过期驱逐**：命中过期条目时 `cache.delete(cacheKey)` 后再返回 null，不再永久占用 Map

### 死代码/冗余清理
3. **`disposeCache()` 保留但改为被调用**：原为无调用者的死代码；因 cache 是 useDocNavigation 模块级单例，index.ts 无法直接访问，故保留该导出函数并在 destroy() 中调用，消除死代码状态（计划原拟删除，此为实现 destroy 清缓存的必要调整）
4. **`removeExistingNav()` 整体删除**（函数定义 + index.ts 调用 + import）：Vue `app.unmount()` 已清空容器内容，容器 DOM 被 __docNavContainer 缓存复用且 after/before 自动移位，DOM 手动移除冗余
5. **`ChildDocDropdown.vue` 删除不可达 v-else 空状态**：父组件 `v-if="childCount > 0"` 已保证非空；连带删除 `.doc-nav-dropdown-empty` 样式（i18n 键 `docNavNoChildren` 保留——TreeNode.vue 仍在使用）
6. **`setCachedBreadcrumb` 去掉 `as BreadcrumbCacheItem`**：`this.set(..., { items })` 由 set() 内部补充 timestamp，类型推断自然通过

### 重复类型
7. **删除 `DocPathInfo` 接口** → 改为 `type DocPathInfo = NonNullable<Awaited<ReturnType<typeof api.getPathByID>>>`（从 api 推导，不重复声明结构）；useDocNavigation 不再 import 它，loadHierarchy 中 `docPathInfo` 中间变量删除、`fetchXxx(currentDoc, cache, pathInfo)` 直接传窄化后的 pathInfo

### 逻辑漏洞
8. **filteredChildDocs 去掉多余 stripHtml**：`doc.content` 来自文件名（iFileToBlock 已去 .sy 后缀）是纯文本，直接 `doc.content.includes("参考")`（首版参照上一轮"参考"实现错误地加了 stripHtml）
9. **visibilityMap 语义注释**：`=== false` 严格比较，undefined（未观测）不拦截首渲，仅拦截已确认不可见元素

### 样式违规
10. **styles/index.scss 补 `@use "@/variables.scss" as *;` + 全部硬编码替换 Token**：font-size 12px→$font-size-xs、11px→$font-size-2xs、font-weight 600→$font-weight-semibold、line-height 1.4→$line-height-normal、padding 1px 4px→$spacing-2px $spacing-1、padding 1px 6px→$spacing-2px $spacing-2、border-radius 3px/4px→$radius-sm、margin 2px 0 6px→$spacing-2px 0 $spacing-2、gap 2px/4px/8px→$spacing-2px/$spacing-1/$spacing-2

### 涉及文件（6 个）
`src/index.ts`（DESTROYABLE_KEYS）、`docNavigation/index.ts`（重写）、`composables/useDocNavigation.ts`（重写）、`types/storage.ts`（重写）、`components/ChildDocDropdown.vue`、`styles/index.scss`（重写）+ `styles/ChildDocDropdown.scss`

### 验证
- `read_lints` docNavigation + src/index.ts → 0 error（仅预置 HINT：storage.ts fetchBreadcrumb map 回调可转 async，非本次引入）
- 待用户自行执行：`npx tsc --noEmit`、`pnpm lint`

### 教训
- 模块级单例（cache / observer）的生命周期清理应归属 register 的 destroy()；无调用者的导出函数若承担资源清理职责，保留并接入生命周期比直接删除更合理

## docNavigation 上一篇/下一篇改为同级下拉框（2026-08-08）

将"上一篇"/"下一篇"平铺式导航栏重构为下拉框模式（与「下级文档」「参考」样式一致），按钮名称改为「同级」。

### 改动文件
1. `components/SiblingDropdown.vue` — **新建**：复用 ChildDocDropdown 外壳（触发按钮 + Transition + 面板 + 点击外部关闭 document click），面板内容改为扁平同级文档列表（`v-for doc in siblings`，当前文档 `doc-nav-sibling-item--current` 高亮：左侧 2px 色条 + 主色加粗）
2. `styles/SiblingDropdown.scss` — **新建**：Trigger/Panel/Caret/Fade 动画复用 ChildDocDropdown 同款样式 + 新增 `.doc-nav-sibling-item`（28px 高、hover 底色、当前文档左侧色条 + font-weight-semibold）
3. `components/DocNavContainer.vue` — 替换原 39-99 行（`.doc-nav-siblings` 整个 div）为 `<SiblingDropdown v-if="hasSiblings" :siblings="..." :sibling-count="..." .../>`；新增 `import SiblingDropdown`
4. `styles/index.scss` — 删除 `.doc-nav-siblings/.doc-nav-sibling/.doc-nav-sibling-text/.doc-nav-sibling-disabled/.doc-nav-sibling-count` 5 个旧样式块（约 44 行）
5. `icons.ts` — 新增 `docNavSiblings: { icon: "mdi:format-list-bulleted-square", color: "#8b5cf6" }`
6. i18n 分片 ×2 — 新增 `docNavSiblings`（"同级"/"Siblings"）+ `docNavSiblingPanelTitle`（"同级文档"/"Sibling Documents"）

### 验证
- `read_lints` docNavigation → 0 new error（2 个预存 HINT/ERROR 与改动无关）
- 待用户自行执行：`pnpm i18n:merge`、`pnpm i18n:verify`、`pnpm validate:icons`、`npx tsc --noEmit`、`pnpm lint`

## 修复：docNavigation 上级文档重复显示（2026-08-08）

用户反馈"上一级显示好像显示了两个位置"。根因：面包屑路径（`Root > Parent > Current`）的倒数第二项就是上级文档，而下方独立的 `doc-nav-parent` 块又渲染一遍同一条上级链接，`hasBreadcrumbs` 为 true 时上级文档出现在两个位置。

### 修复
`DocNavContainer.vue` 第 51 行：`v-if="parentDoc"` → `v-if="parentDoc && !hasBreadcrumbs"`（附中文注释）。面包屑可见时隐藏独立上级链接（面包屑已含上级）；面包屑不可见时显示（作为唯一导航入口）。仅改 1 文件 1 行。

### 验证
- `read_lints` DocNavContainer.vue → 0 error
- 无需 i18n/图标改动
