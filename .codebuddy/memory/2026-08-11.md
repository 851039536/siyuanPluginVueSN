# 2026-08-11

## statistics StatisticsHeader 右侧边距审查（结论：符合规则，无需修改）
按「Dock 面板侧边栏间距」规则审查 `StatisticsHeader.vue` + `styles/StatisticsHeader.scss` + `styles/index.scss`：
- 布局：Header/Tab-bar 是根容器 `.statistics-panel` 的固定子元素，唯一滚动容器是 `.statistics-content`
- 结论：右侧边距均达标，无 `padding: xxx 0` 省略右值违规——`.statistics-header` 右 `$spacing-4`(16px)、`.tab-bar` 右 `$spacing-3`(12px)、`.statistics-content` 右 `$spacing-2`(8px)、`.storage-paths` 右 12px，均满足≥`$spacing-2` 要求
- 教训：Header 即使脱离滚动容器，只要自身有足够右侧 padding（16px）就不算紧贴侧边栏。此前推断"Header 脱离滚动容器必紧贴"是错的，已回退无效改动
- 用户偏好：statistics 模块**不需要移动端兼容**，`@include mobile-only` 部分可忽略
- lint 零错误

审查 `src/features/quickNote/` 发现冗余与逻辑漏洞，按计划完成全部修复：

### P0 逻辑漏洞
1. **useProjects.remove() 串行锁绕过**：原 `remove()` 直接 `loadOrDefault() + save()`，绕过 `persist()` 的 `_saveLock`。修复：`persist()` 新增可选参数 `todos?: TodoItem[]`，传入时同时写 todos 槽；`remove()` 改为 `await persist(todosRef?.value)`，与 add/update 共用同一把锁。
2. **跨槽写入不一致**：删除项目需同时更新 projects + todos（projectId 置空），现统一走 `persist(todos)` 路径，消除竞态覆盖风险（项目"复活"）。

### P1 规范
3. **`.close-btn` 样式重复**：删除 `styles/index.scss` 中 `.close-btn` 完整样式块（20 行），`index.vue` 最小化按钮改用 `.qn-icon-btn`。
4. **i18n 中文注释缺失**：`index.vue` 补 4 处注释（restore/minimize/close/done），关闭按钮补 `:title="i18n.close"`。

### P2 冗余
5. **ProjectForm.vue `submitLabel` 未使用**：删除该 computed（其引用的 `computed` 也未导入，属潜在运行时错误）。
6. **i18n `close` 键缺失**：quickNote 分片无 `close` 键（i18n 仅从 `plugin.i18n?.quickNote` 读取），已在 `zh_CN/en_US/quickNote.json` 各补 `"close"` 键。

涉及文件：`composables/useProjects.ts`、`styles/index.scss`、`index.vue`、`components/project/ProjectForm.vue`、`src/i18n/{zh_CN,en_US}/quickNote.json`。

lint 全部通过。注意：`persist(todosRef?.value)` 中 `todosRef` 为 null 时传 undefined，仅写 projects（与原行为等价）。

## quickNote 项目跟进 Tab — 新增编辑修改功能（4 个文件）

用户反馈项目跟进 Tab 只有新增，无法修改已有项目。打通"卡片编辑按钮 → 表单回填 → 保存更新"链路：

1. **ProjectItem.vue**：卡片头部 actions 新增编辑按钮（铅笔图标），emit `edit` 事件
2. **ProjectForm.vue**：新增 `editingProject` prop（非 null 为编辑模式）；`watch(editingProject, { immediate })` 自动回填/清空表单；提交 payload 新增可选 `id`（编辑模式携带）；新增 `cancel` emit；新增/编辑共用组件，模式通过 prop 区分
3. **index.vue**：管理 `editingProject: Ref<ProjectItemType | null>` 状态；新增 `startEdit(project)`、`handleProjectSubmit(payload)`（id 有→update、无→add）、`handleCancelEdit`（置 null→触发 watch 清空表单）
4. **ProjectForm.scss**：保存按钮与 add-btn 合并选择器；新增取消按钮 `.qn-proj-form__cancel-btn`（灰色边框透明底 + `margin-right: auto` 左对齐）

数据流：ProjectItem emit edit → index.vue startEdit 设 editingProject → ProjectForm watch 回填 → 用户修改后保存 → index handleProjectSubmit 调 update + 置 null → ProjectForm watch 清空表单。

i18n 无需改动（edit/save/cancel 键已存在）。lint 零错误。类型冲突修复：导入 `ProjectItem as ProjectItemType`（避免与组件 `import ProjectItem` 重名）。

## quickNote 待办 Tab — 编辑 + AI 润色功能（4 个文件）

用户需求：待办也要可编辑，内容可用 AI 润色。复用 Project 编辑模式 + Inspiration 润色模式：

1. **TodoItem.vue**：actions 区域新增编辑按钮（`edit` 铅笔图标，`qn-icon-btn`），emit `edit` 事件
2. **TodoForm.vue**：新增 `plugin` + `editingTodo` prop；`watch(editingTodo, { immediate })` 回填/清空表单；emit 由 `add` 改为 `submit`（payload 可选 `id`）+ 新增 `cancel`；`useAiPolish(plugin)` + `polishText` 实现润色按钮（sparkles 图标，输入框右侧，新增/编辑模式均可用）；编辑模式按钮区为「取消 + 保存」，新增模式为「+」；Enter 统一走 `handleSubmit`（新增提交后清空、编辑提交不清空由父层 watch 复位）
3. **TodoForm.scss**：新增 `.qn-todo-form__icon-btn`（润色）、`__cancel-btn`（边框）、`__save-btn`（品牌蓝块，参考 InspirationForm/ProjectForm 样式）
4. **index.vue**：管理 `editingTodo: Ref<TodoItemType | null>`；`startTodoEdit(todo)`、`handleTodoSubmit(payload)`（id→update+置null、无→add）、`handleTodoCancel()`；两处 TodoItem（未完成分组 + 已完成列表）均绑定 `@edit`；TodoForm 传 `:plugin` `:editing-todo`

i18n 键（edit/save/cancel/polish/polishing/polishFailed/polishNoApiKey）与图标（edit/sparkles/plus）均已存在，零新增。lint 零错误。

## 项目列表空状态 Bug 修复 + editingProject 状态泄漏修复（1 个文件）

用户反馈：新增项目后在项目列表看不到，但待办关联下拉和复盘看得到。

审查发现 2 个问题：

1. **`project-list__empty` 空判断与渲染数据源不一致**（根因）：
   - 第 238 行 `v-if="projects.length === 0"` 判断的是全量项目，但列表渲染的是 `activeProjects`（排除 completed）
   - 当所有项目都是 completed 状态时：空提示不显示（projects > 0），列表也不显示（activeProjects 为空）→ 一片空白
   - 修复：`projects.length === 0` → `activeProjects.length === 0`

2. **`editingProject`/`editingTodo` 状态泄漏**（潜在 Bug）：
   - 进入编辑模式后切换 Tab，编辑状态不会被清空
   - 下次回到原 Tab，表单仍回填旧数据，用户保存会 update 旧条目而非 add 新条目
   - 修复：新增 `watch(activeTab, () => { editingProject = null; editingTodo = null })` 在 Tab 切换时自动清空
   - import 补充 `watch`

lint 零错误。仅改动 `index.vue`。

## gitPush 列表右侧遮挡修复（1 个文件）
- 问题：`.gp-list` 滚动容器 `padding-right: 4px` 不足，列表右侧内容紧贴侧边栏被遮挡
- 修复：`src/features/gitPush/styles/index.scss` 中 `.gp-list` 的 `padding-right` 改为 `$spacing-2`(8px)，与 `.gp-stats-panel`/`.gp-log-panel`/`.gpa-panel`/`.gpr-panel` 保持一致，并补充「Dock 面板侧边栏间距」规则注释
- lint 通过

## gitPush 列表右侧遮挡修复（第二次，找到根因）
- 第一次只改 padding-right 4px→8px 无效，根因：`.gp-list` 有 `width: 100% !important` 但缺 `box-sizing: border-box`，content-box 下 padding 溢出容器被 `.git-push-panel` 的 overflow:hidden 裁掉，留白永不生效
- 修复：`.gp-list` 补 `box-sizing: border-box !important`（与 `.gp-card` 一致）+ `padding-right: $spacing-2 !important`
- 教训：`width: 100%` + padding 的场景必须配 border-box，否则视觉无变化

## gitPush 列表切换分类/视图时右侧间距不一致（审查修复）
- 根因：`.gp-list` 是 `overflow-y: auto` 滚动容器，分类/视图切换导致项目数增减 → 纵向滚动条动态出现/消失（占约 10px 宽度）→ 右侧视觉留白跳动，与 padding-right 值无关
- 修复（index.scss）：`.gp-list` 加细滚动条 `&::-webkit-scrollbar { width: 4px }`，滚动条显隐时宽度占用仅 4px，差异极小；padding 统一为 `$spacing-1 $spacing-2 $spacing-1 0`（右 8px 对齐统计报告页规则）
- 注意：gitPush Dock 宽 420px，`.gp-card` min-width 390px 强制单列；若 Dock 拉宽 >786px 会出现双列+最后一行单卡拉伸的奇偶差异（未处理）

## quickNote 表单状态泄漏 + persist 竞态修复（3 个文件）

复盘确认项目列表不显示 Bug 的另外 3 个隐患，完成修复：

1. **ProjectForm.vue**：新增模式 `handleSubmit()` 提交后原只清空 name/currentStep/nextStep/blockers，未重置 `status`。若用户上次选过「已完成」，后续所有新增项目默认 completed，被 `activeProjects`（过滤 `status !== "completed"`）全部隐藏。修复：新增分支补 `status.value = "active"`。
2. **TodoForm.vue**：同类缺陷——新增后未重置 `priority`。修复：新增分支补 `priority.value = "medium"`。
3. **useTodoList.ts**：`persist()` 原为无锁 read-modify-write，而 useProjects/useInspirations 均用 `_saveLock` 串行锁共写 `quick-note-data` 槽，TodoList 无锁写入可能读到旧数据产生竞态。修复：参照 useProjects 的 `_saveLock` 链式模式为 `persist()` 加锁。

lint 零错误。

## quickNote 项目跟进 Tab — 已完成项目改为也显示（2 个文件）

用户确认根因：项目状态为「已完成」时被 `activeProjects` 的 `status !== "completed"` 过滤，列表中完全不显示，新增后无感知。需求为已完成项目也要展示，靠状态徽章（绿色"已完成"）区分即可。

1. **useProjects.ts**：`activeProjects` 更名为 `visibleProjects`，取消 completed 过滤，改为分组排序——未完成（active/blocked）在前、已完成（completed）沉底，组内按 `updatedAt` 倒序。排序在 `[...projects.value]` 副本上执行，避免原地修改响应式数组。`blockedProjects`（TodayFocus 专用）未动。
2. **index.vue**：3 处 `activeProjects` 引用同步改名 `visibleProjects`（空态判断 `v-if`、列表渲染 `v-for`、取用 `projectsApi.activeProjects`）。

ProjectItem.vue 零改动（STATUS_META 已渲染状态徽章）。全项目 `activeProjects` 无残留引用，lint 零错误。空态 `visibleProjects.length === 0` 仅在项目真正为空时成立。

## quickNote 模块代码简化重构（6 项，净减约 200 行）

对 quickNote 全面审查后按计划执行去冗余重构：

1. **types 拆分**：`types/index.ts`（213 行）拆为 `types/position.ts`（位置类型 + QUICK_NOTE_POSITIONS/POSITION_ALIGN_MAP/POSITION_MINIMIZE_META）+ `types/data.ts`（TodoItem/InspirationItem/ProjectItem/AppData + PRIORITY_META/STATUS_META），原 index.ts 仅 re-export，外部 8 处导入路径零改动。
2. **串行锁去重**：`utils.ts` 新增 `createPersistLock(data: TypedStorage<AppData>, buildPayload)` 泛型工厂（支持 TArgs 额外参数），useProjects/useTodoList/useInspirations 三处完全重复的 `_saveLock` 链式锁各缩为 1 行调用；useProjects 的可选 `todos` 参数通过 buildPayload 的第二参数实现。
3. **setTodos 消除**：`useProjects(storage)` → `useProjects(storage, todosRef?)` 构造参数注入，删除 `setTodos()` 中间方法及 index.vue 中的调用行。
4. **index.vue 拆 Tab**：517 行 → 287 行。四个 Tab 内容区拆为独立组件 `components/{todo/TodoTab,inspiration/InspirationTab,project/ProjectTab,review/ReviewTab}.vue`，各组件直接接收 composable 实例 props（`ReturnType<typeof useXxx>`）并自持编辑状态（editingTodo/editingProject 移入子组件），原 `watch(activeTab)` 清空编辑状态逻辑随之删除（组件 v-if 卸载自动重置）。
5. **CSS @extend 去重**：`styles/index.scss` 新增 5 个占位类 `%qn-icon-btn-28` / `%qn-primary-btn` / `%qn-cancel-btn` / `%qn-helper-text` / `%qn-empty-state`，6 个子 SCSS 文件加 `@use "./index.scss" as *` 后 @extend 复用；子类差异属性（hover 色、尺寸、padding）写在 extend 之后覆盖。
6. **README 更新**：目录职责同步新结构。

关键验证：`@extend` 跨模块 @use 占位类语法用临时脚本（sass compileStringAsync）验证通过（基础属性展开 + 子类 hover 覆盖均正确），已删除临时脚本。lint 零错误。SCSS 中 `@/variables.scss` 由 Vite alias 解析到 `src/_variables.scss`（sass partial 探测）。

## 新增功能模块路径别名（3 个文件 + 2 个规则文档）

为 `src/features/` 下全部 40 个功能模块新增 `@<featureName>` 别名（如 `@gitPush` → `src/features/gitPush`），便于模块内短路径引用：

1. **vite.config.ts** `resolve.alias`：新增 40 个 `@<featureName>` → `resolve(__dirname, "src/features/<featureName>")`，含顶部注释说明"新增模块需同步四处"
2. **tsconfig.json** `compilerOptions.paths`：新增 40 个 `@<featureName>/*` → `./src/features/<featureName>/*`（带 `/*` 通配符）
3. **AGENTS_RULES.md**：标题下新增「## 路径别名」章节——别名对照表（`@/`、`@/libs/`、`@<featureName>`）、40 个别名清单、gitPush 使用示例、强制规则（新增模块必须同步注册两处并更新清单）
4. **CODEBUDDY.md**：「构建流程」补充别名说明；硬规则新增「路径别名同步」条目，链到 AGENTS_RULES.md § 路径别名

别名清单（40 个）：aiContentGenerator、apiDebugger、bookmarkMarker、compactMode、dataSnapshot、diskBrowser、docAnalysis、docNavigation、encryption、everythingSearch、flashcardReading、floatingBox、floatingToolbar、formatAssistant、generalSettings、gitPush、htmlViewer、imageCompressor、imageCreation、pageLock、passwordVault、prompts、quickNote、resourceManager、rssReader、s3Backup、s3FileManager、scriptLauncher、shortcut、skillLearning、skillsViewer、statistics、statusBar、superPanel、tableOfContents、textDiff、themeColor、toolCollection、video、websiteNavigation。

lint 通过（vite.config.ts 0 error）。注意 tsconfig 别名需带 `/*` 通配符，vite alias 不需要。

## quickNote 最小化后展开无响应 Bug 修复（1 个文件）

用户反馈：将速记弹窗拖到顶部后点击最小化，再点击小条无法展开，点击无反应。

**根因**：`applyMinimized(false)` 展开分支（index.ts 第149-156行）只还原了容器尺寸/遮罩样式，未调用 `applyPosition()` 重新应用位置。拖拽后容器残留 `position:absolute` 的旧坐标（最小化小条坐标），与展开后 624x70vh 尺寸不匹配，导致面板定位异常/点击穿透。

**修复**：在 `applyMinimized()` 的 `else` 分支末尾新增 `this.applyPosition()`——custom 模式重新 clamp 坐标设置 absolute+left/top，预设模式清除 absolute 残留并改回 flex 对齐。仅改 1 处，lint 零错误。

## quickNote 弹窗卡死「恢复」兜底按钮（6 个文件）

用户反馈：弹窗拖到顶部后点击无反应、无法拖动。不再排查根因，改为新增状态栏「恢复默认位置」兜底按钮，一键复位弹窗为居中展开态：

1. **quickNote/index.ts**：`QuickNoteManager` 新增 `async reset()` 方法——重置 `position="center"`/`customX=0`/`customY=0`/`minimized=false`，复用 `open()`（内部依次 `modal.open → applyPosition → applyMinimized`，展开分支自动清除绝对定位残留、还原遮罩交互）后 `persistSettings()` 持久化
2. **statusBar/index.vue**：快捷入口循环后新增恢复按钮（`ph:arrow-counter-clockwise` 图标，`quick-note-reset-item` 类），`v-if="isFeatureEnabled('quickNote')"` 控制显隐，点击 `handleResetQuickNote()` 派发 `resetQuickNote` 事件；tooltip 用 `quickNoteResetTitle` computed（i18n `quickNote.reset`）
3. **statusBar/styles/index.scss**：新增 `.quick-note-reset-item { color: var(--b3-theme-warning) }`（警示色提示兜底操作）
4. **App.vue**：onMounted 在 `toggleQuickNote` 监听旁新增 `resetQuickNote` → `plugin.__quickNote?.reset()`（可选链短路）
5. **i18n 分片**：zh_CN/en_US `quickNote.json` 各新增 `"reset"` 键（"恢复默认位置"/"Reset position"）
6. **quickNote/README.md**：新增「恢复兜底」小节 + 联动链路补充 resetQuickNote 链路

lint 零错误。事件链路与 `toggleQuickNote` 完全同构，复用现有 applyPosition/applyMinimized/open/persistSettings，未复制定位逻辑。

## 速记恢复按钮迁移到工具合集 + 最小化小条贴左修复（4 项）

用户两个需求：(1) 状态栏恢复按钮迁移到工具合集；(2) 弹窗拖到最右边展开后最小化，小条停在最右侧而非期望的最左边。

1. **最小化贴左修复（quickNote/index.ts）**：`applyMinimized()` minimized 分支在 `position === "custom"` 时清除容器 `position/left/top` 内联样式（回归遮罩 flex 流），设 `mask.alignItems="center"`/`justifyContent="flex-start"`/`padding=EDGE_PADDING`，小条横条贴最左垂直居中；customX/customY 缓存保留，展开分支 `applyPosition()` 仍还原拖拽坐标。仅加 10 行。
2. **新增工具合集「速记恢复」工具**：新建 `toolCollection/tools/quickNoteReset/`（index.vue + styles/index.scss，Codex 规范），i18n 分片 `src/i18n/{zh_CN,en_US}/quickNoteReset.json`（title/description/reset/done），`registry.ts` 注册（icon: `ph:arrow-counter-clockwise`，label key 映射）。按钮点击 `emitCustomEvent("resetQuickNote")` + 2 秒「已恢复」绿色反馈。
3. **状态栏清理**：删除 statusBar/index.vue 恢复按钮模板块、`quickNoteResetTitle` computed、`handleResetQuickNote` 函数（`quickNoteI18n` 保留，FEATURES quickNote 条目仍用）；删除 styles/index.scss `.quick-note-reset-item`；quickNote.json 删 `reset` 键（中英）。
4. **README 同步**：toolCollection/README 工具表格加「PDF 查看」「速记恢复」两行；quickNote/README 恢复兜底入口改工具合集、联动链路改 toolCollection 工具 → resetQuickNote、最小化机制补 custom 贴左说明。

事件链路唯一：quickNoteReset 工具 → emitCustomEvent("resetQuickNote") → App.vue → `plugin.__quickNote?.reset()`。statusBar 现有 9 个 ts-plugin 错误（`props.plugin?.i18n?.xxx` 类型访问）为 pre-existing，非本次引入。AI 不执行 lint/build，由用户验证。

## 速记恢复入口从工具合集迁回状态栏功能列表抽屉（4 项）

用户需求（与上次"迁移到工具合集"反向）：「恢复」入口集成到状态栏功能列表抽屉（FeatureDrawer），并移除工具合集入口，避免重复。

1. **statusBar/index.vue**：新增 `quickNoteResetI18n` 显式收窄常量（同 quickNoteI18n 模式）；FEATURES 新增 `quickNoteReset` 条目——icon `ph:arrow-counter-clockwise`、color `#f59e0b`、`pinnable: false`（应急兜底不固定状态栏）、action `() => emitCustomEvent("resetQuickNote")`。`isFeatureEnabled("quickNoteReset")` 因 settings 无 `enableQuickNoteReset` 键返回 `undefined !== false` → 恒启用，符合兜底定位。
2. **toolCollection/tools/registry.ts**：删除 quickNoteReset 的 import、TOOL_REGISTRY 条目、TOOL_LABEL_KEYS 条目；删除 `tools/quickNoteReset/index.vue` + `styles/index.scss` 两个文件。
3. **i18n 分片精简**：`src/i18n/{zh_CN,en_US}/quickNoteReset.json` 从 title/description/reset/done 精简为仅 `title`（工具 UI 键随组件删除，状态栏只需标题）。
4. **README 同步**：toolCollection/README 工具表格删「速记恢复」行；quickNote/README「恢复兜底」与「联动链路」入口改为状态栏功能列表抽屉。

事件链路不变：抽屉条目 → emitCustomEvent("resetQuickNote") → App.vue → `plugin.__quickNote?.reset()`。残留引用检查通过（仅 i18n 分片 + statusBar）。statusBar 9 个 ts-plugin 错误仍为 pre-existing，本次无新增。AI 不执行 lint/build，由用户验证。

## gitPush 提交分析新增「代码行数统计」排行榜（10 个文件）

用户需求：提交分析页面分析全部项目的代码行数并生成排行榜。

核心设计：工具栏新增「行数统计」toggle（默认关闭，避免 numstat 开销），开启后 `runAnalysis()` 对每项目并行执行 `getCommitLog` + `getNumstatLog(path, undefined, commitCount)`，聚合成**项目行数排行**（+新增/-删除/净增，按新增行降序）+ **作者行数排行**。净增正绿负红（`--b3-theme-success`/`--b3-theme-error`）。

改动清单：
1. **types/meta.ts**：`CommitAnalysisStats` + `CommitAnalysisCache` 各新增 `projectLineRanking`/`authorLineRanking` 字段；新增 `LineRankBase`/`ProjectLineRankItem`/`AuthorLineRankItem` 接口
2. **reportMetrics.ts**：新增纯函数 `sumProjectLines(commits)`（单项目增删行汇总）、`sumAuthorLines(commits)`（Map<作者,{added,deleted}>）
3. **managers/ReportOps.ts**：`getNumstatLog` 新增第 3 参 `maxCount?: number`，传入时插入 `-${maxCount}`（向后兼容，code report 调用不受影响）
4. **GitPushManager.ts**：`getNumstatLog` 透传 `maxCount`
5. **types/storage.ts**：`DEFAULT_ANALYSIS_CACHE` 补新字段；新增槽位 `commitAnalysisLineToggle: TypedStorage<boolean>`（键 `git-push-analysis-line-toggle`，独立于缓存）
6. **composables/useCommitAnalysis.ts**：新增 `enableLineCount` ref + `loadLineToggle()`/`setEnableLineCount()`（开启且已有结果时自动重跑，关闭仅清空本地排行）；`runAnalysis()` 内 numstat 并行抓取（失败降级为空不阻塞）；`buildLineRankings()` 聚合排序；`loadCachedAnalysis()` 恢复排行（旧缓存空数组兜底）
7. **CommitAnalysisPanel.vue**：工具栏条数选择与「开始分析」按钮间插入 toggle 按钮（`mdi:code-tags` 图标，`is-active` 高亮）；底部新增双栏行数排行区块（`v-if="enableLineCount"`）；`toLineRows()` 计算条形宽度（按新增行相对最大）；`netClass()` 语义着色
8. **styles/CommitAnalysisPanel.scss**：`.gpa-line-toggle`（ghost 按钮 + 激活态）、`.gpa-line-nums`/`.gpa-line-num--add/--del/--net`、`.gpa-net--pos/--neg/--zero`（fill 与数字共用）
9. **index.vue**：解构 `enableLineCount: analysisEnableLineCount`/`setEnableLineCount`，传递 `:enable-line-count` + `@toggle-line-count`
10. **i18n**：zh_CN/en_US `gitPush.json` 各新增 7 键（analysisLineToggle/analysisLineToggleTip/analysisLineProjectRanking/analysisLineAuthorRanking/analysisLineAdded/analysisLineDeleted/analysisLineNet）；README.md 补 `getNumstatLog` API 行

lint 零错误。验证（用户执行）：`pnpm i18n:verify`、`pnpm lint`、`npx tsc --noEmit`、`pnpm validate:icons`（无新图标）。

## gitPush 行数统计独立为「行数统计」Tab + 数字粘连 bug 修复（10 个文件）

用户需求：(1) 行数统计从提交分析面板独立为新的顶部 Tab；(2) 修复数字显示粘连 `+19323232-213123432213`（三列无间距）。

核心设计：`PanelView` 新增 `"linestats"`；`useCommitAnalysis` 抽私有 `runCore(needNumstat)`（提交分析与行数统计共用核心，杜绝复制），`runAnalysis()` 仅抓 commit log（保存缓存时沿用旧缓存行数排行，不覆盖），`runLineStatsAnalysis()` 始终抓 numstat 重算排行并覆盖缓存行数数据；删除 toggle 机制（`enableLineCount`/`setEnableLineCount`/`loadLineToggle` + storage 槽位 `commitAnalysisLineToggle`）。

改动清单：
1. **types/meta.ts**：`PanelView` 新增 `"linestats"`；`CommitAnalysisStats` 行数排行注释更新（去掉"启用"措辞）
2. **types/storage.ts**：删除 `commitAnalysisLineToggle` 槽位（键 `git-push-analysis-line-toggle`）
3. **composables/useCommitAnalysis.ts**：`runCore(needNumstat)` 合并原 runAnalysis numstat 分支；导出 `runLineStatsAnalysis`/`ensureLineStats`/`failedCount`/`projectLineRanking`/`authorLineRanking`；`setCommitCount(n, needNumstat?)`（行数视图改条数传 true 同步重抓 numstat，修复"排行与条数不一致"）
4. **CommitAnalysisPanel.vue**：移除 toggle 按钮、底部双栏行数区块、`enableLineCount` prop、`toggleLineCount` emit、`netClass`/`toLineRows`/`lineProjectRows`/`lineAuthorRows`
5. **LineStatsPanel.vue（新）**：独立 Tab 组件——工具栏（条数选择 30/50/100/200 + 「开始行数分析」按钮 + 状态文案）+ 双栏排行（项目/作者）；数字 `toLocaleString()` 千位分隔
6. **LineStatsPanel.scss（新）**：`.gls-*` 前缀复用 bar-list 模式；数字列 `.gls-line-num + .gls-line-num { margin-left: $spacing-2 }`（相邻兄弟 margin 替代 flex gap，不依赖 gap 支持根除粘连）+ 移除固定 `min-width:44px`（改 `flex-shrink:0; white-space:nowrap` 自适应）
7. **PanelHeader.vue**：log 与 report 之间插入第 6 个 Tab 按钮（`mdi:code-tags`，tooltip `lineStatsView`）
8. **index.vue**：解构更新（移除 toggle，新增 `runLineStatsAnalysis`/`ensureLineStats`/`failedCount`/`projectLineRanking`/`authorLineRanking`）；LineStatsPanel 渲染（`@run-analysis="runLineStatsAnalysis"`、`@update-count="(n) => setCommitCount(n, true)"`）；`watch(currentView)` 与 `watch(gitOpsPaused)` 各补 `"linestats"` 分支（复用缓存，不自动分析）
9. **CommitAnalysisPanel.scss**：删除 `.gpa-line-toggle`/`.gpa-line-nums`/`.gpa-line-num*`/`.gpa-net--*`（迁移到 LineStatsPanel.scss）
10. **i18n**：zh_CN/en_US `gitPush.json` 删 `analysisLineToggle`/`analysisLineToggleTip`，新增 `lineStatsView`/`lineStatsRun`/`lineStatsNotRun`/`lineStatsNoData` 4 键；README.md 更新功能列表/目录结构/使用说明

设计要点：行数统计 Tab 进入视图仅复用缓存（`ensureLineStats` 只调 `loadCachedAnalysis`），无缓存需手动点「开始行数分析」——与提交分析的"无缓存自动分析"刻意不同。lint 零错误。验证（用户执行）：`pnpm i18n:verify`、`pnpm lint`、`npx tsc --noEmit`、`pnpm validate:icons`（无新图标）。
