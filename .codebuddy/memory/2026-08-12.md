# 2026-08-12

## gitPush 行数统计面板单栏布局 + 汇总卡片优化
用户需求："改为一行一列，ui界面优化丰富一点点"，经澄清确认方向为双栏改单栏堆叠 + 顶部汇总统计卡片 + 轻微润色。改动 3 个文件：

1. **LineStatsPanel.vue**
   - 文件头注释更新为"顶部汇总卡片 + 单栏堆叠的项目/作者代码行数排行"
   - 新增 `summary` computed：对 projectRanking 累加 added/deleted，net = added − deleted（无需后端/composable 改动）
   - 模板在 `.gls-pair` 最顶部新增 `.gls-cards` 区：3 张卡片（总新增绿 / 总删除红 / 总净增随正负动态变色，复用 netClass()），数值用 toLocaleString() 千位分隔
   - 卡片区位于 v-else 有数据分支内，无数据时不渲染全 0 卡片

2. **LineStatsPanel.scss**
   - `.gls-pair` 从 `grid-template-columns: repeat(2, minmax(0, 1fr))` 改为 `flex column` 单栏堆叠
   - 新增 `.gls-cards/.gls-card/.gls-card-value/.gls-card-label` 样式，复用 CommitAnalysisPanel `.gpa-card` 视觉模式（border + $radius-base + surface 背景 + $vp-mono 大号数值 + 大写弱化标签），数值用 $font-size-lg 已加"数据突出展示"注释
   - 条形轨道高度 4px→5px、圆角 2px 硬编码→$radius-sm token
   - 全部符合 Codex UI 规范（token-first、border 而非 box-shadow、0.12s、数字后缀 spacing）

3. **i18n 分片**：zh_CN/en_US gitPush.json 在 lineStatsNoData 后新增 lineStatsTotalAdded（总新增/Total Added）、lineStatsTotalDeleted（总删除/Total Deleted）、lineStatsTotalNet（总净增/Net Lines）

lint 0 error。验证（用户执行）：pnpm i18n:verify / pnpm lint / npx tsc --noEmit。

## gitPush 分析流程审查（仅审查，未改代码）
用户要求审查分析流程执行路径与优化空间，最终选择"只要审查结论"。三条链路已梳理：提交分析（ensureAnalysis→缓存复用→runCore(false)）、行数统计（ensureLineStats→手动触发→runCore(true)）、代码统计报告（ensureReport→每次自动生成）。

发现 6 个优化点（用户后续如实施优先 A+B）：
- **A（高）**：runCore(true) 每项目跑 getCommitLog + getNumstatLog 两条命令。合并方案：getNumstatLog 的 pretty format 追加 %h%x1f%s，NumstatCommit 加 hash/message 字段，行数统计直接由 numstat 构造 entries 省掉 getCommitLog（2N→N），提交分析 runCore(false) 保持原样
- **B（高）**：failedCount 恒为 0。根因：getCommitLog 内部 try-catch 返回 []、getNumstatLog 失败被本地 catch 置 []、resolveValidPath 降级不抛错，allSettled rejected 分支永不触发。方案：runCore 预检 fs.existsSync 或让 getCommitLog/getNumstatLog 返回成功标记
- C（中）：reportMetrics.ts 同步 fs 阻塞 UI（fileExistsInRepo statSync 全遍历 + countFileLines readFileSync + fetchFileDiff execFileSync），建议异步化
- D（中）：缓存只过滤已删项目，新增项目不自动纳入，需新鲜度检测
- E（低）：行数统计无缓存时不自动分析，与提交分析体验不一致
- F（低）：分析无进度反馈

## gitPush 分析优化实施（A 合并抓取 + B 失败计数修复）
用户确认实施，且要求"新开分析方法"隔离影响面。改动 4 个源文件 + 1 个 README：

1. **reportMetrics.ts**：`NumstatCommit` 新增可选 `hash?`/`message?` 字段；`parseNumstatBlocks` 升级为 header 段数自适应解析——4 段（`%x1e%h%x1f%an%x1f%aI%x1f%s`）解析 hash/author/date/message（message 用 slice(3).join 防 \x1f），2 段（旧 `%x1e%an%x1f%aI`）走原逻辑。抽公共 parseFileLines。报告视图旧格式零影响
2. **ReportOps.ts**：新增 `getCommitStatsLog(projectPath, maxCount?)` 独立方法（format 追加 %h/%s，60s 超时）；`getNumstatLog` 保持不动
3. **GitPushManager.ts**：透传 getCommitStatsLog，getNumstatLog 注释更新
4. **useCommitAnalysis.ts runCore**：① 项目循环开头 `getNodeFsPathOs().fs.existsSync(path)` 预检，路径无效直接 throw 计入 failedCount（绕过 getCommitLog 内部吞错）；② needNumstat 分支改用 getCommitStatsLog 单命令构造 entries（hash/message/author/date 来自 numstat）+ 行数数据，删除 getCommitLog + getNumstatLog 双命令与本地 try-catch 降级（git 失败也计入 failedCount）；提交分析分支保持 getCommitLog 原样
5. **README.md**：方法清单补充 getCommitStatsLog

行为变化：行数统计每项目 git 命令 2N→N；失败项目（路径无效/git 异常）从静默降级改为计入 failedCount 触发 gls-fail-hint/gpa-fail-hint 提示条。lint 0 error。验证（用户执行）：pnpm lint / npx tsc --noEmit，回归行数统计数值一致性 + 失败提示条。

## gitPush 分析按钮旋转反馈修正（最终方案）
用户反馈"点击开始分析按钮图标会旋转"且"旋转的不是旋转图标，导致很别扭"——即旋转业务静态图标（图表/代码标签）违和。最终修正方案：分析中**切换为环形 `mdi:loading` 图标 + 保留 `gp-spin` 旋转**（标准 loading 反馈），分析结束恢复原业务图标。修改 2 个文件：
- CommitAnalysisPanel.vue：`icon="mdi:chart-timeline-variant"` + 无条件 `gp-spin` → `:icon="analyzing ? 'mdi:loading' : 'mdi:chart-timeline-variant'"` + `:class="{ 'gp-spin': analyzing }"`
- LineStatsPanel.vue：同样改为 analyzing 时切换 `mdi:loading` 并旋转，恢复 `mdi:code-tags`

关键认知：旋转对象应为环形 loading 图标而非业务图标；若旋转业务图标违和，若 loading 图标静止也同样别扭。按钮 `:disabled="analyzing"` 保持。gitPush 其他按钮（push/pull/refresh 等）沿用同一 gp-spin 模式，未改。lint 0 error。

## 新增强制规则：请求中的加载反馈
将 gitPush 分析按钮的加载反馈经验上升为项目级强制规则，写入 AGENTS_RULES.md + CODEBUDDY.md（硬规则区）。
规则要点：发起请求（AI/网络/耗时操作）时按钮图标切换为环形 `mdi:loading` 并旋转（`animation: spin 1s linear infinite`），期间 `disabled` 防重复提交，结束（成功/失败/超时）还原图标与禁用态；旋转动画放 SCSS 文件、禁止内联 style 硬编码；图标需在 icons.ts 已注册（`mdi:loading` 已在 src/config/icons.ts 第 472 行注册，无需新增）。CODEBUDDY.md 引用锚点 `#强制规则请求中的加载反馈`，与 AGENTS_RULES.md 新章节保持一致。

## gitPush 行数统计增加文件格式过滤功能
用户需求：为行数统计视图增加可选的「文件格式过滤」——工具栏新增多选芯片按钮，按文件扩展名（.ts/.vue/.js/.scss/.css/.json/.md/.html/.jsx/.tsx）白名单过滤统计结果。不选择任何扩展名时不过滤（维持现有行为）。改动 9 个文件：

1. **reportMetrics.ts**：新增 `shouldIncludeFile(filePath, extensions?)` 辅助函数；`sumProjectLines(commits, extensions?)` 和 `sumAuthorLines(commits, extensions?)` 新增可选 `extensions` 参数，遍历文件时调用 `shouldIncludeFile` 过滤。未传/空数组时全部通过，向后兼容。
2. **types/meta.ts**：`LineStatsCache` 接口新增 `selectedExtensions: string[]` 字段
3. **types/storage.ts**：`DEFAULT_LINE_STATS_CACHE` 新增 `selectedExtensions: []`
4. **useCommitAnalysis.ts**：新增 `LINE_STATS_EXTENSIONS` 常量（10 种常见扩展名 as const）；新增 `selectedExtensions` ref（初始 []）；`buildLineRankings` 新增 `extensions?` 参数透传给聚合函数；`runCore` 中 `buildLineRankings` 调用传入 `selectedExtensions.value` + `lineStatsCache.save` 持久化 `selectedExtensions`；`loadLineStatsCache` 恢复 `selectedExtensions`（`??` 降级兼容旧缓存）；`return` 导出 `selectedExtensions`
5. **LineStatsPanel.vue**：工具条 `gls-toolbar-right` 内新增 `.gls-ext-chips` 芯片按钮组（在 `<select>` 之前），`v-for` 遍历 `LINE_STATS_EXTENSIONS` 生成 chip，`:class` 动态绑定 `gls-ext-chip--active`；script 新增 `selectedExtensions` prop + `update:selectedExtensions` emit + `toggleExt` 函数（Set 多选切换）
6. **index.vue**：`useCommitAnalysis` 解构新增 `selectedExtensions`；LineStatsPanel 绑定 `:selected-extensions` prop + `@update:selected-extensions` 事件回写
7. **LineStatsPanel.scss**：新增 `.gls-ext-chips`（flex + gap 2px）、`.gls-ext-chip`（$font-size-2xs + $vp-mono + border + $radius-sm + 过渡 + focus-ring + disabled 半透明）、`.gls-ext-chip--active`（primary-lightest 背景 + primary 边框/文字）
8. **zh_CN/gitPush.json**：新增 `lineStatsExtFilter`: "文件格式过滤：勾选后仅统计匹配格式的文件，不选则统计所有文件"
9. **en_US/gitPush.json**：新增 `lineStatsExtFilter`: "File filter: only count matching extensions when selected, count all files when empty"

设计要点：过滤发生在内存聚合层（不修改 git 命令）；selectedExtensions 变更不自动重分析（仅下次手动分析时生效）；选中状态通过 lineStatsCache 持久化；旧缓存无此字段自动降级为 []（向后兼容）。lint 0 error。验证（用户执行）：pnpm lint / pnpm i18n:verify / npx tsc --noEmit。
