# 2026-08-12

## gitPush 行数统计面板单栏布局 + 汇总卡片优化
用户需求："改为一行一列，ui界面优化丰富一点点"，经澄清确认方向为双栏改单栏堆叠 + 顶部汇总统计卡片 + 轻微润色。改动 3 个文件：

1. **LineStatsPanel.vue**
   - 文件头注释更新为"顶部汇总卡片 + 单栏堆叠的项目/作者代码行数排行"
   - 新增 `summary` computed：对 projectRanking 累加 added/deleted，net = added − deleted（无需后端/composable 改动）
   - 模板在 `.gls-pair` 最顶部新增 `.gls-cards` 区：3 张卡片（总新增绿 / 总删除红 / 总净增随正负动态变色，复用 netClass()），数值用 toLocaleString() 千位分隔
   - 卡片区位于 v-else 有数据分支内，无数据时不渲染全 0 卡片

2. **LineStatsPanel.scss**
   - `.gls-pair` 从 `grid-template-columns: repeat(2, minmax(0, 1fr))` 改为 `flex column` 单栏堆叠
   - 新增 `.gls-cards/.gls-card/.gls-card-value/.gls-card-label` 样式，复用 CommitAnalysisPanel `.gpa-card` 视觉模式（border + $radius-base + surface 背景 + $vp-mono 大号数值 + 大写弱化标签），数值用 $font-size-lg 已加"数据突出展示"注释
   - 条形轨道高度 4px→5px、圆角 2px 硬编码→$radius-sm token
   - 全部符合 Codex UI 规范（token-first、border 而非 box-shadow、0.12s、数字后缀 spacing）

3. **i18n 分片**：zh_CN/en_US gitPush.json 在 lineStatsNoData 后新增 lineStatsTotalAdded（总新增/Total Added）、lineStatsTotalDeleted（总删除/Total Deleted）、lineStatsTotalNet（总净增/Net Lines）

lint 0 error。验证（用户执行）：pnpm i18n:verify / pnpm lint / npx tsc --noEmit。

## gitPush 分析流程审查（仅审查，未改代码）
用户要求审查分析流程执行路径与优化空间，最终选择"只要审查结论"。三条链路已梳理：提交分析（ensureAnalysis→缓存复用→runCore(false)）、行数统计（ensureLineStats→手动触发→runCore(true)）、代码统计报告（ensureReport→每次自动生成）。

发现 6 个优化点（用户后续如实施优先 A+B）：
- **A（高）**：runCore(true) 每项目跑 getCommitLog + getNumstatLog 两条命令。合并方案：getNumstatLog 的 pretty format 追加 %h%x1f%s，NumstatCommit 加 hash/message 字段，行数统计直接由 numstat 构造 entries 省掉 getCommitLog（2N→N），提交分析 runCore(false) 保持原样
- **B（高）**：failedCount 恒为 0。根因：getCommitLog 内部 try-catch 返回 []、getNumstatLog 失败被本地 catch 置 []、resolveValidPath 降级不抛错，allSettled rejected 分支永不触发。方案：runCore 预检 fs.existsSync 或让 getCommitLog/getNumstatLog 返回成功标记
- C（中）：reportMetrics.ts 同步 fs 阻塞 UI（fileExistsInRepo statSync 全遍历 + countFileLines readFileSync + fetchFileDiff execFileSync），建议异步化
- D（中）：缓存只过滤已删项目，新增项目不自动纳入，需新鲜度检测
- E（低）：行数统计无缓存时不自动分析，与提交分析体验不一致
- F（低）：分析无进度反馈

## gitPush 分析优化实施（A 合并抓取 + B 失败计数修复）
用户确认实施，且要求"新开分析方法"隔离影响面。改动 4 个源文件 + 1 个 README：

1. **reportMetrics.ts**：`NumstatCommit` 新增可选 `hash?`/`message?` 字段；`parseNumstatBlocks` 升级为 header 段数自适应解析——4 段（`%x1e%h%x1f%an%x1f%aI%x1f%s`）解析 hash/author/date/message（message 用 slice(3).join 防 \x1f），2 段（旧 `%x1e%an%x1f%aI`）走原逻辑。抽公共 parseFileLines。报告视图旧格式零影响
2. **ReportOps.ts**：新增 `getCommitStatsLog(projectPath, maxCount?)` 独立方法（format 追加 %h/%s，60s 超时）；`getNumstatLog` 保持不动
3. **GitPushManager.ts**：透传 getCommitStatsLog，getNumstatLog 注释更新
4. **useCommitAnalysis.ts runCore**：① 项目循环开头 `getNodeFsPathOs().fs.existsSync(path)` 预检，路径无效直接 throw 计入 failedCount（绕过 getCommitLog 内部吞错）；② needNumstat 分支改用 getCommitStatsLog 单命令构造 entries（hash/message/author/date 来自 numstat）+ 行数数据，删除 getCommitLog + getNumstatLog 双命令与本地 try-catch 降级（git 失败也计入 failedCount）；提交分析分支保持 getCommitLog 原样
5. **README.md**：方法清单补充 getCommitStatsLog

行为变化：行数统计每项目 git 命令 2N→N；失败项目（路径无效/git 异常）从静默降级改为计入 failedCount 触发 gls-fail-hint/gpa-fail-hint 提示条。lint 0 error。验证（用户执行）：pnpm lint / npx tsc --noEmit，回归行数统计数值一致性 + 失败提示条。

## gitPush 分析按钮旋转反馈修正（最终方案）
用户反馈"点击开始分析按钮图标会旋转"且"旋转的不是旋转图标，导致很别扭"——即旋转业务静态图标（图表/代码标签）违和。最终修正方案：分析中**切换为环形 `mdi:loading` 图标 + 保留 `gp-spin` 旋转**（标准 loading 反馈），分析结束恢复原业务图标。修改 2 个文件：
- CommitAnalysisPanel.vue：`icon="mdi:chart-timeline-variant"` + 无条件 `gp-spin` → `:icon="analyzing ? 'mdi:loading' : 'mdi:chart-timeline-variant'"` + `:class="{ 'gp-spin': analyzing }"`
- LineStatsPanel.vue：同样改为 analyzing 时切换 `mdi:loading` 并旋转，恢复 `mdi:code-tags`

关键认知：旋转对象应为环形 loading 图标而非业务图标；若旋转业务图标违和，若 loading 图标静止也同样别扭。按钮 `:disabled="analyzing"` 保持。gitPush 其他按钮（push/pull/refresh 等）沿用同一 gp-spin 模式，未改。lint 0 error。

## 新增强制规则：请求中的加载反馈
将 gitPush 分析按钮的加载反馈经验上升为项目级强制规则，写入 AGENTS_RULES.md + CODEBUDDY.md（硬规则区）。
规则要点：发起请求（AI/网络/耗时操作）时按钮图标切换为环形 `mdi:loading` 并旋转（`animation: spin 1s linear infinite`），期间 `disabled` 防重复提交，结束（成功/失败/超时）还原图标与禁用态；旋转动画放 SCSS 文件、禁止内联 style 硬编码；图标需在 icons.ts 已注册（`mdi:loading` 已在 src/config/icons.ts 第 472 行注册，无需新增）。CODEBUDDY.md 引用锚点 `#强制规则请求中的加载反馈`，与 AGENTS_RULES.md 新章节保持一致。

## gitPush 行数统计增加文件格式过滤功能
用户需求：为行数统计视图增加可选的「文件格式过滤」——工具栏新增多选芯片按钮，按文件扩展名（.ts/.vue/.js/.scss/.css/.json/.md/.html/.jsx/.tsx）白名单过滤统计结果。不选择任何扩展名时不过滤（维持现有行为）。改动 9 个文件：

1. **reportMetrics.ts**：新增 `shouldIncludeFile(filePath, extensions?)` 辅助函数；`sumProjectLines(commits, extensions?)` 和 `sumAuthorLines(commits, extensions?)` 新增可选 `extensions` 参数，遍历文件时调用 `shouldIncludeFile` 过滤。未传/空数组时全部通过，向后兼容。
2. **types/meta.ts**：`LineStatsCache` 接口新增 `selectedExtensions: string[]` 字段
3. **types/storage.ts**：`DEFAULT_LINE_STATS_CACHE` 新增 `selectedExtensions: []`
4. **useCommitAnalysis.ts**：新增 `LINE_STATS_EXTENSIONS` 常量（10 种常见扩展名 as const）；新增 `selectedExtensions` ref（初始 []）；`buildLineRankings` 新增 `extensions?` 参数透传给聚合函数；`runCore` 中 `buildLineRankings` 调用传入 `selectedExtensions.value` + `lineStatsCache.save` 持久化 `selectedExtensions`；`loadLineStatsCache` 恢复 `selectedExtensions`（`??` 降级兼容旧缓存）；`return` 导出 `selectedExtensions`
5. **LineStatsPanel.vue**：工具条 `gls-toolbar-right` 内新增 `.gls-ext-chips` 芯片按钮组（在 `<select>` 之前），`v-for` 遍历 `LINE_STATS_EXTENSIONS` 生成 chip，`:class` 动态绑定 `gls-ext-chip--active`；script 新增 `selectedExtensions` prop + `update:selectedExtensions` emit + `toggleExt` 函数（Set 多选切换）
6. **index.vue**：`useCommitAnalysis` 解构新增 `selectedExtensions`；LineStatsPanel 绑定 `:selected-extensions` prop + `@update:selected-extensions` 事件回写
7. **LineStatsPanel.scss**：新增 `.gls-ext-chips`（flex + gap 2px）、`.gls-ext-chip`（$font-size-2xs + $vp-mono + border + $radius-sm + 过渡 + focus-ring + disabled 半透明）、`.gls-ext-chip--active`（primary-lightest 背景 + primary 边框/文字）
8. **zh_CN/gitPush.json**：新增 `lineStatsExtFilter`: "文件格式过滤：勾选后仅统计匹配格式的文件，不选则统计所有文件"
9. **en_US/gitPush.json**：新增 `lineStatsExtFilter`: "File filter: only count matching extensions when selected, count all files when empty"

设计要点：过滤发生在内存聚合层（不修改 git 命令）；selectedExtensions 变更不自动重分析（仅下次手动分析时生效）；选中状态通过 lineStatsCache 持久化；旧缓存无此字段自动降级为 []（向后兼容）。lint 0 error。验证（用户执行）：pnpm lint / pnpm i18n:verify / npx tsc --noEmit。

### 迭代修正：chip 直显改为「过滤按钮 + 配置弹窗」，扩展名扩充
用户反馈"过滤内容不是直接显示出来，点击弹出配置页面，增加.dll,sdb,pdb,.ini,.xml,等"。对上一版 chip 直显方案做了重构，改动 8 个文件：

1. **useCommitAnalysis.ts**：`LINE_STATS_EXTENSIONS` 扩充为 19 种（原 10 种 + .cs/.xaml/.py/.go/.dll/.sdb/.pdb/.ini/.xml）；新增 `updateSelectedExtensions(exts)` 方法——更新 ref 并即时持久化到 lineStatsCache（修复了原实现"只改扩展名不重分析时选择不持久化、切换视图丢失"的缺陷）；return 导出
2. **新增 ExtFilterDialog.vue**（components/analysis/）：配置弹窗，mask + dialog 模式（仿 ConfirmDialog），含标题/说明/全选/清空/已选计数/19 个扩展名 chip 网格（5 列）/取消+应用按钮；内部维护 draft 副本，apply 时 emit；`useDialogKeyboard()` 支持 Esc 关闭；父以 v-if 挂载每次打开为新实例
3. **新增 ExtFilterDialog.scss**：.gfe-mask/.gfe-dialog/.gfe-header/.gfe-hint/.gfe-actions/.gfe-grid（repeat(5,1fr)）/.gfe-chip(+--active)/.gfe-footer，沿用 Codex token
4. **LineStatsPanel.vue**：工具条移除 chip 组 → 改为「过滤」按钮（mdi:filter-variant + 选中数徽标，有生效过滤时主色描边）；移除 toggleExt；新增 showExtDialog ref + onApplyExt；模板末尾挂载 ExtFilterDialog
5. **LineStatsPanel.scss**：移除 .gls-ext-chips/.gls-ext-chip，新增 .gls-ext-btn(+--active)/.gls-ext-badge（右上角数量徽标）
6. **index.vue**：解构 updateSelectedExtensions；`@update:selected-extensions="updateSelectedExtensions"`（直接赋值改为走持久化方法）
7. **i18n**：新增 6 键 lineStatsExtFilterTitle/Hint/SelectAll/ClearAll/SelectedCount({0} 个)/Apply，原有 lineStatsExtFilter 作按钮 tooltip
8. **README.md**：功能描述/目录树/用法指南同步更新

图标：mdi:filter-variant（已注册）、mdi:close（已注册）。lint 0 error。验证（用户执行）：pnpm lint / pnpm i18n:verify / npx tsc --noEmit。

### Bug 修复：过滤 .dll 等二进制扩展名后分析无数据显示
用户反馈"过滤了dll，分析没有数据显示"。根因：`git log --numstat` 对二进制文件（.dll/.sdb/.pdb 等）输出 `-\t-\tpath`（无行数概念），`parseFileLines` 中 `parseInt("-")` = NaN 直接 `continue` 跳过 → 二进制文件根本不会进入 `NumstatCommit.files` 列表 → 按扩展名白名单聚合时匹配不到任何文件 → `buildLineRankings` 的 `added+deleted>0` 过滤把所有 0 行数项目/作者剔除 → 排行全空显示"暂无行数数据"。

修复 3 个文件（保持报告视图零影响）：
1. **reportMetrics.ts**：
   - `parseNumstatBlocks(raw, keepBinary=false)` 和 `parseFileLines(lines, keepBinary=false)` 新增可选参数；`keepBinary=true` 时二进制文件 `-` 解析为 0 行并保留在 files 列表，否则跳过（报告视图 `getNumstatLog` 调用不带参，行为不变）
   - `sumProjectLines`/`sumAuthorLines` 返回类型改为 `LineAgg { added, deleted, fileCount }`（新增导出接口），fileCount 统计匹配文件数（二进制 0 行也计入）
2. **ReportOps.ts**：`getCommitStatsLog` 改为 `parseNumstatBlocks(raw, true)`（仅行数统计路径保留二进制）
3. **useCommitAnalysis.ts**：`buildLineRankings` 聚合 Map 类型改用 `LineAgg`，累加 fileCount；保留条件从 `added + deleted > 0` 改为 `fileCount > 0`

效果：选 .dll 过滤后，项目/作者因匹配到 .dll 文件而保留展示（行数 0，+0/−0/净0）；若仓库 git 历史从未提交过 .dll（如在 .gitignore），选 .dll 仍无数据（无匹配文件，符合预期）。lint 0 error。验证（用户执行）：pnpm lint / npx tsc --noEmit。

### 语义纠正：白名单 → 黑名单排除
用户澄清"选了.dll，就是过滤不统计dll"——即**排除语义**（勾选 = 跳过该格式），上一版白名单语义（勾选 = 只统计该格式）理解反了。本次改动：
1. **reportMetrics.ts**：`shouldIncludeFile` 语义反转——`!extensions.some(ext => path.toLowerCase().endsWith(ext))`，勾选即排除该扩展名；`sumProjectLines`/`sumAuthorLines` 回退返回 `{added, deleted}`（移除 LineAgg/fileCount）；`parseNumstatBlocks`/`parseFileLines` **回退 keepBinary 改动**（黑名单下二进制文件无保留意义，恢复跳过行为）
2. **ReportOps.ts**：`getCommitStatsLog` 回退 `parseNumstatBlocks(raw)`（去掉 keepBinary=true）
3. **useCommitAnalysis.ts**：`buildLineRankings` 聚合 Map 回退 `{added, deleted}`，过滤条件恢复 `added + deleted > 0`
4. **i18n**：tooltip/弹窗说明改为"勾选要排除的文件格式，统计时跳过"；`lineStatsExtSelectAll` 改"排除全部/Exclude All"；`lineStatsExtSelectedCount` 改"已排除 {0} 种格式/{0} excluded"
5. **注释同步**：useCommitAnalysis.ts / LineStatsPanel.vue / ExtFilterDialog.vue / README 中"白名单"全部改为"排除列表"语义

行为：勾选 .dll → 统计时跳过 .dll 文件，其余格式照常统计（有数据显示）；不勾选 → 统计所有。lint 0 error。验证（用户执行）：pnpm lint / pnpm i18n:verify / npx tsc --noEmit。

## 文档字体设置扩充预设字体列表
用户参考思源美化仓库 langzhou/siyuan-note 及其 Toy 主题，希望把字体加入「文档字体设置」预设下拉列表。经抓取该仓库 README + toy-theme 源码（theme.css 仅 @import、base.css 含排版/配色变量但**无任何 font-family 配置**，主题依赖思源内置字体），确认仓库层面提取不出具体字体清单，采用合理假设加入思源美化社区通用开源中文字体。

改动单点单文件：`src/features/generalSettings/components/DocumentFontSettings.vue` 的 `PRESET_FONTS` 数组，在原有 11 条后追加 10 条（保持 `{value,label}` 格式、字体名专有名词不走 i18n）：
- Source Han Serif SC → 思源宋体
- Source Han Serif CN → 思源宋体 (CN)
- Source Han Sans SC → 思源黑体
- Source Han Sans CN → 思源黑体 (CN)
- LXGW WenKai → 霞鹜文楷 (LXGW WenKai)
- PingFang SC → 苹方 (PingFang SC)
- HarmonyOS Sans SC → HarmonyOS Sans
- Sarasa Gothic SC → 更纱黑体 (Sarasa Gothic)
- 仓耳今楷01 W03 → 仓耳今楷
- ZCOOL XiaoWei → 站酷小薇 (ZCOOL XiaoWei)

设计要点：思源系列提供 SC/CN 双变体兼容不同系统；`applyDocumentFontStyles`（styles.ts）已支持多字体逗号分隔字符串，previewStyle 直接消费 fontFamily 自动生效，无需额外改动。无新依赖/存储/i18n/图标/SCSS/注册清单改动。lint 0 error。验证（用户执行）：pnpm lint / npx tsc --noEmit。

### 迭代修正：二进制文件过滤"无变化"确认为预期行为
用户反馈"勾选 .dll/.sdb/.pdb 过滤失败，统计没变化"。根因分析确认：git `--numstat` 对二进制文件输出 `-\t-`（无行数），`parseFileLines` 中 `parseInt("-")`=NaN 直接 continue 跳过——二进制文件从未进入统计，过滤无从作用。经用户澄清确认「只过滤文本文件」为正确行为，不改 numstat 解析层。验证过滤生效需用文本格式（.ts/.vue）。本次无代码改动。

### Bug 修复：selectedExtensions 持久化恢复缺口（loadLineStatsCache 无条件恢复）
用户需求「勾选的要持久化」。保存链路已正确（updateSelectedExtensions 即时写 lineStatsCache；runCore needNumstat 分支全量保存），缺口在恢复链路：`loadLineStatsCache` 中 `selectedExtensions.value = cache.selectedExtensions ?? []` 被包在 `if (cache.projectLineRanking.length > 0 || cache.authorLineRanking.length > 0)` 条件内，当行数排行缓存为空（只设过滤从未分析/排除全部格式/项目全删）走 loadCachedAnalysis 回退分支时勾选丢失。

修复（单点）：将该赋值语句上移到 `const cache = await manager.storage.lineStatsCache.loadOrDefault()` 之后、排行条件判断之前，无条件恢复。不改函数签名、不新增槽位、不触碰类型（selectedExtensions 字段已存在，老缓存 `?? []` 兜底）。仅改 useCommitAnalysis.ts 一处，lint 0 error。验证（用户执行）：npx tsc --noEmit；手工：勾选扩展名→应用→重开面板/重载插件→进入行数统计视图→徽标与弹窗勾选应恢复。

### 扩展名过滤列表扩充 .log/.txt
用户需求"增加.log,.txt"。`LINE_STATS_EXTENSIONS` 常量（useCommitAnalysis.ts）新增 `.log`、`.txt`，共 21 种。过滤弹窗 chip 网格 v-for 遍历该常量，自动显示，无需改其他文件。lint 0 error。

### 扩展名过滤列表扩充 .zip/.7z
用户需求"zip 7z"。`LINE_STATS_EXTENSIONS` 常量新增 `.zip`、`.7z`，共 23 种。lint 0 error。

## 排查：HDT525DG-SMT 行数统计千万行根因（.xuv 固件镜像）+ 修复
用户疑问："E:\model\HDT525\1.Code\HDT525DG-SMT 这个代码统计一千多万行，其他项目还正常点，看下是统计了什么"。

**实测诊断**（git -C 该仓库 log --numstat --no-renames -100 用 PowerShell 原生聚合）：
- 最近 100 条提交累计新增 19,546,323 行、删除 19,542,889 行（新增≈删除 → 文件被整替换）
- 行数 Top4 各 4,194,304 行（= 2^22），全部是 `.xuv` 文件：Template/TestItem/Quadrun_PB1_Release_Version_1_42_1_Program_image.xuv、CodeExecutionHub/bin/HDT525-DGSMT/Quadrun_PB1_Release_Version_1_38_1_Program_image.xuv、Template/TestItem/Quadrun_PB1_Release_Version_1_29_1_Flash_image.xuv、CodeExecutionHub/bin/Debug/Quadrun_PB1_Release_Version_1_38_1_Program_image.xuv（合计约 1677 万行，占 86%）
- 其余为 Test Logging 23年*.txt 测试日志（.txt 已在黑名单可滤）、TestEngine.py/h、TestEngineAPI.py、MerryDll.cs 等真实源码（每项 ≤3.6 万行）

**根因**：`.xuv` 是 Xilinx 固件 Program/Flash 镜像，**文本格式**每行一个地址/字节（约 2^22 行），被整包提交进 git（bin/ 构建产物 + Template 目录）。发布新版本固件 = 整删旧镜像 + 整增新镜像，numstat 把 419 万行算删除 + 419 万行算新增，反复叠加出千万级。其他项目无此类型文件故正常。旧过滤黑名单含 .txt 但**不含 .xuv**，故无法排除。

**修复**（单点最小改动）：`LINE_STATS_EXTENSIONS` 常量（useCommitAnalysis.ts）末尾追加 `".xuv"`，共 24 种。过滤弹窗 chip 网格 v-for 遍历该常量自动显示新选项。勾选 .xuv（黑名单排除语义）并重新「开始行数分析」后，HDT525DG-SMT 行数从千万级骤降至真实源码量级。未勾选不过滤，不影响其他项目。lint 0 error。验证（用户执行）：npx tsc --noEmit + 手工重新分析验证。

**经验沉淀**：行数统计面板的数字 = 最近 N 条提交的 numstat 新增行数累计（非仓库代码总量）；排查虚高行数时优先查二进制/镜像类文本格式文件（.xuv/.hex/.s19 等）是否被整包入库。

## Bug 修复：gitOpsPaused 拦截 ensureLineStats 导致勾选重启后丢失
用户反馈"重启程序，勾选的内容变成未勾选，看起持久化失败了？"。

**排查过程**：
- 检查落盘 `E:/siyuan2/data/storage/petal/siyuan-plugin-vite-vue-sn/git-push-line-stats-cache`：`selectedExtensions: [".xuv"]` 已正确保存（analyzedAt 今天、HDT525DG-SMT 已从千万降到 32 万行）→ **保存链路正常**
- 检查构建产物 index.js：`.xuv` 在 LINE_STATS_EXTENSIONS、第 187940 行有无条件恢复 `selectedExtensions.value = cache2.selectedExtensions ?? []` → 恢复代码已打包
- **根因**：`git-push-ops-paused` 文件内容为 `true`（用户上次会话处于暂停 git 操作状态且持久化）。`index.vue` 视图切换 watch 中 `if (view === "linestats" && !gitOpsPaused.value) await ensureLineStats()` —— 暂停时 `ensureLineStats` 被拦截，`loadLineStatsCache` 不执行，`selectedExtensions` 保持初始 `[]`，UI 显示未勾选。解除暂停时才会补加载（watch(gitOpsPaused)），用户一直暂停则永不恢复

**修复**（src/features/gitPush/index.vue，两处注释+一处条件）：
1. 视图 watch：`if (view === "linestats" && !gitOpsPaused.value) await ensureLineStats()` → `if (view === "linestats") await ensureLineStats()`。理由：`loadLineStatsCache` 是纯本地存储读取（loadOrDefault + loadCachedAnalysis），不触发任何 git 操作，暂停拦截属于过度拦截。`ensureAnalysis`/`ensureReport` 因会自动跑 git（runAnalysis/生成报告）仍保留暂停条件
2. gitOpsPaused watch 中 linestats 分支注释更新为"冗余兜底"

lint 0 error。验证（用户执行）：npx tsc --noEmit + 重启后进入行数统计视图确认 .xuv 勾选/徽标恢复。注意：当前 git-push-ops-paused=true，若不想暂停可在面板解除。

**经验沉淀**：排查"持久化失效"类问题先看落盘数据（区分保存失败 vs 读取/UI 链路问题）；gitPush 的 gitOpsPaused 会拦截视图切换的缓存加载，恢复纯存储读取的状态（如过滤勾选）不应被 git 暂停条件拦截。

## gitPush 行数统计「项目详情弹窗」功能（方案 B）
用户选择方案 B：点击行数统计面板项目行弹出独立 Modal，双 Tab 展示该项目的文件/作者行数明细。改动 9 个文件：

1. **types/meta.ts**：新增 `FileLineDetailRow` 接口（path/added/deleted/net/modCount/authorCount/pct/share）；types/index.ts 重导出
2. **useCommitAnalysis.ts**：新增 `perProjectNumstat: Ref<Map<string, NumstatCommit[]>>`（仅内存不持久化）；runCore 的 needNumstat 分支在 buildLineRankings 后填充（仅 fulfilled 且 numstat 非空的项目）；新增 `getProjectNumstat(projectId)` 方法并导出
3. **新建 ProjectLineDetail.vue**（components/analysis/）：自包含弹窗（mask + dialog + Transition gp-dialog-fade + useDialogKeyboard ESC），props 接收 projectId/projectName/getNumstat/extensions，内部 computed 即时聚合：fileRows（aggregateFileStats + shouldIncludeFile 过滤 + 按 added 降序）、authorRows（sumAuthorLines + 同模式）；双 Tab（文件明细表格：rank/路径/修改/作者/增删净/占比迷你条形；作者明细：与全局排行同模式 bar-row），空态用 EmptyState
4. **新建 ProjectLineDetail.scss**：Codex 风格（token-first、border 非 box-shadow、0.12s、数字后缀 spacing、表头 sticky）；`.pld-share-fill/.pld-bar-fill` 按净增语义着色
5. **LineStatsPanel.vue**：项目行加 `gls-bar-row--clickable`（cursor/hover 项目名变主题色）+ `@click="emit('viewProject', row.id)"` + title 提示；props 新增 `lineDetailProjectId`/`getProjectNumstat`；emit 新增 `viewProject`/`closeLineDetail`；模板末尾 v-if 挂载 ProjectLineDetail（extensions 传 selectedExtensions，保证明细与排行口径一致）；新增 `lineDetailProjectName` computed（从 projectRanking find，找不到回退 id）
6. **LineStatsPanel.scss**：新增 `.gls-bar-row--clickable`（padding $spacing-1 + 负 margin 抵消 + hover background + label 变 primary）
7. **index.vue**：解构 `getProjectNumstat`；新增 `lineDetailProjectId = ref("")`；绑定 `:line-detail-project-id`/`:get-project-numstat` + `@view-project="lineDetailProjectId = $event"` + `@close-line-detail="lineDetailProjectId = ''"`
8. **icons.ts**：COMMON_ICONS 新增 `fileOutline`（mdi:file-document-outline）、`accountGroup`（mdi:account-group-outline）
9. **i18n**：zh_CN/en_US 各新增 12 键（lineDetailTitleHint/FileTab/AuthorTab/FileColumn/Mods/Authors/Share/FileEmpty/AuthorEmpty/ClickHint/ModsCount({0} 次)/AuthorCount({0} 位)）

设计要点：明细数据仅存内存（Map<projectId, NumstatCommit[]>），不持久化避免缓存膨胀；弹窗关闭 v-if 卸载零残留；getNumstat 取不到数据时空态兜底；切换视图离开 linestats 时弹窗随面板卸载自动消失。校验：lint 0 error、i18n:verify 3610 键全对齐、validate:icons 新图标有效、tsc 无 gitPush 相关报错。

**附带发现**：validate:icons 报 `mdi:chart-finance` 无效，为**预存问题**（chartCandlestick 条目，mdi.json 中位于 aliases 段 parent:finance，而校验脚本只检查 icons 段，属误报），与本次改动无关，未修改。

## gitPush 操作日志面板（LogPanel）优化 + 详情弹窗（6 项）
用户需求：审查操作日志页面 → 优化界面 + 扩展建议，确认实施 6 项：状态统计条、仅失败快捷筛选、项目搜索、日期分组 sticky 标题、单条复制、条目详情弹窗。改动 4 个文件 + 2 个新建：

1. **LogPanel.vue**（214→433 行，500 硬阈值内未拆子组件）
   - `stats` computed：按 push/pull/commit 聚合成功/失败数（基于全量 logs，与筛选解耦），顶部渲染 `推送 12✓/2✗ · 拉取 8✓`，失败数 >0 才显示
   - `failOnly` ref（仅失败）+ `searchQuery` ref（项目名搜索）+ `filteredLogs` 三层过滤链（搜索→类型→仅失败，AND 叠加）
   - `watch([activeFilter, failOnly, searchQuery], () => pagedReset())`——`usePagedList` 解构补 `reset`，筛选变化重置分页防空页
   - `groupedLogs` computed：`pagedLogs` 上按自然日聚合（`LogDateGroup` 接口内部定义），同天条目因时间倒序必然连续，单次遍历合并；`dateKeyOf`/`dateLabelOf` 纯函数（今天/昨天/完整日期）
   - `copiedIds` Set + `copyTimers` Map（id→timer，onUnmounted 统一清理），`handleCopy` 写 `[HH:mm] 项目名 — 摘要` 到剪贴板，2s 勾选反馈
   - `selectedEntry` ref + `openDetail`/`handleViewProject`；条目行 `@click="openDetail"`，项目名/复制/展开按钮均 `.stop` 防冒泡
   - `logCount` 改显示 `filteredLogs.length`；筛选后空态用 EmptyState（mdi:filter + logNoMatch）
2. **新建 LogDetailDialog.vue**：常驻挂载 + 内部 `v-if="entry"` + Transition（leave 动画生效），Teleport to body + `useDialogKeyboard(computed(() => !!entry))`；头部徽章+项目名（可点跳转）+状态徽章+关闭；元信息区（操作类型/时间/状态）、摘要完整展示、平台明细逐平台行、commit 提交信息等宽块、底部复制/查看项目/关闭；复用 gp-dialog-fade 过渡类仅补充 `.gp-logd-dialog` 缩放
3. **新建 LogDetailDialog.scss**：完全自包含（`gp-logd-*` 前缀），Codex 规范（border 非 box-shadow、0.12s、$radius-lg 弹窗圆角、成功/失败徽章用 success-lightest/error-lightest）
4. **LogPanel.scss**：新增 `.gp-log-stats`（surface 底 + border 卡）、`.gp-log-search`（icon+input 组合，focus-within 用 focus-ring）、`.gp-log-filter-btn--fail`（active 红色 error-lightest 底）、`.gp-log-group-title`（sticky top:0 + z-index:1 + 实色背景）、`.gp-log-copy-btn`（默认 opacity:0，`.gp-log-item:hover` 时显示）、`.gp-log-item` 加 cursor:pointer + hover 背景
5. **icons.ts**：COMMON_ICONS 新增 `alertCircleOutline`（mdi:alert-circle-outline，#ef4444）；contentCopy/magnify 已在册无需新增
6. **i18n**：zh_CN/en_US 各新增 15 键（logFailOnly/Tip、logSearchPlaceholder、logDateToday/Yesterday、logNoMatch、logCopyEntry、logDetailAction/Time/Status/Summary/CommitMsg/Platforms、logStatusSuccess/Failed）；`logDetailTitle` 加了又删（头部直接徽章+项目名+状态，无标题位）

关键认知：弹窗组件常驻 + 内部 v-if 才能获得 Transition leave 动画（v-if 整体挂载/卸载无动画）；`.gp-log-search` 的 focus-within 不能写 `outline:none`（会覆盖 focus-ring 的发光）；复制反馈用 Map 管理 timer 比每行单独 onUnmounted 更干净。lint 0 error。验证（用户执行）：pnpm i18n:verify / pnpm validate:icons / npx tsc --noEmit / pnpm lint。

### 迭代：操作日志 UI 表格化
用户要求"ui改成表格化"。把日志列表从卡片式行改为 div-based 表格（对齐 `.gpr-*` 表格模式）：

1. **LogPanel.vue**：列表区重构为「sticky 表头 + 数据行 + 子行」结构
   - 表头 6 列：类型/状态/项目/摘要/时间/操作（新增 logColType/Status/Project/Summary/Time/Ops 6 个 i18n 键，zh/en 各）
   - 数据行 `.gp-log-trow`：6 列 flex 对齐（action 64px / status 18px / project 140px / summary flex:1 / time 104px / ops 44px），点击整行打开详情弹窗
   - 展开/收起按钮从文字改为图标（mdi:chevron-down/up，title 用 expandPlatforms/collapsePlatforms），与复制按钮并排放入操作列
   - 平台明细/commit 信息改为**子行**（`.gp-log-trow--sub`）：前 3 列空 span 占位对齐摘要列（无 magic number padding），内容在 summary 列内
   - 日期分组标题从 sticky 改为**普通分隔行**（表头独占 sticky top:0，z-index:2）
2. **LogPanel.scss**：`.gp-log-item`/`.gp-log-item-head` → `.gp-log-trow`/`.gp-log-thead`/`.gp-log-th`/`.gp-log-tcol--*` 列宽体系；`.gp-log-badge`/`.gp-log-dot` 改 inline-block；`.gp-log-project-name`/`.gp-log-summary`/`.gp-log-time` 改 block + width:100% + ellipsis（时间右对齐）；`.gp-log-expand-btn` 改 18px 图标按钮；复制按钮 hover 父选择器改为 `.gp-log-trow:hover &`；`.gp-log-platforms`/`.gp-log-commit-msg` 去掉 margin/padding-left（由占位列对齐）

设计要点：列宽用统一类 `gp-log-tcol--*`（表头与数据行共用保证逐列对齐）；子行用"空占位列 + 内容"而非固定 padding-left（无 magic number）；表头 sticky 与分组标题不可同时 sticky（二选一，表格化后表头优先）。lint 0 error。验证（用户执行）：pnpm i18n:verify / npx tsc --noEmit / pnpm lint。

## 文档字体设置内置字体（霞鹜文楷 LXGW WenKai）
用户选择方案 B（内置 1 款字体），之前已为 DocumentFontSettings.vue 的 PRESET_FONTS 追加 10 个思源美化社区常用字体（上一轮完成）。本轮把「霞鹜文楷」升级为**随插件内置**字体，解决"系统未安装所选字体时回退到思源默认字体、看起来没反应"的问题。

**字体资源**：用户提供 `lxgw-wenkai-v1.522-rc4.tar.gz`（完整版，含 LXGWWenKai-Regular.ttf 25.5MB）。文件放于 `src/features/generalSettings/assets/fonts/LXGWWenKai-Regular.ttf` + `OFL.txt`（SIL OFL 1.1 许可证）。已删除中间产物（80MB tar.gz 与解压目录）。注意：该字体完整版 25.5MB 较大，会明显增加插件包体积。

**改动 5 个文件**：
1. **vite.config.ts**：`viteStaticCopy` 新增 2 个 target——复制 `LXGWWenKai-Regular.ttf` 与 `OFL.txt` 到插件产物 `assets/fonts/` 目录
2. **utils/styles.ts**：新增 `BUILTIN_FONT` 常量（fontFamily: "LXGW WenKai"、fileName、subDir: "fonts"）、`BUILTIN_FONT_STYLE_ID`、`injectBuiltinFont(assetsPath)` 函数——注入 `@font-face`，src 指向 `${assetsPath}/fonts/LXGWWenKai-Regular.ttf`，font-display: swap
3. **GeneralSettings.ts**：`init()` 新增 `injectBuiltinFonts()` 私有方法，用类型断言读 `plugin.assetsPath`（siyuan.d.ts 未声明），缺失时兜底 `/plugins/${plugin.name}/assets/`；导入 `injectBuiltinFont`
4. **DocumentFontSettings.vue**：PRESET_FONTS 的 LXGW WenKai 条目标记 `builtin: true`；新增 `isBuiltinFontSelected` computed；模板字体族下拉下新增内置提示（`checkCircle` 图标 + i18n.builtinFontHint）；`onMounted` 后 `preloadBuiltinFont()` 用 `document.fonts.load` 预加载；`loadSettings` 回填后同步 `presetFont`（命中预设时显示对应项，用 String() 处理 SelectOption.value 类型）
5. **i18n**：zh_CN/en_US generalSettings.json 各新增 `builtinFontHint` 键；**SCSS** DocumentFontSettings.scss 新增 `.builtin-font-hint`/`.builtin-font-hint-icon` 样式

**验证结果**：lint 0 error；i18n:verify 3643 键全对齐；tsc 无 generalSettings 相关错误（项目存在大量预存类型错误与本改动无关）；validate:icons 报 `mdi:chart-finance` 为预存误报（chartCandlestick 条目，历史已知）。用户需自行验证：`pnpm vite build` 后确认插件目录出现 `assets/fonts/` 且面板选中霞鹜文楷后文档字体生效。

**关键认知**：思源插件静态资源通过 `plugin.assetsPath`（运行时注入，指向 `/plugins/<name>/assets/`）访问，siyuan.d.ts 类型未声明需断言；`viteStaticCopy` 是打包静态资源到插件目录的标准手段。

## gitPush 操作日志冗余审查与简化（3 个重复函数提取 + stats 优化 + dateLabelOf 简化）
经审查 LogPanel.vue 与 LogDetailDialog.vue，发现并修复 3 类冗余：

1. **函数重复提取**：`hasPlatforms`、`actionLabel`、`formatTime` 三个函数在两个组件中逐字相同，违反模块内代码分层规则。提取到 `utils.ts` 并重命名为 `hasLogPlatforms`/`logActionLabel`/`formatLogTime`（避免 utils.ts 中潜在命名冲突），LogPanel.vue + LogDetailDialog.vue 各删除 23 行本地定义改为 import。`actionLabel` 需额外传 i18n 参数（utils 无 props 访问）。同时移除 LogPanel.vue 中不再使用的 `GitOpAction` 导入。

2. **stats computed 优化**：从 6 次 filter（按 push/pull/commit 分别 filter 全量 logs + 再 filter ok）改为 1 次 for-of 循环 + Record 聚合。O(3n)→O(n)，代码行数从 8 行增为 13 行但更清晰。

3. **dateLabelOf 简化**：去掉手动 pad 拼接 todayKey/yesterdayKey 字符串的重复逻辑，改为 `new Date().setHours(0,0,0,0)` 取今天午夜 + `new Date(year, month, date)` 取目标日期午夜 → `Math.round(diff/86400000)` 比较天数差。DST 场景（23h/25h 日长）用 Math.round 兜底。

改动 3 个文件：utils.ts（+28 行，GitOpLogEntry 导入 + 3 个导出函数）、LogPanel.vue（净减约 17 行）、LogDetailDialog.vue（净减约 22 行）。lint 0 error。验证（用户执行）：pnpm lint / npx tsc --noEmit。
