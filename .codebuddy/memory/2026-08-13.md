# 2026-08-13

## 文档字体设置内置字体扩展：新增新晰黑 Code（NeoXiHei Code）
用户最初要求"移除 LXGWWenKai-Regular.ttf，增加 NeoXiHeiCode-Regular.ttf"（替换），随后改主意为**保留霞鹜文楷 + 新增新晰黑 Code**（多内置字体方案）。

**字体资源**（`src/features/generalSettings/assets/fonts/`）：
- `LXGWWenKai-Regular.ttf`（25.5MB，保留）+ `OFL.txt`（SIL OFL 1.1）
- `NeoXiHeiCode-Regular.ttf`（8.3MB，新增）+ `License.txt`（20446 字节，IPA Font License v1.0，从 github.com/lxgw/NeoXiHei-Code main 分支下载，注意文件名是 `License.txt` 大写 L）

**新晰黑 Code 背景**：lxgw（霞鹜）作品，基于 M+ 1M + IPA Gothic 合并的 Migu 1M 衍生，汉字部分换成霞鹜新晰黑，等宽中文字体适合代码/笔记。**font-family 名 = `NeoXiHei Code`**，仅 Regular 一个字重。许可证为 IPA Font License v1.0（不同于霞鹜文楷的 OFL）。

**改动文件**：
1. **utils/styles.ts**：`BUILTIN_FONT` 单对象 → `BUILTIN_FONTS` 数组（含 LXGW WenKai + NeoXiHei Code 两条），`injectBuiltinFont` 改为循环 map 生成多个 `@font-face`（保持 `BUILTIN_FONT_STYLE_ID` 单一注入点）
2. **vite.config.ts**：`viteStaticCopy` 新增 `NeoXiHeiCode-Regular.ttf` + `License.txt` 两个 target（均带 `rename: { stripBase: true }`）
3. **DocumentFontSettings.vue**：PRESET_FONTS 新增 `{ value: "NeoXiHei Code", label: "新晰黑 Code (NeoXiHei)", builtin: true }`；`isBuiltinFontSelected` 从 `=== BUILTIN_FONT.fontFamily` 改为 `BUILTIN_FONTS.some(...)`；`preloadBuiltinFont` 循环加载所有内置字体
4. **i18n**：`builtinFontHint` 文案通用化（zh: "该字体已随插件内置，未在系统安装也能正常使用" / en: "This font is bundled with the plugin and works without system installation"）
5. **注释同步**：GeneralSettings.ts / DocumentFontSettings.scss / Vue 模板注释中"霞鹜文楷"改为"内置字体"泛称
6. **README.md**：内置字体章节改为表格（霞鹜文楷 25.5MB OFL + 新晰黑 8.3MB IPA），合计约 33.8MB

**注意**：用户已自行精简了 PRESET_FONTS（当前仅 8 项：微软雅黑 Light/Segoe UI/等线/system-ui/思源宋体 CN/思源黑体 CN/霞鹜文楷/新晰黑 Code），此前追加的 10 个开源字体条目大部分被移除——用户偏好精简列表，后续改 PRESET_FONTS 前应确认当前状态。

**验证**：lint 0 error；i18n:verify 3644 键对齐；tsc 无 generalSettings 相关错误。用户需重启 `pnpm dev` 后验证字体生效。

## 代码块美化也支持两个内置字体
用户要求 `CodeBlockSettings.vue`（代码块美化）也把这两个内置字体（霞鹜文楷 + 新晰黑 Code）加入代码字体预设。

**改动**（`src/features/generalSettings/components/CodeBlockSettings.vue`）：
1. `presetFonts` 从**纯字符串数组**（`["Consolas", ...] as const`）改为**对象数组** `{ value, label, builtin? }`：原 5 个等宽字体（Consolas/Courier New/JetBrains Mono/Cascadia Code/Hack）+ 新增 `{ value: "NeoXiHei Code", label: "新晰黑 Code (NeoXiHei)", builtin: true }` 和 `{ value: "LXGW WenKai", label: "霞鹜文楷 (LXGW WenKai)", builtin: true }`
2. 新增 `isBuiltinCodeFontSelected` computed（`BUILTIN_FONTS.some(...)`），**注意必须放在 `settings` ref 定义之后**（TS 禁止 used before declaration）
3. `presetCodeFont` computed 的 `get` 从 `presetFonts.includes(family)` 改为 `presetFonts.some(f => f.value === family)`
4. 模板 `<select>` 的 `<option>` 循环改为 `:key="f.value"` `:value="f.value"` 显示 `{{ f.label }}`
5. 字体族行下方新增内置字体提示 `<p v-if="isBuiltinCodeFontSelected">`（复用 `i18n.builtinFontHint` + `checkCircle` 图标）
6. **CodeBlockSettings.scss** 新增 `.builtin-font-hint` / `.builtin-font-hint-icon` 样式（与 DocumentFontSettings.scss 一致）

**生效机制**：`applyCodeBlockStyle` 中 `font-family: '${codeSettings.codeFontFamily}', monospace !important;` 直接拼接 family 名，配合全局已注入的 `@font-face` 即可生效，无需额外逻辑。

**注意**：`presetFonts` 是组件内常量（仅 1 处使用，未提取到 types/utils——符合 Rule of Three 不提前抽象）。

**验证**：lint 0 error；tsc 无 CodeBlockSettings 相关错误。

## 提取内置字体提示共享组件与工具函数（去重）
审查 `CodeBlockSettings.vue` 与 `DocumentFontSettings.vue`，结论：**不整体合并**（不同设置域/存储槽/父面板独立 Tab，合并超 500 行硬阈值），但提取 3 处重复：

1. **utils/styles.ts**：`BUILTIN_FONTS` 数组每条新增 `label` 字段（霞鹜文楷/新晰黑 Code 的展示名），并新增 `isBuiltinFontFamily(fontFamily)` 纯函数
2. **新增 `components/BuiltinFontHint.vue`**：props 接收 `fontFamily` + `i18n`，内部 `isBuiltinFontFamily` 判断渲染提示条（`checkCircle` 图标 + `i18n.builtinFontHint`）；样式迁移到新增 `styles/BuiltinFontHint.scss`
3. **CodeBlockSettings.vue**：删除 `isBuiltinCodeFontSelected` computed（改用共享组件）；`presetFonts` 内置字体条目改为 `...BUILTIN_FONTS.map(f => ({ value: f.fontFamily, label: f.label }))` 派生，移除 `builtin: true` 死字段
4. **DocumentFontSettings.vue**：删除 `isBuiltinFontSelected` computed；`PRESET_FONTS` 同样从 `BUILTIN_FONTS` 派生内置条目
5. **删除两个 SCSS 中重复的 `.builtin-font-hint` / `.builtin-font-hint-icon` 样式**

**注意**：CodeBlockSettings 中 `presetFonts` 是 `as const` 数组，其中 spread `BUILTIN_FONTS.map(...)` 结果；DocumentFontSettings 的 `PRESET_FONTS` 声明为 `SelectOption[]`（含 `[key: string]: any`，类型兼容）。组件内 `computed` 仍被其他逻辑使用，import 保留。

**验证**：lint 0 error；`isBuiltinCodeFontSelected`/`isBuiltinFontSelected`/`builtin: true` 已无残留。用户需自行跑 tsc 验证。

## CodeBlockSettings 冗余审查与样式应用优化
审查 `CodeBlockSettings.vue` 及其样式应用链路，完成 3 个文件的等价重构（行为不变）：

1. **utils/styles.ts**（3 处）：
   - `hexToRgba` 增强容错：非 `#` 开头/非法 hex 输入原样返回（不再产出 NaN）；支持 3 位 hex（`#abc`→`#aabbcc`）自动展开；opacity 钳制到 `[0,1]`
   - 移除 `codeSettings.backgroundColorOpacity ?? 1` 的 `?? 1`（类型必填 number + 默认值恒 1，防御性冗余）
   - 「行号样式」块与 `showLineNumber` 三元模板字符串合并为单一 CSS 块（关闭时追加 `display: none !important`），消除模板字符串内嵌三元表达式的割裂
2. **styles/CodeBlockSettings.scss**：删除 `.style-card-icon`（删后为空块，整体移除）+ `.style-card-name`/`.style-card-desc` 中 3 处冗余 `position: relative; z-index: 1`（`.style-card` 的 hover/active 仅 background 变化，非覆盖层，子元素 z-index 无实际作用）
3. **components/CodeBlockSettings.vue**：删除 `opacityPercent` computed（模板 `:value` 直接调用 `formatPercent(settings.backgroundColorOpacity)`）；折叠高度 SettingLabel 值显示由字符串拼接 `${...}px` 改为复用 `formatPx(settings.collapseHeight)`，与滑块格式统一

**注意**：`.style-card-check`（选中角标）的 `z-index: 2` 保留——其自身为绝对定位元素，层叠表现不受影响，且不在本次改动范围。模板中 `style-card-icon` 类名保留（无对应样式规则，无害）。

**验证**：3 个文件 lint 均 0 error；`opacityPercent` 无遗留引用；codex-ui-style-guide skill 校验无新违规（删除操作未触碰 token 化声明）。用户需自行跑 `npx tsc --noEmit` 验证。

## 优化代码折叠效果：移除底部模糊渐变遮罩
用户要求"优化代码折叠效果，不要有模糊阴影，简洁大方"。在 `src/features/generalSettings/utils/styles.ts` 的 `applyCodeBlockCollapse()` 函数中删除了两处 `.code-collapse-fade` 渐变遮罩：

1. **CSS 规则**：`.code-block.code-block-collapse-wrapper .code-collapse-fade`（absolute 定位 + `linear-gradient(to bottom, transparent, var(--b3-theme-surface))` 40px 渐变）与 `:not(.code-block-collapsed)` 时的 `opacity: 0` 规则
2. **JS 创建逻辑**：`const fade = document.createElement('div'); fade.className = 'code-collapse-fade protyle-custom'; ...` 共 4 行

**保留不动**：`.code-collapse-bar`（sticky bottom + `border-top: 1px solid var(--b3-border-color)` 提供清晰折叠分隔线）、`.code-collapse-btn`、`.code-collapse-line-info`、`.code-block-collapse-wrapper { position: relative; }`（保守保留，不影响 sticky 子元素）、折叠展开/收起逻辑。

**效果**：折叠边界由 maxHeight 截断 + border-top 清晰分隔，无渐变模糊过渡。删除后 `code-collapse-fade` 类名全项目 0 引用。lint 0 error，用户需自行验证 `pnpm dev` 下实际效果。

## 代码折叠仅保留展开图标（去除一切遮挡 + CSS 性能审查）
用户要求"不要有任何遮挡，只保留展开图标（图标改一下），并审查 css 性能问题和冗余"。在 `applyCodeBlockCollapse()` 中完成（仍仅 `utils/styles.ts` 一个文件）：

**CSS 改造**（style.innerHTML）：
- `.code-collapse-bar`：`position: sticky; bottom:0; display:flex; justify-content:center; padding; background; border-top` → `position: absolute; right:8px; bottom:8px; z-index:2`（去掉背景条与分隔线，图标悬浮右下角，无遮挡）
- `.code-collapse-btn`：删 `gap/padding/border/border-radius/font-size/line-height`（文字标签服务，冗余）→ 纯图标按钮 `width/height:20px; background:none; border:none; opacity:0.55; transition:opacity .15s`
- `.code-collapse-btn:hover`：`background:surface-variant` → `opacity:1`（透明度反馈替代背景高亮）
- `.code-collapse-btn svg`：12px→14px，新增 `transition:transform .2s ease`
- `.code-collapse-btn svg.collapsed`：`rotate(-90deg)` → `rotate(180deg)`
- 删除 `.code-collapse-line-info` 整条规则

**JS 改造**（script.innerHTML）：
- 删 `countLines()` 函数、`isZh` 变量、`totalLines`/`lineInfo` 生成逻辑
- `bar.innerHTML` 简化为单个 `<button class="code-collapse-btn" aria-label="expand">` + 单个 svg（无 `<span>` 文字、无 lineInfo）
- 换图标：原 chevron(16×16) → Material expand_more 风格（`viewBox 0 0 24 24`，path `M7.41 8.59 12 13.17 16.59 8.59 18 10l-6 6-6-6z`）
- `onclick`：删 `label` 查询与 `label.textContent` 更新，仅保留折叠类切换 + `hljs.style.maxHeight` 切换 + `svg.collapsed` 类切换

**性能/冗余审查结论**：sticky 滚动触发 layout → 改 absolute 不再随滚动重排；文字标签相关 CSS 与 JS 全部冗余删除。

**保留不动**：`.code-block-collapse-wrapper { position:relative }`（absolute 定位锚点）、`.hljs { overflow-y:auto; overflow-x:hidden }`、`addCodeExtends`/MutationObserver/scroll 监听/cleanup、`bar.className` + `querySelector('.code-collapse-bar')` 去重守卫。

**验证**：lint 0 error。用户需自行 `pnpm dev` 验证渲染效果。

**后续微调（用户反馈"展开按钮不够明显"）**：给按钮补上小圆底衬 + 边框提升可见度（仍仅右下角 24px，不遮挡代码主体）：
- `.code-collapse-btn`：尺寸 20px→24px、svg 14px→16px、`opacity 0.55→0.85`、补 `background: var(--b3-theme-surface)` + `border: 1px solid var(--b3-border-color)` + `border-radius: 4px`
- hover：补 `background: var(--b3-theme-surface-variant)`，transition 增加 `background .15s`

## 代码块折叠注入脚本冗余与性能优化
针对 `styles.ts` 的 `applyCodeBlockCollapse()` 注入 IIFE 脚本（285-455 行）做纯内部精简（行为不变，仍仅 `styles.ts` 单文件）：

**冗余消除**：
1. 提取 `bindScroll(protyle)` 函数，合并 `initCodeBlockCollapse` 与 `observeProtyleAddition` 回调中重复的 scroll 绑定逻辑（原两处各 4 行几乎相同）
2. 移除 `running` 锁 + `setTimeout 300ms`——该锁在 forEach(async) 前同步置位、300ms 后盲目解除，与 async 不同步无防并发作用；外层 scroll debounce 100ms 已足够
3. `isMobile()` 结果缓存为脚本顶层 `const mobile = isMobile()`（生命周期不变，替换两处重复调用）
4. `bindScroll` 内部独立 `debounceTimer`，修复原来两个绑定闭包共享同一 debounceTimer、多 protyle 互相 clearTimeout 的隐患
5. `scrollContainer` 的 `let` 改 `const`

**性能优化**：
6. `addCodeExtends` 中 `.code-collapse-bar` 去重守卫从 `await whenElementExist` 之后提到函数最前——已处理代码块不再走 isCursorInCodeBlock/await/querySelector('.hljs')/scrollHeight 读取
7. scroll 监听加 `{ passive: true }`
8. `whenElementExist(selector, timeout = 5000)` 加超时上限，超时 `resolve(null)`（调用方已有 `if(!hljs...)`/`if(!protyle...)` 兼容，无需 try-catch），防元素永不出现时 rAF 永久空转

**不改动**：MutationObserver 观察 body 全 subtree（收窄风险高）、CSS 部分、按钮 onclick、清理事件逻辑。

**验证**：lint 0 error。用户需自行 `pnpm dev` 验证折叠/展开、滚动加载新代码块、开关关闭时清理正常。

**后续审查修正**：加超时 `resolve(null)` 后，`initCodeBlockCollapse` 中 `await whenElementExist(...)` 原直接 `protyle.querySelectorAll`，超时场景会抛 TypeError（原无限轮询不会触发）。补两处 null 检查：`.then(async el => { if(!el) return; ... })` 和 `await` 后 `if(!protyle) return;`。lint 0 error。

## DocCountSettings 审查修复：防抖 + emit 事件驱动重构
针对 `DocCountSettings.vue` 上一轮审查结论实施修复，共改 2 个文件：

1. **components/DocCountSettings.vue**：
   - 字体样式连续输入加 150ms 防抖：新增模块级 `fontStyleTimer` + `onUnmounted` 清理，`handleFontStyleChange` 由 async 直调改为「清旧定时器→设 150ms 定时器→到点 persistAndApply」，解决颜色逐字符输入/透明度滑块拖动导致的 save + updateDocCount 全量重绘 + showMessage 风暴
   - `persistAndApply` 移除 `getGeneralSettings()?.updateDocCount(settings)` 直调，改为 `emit("change", settings)`；`handleToggleChange` 删除重复 emit，4 个 handler 统一经 persistAndApply emit
   - 移除 `getGeneralSettings` 函数及 `import type { GeneralSettings }`（消除与 HighlightSettings.vue 的重复逻辑）
   - `gsStorage` 由 `computed(() => ...)` 改为普通 `const`（`props.plugin` 由 createVueDockApp 在 setup 前注入、生命周期不变），`ensureStorage`/`loadSettings` 统一 `if (!gsStorage)` 判空

2. **GeneralSettings.ts**：`handleSettingsChange` 在 tabPin 分支后新增 `else if (moduleId === "docCount") { this.updateDocCount(...) }`，接通 docCount 事件消费链（此前 emit 是死代码，全项目无监听者）。

**关键事实**：`createVueDockApp`（`utils/vueAppHelper.ts`）会自动注入 `plugin` prop，故子面板 `props.plugin` 在 setup 时已就绪，gsStorage 改 const 安全。docCount 生效链变为 `persistAndApply → emit("change") → index.vue handleDocCountChange → onSettingsChange({moduleId:"docCount"}) → GeneralSettings.handleSettingsChange → updateDocCount`，与兄弟子面板 codeblock/heading 模式一致，行为等价。

**未处理**（按计划搁置）：`updateInterval: string → number` 类型修正（涉及旧持久化数据兼容风险）。验证：2 文件 lint 均 0 error，用户需自行 `npx tsc --noEmit` + `pnpm dev` 验证防抖与文档数实时刷新。

## gitPush 行数统计面板（LineStatsPanel）审查修复
用户要求审查 `src/features/gitPush/components/analysis/LineStatsPanel.vue` 的逻辑漏洞/冗余/内存泄露/死代码/重复类型，并按结论修复 4 项：

1. **汇总卡片统计口径错误（高危）**：原 `summary` computed 用截断后的前 20 个项目（`PROJECT_RANK_LIMIT=20`）累加却标注「总新增/总删除/总净增」。改为 `buildLineRankings` 在 slice 前基于全量 `projectLines` 累加 `summary`，新增 `lineStatsSummary` 状态随 `lineStatsCache`（新增可选 `summary` 字段）持久化，`LineStatsPanel` 改从 prop 读取。旧缓存降级用 `deriveSummary` 从截断排行累加。
2. **netClass 三处重复**：提取参数化 `netClass(net, prefix, zeroSuffix)` 到 `utils.ts`，三组件薄委托复用（gls-net / pld-net / gpr-cell，zero 传空串返回空串）。
3. **pct/share 计算重复**：提取 `withLineBarPct` 到 `utils.ts`，`LineStatsPanel` 与 `ProjectLineDetail.authorRows` 复用（fileRows 因 share 兜底写法特殊保留现状）。
4. **relativeTime 空串兜底**：模板 `relativeTime(...) || i18n.timeJustNow`。

改动文件：`utils.ts`、`types/meta.ts`、`types/index.ts`、`composables/useCommitAnalysis.ts`、`index.vue`、`components/analysis/LineStatsPanel.vue`、`ProjectLineDetail.vue`、`report/AuthorContributionSection.vue`。lint 0 error，用户自行验证 tsc + dev。

## 行数统计面板加表头图例（解决"哪个是实际行数"困惑）
用户反馈 LineStatsPanel 排行列表三列数字（+新增/−删除/净增）无标签、字号透明度一致，分不清哪列是"实际行数"。需求澄清后确定：净增是主角，新增/删除是辅助；改动最小化——保留三列数字布局与字号不变，仅加对齐表头行。

**改动（3 个文件）**：
1. **LineStatsPanel.vue**：项目排行、作者排行两个 `.gls-bar-list` 顶部各插入一个 `.gls-bar-head` 表头行，复用与数据行一致的 flex 列结构（`.gls-bar-rank`/`.gls-bar-label`/`.gls-bar-track` 占位 + `.gls-line-nums` 放「新增/删除/净增」+ `.gls-bar-share` 放「占比」），保证与数字列右对齐。净增表头 `gls-bar-head-net` 加 `:title="i18n.lineStatsNetHint"` tooltip。
2. **LineStatsPanel.scss**：新增 `.gls-bar-head`（flex + gap:$spacing-2 + 2xs 字号 + bold + letter-spacing + 下边框分隔；内部 `.gls-bar-track` 背景透明化、`.gls-line-num`/`.gls-bar-share` 覆盖 opacity 为 1）+ `.gls-bar-head-net`（主题色强调）。
3. **i18n**：zh/en 分片各新增 `lineStatsNetHint`（"净增 = 实际行数（新增 − 删除）"/"Net = actual lines (added − deleted)"）。

复用已有 key：`analysisLineAdded/Deleted/Net`、`lineDetailShare`（占比）。表头颜色编码：新增绿（success）/删除红（error）/净增主题色（primary）。数据行完全未动。lint 0 error；顶层合并 JSON 由用户自行 `pnpm i18n:merge` 生成。

## gitPush 行数统计新增「当前总行数」（存量指标）
用户要求在行数统计面板新增「当前总行数」（当前工作区已跟踪文件的实际代码行数，**存量口径**），强调与现有「新增/删除/净增」（**增量**，git log --numstat）互不冲突。澄清确认：①汇总卡片加第 4 张卡片（全项目合计）；②项目排行每行末尾加「总行数」列；作者排行不加（总行数是项目属性）；③口径跟随 `selectedExtensions` 过滤 + 排除二进制/>2MB 文件。

**数据链路**：
1. `managers/ReportOps.ts` 新增 `getTrackedFiles(projectPath)` — `git ls-files -c core.quotepath=false`，按行 split 过滤空行，走 GitExecutor 本地并发池
2. `reportMetrics.ts` 新增 `countTrackedFilesLines(project, files, extensions?)` — 循环 `shouldIncludeFile` 过滤 + 复用 `countFileLines`（2MB/二进制跳过），返回合计
3. `GitPushManager.ts` 新增 `getTrackedFiles` 门面方法
4. `composables/useCommitAnalysis.ts`：
   - `runCore(needNumstat=true)` 分支用 `Promise.all` 并行抓取 `getCommitStatsLog` + `getTrackedFiles`，`totalLines = countTrackedFilesLines(p, files, selectedExtensions.value)`
   - `buildLineRankings` settled 类型新增 `totalLines?: number`，聚合进 `projectTotalLines` Map；`summary.totalLines` 全量累加；projectRanking 每行带 `totalLines`
   - `deriveSummary` 降级累加 totalLines（旧条目 `?? 0`）
   - `loadLineStatsCache` 旧缓存 summary 无 totalLines 时 `{ ...summary, totalLines: summary.totalLines ?? 0 }`

**类型**（`types/meta.ts`）：`LineStatsSummary.totalLines: number`（**必填**，新写入缓存必含）；`ProjectLineRankItem.totalLines?: number`（**可选**，兼容旧缓存）。

**UI**：
- `LineStatsPanel.vue`：汇总卡片 3→4 列，第 4 张「当前总行数」卡片（中性色 `.gls-card-value--total` + `:title="i18n.lineStatsTotalHint"`）；项目排行表头与数据行各加 `.gls-line-total` 列（行末尾），数据 `row.totalLines?.toLocaleString() ?? "—"`（旧缓存缺失显示破折号）；作者排行不加列
- `LineStatsPanel.scss`：`.gls-cards` repeat(4,1fr)；`.gls-card-value--total` 中性色 opacity .85；新增 `.gls-line-total`（等宽右对齐 + min-width 44px）；`.gls-bar-head` 的 opacity:1 覆盖加 `.gls-line-total`

**i18n**：zh/en 分片各新增 `lineStatsTotalLines`（当前总行数/Current Lines）、`analysisLineTotal`（总行数/Lines）、`lineStatsTotalHint`（口径 tooltip：存量非增量）。

**验证**：gitPush 目录 lint 仅剩 ProjectLineDetail.vue 预存 `rootRef` 未使用 warning（与本任务无关，未动）。用户需自行 `pnpm i18n:merge`（构建时自动）+ `npx tsc --noEmit` 验证。

## gitPush 项目代码行数排行取消前 20 截断（显示全部项目）
用户询问"为什么只显示 20 个项目"，原因确认为 `useCommitAnalysis.ts` 中硬编码常量 `PROJECT_RANK_LIMIT = 20`，在 `buildLineRankings` 对项目排行 `.slice(0, PROJECT_RANK_LIMIT)` 截断。用户确认需求：去掉截断，显示全部有行数变化的项目。

**改动（3 个文件）**：
1. **composables/useCommitAnalysis.ts**：
   - `buildLineRankings` 中 projectRanking 链式调用移除 `.slice(0, PROJECT_RANK_LIMIT)`（保留 `.filter(added+deleted>0)` 过滤，排序不变）
   - `PROJECT_RANK_LIMIT` 注释更新：明确其现在只服务项目提交数排行（`rankByCount` 第 365 行）与作者行数排行无关；行数排行已显示全部
   - `lineStatsSummary` ref 注释「与截断后的排行解耦」→「独立累加」；全量合计注释「slice 前」措辞更新；`deriveSummary` 注释补充「更早版本缓存排行可能只含前 20」
2. **components/analysis/LineStatsPanel.vue**：summary prop 注释「与截断排行解耦」→「独立累加」
3. **types/meta.ts**：`LineStatsSummary` 接口注释同样更新

**保持不变**：作者行数排行 `AUTHOR_RANK_LIMIT = 10` 仍截断；项目提交数排行复用 `PROJECT_RANK_LIMIT` 截断；汇总卡片本为全量合计不受影响。缓存旧数据（截断后 20 条）无需迁移，重新分析后覆盖为全量。不涉及 i18n/icon 变更。lint 0 error，用户自行验证 tsc + dev。

## gitPush 项目行数详情弹窗头部展示当前总行数 chip
用户要求：点击项目代码行数排行行打开详情弹窗（ProjectLineDetail）后，也显示该项目的总行数（存量），与排行行中「总行数」列口径一致。

**改动（3 个文件，纯展示层，数据链路零改动）**：
1. **components/analysis/LineStatsPanel.vue**：新增 `lineDetailTotalLines` computed（从 `props.projectRanking` 按 `props.lineDetailProjectId` 查 `totalLines`，项目已删除或旧缓存缺失为 undefined），弹窗调用处新增 `:total-lines` prop
2. **components/analysis/ProjectLineDetail.vue**：props 新增可选 `totalLines?: number`（注释说明存量口径）；`.pld-title-wrap` 内 `.pld-title-sub` 之后新增 `.pld-total` chip（`当前总行数` 标签 + 等宽数字值，`title` tooltip 复用 `lineStatsTotalHint`，undefined 显示 "—"）
3. **styles/ProjectLineDetail.scss**：新增 `.pld-total`（inline-flex + 边框 chip + `$font-size-2xs` + `$spacing-*` + `$radius-sm`，无 box-shadow）+ `.pld-total-value`（`$vp-mono` + `$font-weight-semibold`）

**复用 i18n 键**：`lineStatsTotalLines`（当前总行数）+ `lineStatsTotalHint`（存量口径 tooltip），无新增翻译。lint 仅剩 ProjectLineDetail.vue 预存 `rootRef` 未使用 warning（无关）。用户自行验证 tsc + dev。
