# 2026-08-14

## docAnalysis 代码审查修复

对 `src/features/docAnalysis` 模块完成五类问题修复（逻辑漏洞/冗余/内存泄露/死代码/重复类型），改动 4 个文件：

1. **useDocAnalysis.ts**：新增 `resetQueryState()`（清空结果+错误、回到 idle），替代组件内越界直写 `queryState`；`queryByStatsCategory` 同分类切换内联重置复用该方法；删除 `analyzeDocStats` 中 `_t/_d/_bm/_wc` 四个永远为 undefined 的解构变量（改用数组空洞 `[sizeRows, dupRows, , , , platformResult, qualityResult, scanResult]`，保留 9 个 Promise 并发）。
2. **index.vue**：平台过滤清除时调用 `resetQueryState()` 保持 chip/列表一致；`loadAttrs` 新增 `attrsToken` 竞态令牌（onBeforeUnmount 时递增失效）；`IntersectionObserver` 在哨兵移除时 disconnect；`v-memo` 依赖补齐 `notebookName/hpath/unpublishedPlatforms`；`clearStatsFilter`/`handleReset` 统一走 `resetQueryState`，`handleReset` 补清 `duplicateNameFilter` 和 `activePlatformFilter`。
3. **FilterSettings.vue**：移除冗余的 `optionsUpdate` 事件双写链路（`v-model` 直接改 props.options 后 emit 回传 `Object.assign` 等于自我复制）；删除 `handleChange`、emit 声明及三处 `@change` 绑定；`handleDebouncedInput` 内联。
4. **types/index.ts**：新增 `SORT_FIELD_OPTIONS: { value: SortField, label: string }[]` 常量，index.vue 排序下拉改为 `v-for` 类型化渲染。

lint 通过（0 错误）。验证命令由用户自行执行（`pnpm lint`/`tsc --noEmit`）。

## 字体规范全量修复（.scss 硬编码 → 设计 Token）

对项目全部 `.scss` 文件执行字体三要素（font-size/font-weight/line-height）硬编码替换，共改动约 45 个文件：

1. **font-weight**（28 文件，约 100 处）：300/400/500/600/700 → `$font-weight-light/normal/medium/semibold/bold`。注意 `!important` 后缀（`font-weight: 600 !important`）不被 `font-weight: 600;` 分号匹配，需单独处理（htmlViewer 462 行）。
2. **font-size 标准值**（25 文件）：10px → `$font-size-2xs`、12px → `$font-size-xs`。
3. **font-size 中间字号收敛**：9px → `$font-size-2xs`；13px → `$font-size-xs`；15px → `$font-size-sm`；16px → `$font-size-base`；18px → `$font-size-lg`。
4. **11px 按上下文**：辅助文字（标签/徽章/按钮/日期/路径/Tab）→ `$font-size-2xs`；正文（列表/h3标题/文件名/输入框）→ `$font-size-xs`。
5. **14px 按场景**：数据突出（结果数值）→ `$font-size-sm` 加注释；图标尺寸 → `$font-size-sm` 加注释；标题/徽章/空状态 → 降级 `$font-size-xs`。
6. **大字号（24/36px）补注释**：24px → `$font-size-2xl`（统计数值）、36px → `$font-size-4xl`（图标）；32/40/26px 无精确 Token，保留硬编码 + 「图标尺寸（无精确 Token）」注释。
7. **line-height 1.5**（8 文件，10 处）→ `$line-height-normal`。
8. **components/styles 6 文件**：删除过时误导注释「特小号，无对应 Token，保留」，10px → `$font-size-2xs`。

**例外保留**（不改）：`_variables.scss` 的 `$codeblock-font-size: 13px`（Token 定义）；compactMode 的 `line-height: 1.5rem * $s`；statistics 的 8px/28px/20px/22px（SVG/图标字形，已有注释）；aiContentGenerator/textDiff 的 vue-diff 第三方覆盖注释。

**经验**：同一文件多值 replace_all 并发会写锁冲突超时（报 "Acquire write lock ... timeout"），需分轮串行（每轮每文件只发一个值）；`.vue` 内联 style 未纳入本次（计划范围外，后续单独处理）。

lint（IDE）通过 0 错误。`pnpm lint`/`tsc --noEmit` 由用户自行验证。

## 构建报错修复：8 个 .scss 缺 @use 导入

用户运行 vite build 报 `[vite:css] [sass] Undefined variable`（shortcut/DeleteConfirmDialog.scss 的 `$font-size-xs`）。根因：本次字体修复把硬编码 px 换成 Token，但部分文件原本纯用硬编码 + `var()`，**没有 `@use '@/variables.scss' as *;` 导入**。

扫描「使用 $ 变量但无 @use」后修复 8 个文件（均在文件头部补 `@use '@/variables.scss' as *;`）：
1. `shortcut/styles/DeleteConfirmDialog.scss`
2. `shortcut/styles/ShortcutCard.scss`
3. `shortcut/styles/ShortcutDialog.scss`
4. `shortcut/styles/ShortcutGrid.scss`
5. `video/styles/VideoListItem.scss`
6. `video/styles/VideoToolbar.scss`
7. `imageCreation/styles/CodeImageStyles.scss`
8. `toolCollection/tools/unitConverter/styles/index.scss`

**关键经验**：替换硬编码为 Token 时，必须确认目标文件已有 `@use '@/variables.scss' as *;`。原本纯用硬编码的文件（无任何 Token）不会导入变量，替换后必然报 Undefined variable。修复前应先用扫描排查「使用 $ 但无 @use」的文件。扫描 PowerShell 命令（用 .NET API 避免编码问题）：`Get-ChildItem -Path 'src' -Filter '*.scss' -Recurse | ForEach-Object { $c = [System.IO.File]::ReadAllText($_.FullName); if ($c -match '\$[A-Za-z_]' -and $c -notmatch '@use' -and $_.Name -notmatch '^_' -and $_.Name -ne 'variables.scss') { $_.FullName } }`。

## SCSS 设计 Token 重构（$brand-* → $color-*）

对 `src/_variables.scss` / `src/index.scss` 完成彻底重构，消除通用样式设定混乱：

1. **颜色体系重建**：新增 `$color-*` 语义色（`$color-fg/bg/muted/surface/border/primary/secondary/accent/danger/danger-bright/success/warning/info`），替代 `$brand-*`（12 个）+ `$brand-orange/blue/green`（3 个）+ `$font-heading/$font-body`（=$font-zh 纯别名）。全项目 49 个文件、150+ 处旧变量已批量迁移（临时 Node 脚本完成，已删除）。
2. **删除死代码**：`$shadow-*`（6 个，与「禁止 box-shadow」冲突）、`@function hsl-to-hex`、shadcn 三值 HSL 暗色 13 组 + 亮色 11 组（实际使用方 jsonFormatter/pdfViewer/regexTester 改为 `$color-success`/`$color-danger-bright`）、`$tablet/desktop-breakpoint`、`$font-sans`。
3. **引用写法统一**：5 种 → 1 种 `@use '@/variables.scss' as *;`（原 `@/variables`、`./variables.scss`、`../../../_variables.scss` 等 108 个文件已统一）。
4. **index.scss 职责分离**：变量导入（改 `@/variables.scss`）+ 3 个功能样式导入 + `:root` 桥接 + 基准字号规则，分区注释清晰。
5. **gitPush 纯透传层**：`gitPush/styles/variables.scss`（仅 `@forward`）43 处引用已改 `@/variables.scss`，成为死文件——删除操作被用户拒绝仍保留，README 已标注「已废弃，可删除」。
6. **规则文档同步**：AGENTS_RULES.md Token 清单补 `$color-*` + 旧变量废弃说明；CODEBUDDY.md / AGENTS.md 同步补 `$color-*`。

**验证**：全项目 `$brand-`/`$font-heading`/`$font-body`/`$shadow-*`/`hsl-to-hex`/非标准引用写法均 0 残留。`pnpm build`/`pnpm lint` 由用户自行验证（SCSS 编译正确性最终依赖 build）。

## 修正：功能样式从全局 index.scss 移回模块内加载

用户指出 `src/index.scss` 里不该加载 `pageLock`/`compactMode` 的功能样式，应改回各自模块内加载：

1. **pageLock**：`LockDialog.vue` 已 `@use "../styles/index.scss"`（非 scoped，静态 import 随 registerPageLock 注入），全局 index.scss 里的导入是**纯冗余**，直接删除。
2. **compactMode**：样式是 `html.siyuan-compact-mode` 全局选择器，改为 `compactMode/index.ts` 顶部 `import "./styles/index.scss"`（遵循 docNavigation/toolCollection/statusBar 先例）。compactMode 是纯 TS 模块（无 registerFeature、无 index.vue，仅导出 applyCompactMode/getCompactModeState，由 src/index.ts 直接 import 调用），故用 TS import 而非 @use。
3. **codeblockThemes** 保留在 index.scss（用户未选中）。

验证：lint 0 错误。经验：判断功能样式加载位置——组件专属样式（.xxx-dialog 等）应由组件 @use；作用于全局 DOM（html.xxx / 动态 createElement 的样式）但无组件宿主时，用 TS `import "./styles/index.scss"` 而非堆到全局 index.scss。

## codeblockThemes.scss 审查结论：不能删除

审查 `generalSettings/styles/codeblockThemes.scss` 是否可删，结论**保留（不能删）**。它是「代码块风格」预设功能（default/github/mac 三套）的唯一视觉实现，与动态增强层 `applyCodeBlockEnhancedStyles`（styles.ts，`<style id="codeblock-enhanced-style">` 注入 `!important`）是**互补非冗余**关系：

1. `DEFAULT_CODEBLOCK_SETTINGS.enabled=false`，动态增强层默认不注入；而 `applyCodeBlockStyle` 无条件往 body 加 `codeblock-style-*` class。删静态层 → 默认状态下代码块零美化。
2. 静态层独有能力动态层完全没有：Mac 三色按钮、Mac 滚动条、GitHub CSS counter 行号、`::selection` 选中效果、复制按钮 hover 显隐、action bar 布局、hljs 背景。
3. 删掉后 `applyCodeBlockStyle`/`CODEBLOCK_STYLES`/`CODEBLOCK_STYLE_META` 及设置面板风格卡全部沦为死代码。

若真要砍预设风格，需联动删：codeblockThemes.scss + applyCodeBlockStyle/CODEBLOCK_STYLES/CODEBLOCK_STYLE_META + CodeBlockSettings.vue 风格卡区 + i18n 6 键 + icons 3 图标（功能级删减）。已知历史问题（GitHub 风格与增强层同开时双份行号）是协调问题非删除理由。

## codeblockThemes.scss 移回 generalSettings 模块内加载

将 `codeblockThemes.scss` 从全局 `src/index.scss` 的 `@use` 迁移到 `generalSettings/index.ts` 顶部 `import "./styles/codeblockThemes.scss"`。理由：文件本就在 `generalSettings/styles/` 下，且 `generalSettings/styles/index.scss` 未转发它（只含面板布局样式），全局 index.scss 是唯一加载点。模块顶层 import 静态注入，CSS 随 bundle 加载、不受 `enableGeneralSettings` 开关影响，而 body class 切换仍由 `GeneralSettings.init()`→`applyCodeBlockStyle()` 按开关控制——时序正确（同 compactMode 先例）。

至此 `src/index.scss` 彻底收敛为纯「全局设计 Token + 基准字号」入口，无任何功能级样式 import。`codeblockThemes.scss` 现仅被 `generalSettings/index.ts` 引用。lint 0 错误。

## codeblockThemes.scss 完整重构（冗余/死变量/硬编码三合一）

对 `codeblockThemes.scss`（207 行 → 约 200 行）完成纯等价重构，改动 2 个文件：

1. **选择器合并**：提取 `$codeblock-roots` 列表变量（4 路根选择器：`.protyle-wysiwyg .code-block` / `.b3-typography .code-block` / 两个 `[data-node-id][data-type="NodeCodeBlock"]` 变体），用 `#{$codeblock-roots}` 插值复用，全文件 5 处重复 → 1 处定义 + 4 处引用。**不用 `@extend`**（避免特异性膨胀）。
2. **base mixin 合并**：`@mixin codeblock-base` + 三次 `@include` 删除，公共样式移入 `body[class*="codeblock-style-"]` 通用块。安全性依据：`applyCodeBlockStyle()` 保证 body 恒有且仅有一个 `codeblock-style-*` class；已逐一验证 base 与 github/mac/default 差异化块属性空间完全不相交（无覆盖冲突）。注意：通用块特异性 (0,3,3) 高于 `body.codeblock-style-xxx` (0,2,3)，但属性不重叠故无影响。
3. **死 CSS 变量清理**：`var(--codeblock-padding, ...)`/`var(--codeblock-font-size, ...)` → 直接 `$codeblock-padding`/`$codeblock-font-size`；Mac 6 个 `--mac-button-*` var → 直接 hex + 「固定品牌色」注释。全项目搜索 0 残留。
4. **Token 替换**：`min-height:1.5rem`→`$spacing-6`、`8px`→`$spacing-2`、`0.125rem`→`$spacing-2px`、`0.75rem`→`$spacing-3`、`0.5rem`→`$spacing-2`、`0.25rem`→`$radius-sm`、删除冗余 `padding-top` 覆盖行。`line-height:1.6` 在 `_variables.scss` 代码块区新增 `$codeblock-line-height:1.6` Token。保留并注释的精确值：`-0.375rem`、`letter-spacing:0.5px`、`0.875rem`、三色点间距 `1.125rem/2.25rem`、行号 `2em/1em`。
5. **验证**：`codex-ui-style-guide` 技能逐项审查无违规；lint 0 错误。视觉输出完全不变的等价重构，未联动 styles.ts/组件/i18n。`pnpm build` 由用户验证。

## 代码块三风格差异化重做（GitHub/Mac/Default 拉大差异）

用户反馈「三种风格差异不大，参考 github」。澄清后确认：三种都重做增强差异——GitHub 微调贴近真实 GitHub、Mac 改深色标题栏、Default 保持原生。改动 3 个文件：

1. **`src/_variables.scss`**：`$codeblock-border-radius: 8px` → `$codeblock-border-radius: $radius-base`（6px，注释「贴近 GitHub 代码块真实圆角」）。该变量仅 GitHub 块引用一次，改 Token 安全。
2. **`codeblockThemes.scss`**：
   - GitHub 块：`.protyle-action__language` 覆盖通用块居中定位，改 `left: $spacing-2; margin-left: 0` 左对齐（真实 GitHub header 语言居左，复制按钮 `margin-left:auto` 已靠右）。
   - Mac 块：`.protyle-action` 背景改固定深色 `#1f1f1f`（macOS 终端/iTerm 特征色，与三色点同为「固定品牌色 + 注释」例外）；`border-bottom` 改 `rgba(255,255,255,0.08)`；新增整体 `color: rgba(255,255,255,0.85)` 让语言标签/复制/菜单等所有操作元素反色（删除了原语言标签和图标的单独 color 块，改为 action 层统一反色以覆盖复制按钮 hover 显隐场景）。
3. **i18n 分片**：`macStyleDesc` zh「macOS 窗口样式」→「深色标题栏 + 三色点」；en「macOS window style」→「Dark title bar + traffic lights」。仅改分片，合并 JSON 由 `pnpm i18n:merge` 生成。

**Codex 校验**：`codex-ui-style-guide` 技能逐项审查——间距用 `$spacing-*`、圆角用 `$codeblock-border-radius`/`$radius-lg`、无新增 box-shadow、无硬编码字号/行高/字重；Mac 三处硬编码色值（`#1f1f1f`/`rgba(255,255,255,0.85)`/`rgba(255,255,255,0.08)`）均有「macOS 终端固定品牌色」注释，与文件内三色点既有例外模式一致。read_lints 0 错误。`pnpm build`/`pnpm lint`/`pnpm i18n:merge` 由用户自行验证。

## 修复 docAnalysis Tab 选中下划线双层

`docAnalysis/index.vue` 的 Tab 选中时下划线显示双层。根因：`.tab-bar` 用 `align-items: center` 垂直居中，`.tab-btn.active` 用 `border-bottom: 2px` + `margin-bottom: -2px` 想让蓝色线覆盖容器底部灰色线，但按钮居中未贴底，负 margin 无法精确让两条 2px 线重合，导致蓝/灰线错位叠加成双层。

修复（`src/features/docAnalysis/styles/index.scss`）：
1. `.tab-bar` 的 `align-items` 改 `stretch`，`padding` 底部改 `0`（按钮贴底）。
2. `.tab-btn.active` 改用 `::after` 绝对定位绘制蓝色下划线（`position: relative` + `bottom: -2px; height: 2px`），精确压在容器灰色底线上，消除负 margin 错位。
3. `.analyze-btn` 加 `align-self: center`，避免 `stretch` 布局下被拉伸变形。

lint 0 错误。经验：Tab 下划线指示条优先用 `::after` 绝对定位，而非 `border-bottom` + 负 margin（负 margin 依赖精确对齐，容器非 `stretch`/`flex-end` 对齐时易错位出双层）。

后续微调：`.tab-btn` 默认 `border-radius: 6px 6px 0 0` → `0`，Tab 按钮（含未选中/hover/active）统一无圆角；active 态冗余的 `border-radius: 0` 一并移除。

冗余清理（`.tab-bar` 块）：删除 `.tab-btn` 的 `border-radius: 0`（浏览器默认即 0）与 `.tab-bar` 的 `background: var(--b3-theme-background)`（与父级 `.doc-analysis-panel` 同色冗余）。

## FilterSettings.vue 冗余重复清理

审查并清理 `docAnalysis/FilterSettings.vue` 及其 SCSS 的冗余/重复，改动 2 个文件，组件接口（props/emits）与功能行为不变：

**脚本层（FilterSettings.vue）**：
1. 合并两条 `import type { NotebookInfo }` / `import type { FilterOptions }` 为一条；新增运行时 `import { DEFAULT_FILTER_OPTIONS }`（在 computed 中实际使用）。
2. 删除仅转发 `emit("reset")` 的平凡包装函数 `handleClearAll`，模板改 `@click="$emit('reset')"`。
3. 修正 `hasAnyFilter` 恒真 bug：`o.wordCountMin` 改 `> 0` 判断；`o.wordCountMax` 改 `!== DEFAULT_FILTER_OPTIONS.wordCountMax`（默认值 30000 非 0，原判断导致清空按钮几乎永远显示）。这是唯一行为修正项。
4. 删除模板尾部多余空行。

**样式层（FilterSettings.scss）**：
5. 删除未使用的 `@use "./mixins" as *;`（无任何 @include/@extend）。
6. 合并 `.filter-input` 与 `.filter-select` 重复基础样式（padding/border/radius/background/color/font-size/outline/min-width/:focus）为分组选择器，`.filter-select` 单独保留 `cursor: pointer`。
7. 合并同构的 `.title-input`/`.content-input` 为 `.title-input, .content-input { flex: 1; }`（删除冗余 `min-width: 0`，已在基础块声明）。
8. `.notebook-select` 删除冗余 `min-width: 0`，保留 `flex: 1`。

read_lints 0 错误。`pnpm lint`/`tsc --noEmit` 由用户自行验证。**经验**：DEFAULT_FILTER_OPTIONS 默认值（wordCountMax=30000）与「0 表示不过滤」的字段注释语义不一致，判断已过滤时应与默认值比较而非与 0 比较。
