# 2026-08-24

## 新增功能：灵感生成器（ideaGenerator）

为用户新增「灵感生成器」功能模块，独立浮动窗口承载（复用 minimalBrowser 的 addTab + openTab + openWindow 双形态模式）。核心能力：
- 预设 6 个开发方向分类（桌面工具/命令行脚本/思源插件/Web 应用/效率工具/自动化）+ 可选关键词定向生成
- `callAI` 单次非流式批量生成 5 条灵感（system prompt 引导 Markdown 列表 `- **标题**：描述`，`parseIdeasResponse` 解析 + 行拆分兜底）
- 三种选择动作：展开查看 / 一键复制（`copyToClipboard`）/ `callAIStream` 流式细化为完整技术方案（需求说明/技术栈/实现要点/扩展方向），支持 AbortController 取消
- 快捷键 `⌃⌥I` + 超级面板 action（`openIdeaGenerator` 事件联动）+ `addIcons` 注册 iconLightbulb Tab 图标

新增 14 个文件（types/index.ts + utils.ts + index.ts + index.vue + composables/useIdeaGenerator.ts + 3 子组件 + 5 SCSS + README），修改 7 个注册文件。完整走 8 步注册清单。

设计要点：不持久化（无 storage.ts）、双 AbortController 防竞态、IDEA_CATEGORIES 共享常量放 types/、resolveI18nLabel 点路径解析分类标签（utils.ts 纯函数，避免 composable 与组件重复）。

验证：read_lints 0 错误。`pnpm lint` / `pnpm i18n:verify` / `pnpm validate:icons` / `npx tsc --noEmit` 由用户自行验证。

## 修改：toolCollection 独立窗口移除"工具合集"标题字样

用户需求：独立窗口（浮动窗口）打开后，移除面板中的"工具合集"标题字样，不影响功能。
改动：`src/features/toolCollection/index.vue` — header 区域加 `v-if="!isFloating"`（`isFloating` = `getFrontend() === "desktop-window"`）。浮动窗口内整个头部（含 header-title "工具合集"）不渲染，Tab 栏直接置顶；overlay 底部面板与主窗口页签模式照常显示。功能逻辑零改动，read_lints 0 错误。

## 写入项目规则：独立窗体 UI 精简

用户要求将「独立窗体不需要这种显示」写入规则。已在 AGENTS.md（独立窗口承载章节段落）与 AGENTS_API.md（实现步骤新增第 6 条 + 关键点速查表新增"独立窗体 UI"行）写入规则：浮动窗口页签标题已标识功能名，面板头部不显示重复标题字样，用 `isFloating`（`getFrontend() === "desktop-window"`）+ `v-if="!isFloating"` 隐藏，仅移除显示、功能逻辑零改动。同时同步至 MEMORY.md 编码规范偏好。

## 迭代：ideaGenerator 新增「引导发散」模式 + 修复右侧遮挡

用户反馈"当前功能太简陋，参考 github（BrainBoost/Mind-Map-Wizard 类），希望输入主题 → AI 生成分类选项让我选 → 一步步引导 → 灵感发散"。确定需求：AI 动态多轮引导 + 先方向后灵感 + 保留预设分类一键生成（双模式并存）。

实现：
- `index.vue` 顶部加模式 Tab（快捷生成 / 引导发散，`activeMode: "quick" | "guide"`）
- 新增 `composables/useGuideDiverge.ts`：引导状态机 `GuideStage = "input" | "guiding" | "ideas"`，维护 topic/directions/steps/ideas；方法 startDiverge/selectDirection/regenerateDirections/stepBack/divergeIdeas/restart
- 新增 `composables/useIdeaDetail.ts`：提取展开/复制/流式细化公共逻辑（两模式复用），`refineIdea(idea, contextLabel)` 接收分类标签或发散路径作为上下文；RefineStatus 类型移至此
- `types/index.ts` 新增 GuideDirection/GuideStage；`utils.ts` 新增 buildDirectionsPrompt/buildDivergePrompt/parseDirectionsResponse，并泛化 parseIdeasResponse 为底层 parseOptionList（方向与灵感共用）
- 新增组件 GuideDiverge.vue（三态：主题输入/方向深挖/灵感结果）+ DirectionCard.vue + 两个 SCSS
- 右侧遮挡修复：`.ig-body` 加 flex-wrap，`.ig-detail` 由固定 width:300px+shrink:0 改为 flex:0 1 300px + min-width:280px（窄容器换行而非溢出被裁切）

设计要点：双模式状态互不共享、双 AbortController 防竞态、方向与灵感共用 parseOptionList 解析器、路径面包屑可视化。read_lints 0 错误，i18n 中英键对齐。

## 集成：灵感生成器接入状态栏功能抽屉（底部功能列表）

用户需求"开关集成到底部功能列表中"。底部功能列表 = 状态栏 FeatureDrawer（功能抽屉，`ph:grid-four` 图标弹出）。

改动：`src/features/statusBar/index.vue` — (1) 新增 `ideaGeneratorI18n` 常量（i18n 分片收窄）；(2) `FEATURES` 注册表在 minimalBrowser 后添加 ideaGenerator 条目（icon `mdi:lightbulb-on-outline`、color `#9333ea`、title 取 i18n、pinnable true、action `emitCustomEvent("openIdeaGenerator")`）。

开关自动生效：`hasToggle("ideaGenerator")` 检测到 `enableIdeaGenerator` 设置键（settings.ts 已加），抽屉项显示启用/禁用角标；`isFeatureEnabled` 按该键控制置灰。点击 action 触发 `openIdeaGenerator` 事件 → IdeaGeneratorManager 监听打开。read_lints 0 错误。

## 重构：docAnalysis 统计功能按 AGENTS.md 规则拆分

先输出功能分类（Facade 容器含统计/列表/排版/辅助面板 4 类平级功能，统计为优先项），再重构统计功能使其符合「单文件 500 行硬阈值 / 代码分层 / Composable 复用」。

新增 5 个文件：
- `composables/platformMeta.ts` — PLATFORM_META 响应式单例迁移（消除拆分时循环依赖），4 个引用文件（index.vue/StatsOverview.vue/PlatformManageModal.vue/AttrsPanel.vue）导入路径同步更新
- `utils/sortDocs.ts` — SORT_CMP + sortDocs 纯函数
- `utils/categoryQueryConfig.ts` — DocQueryConfig 接口 + SIZE_CONDITIONS/TIME_INTERVALS/buildTimeConfig/existsCond/EXISTS_MAP
- `composables/useDocStats.ts` — 统计逻辑（docStats/depthStats/statsFilter/bookmarkDetails/duplicateGroups 等状态 + analyzeDocStats 9 路并行 SQL + queryByStatsCategory 下钻 + fetchBookmarkDetails/queryByBookmark + loadDuplicateNameFilter/watch），依赖经 UseDocStatsDeps 注入（filterOptions/queryState/buildNotebookCondition/runDocQuery/setEmptyState/resetQueryState）
- `composables/useStatsOverview.ts` — StatsOverview 视图计算（健康度/卡片值/柱状图比例/平台分布/重名过滤），filterVisibleCards 改 hideZero 参数传入

重构文件：
- `useDocAnalysis.ts` 643→316 行（聚合入口：查询执行器 + 平台操作 + 笔记本加载，内部调用 useDocStats，对外 28 项返回接口不变）
- `StatsOverview.vue` 518→371 行（script 计算逻辑改用 useStatsOverview）

关键点：queryByMissingPlatform 中 `stats.statsFilter.value` 解构为局部 `statsFilter`；index.vue 编排逻辑零改动。read_lints 0 错误，`pnpm lint`/`tsc` 等由用户验证。后续文档列表/排版/辅助面板可按相同模式处理。

## 分类：docAnalysis/components/ 按「功能专属文件夹」规则（AGENTS_ARCH.md § 四）

用户要求仅分类不重构（保留报告即可）。引用链分析（13 组件全部模块内专属，无跨 feature 复用，无需 common/）：

- **统计 StatsView**（6 组件）：StatsOverview（入口容器）→ StatCard/StatSection/BarRow/BookmarkDetailModal/DuplicateNameFilterModal
- **文档列表 DocListView**（2 组件）：FilterSettings、DocListItem（当前内联在 index.vue Facade，无独立入口容器）
- **排版 PublishPanel**（3 组件）：PublishPanel（入口容器）→ MarkdownEditor/PreviewPane
- **辅助·属性 AttrsPanel**（1 组件）：AttrsPanel
- **辅助·平台管理 PlatformManage**（1 组件）：PlatformManageModal

规则违规点：13 组件平铺未分文件夹（接近 15 文件导航警戒）；StatsOverview.vue/PublishPanel.vue 入口未用 index.vue 命名。建议目标结构为 StatsView/DocListView/PublishPanel/AttrsPanel/PlatformManage 五个语义化文件夹（入口均 index.vue），DocListView 入口容器需从 index.vue 拆分（与 Facade 拆分工作联动）。用户选择只分类不重构，暂不动目录。

## 执行：docAnalysis components/ 目录重组（按功能专属文件夹规则）

用户确认后执行全量重组。用 PowerShell Move-Item 移动 + 批量 `../`→`../../` 引用替换（目录深度+1）。最终结构：

```
components/
├── StatsView/      index.vue(原StatsOverview) + StatCard/StatSection/BarRow/BookmarkDetailModal/DuplicateNameFilterModal
├── DocListView/    index.vue(新增入口容器) + DocListItem + FilterSettings
├── PublishPanel/   index.vue(原PublishPanel) + MarkdownEditor + PreviewPane
├── AttrsPanel/     index.vue(原AttrsPanel)
└── PlatformManage/ index.vue(原PlatformManageModal)
```

关键改动：
- 新增 DocListView/index.vue：从 index.vue Facade 拆出排序栏/结果计数/列表渲染/分批渲染（PAGE_SIZE/IntersectionObserver），props 注入 queryState/filterOptions/statsFilter/activePlatformFilter/activePlatformName/i18n，emits open/attrs/sortChange/toggleSortOrder/clearStatsFilter
- index.vue 640→441 行：删除内联列表模板与分批渲染逻辑，handleSortChange 改为接收 field 字符串
- 行数结果：index.vue 441 / StatsView 371 / DocListView 249 / PublishPanel 376 / AttrsPanel 493 / PlatformManage 282，全部 <500 硬阈值
- 新路径文件 read_lints 0 错误；期间出现旧路径 lint 报错（components/StatsOverview.vue 等）为 IDE 语言服务器陈旧缓存，磁盘上旧文件已不存在，重新加载窗口/重启 TS server 后消失

注意事项：AttrsPanel/index.vue 493 行接近 500 硬阈值，后续可按需拆分（辅助面板属后续工作）。

## 重构：docAnalysis 重名分析链路去冗余（4 点）

审查 StatsView 重名（duplicate）分析链路发现 4 个冗余点并全部修复：

1. **死配置清理**：`types/index.ts` STAT_SECTIONS 中 duplicate 卡片的 `statKey`/`suffixStatKey` 被 `useStatsOverview` 的 `id==="duplicate"` 硬编码分支短路永不生效。方案：扩展 `StatCardDef` — `statKey` 改可选、`resolveValue` 签名升级为 `(stats, ctx: CardValueContext) => number`、新增 `suffixValue`，新增 `CardValueContext { effectiveDupDocs, effectiveDupGroupCount }`。duplicate 卡片改为 `resolveValue`/`suffixValue` 元数据驱动，删除硬编码分支。
2. **过滤单一入口**：`filterDuplicateGroups` 原在 useStatsOverview（显示）与 useDocStats.queryByStatsCategory（下钻）各调一次。现收敛为 `useDocStats` 新增 `effectiveDuplicateGroups` computed 唯一入口，视图层只消费结果。
3. **卡片语义归位**：重名卡片从"大小分布"分区移入 QUALITY_CARDS（质量 Tab，与孤文档并列）；`.card-value.dup` 颜色类已在 StatCard.scss 定义，样式零改动。
4. **判断标准统一**：toolbar 名称排除按钮 `v-if` 从 `stats.duplicateNameDocs > 0` 改为 `effectiveDupDocs > 0`，与 Hero 徽章一致。

另：`DocStats` 删除 `duplicateNameDocs`/`duplicateNameGroups` 冗余字段（可由 duplicateGroups 派生），全局 0 残留引用。

涉及 5 文件：types/index.ts、useDocStats.ts、useStatsOverview.ts、index.vue、StatsView/index.vue。read_lints 0 错误，SQL 保持唯一查询不重复。用户自行验证 lint/i18n/icons/tsc。

**经验教训**：同一文件的多个 replace_in_file 并行调用存在竞态，后续编辑会被覆盖丢失（本次 useStatsOverview.ts 重名区块与 useDocStats.ts return 导出两次丢失，均已二次补回）。同一文件多处修改应串行执行。

## 修复：useDocStats.ts 遗漏 computed 导入（运行时 ReferenceError）

用户在浏览器控制台报 `ReferenceError: computed is not defined at useDocStats`。根因：本次重构新增 `effectiveDuplicateGroups` computed 时，`useDocStats.ts` 第 5 行的 vue 导入（原为 `nextTick, onScopeDispose, reactive, ref, watch`）漏加 `computed`。修复：导入补齐为 `computed, nextTick, onScopeDispose, reactive, ref, watch`。

注意：read_lints 持续报「找不到名称 computed」（Line 278）与「参数 g 隐式 any」（Line 198，computed 未定义的次生错误），但磁盘源码已确认无误，属 IDE 语言服务器缓存陈旧，重启 TS server / IDE 后消失。用户需重新构建插件生效（旧构建产物才是运行时错误来源）。

## 修复：websiteNavigation 添加网址保存后窗体消失

用户反馈「添加网址保存后，窗体直接消失，要重启打开」。根因：`index.vue` 的 `closeDialog` 同时被 WebsiteDialog 的 `@close` 和 `@saved` 复用，而其中调用了 `props.onClose?.()`；`onClose` 绑定 `types/index.ts` buildProps 里的 `() => this.closeModal()`，导致保存成功 → emit("saved") → closeDialog → onClose → 整个面板关闭（且 `this.modal = null`，需重启）。

修复（3 处，均只删不改逻辑）：
1. `index.vue` Props 接口删除 `onClose?: () => void`
2. `index.vue` `closeDialog` 删除 `props.onClose?.()`（只关弹窗，不再关面板）
3. `types/index.ts` buildProps 删除 `onClose: () => this.closeModal()`

面板真正的关闭仍由 `getCloseHandler`（ESC/遮罩点击）负责，不受影响。read_lints 0 错误。

## 重构：websiteNavigation 冗余清理（用户确认范围后执行）

用户审查冗余，确认范围：仅 websiteNavigation 内部 + 删除薄 re-export 兼容层。4 个 todo 全部完成：

1. **删除兼容层**：删除 `types/constants.ts`、`types/storage.ts`（纯 re-export）；`types/index.ts` 移除类型/常量 re-export，仅保留 I18n + WebsiteNavigation 类，文件头注释更新为"类型与常量已下沉至共享层"
2. **重构 useWebsiteNavigation.ts**：类型/常量/存储类改从 `@/utils/sharedStorage/websiteStorage` 直接导入；删除 `categoriesMap` computed（唯一使用者 getCategoryById 改用 `categories.value.find`）；`useWebsiteNavigation()` 去掉从未被消费的返回对象（仅初始化+onMounted 加载）；vue 导入移除 `computed`
3. **index.vue**：`ALL_CATEGORY_ID` 改 sharedStorage；删除 `loadData` 导入与 `handleCategoriesSaved` 函数及 CategoryManager 的 `@saved` 绑定（分类增删已在 composable 更新内存态，筛选回退由 watch(categories) 兜底）
4. **4 个子组件**：WebsiteDialog/WebsiteCard/CategoryManager/FilterBar 的类型与常量导入改 sharedStorage（I18n 保留 from ../types）；CategoryManager 删除 `saved` emit 声明及 2 处 `emit("saved")` 调用

git 状态确认：2 删 7 改。全局搜索无 `types/constants|types/storage` 残留引用。minimalBrowser 未触碰。

**遗留（预存在问题，非本次引入）**：index.vue 第 152/155/166/168 行 `showMessage(props.i18n.deleteSuccess/deleteFailed/urlCopied/copyUrl, ...)` 报 TS2345（`string | undefined` 传 `text: string`）。git diff 确认这些行与 HEAD 一致；但 WebsiteDialog/CategoryManager 中相同模式（`showMessage(props.i18n.saveFailed...)`）不报错，判断为 ts-plugin 检查不一致/预存在问题。本次未修复，用户可自行决定加 `?? ""` 或收紧 I18n 类型。

## 修复：websiteNavigation showMessage 类型警告

用户确认修复上条遗留的 TS 警告。修复方式：`showMessage(props.i18n.xxx)` 传参全部加 `?? ""` 兜底（空字符串，非中文硬编码兜底，符合 AGENTS.md）。共修 3 个文件 12 处调用：
- `index.vue`：deleteSuccess/deleteFailed/urlCopied/copyUrl（4 处）
- `components/WebsiteDialog.vue`：saveFailed×2/updateSuccess/createSuccess（4 处，ts-plugin 虽未报但类型层面同类问题，tsc 时会暴露，一并修）
- `components/CategoryManager.vue`：categoryExists/saveFailed/categoryNotEmpty/saveFailed（4 处）

修复后整个 websiteNavigation 模块 read_lints 0 错误。i18n 分片键运行时均有值，`?? ""` 仅为类型安抚不改变运行时行为。

## 修复：docNavigation 卸载逻辑调用 WeakMap.clear() 报错

用户报 `src/features/docNavigation/index.ts` L73 TS 错误「WeakMap<Element, boolean> 上不存在属性 clear」。根因：`visibilityMap` 是 `WeakMap`，而 WeakMap 没有 `clear()` API（仅 Map 有）。运行时执行 `destroy()` 会抛 `TypeError`，中断销毁流程导致后续 `disposeCache()` 不执行。

修复：删除 `visibilityMap.clear()` 行并加注释（WeakMap 键为弱引用，随 Element 被 GC 自动回收，无需也无法手动清空）。同级 `timerMap` 无需 clear——其键已全部在 mountedProtyles 循环中逐个 delete。read_lints 0 错误。

## 修复：formatAssistant 快捷键注册用了 ICommand 不存在的字段

用户报 `src/features/formatAssistant/types/index.ts` L35 TS 错误「command 不在类型 ICommand 中」。根因：`plugin.addCommand()` 的 `ICommand` 类型合法字段为 `langKey`(必填)/`langText?`/`hotkey?`/`callback?` 等，原代码误用 `command: "openFormatAssistant"` 和 `title: "打开排版助手"`。

修复：`command` → `langKey`，`title` → `langText`（langText 提供显示文本，可省略 i18n 键）。与项目其他 16 处 addCommand 用法一致（video/toolCollection/passwordVault 等均用 langKey+langText 模式）。read_lints 0 错误。

## 修复：imageCreation 封面风格 re-export 类型本地不可用

用户报 `src/features/imageCreation/types/coverStyles.ts` L22「找不到名称 CoverSizePreset」。根因：`export type { X } from "./coverStylesShared"` 是纯转发导出，不创建当前模块的本地绑定（ESM 语义），本文件 L22（CoverSizePreset[]）与 L260（CoverStylePreset[]）直接用这些类型注解时报错。

修复：改为「先 `import type { CoverSizePreset, CoverStylePreset, StyleColors, StyleDefinition } from "./coverStylesShared"` 建立本地绑定，再 `export type { ... }`（无 from）转发导出」，对外 re-export 不变，types/index.ts 引用链不受影响。coverStyles.ts 与 types/index.ts 均 read_lints 0 错误。

**经验教训**：`export { X } from "m"` / `export type { X } from "m"` 不会把 X 引入当前模块作用域，仅转发。同文件要使用须额外 `import`。

## 修复：imageCreation 主题色覆盖层漏掉 subtitleColor

用户报 `src/features/imageCreation/utils/coverHtml.ts` L30「缺少属性 subtitleColor，但 StyleColors 需要该属性」。根因：`resolveCoverColors` 返回类型 `StyleColors`（5 必填字段），主题色覆盖分支只填了 bg/titleColor/accent + accentAlt(沿用风格默认)，漏 subtitleColor（运行时为 undefined，导致水印颜色渲染异常）。

修复：补 `subtitleColor: style.colors.subtitleColor`（沿用风格默认，与 accentAlt 同策略），更新函数注释。`CoverColorOverrides`（settings.colors）本身只有 enabled/bg/titleColor/accent 4 字段，无 subtitleColor，故只能沿用风格默认。read_lints 0 错误。

## 修复：video crypto.ts 双重解压类型推断错误 + 清理死代码

用户报 `src/features/video/utils/crypto.ts` L164「Uint8Array<ArrayBufferLike> 不能赋给 Uint8Array<ArrayBuffer>」。根因：TS 5.7+ 的 typed array 泛型——`encryptedData.slice(1)` 推断为 `Uint8Array<ArrayBuffer>`（slice 新建 buffer），导致 `let processedData` 被收窄；`decompressData()` 声明返回 `Uint8Array`（默认 `ArrayBufferLike`），两次调用把宽类型赋给窄类型报错。

修复：`let processedData: Uint8Array` 显式标注宽类型（兼容 slice 窄结果与解压宽返回值）。encryptVideo 无此问题（初始参数即 ArrayBufferLike）。

**顺带清理死代码**：`deriveKey`/`encryptData`/`decryptData` 三个函数确认无任何引用（旧密码加密方案遗留，当前已改为无密码压缩加密 encryptVideo/decryptVideo），已全部删除。删除后 crypto.ts 保留 compressData/decompressData/encryptVideo/decryptVideo/isEncryptedVideo/getOriginalFileName/getEncryptedFileName，read_lints 0 error/0 warning。

## 修复：aiApi.ts 旧供应商降级比较与 AiProvider 类型无重叠

用户报 `src/utils/aiApi.ts` L697「AiProvider 与 openai 没有重叠」。根因：`AiProvider = "tongyi" | "deepseek" | "xiaomi" | "custom"`（src/types/ai.ts）已不含 openai/zhipu，但 `getApiConfigFromPlugin` 里 `let provider: AiProvider = settings.aiApiProvider || "tongyi"` 先收窄再与 "openai"/"zhipu" 比较，永远不成立。

修复：改为先以 `string` 类型读取旧值（`const rawProvider: string = settings.aiApiProvider || "tongyi"`），三元判断旧值降级为 tongyi，再 `rawProvider as AiProvider` 收窄。保留兼容迁移意图（老用户存储的 openai/zhipu 值降级），消除类型错误。read_lints 0 错误。

## 修复：vueAppHelper createVueDockApp 的 init 回调签名与 addDock API 不符

用户报 `src/utils/vueAppHelper.ts` L77「(dock: { element?: Element }) => void 不能赋给 (this: Custom | MobileCustom) => void」。根因：`plugin.addDock()` 的 `init` 是**无参数**回调（this 指向 Dock 模型），代码却声明了 1 个 `dock` 参数——TS 函数赋值规则（源参数不能多于目标）报错，且运行时思源不传参，`dock` 实为 undefined，`dock.element` 会抛错（隐藏运行时缺陷）。

修复：`init` 改为普通函数 `function (this: { element?: Element })`，用 `this.element?.appendChild(container)` 挂载容器。Custom/MobileCustom 均有 element 属性，this 类型 `{ element?: Element }` 为其 supertype 可兼容。read_lints 0 错误。

## 新增：docAnalysis 独立窗体承载（addTab + openTab + openWindow 双形态）

用户需求：docAnalysis 增加独立窗体按钮，参考项目独立窗口承载规则。6 步全部完成：

1. **新建 `types/manager.ts`** — DocAnalysisManager 类（参考 ToolCollectionManager）：模块级 `tabRegistered` 防重复注册；`registerTabModel()` 构造时 addTab（init 的 this.element 挂载，容器补 vp-dock-root + 全高 + overflow hidden）；`openFloating()` = openTab(主窗口页签, id=`${plugin.name}doc-analysis-tab`, icon=iconDocAnalysis) → openWindow(1280×800)；`destroy()` unmount + 移除容器
2. **index.ts** — 注册 addIcons(iconDocAnalysis symbol) + 实例化 Manager + 挂载 `(plugin as any).__docAnalysis = { openFloating, destroy }`；保留 createVueDockApp 与快捷键
3. **index.vue** — tab-bar 的 spacer 后新增 float-btn（mdi:open-in-new，`v-if="!isFloating"`，title=i18n.openFloatingWindow）；`isFloating` = getFrontend()==="desktop-window"；`openFloatingWindow()` 经 `plugin.__docAnalysis.openFloating()` 调度；Props 新增 `mode?: "dock" | "tab"`（withDefaults 默认 dock）
4. **styles/index.scss** — `.tab-bar` 内新增 `.float-btn`（描边按钮，$vp-radius + $font-size-xs）
5. **i18n** — zh_CN「在独立窗口打开」/ en_US「Open in floating window」分片同步
6. **src/index.ts** — DESTROYABLE_KEYS 加 `"__docAnalysis"`（addTab 模型 + 独立窗口）

独立窗体 UI 精简：docAnalysis 面板头部是功能 Tab 栏（无重复标题字样），无需隐藏；仅 float-btn 在浮动窗口内隐藏。DocI18n 是 Record<string,string>，无需改类型。read_lints 0 错误。`pnpm i18n:verify` 等由用户自行验证。

**间距修复**：用户反馈 float-btn 与其他按钮间距对不上。根因：tab-bar 的 `gap: 2px` 对无边框 tab-btn 够用，但 float-btn 与 analyze-btn 都是实心描边按钮，2px 太挤。修复：`.float-btn` 加 `margin-right: $spacing-1`（4px），实际间距 6px，按钮簇内部协调。read_lints 0 错误。

## 重构：docAnalysis StatsView 表格化布局（去 Tab）

用户需求"完全重构 UI 布局，不需要 tab，内容表格化"。澄清结论：全部转表格（含柱状图）、保留 Hero 但 UI 统一、保留下钻交互。

改动（6 文件 + 删 6 文件）：
- `types/index.ts`：新增 `StatTableRow { id, label, count, pct, colorClass?, clickable? }` 表格行接口
- `components/StatsView/StatTable.vue`（NEW）：通用表格区块组件（标题 + 三列「名称/数量/占比」表格 + 行点击下钻 + headerExtra slot），`handleRowClick` 仅 `clickable` 行触发 `emit("select", id)`
- `components/StatsView/index.vue`（重写）：去 Tab，渲染 Hero + 工具栏（名称排除/隐藏零值）+ 九个表格区块（大小/时间/书签/发布/质量 5 卡片类 + 平台/字数/深度/书签分类 4 分布类）。卡片分区用 `cardRowsMap`（computed 按 key 缓存）+ 书签表 headerExtra「详情」按钮
- `composables/useStatsOverview.ts`（重写）：删 `barPct/maxCount/maxWordCount/maxCustomBm/maxDepthCount` 柱状图计算与 `filterVisibleCards`；`pctStr` 去掉 `Math.min(100,...)` 上限（平台分布可超 100%）；新增 `toCardRows(cards)` 把卡片元数据映射为表格行；`platformEntries` 去 pct 字段
- `styles/StatTable.scss`（NEW）：表格区块样式 + 原 StatCard.scss 的颜色类（`.card-value.zero/.dup/.pending-color` 等）迁移为 `.st-td-count.xxx`
- `styles/StatsOverview.scss`（重写）：删 `.stats-tab-btn/.section-cards/.bar-chart`，保留 Hero/issue-item/toolbar-btn/placeholder/bookmark-detail-btn
- 删除：StatCard.vue / StatSection.vue / BarRow.vue + 对应 3 个 SCSS

关键决策：占比统一 = `count/totalDocs`（替换原卡片 pctStr + 柱状图 barPct 两套逻辑）；下钻映射与原一致（卡片类行→selectCategory(id)、深度行→selectDepth(Number(id))，平台/字数/书签分类纯展示）；隐藏零值 `filterZeroRows` 作用于全部表格。read_lints 0 错误，SQL 层零改动。用户自行验证 lint/i18n/icons/tsc。

**经验教训补充**：本次对 useStatsOverview.ts 采用整文件 write_to_file 重写（而非多段 replace_in_file），彻底规避了上次并行/串行 replace 的竞态丢失问题，一次成型。

## 调整：StatsView 表格区块 2 列布局（grid → 瀑布流）

用户反馈"2列显示"，实现后用 grid 2 列；随后反馈"间距不好看，改成瀑布流"。

最终方案：`.stats-grid` 用 CSS multi-column 瀑布流（`columns: 2; column-gap: $spacing-2;`），`.stat-table` 加 `break-inside: avoid` 防跨列拆分、`margin-bottom: $spacing-2` 提供列内垂直间距。列内为纵向填充（首列填满再入次列），与 grid 的横向排列不同。

**调试经验**：用户报"没有效果"时，排查构建产物（`E:/siyuan2/data/plugins/siyuan-plugin-vite-vue-sn/index.css`）发现 docAnalysis 的 `.stats-grid[data-v-c12a9339]` 缺失、产物时间戳停留在改动前 → 判断 `pnpm dev` 的 watch 进程停止/未重建，非代码问题。教训：改动后若未生效，先查构建产物时间戳与 scope hash 对应类名，确认是否真的重建。

## 重构：docAnalysis Hero 汇总卡组件化 + 健康度扣分项可配置

用户需求：Hero 汇总卡（总文档+健康度+问题速览）改为组件；健康度信息图标从 title 悬浮改为点击弹出显示；扣分项可动态设置，勾选到的才参与扣分分析。

改动（8 改 + 2 新建）：
1. `types/index.ts` — 新增 `DeductionKey`（7 项联合类型）、`DeductionOptionDef { key/label/resolve(stats, ctx, depthStats) }`、`DEDUCTION_OPTIONS` 注册表（0B空/重名超出/不使用/无书签(排除0B)/部分发布/深度>7/字数>2万，resolve 与旧硬编码公式逐项一致，字数项引用 `WC_TOP_BIN_LABEL` 常量防字符串耦合）、`HealthSettings { enabledDeductions }`、`DEFAULT_HEALTH_SETTINGS`（默认全启用保持旧行为）、`DeductionRow` 明细行类型
2. `types/storage.ts` — `DocAnalysisStorage` 新增 `healthSettings` TypedStorage（key `doc-analysis-health-settings`）
3. `composables/useStatsOverview.ts` — `_healthBreakdown` 改为遍历 `DEDUCTION_OPTIONS` 仅累加启用项；`healthTooltip` 按启用项生成；新增 `deductionRows` computed 导出（禁用项也展示数值）；Props 增加 `healthSettings`
4. `composables/useDocAnalysis.ts` — 新增 `healthSettings` ref + `loadHealthSettings()` + watch 持久化（沿用 dupFilterLoaded 防回写覆盖模式，`makeDefaultHealthSettings` 深拷贝数组）
5. `components/StatsView/HeroCard.vue`（NEW）— 承接原 .stats-hero 渲染（hero-top + 问题徽章 emit selectCategory）；信息图标改 button `@click` 弹出 Modal（Teleport + %modal-* mixin）含健康文档汇总 + 7 项扣分项 checkbox（`healthyDocs` computed 与 healthPct 口径一致）；勾选 emit `update:healthSettings`
6. `styles/HeroCard.scss`（NEW）— 迁移 .hero-*/.issue-item 样式（根类 .stats-hero→.hero-card）+ 面板/checkbox 样式（Codex Token，无 box-shadow）
7. `components/StatsView/index.vue` — 内联 hero 替换 `<HeroCard>`，Props/Emits 增 healthSettings/update:healthSettings，透传 deductionRows
8. `styles/StatsOverview.scss` — 删除已迁移的 hero/issue 样式（151 行）
9. `docAnalysis/index.vue` — 解构 healthSettings/loadHealthSettings，onMounted 加载，`:health-settings` 传入 + `@update:health-settings="handleUpdateHealthSettings"`（watch 自动持久化）
10. `README.md` — 补充健康度配置说明

数据流：HeroCard 勾选 → emit → index.vue → healthSettings ref → useStatsOverview 重算 healthPct → watch 回写存储。

注意点：`types/index.ts` 新增运行时 import `WC_TOP_BIN_LABEL` from `../utils/docStatsAnalyzer`（该文件对 types 仅 type-only import，无运行时循环）；图标复用已注册 `mdi:information-outline`/`mdi:close`（`mdi:heart-pulse` 未注册已替换）。

**经验教训复现**：index.vue 单次消息内 6 个并行 replace_in_file，其中 1 个（handleUpdateHealthSettings 函数定义）报告成功但实际丢失，read_lints 报「HealthSettings 未使用 + handleUpdateHealthSettings 不存在」才暴露，已串行补回。再次确认：同一文件多处编辑必须串行或整文件重写。read_lints 0 错误，`pnpm lint`/`tsc` 等由用户自行验证。
