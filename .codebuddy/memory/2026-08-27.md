# 2026-08-27

## gitPush ListView 架构审查与层级瘦身（Plan 56497edb 执行完成）

背景：用户对 ListView/index.vue 只引用 2 个组件（ListViewToolbar/ProjectCard）而目录下 14 个组件存疑。经架构审查确认三层结构（容器→编排→叶子）合理——v-for 模板内无法调用 setup hooks，卡片必须独立组件实例，index 只引用 2 个是正确抽象非缺陷。调整方向定为"层级内瘦身"。

### 改动清单（5 步，纯重构行为零变化）

1. **拆分 CardHeader（486→约190行）**：操作按钮区（分类下拉/平台/IDE/刷新菜单/编辑/Git配置/删除）→ 新建 `CardHeaderActions.vue`（自包含 useCardServices/useCardMenu/useCardActions，props 仅 project+reloadLog+refreshTags）。CardHeader 只留信息区（星标/名称/路径/md 徽章/分支/备注）。
2. **提取 CardTabs.vue**：ProjectCard 内嵌 4-Tab 条（CHANGES/LOG/STASH/TAG + 计数徽标）→ 独立组件（`v-model` + 4 个 count props + TABS 元数据表 + `counts` computed 按 countKey 取数），导出 `CardTabId` 类型，ProjectCard 的 `stashTagTab` 改用该类型。
3. **简化 outputPanels**：删除 ProjectCard 的 `outputPanels` computed（v-for 绕弯），直接渲染 pull/push 两个 OutputPanel；`computed`/`PushOutputEntry` import 清理。
4. **修复 i18n 违规**：ConflictSection.vue 4 处 `{{ i18n.xxx || '中文兜底' }}` 全删（键已存在）；BranchCommitList.vue 硬编码 placeholder/title/加载中/空态改 i18n 键；新增 4 个分片键（zh/en 同步）：`conflictFileCount`（共 {0} 个文件）、`commitSearchPlaceholder`、`commitListEmpty`、`commitListNoMatch`；所有 i18n 键上方补中文 HTML 注释。
5. **样式迁移**：CardHeaderActions.scss（新，操作区样式自 CardHeader.scss 迁出）；CardTabs.scss（新，`.gp-stash-tag-*` 自 index.scss 迁出）；`.gp-caret-icon`（被 CardActionBar 复用）从 CardHeader.scss 迁至 Shared.scss（共享原子样式）；CardHeader.scss 更新头注释。README 目录结构更新（ListView 15→17 个组件）。

### 关键决策
- **不取消 ProjectCard 中间层**：v-for + setup hooks 技术约束，取消需父层 Record 膨胀（项目早期重构掉的模式）。
- **`.gp-caret-icon` 迁移**：确认 CardActionBar.scss 未定义它，仅 CardHeader.scss 有定义（跨组件依赖），迁到 Shared.scss 而非留在 CardHeader.scss（语义不符）。
- 未做：WorkingTreePanel（~300 行）与 AiErrorAnalysisDialog（317 行）未拆分——两者都在警戒线附近但职责完整独立，且本次范围聚焦 ListView 编排层。StashSection/TagPanel 模式相似但 Rule of Three 判定暂不抽象。

验证：read_lints 0 error（gitPush 全目录）。用户需自行验证 `npx tsc --noEmit`、`pnpm i18n:verify`。

### 修复：ProjectCard.vue Invalid end tag 编译错误

用户在 vite build 时遇到 `ProjectCard.vue (124:3): Invalid end tag`。根因：用 CardTabs 替换内嵌 tab-bar 时，replace 删除了 `.gp-stash-tag-tabs` 的**开标签** `<div>` 但**残留了对应的闭合 `</div>`**（原第 89 行），导致模板多一个结束标签。修复：删除残留 `</div>`；同时将 4 个面板组件（WorkingTreePanel/BranchCommitList/StashSection/TagPanel）的 6 空格缩进统一改为 4 空格（原容器删除后与 CardTabs 同级）。lint 0 error，模板标签闭合验证通过。

教训：用 replace_in_file 删除成对的开/闭标签之一时，务必确认对应闭合/开标签是否也需删除；SFC 模板改完最好通读一遍结构。

## gitPush BranchCommitList：移除修正图标 + 增加绝对时间显示

用户要求"移除修正图标，只是移除图标，增加时间显示"。改动 2 个文件：

1. **BranchCommitList.vue**：
   - 移除 `bcl-fix-btn` 铅笔图标按钮（`mdi:pencil-outline`）；修正入口保留——提交消息文本 `bcl-msg--clickable` 的点击仍 emit `fixCommit`，emit 定义不动
   - `bcl-date` 内新增绝对时间显示：`{{ entry.relativeDate }}`（相对）+ `{{ formatDate(entry.date) }}`（格式化为 `YYYY-MM-DD HH:mm`）
   - 新增 `formatDate()` 纯函数：ISO `%aI` 字符串 → 本地时区 `YYYY-MM-DD HH:mm`（无效日期回退原值）
2. **BranchCommitList.scss**：删除无用 `.bcl-fix-btn` 样式；`bcl-date` 改 inline-flex + gap 6px，`.bcl-date-absolute` 提高不透明度（0.65）与相对时间区分层级

lint 0 error。行为变化：修正弹窗入口只剩点击提交消息文本；时间区显示"相对时间 · 绝对时间"双信息。

## reportMetrics.ts 审查与修复（逻辑漏洞/死代码/冗余）

审查 `src/features/gitPush/reportMetrics.ts`（760 行纯函数指标引擎），修复 7 处问题：

### 逻辑漏洞修复（3 处）
1. **`maxCommitStreak` UTC 日 bug**：原 `new Date(t).toISOString().slice(0, 10)` 取 UTC 日历日，注释声称"本地日历日"，与 weekday/daily 统计口径冲突（东八区 08:00 前提交会被归到前一天）。改为 `formatLocalDate(new Date(t))`
2. **`gap === 1` 严格浮点比较**：改本地口径后 DST 时区相邻日跨度 23/25h 会误断连续，改为 `gap < 2`
3. **`aggregateDailyStats` 日期 key 口径**：原 `c.date.slice(0, 10)`（提交时区日期）与 `hourOfDay`（查看者本地时刻）矛盾，改为 `formatLocalDate(new Date(t))` 统一本地口径

### 死代码删除（2 处）
4. **`countTrackedFilesLines`** 整个函数无任何外部调用（全项目搜索确认）→ 删除
5. **`AuthorAgg.days` Set**：收集后从未被读取（activeDays 用 firstDate/lastDate 跨度计算）→ 删除字段 + 初始化 + add 语句

### 冗余消除（2 处）
6. `buildReportData` 手写 `["hot","warm","cool","cold"]` → 复用 `HOTSPOT_LEVEL_ORDER`（buildEmptyReport 已用后者）
7. `debtSummary` 零值对象两处手写 → 提取 `emptyDebtSummary()` 私有函数共用

### 遗留建议（未改，保守）
- **导出冗余**：约 17 个符号内部使用却 export 且外部无引用（`WICK_PAD_HOURS`、`aggregateDailyStats`、`aggregateWeekdayStats`、`aggregateHourlyStats`、`maxCommitStreak`、`buildRhythmStats`、`emptyRhythmStats`、`HOTSPOT_LEVEL_ORDER`、`heatAdviceKey`、`suggestionKey`、`debtRiskScore`、`debtSeverity`、`heatScore`、`heatLevel`、`aggregateAuthorStats`、`isCodeFile`、`fileExistsInRepo`、`fetchFileDiff`）——非缺陷，去 export 会改变模块公共 API 表面，留待用户决策
- `debtRows.loc` 恒为 null（只在 hotspotRows Top12 填充）是设计权衡，DebtFileDetail.vue 展开时懒加载兜底，非 bug
- 重复内联类型 `{ added, deleted }`（sumProjectLines/sumAuthorLines）可提取 `LineDelta`，轻微未做

验证：read_lints 0 error。用户需自行验证 `npx tsc --noEmit`。

## reportMetrics.ts 遗留优化执行完成（Plan 18e19d2f 完成）

基于上一轮审查的两项遗留建议实施纯瘦身重构，仅改 reportMetrics.ts 单文件：

1. **去除 18 个内部符号 export**：`aggregateAuthorStats`、`WICK_PAD_HOURS`、`aggregateDailyStats`、`aggregateWeekdayStats`、`aggregateHourlyStats`、`maxCommitStreak`、`emptyRhythmStats`、`buildRhythmStats`、`fetchFileDiff`、`fileExistsInRepo`、`isCodeFile`、`debtRiskScore`、`debtSeverity`、`heatLevel`、`heatScore`、`heatAdviceKey`、`suggestionKey`、`HOTSPOT_LEVEL_ORDER` 全部去 export 改模块私有。外部有引用的 15 个符号保留 export（NumstatCommit/parseNumstatBlocks/aggregateFileStats/shouldIncludeFile/sumProjectLines/sumAuthorLines/calcMovingAverage7/countFileLines/countTrackedFileLinesMap/DEBT_MIN_MOD_COUNT/countDebtFiles/sinceForRange/buildReportData/buildEmptyReport/DEBT_SEVERITY_ORDER）
2. **提取 `LineDelta` 接口**（`{added, deleted}` 私有接口）：sumProjectLines 返回 LineDelta，sumAuthorLines 返回 `Map<string, LineDelta>`，调用方 useCommitAnalysis.ts/ProjectLineDetail.vue 仅读成员零改动
3. **提取空桶辅助函数**：`emptyWeekdayBuckets()`（7 项）+ `emptyHourBuckets()`（12 项），emptyRhythmStats 复用，消除与聚合函数的构造重复
4. **顺带补漏**：buildReportData 中 `debtSummary` 手写零值对象上一轮漏改，本轮统一为 `emptyDebtSummary()`（与 buildEmptyReport 对齐）

经验教训：**对同一文件并行执行多个 replace_in_file 会因文件写锁竞争超时**（19 个并行仅 8 个成功，失败报 "Acquire write lock ... timeout"），同一文件的写操作必须串行执行。已改为逐个 replace 补做成功。

验证：read_lints 0 error。用户需自行验证 `npx tsc --noEmit` + `pnpm lint`。

## globalRelations 模块代码审查与修复（Plan 8a161801 执行完成）

审查 `src/features/globalRelations`（5 文件）的逻辑漏洞/冗余/内存泄露/死代码/重复类型，修复 7 类问题：

1. **注册模式对齐 AGENTS.md**：registerGlobalRelations 内部自挂载 `(plugin as any).__globalRelations = manager`；`__globalRelations` 加入 DESTROYABLE_KEYS；删除 onunload 特例分支；`__globalRelations` 字段类型补 `toggle`。src/index.ts 注册行改为不接收返回值。
2. **类型归位 + 复用**：`GlobalRelationRow`/`DirectionFilter` 从 composable 迁至 types/index.ts；删除 `BacklinkDoc` 改复用 `@/api` 的 `IRefFile`。
3. **死代码清理**：index.vue 删除未使用 `plugin` prop（含 buildProps 传参）；composable 返回移除 `rows`；`parseAnchorText` 去 export；i18n 删除 4 死键（source/target/openDoc/rows，中英分片同步）。
4. **i18n 兜底违规修复**：index.vue 删除 `t(key, fallback)` 函数改直用 `i18n.xxx`（i18n prop 类型 Partial→全量 GlobalRelationsI18n），所有 i18n 使用处补中文 HTML 注释；composable `loadFailed || "加载失败"` 兜底删除。
5. **SQL 性能保护**：queryGlobalRelations 加 `LIMIT 501`（多取一条判截断），新增 `truncated` ref + `truncatedHint` i18n 键（zh/en），列表底部渲染截断提示条，stats 与列表同口径。
6. **新建 `src/utils/sqlHelpers.ts`** 公共 SQL 工具（escapeSql/quoteSql/quoteSqlList，从 docAnalysis 版本迁移），composable 改从 `@/utils/sqlHelpers` 导入并删本地定义；docAnalysis 等既有模块暂不迁移（控制爆炸半径）。
7. **SCSS 色值去重**：`styles/index.scss` 定义 `$gr-accent: #06b6d4` 替换两处硬编码，新增 `.gr-truncated-hint` 样式。

经走查确认不改：INNER JOIN 过滤孤儿 refs（思源保证一致性）、toggleDetails 竞态（行为正确）、反链去重与 docNavigation 同源逻辑（Rule of Three 未满不提取）。

验证：read_lints 0 error。用户需自行验证 `pnpm i18n:merge`（顶层 JSON 已过时）+ `pnpm i18n:verify` + `npx tsc --noEmit` + `pnpm lint`。
