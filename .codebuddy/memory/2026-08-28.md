# 2026-08-28

## 审查 floatingToolbar 功能模块
- 审查了 src/features/floatingToolbar/ 全模块（core/FloatingToolbar.ts 为核心）。
- 定位：为思源原生选中文本浮动工具栏（.protyle-toolbar）注入自定义按钮的增强模块。
- 机制：document mouseup 监听 + 300ms 防抖 → 定位 .protyle 容器 → 向工具栏注入已注册 Action 按钮；MutationObserver 监听工具栏隐藏(fn__none)复位 data-custom-buttons-added 标记。
- Action 体系：ToolbarActionManager 管理，内置复制；扩展有 二维码/谐音翻译/翻译替换(Ctrl+Z 可撤销)/存密码，按 enableQRCode/enablePronunciation/enableTranslate/enablePasswordVault 开关注册；对话框类通过 createDialogAction 工厂 + 事件总线(App.vue 调度) 解耦。
- 附加能力：选中英文单词命中闪卡单词本时黄色高亮（word-from-notebook）；HeatmapMarker 按 practiceCount 4 级热度着色扫描文档单词。
- 集成合规：实例自挂载 (plugin).__floatingToolbar，已在 DESTROYABLE_KEYS 清单，onunload 统一销毁。
- 发现的规范问题（未修复，仅记录）：(1) createActionButton 硬编码 font-size:14px；(2) registerBuiltinActions 用 as any + 中文兜底，违反 i18n 禁止兜底规则；(3) ensureStyles 硬编码 rgba 色值；(4) cleanupAllToolbars 不清理已注入按钮 DOM（依赖存在性检查防重复）。

## 确认：「文档单词文本自动高亮」= heatmapMarker 功能
- 归属：floatingToolbar 模块子能力，核心实现 core/HeatmapMarker.ts（405 行）。
- 机制：启动加载闪卡单词本(practiceCount)→TreeWalker 扫描可见文档文本（跳过代码块/标题/kbd）→命中词汇包 span.heatmap-word + data-heat 4 级热力（≤3/≤10/≤30/>30 次）→黄色渐变 rgba(255,213,79,.85)→disable 时 unwrap 还原并 normalize。
- 设置项：enableHeatmapMarker（settings.ts 第43行，默认 true），i18n 描述见 heatmapMarker.json。
- 配套：选中单词命中单词本时 ::selection 黄色背景高亮（word-from-notebook），与自动扫描高亮是两回事。

## 定时任务统一入口（TimerRegistry）改造完成
- 新增 `src/utils/timerRegistry.ts`：`TimerRegistry` 类（实例级，非单例），`setInterval/setTimeout/clear/clearAll`，导出 `TimerHandle`（=`ReturnType<typeof setInterval>`），内部 `Set` 托管句柄，`clear` 幂等接受 null。
- 改造 4 处启动时定时器接入（业务回调零改动，S3 调度状态机与 retry 有界轮询语义保留）：
  - `s3Backup/index.ts`：autoBackupTimer → `timers.setInterval`，stopAutoBackupTimer 用 `timers.clear`，destroy 加 `clearAll()`
  - `pageLock/index.ts`：缓存清理定时器 → register 内局部 `timers`，destroy 用 `clearAll()`
  - `generalSettings/modules/DocCountManager.ts`：updateTimer + initRetryTimer 接入，stop 加 `clearAll()`
  - `bookmarkMarker/modules/BookmarkMarker.ts`：autoUpdate + fileTreeRetry + protyleRetry + 2 个防抖全部接入
- 硬规则落点：`AGENTS.md` 统一入口表格加"定时器"行 + 硬规则新增「定时任务统一入口」（禁止裸 setInterval/setTimeout）；`AGENTS_API.md` API 参考新增「### 定时器」小节。
- 已知遗留（本次范围外）：`pageLock/index.ts:336` 密码弹窗 focus 的裸 setTimeout；statusBar/rssReader/quickNote 组件内 onMounted 定时器，由新规则约束未来新增。
- 全部改动文件 lint 0 错误；架构审查（universal-arch-skill 模式 C）通过。
- 修复 tsc 类型错误：`timerRegistry.ts` 的 `TimerHandle` 不能用 `ReturnType<typeof setInterval>`（@types/node 全局声明覆盖为 `Timeout`），也不能用 `ReturnType<typeof window.setInterval>`（`window: Window & typeof globalThis` 合并重载仍解析为 Timeout）；直接定义为 `export type TimerHandle = number`（Electron 渲染进程定时器句柄即 number）后 tsc/lint 通过。教训：本项目全局 setInterval 类型被 @types/node 污染，凡是浏览器句柄一律用 `number`。
- pageLock 缓存清理定时器已移除（用户确认）：`index.ts` 删除 `TimerRegistry` 定时器与 `timers.clearAll()`；`utils/cache.ts` 改为惰性清理——`setCachedLockState` 写入前调 `cleanupCache()` 淘汰过期/超量条目，`getCachedLockState` 命中过期条目时顺带 `delete`。`cacheCleanupInterval` 配置项与 `CACHE_CLEANUP_INTERVAL` 常量保留未删（无引用无副作用）。lint 通过。
- statistics 新增定时自动刷新（可配置间隔）：storage.ts 加 `StatisticsSettings`（autoRefreshEnabled/refreshInterval，默认关/30min）+ `STATISTICS_STORAGE_KEYS.SETTINGS` + load/saveSettings；core.ts Statistics 类加 `TimerRegistry` 定时器 + `applyAutoRefresh()`，extraProps 暴露 `onAutoRefreshChange`；index.vue 挂载时 `initAutoRefresh()` 读设置并应用，`handleAutoRefreshChange` 保存+通知；StatisticsHeader 加原生 select 下拉（关闭/5/10/30分钟/1/2/6小时，emit autoRefreshChange）；zh_CN/en_US 各加 8 个键。数据流：面板 onMounted→读设置→onAutoRefreshChange→core 启停定时器→回调 manualRefresh→panelRefreshFn 全量刷新；destroy→timers.clearAll()。lint 通过。后续增加 1 分钟档位（interval1min 键，zh_CN/en_US 同步）。
- statistics 刷新状态接入底部状态栏：index.vue 用 `useStatusBarTask("statistics-refresh", "mdi:chart-bar")`（统一入口，跨 feature 允许，无循环依赖），refreshData 里 progress/complete/fail 三态；i18n 加 statusRefreshing/statusRefreshDone/statusRefreshFailed 键（zh_CN/en_US）。
- **重要修正**：思源 `addDock` 的 init 是懒加载，Dock Vue 组件在首次展开面板时才挂载——因此程序启动时 onMounted 的刷新不触发（用户实测发现）。已重构为"模块级单例 + core 启动预载"方案：
  1. `useStatistics.ts` 改为模块级单例状态（loading/stats/lastUpdateTime/viewMode/dayRange 等），导出 `refreshStatisticsData()`（含 loading 防重 + statusTask 三态）与 `setStatisticsI18n()`；
  2. `core.ts` 删除 panelRefreshFn/onRegisterRefresh 依赖，`init()` 注入 i18n + `preload()` 启动预载（`void this.preload()` 调 refreshStatisticsData），manualRefresh 直调模块级刷新；
  3. `index.vue` 删除 onRegisterRefresh/statusTask，onMounted 按预载状态分流：stats 就绪→仅 loadHistoricalData；预载中→watch 等数据到达后补历史；否则→兜底 refreshData。
  效果：程序启动即刷新（底部状态栏可见"正在刷新统计..."），面板打开直接显示预载数据且不重复全量刷新。lint 通过。
- **新增通用 Dock 预加载工具** `src/utils/dockPreload.ts`（用户要求统一通用化，方便后续 Dock 功能复用）：模块级注册表 + 状态机（idle/loading/ready/error）+ useStatusBarTask 三态。API：`registerDockPreload({id, icon, labels, refresh})`（幂等）、`runAllDockPreloads()`（插件启动串行统一执行）、`refreshDockPreload(id)`（手动/定时刷新，loading 防重）、`getDockPreloadState(id)`（面板分流）、`clearDockPreloads()`（卸载清理）。
  - statistics 切换接入：core.ts 删 preload/setStatisticsI18n，init 内 registerDockPreload（labels 从 plugin.i18n.statistics 提取，解决启动时面板未挂载取不到 i18n），manualRefresh/定时走 refreshDockPreload；useStatistics.ts 删状态栏逻辑变纯数据层（保留模块级 loading 供面板 UI）；index.vue 分流改 getDockPreloadState。
  - src/index.ts：onload registerFeatures() 后 `void runAllDockPreloads()`；onunload 加 `clearDockPreloads()`。
  - 规则落点：AGENTS.md 统一入口表格加"Dock 预加载"行 + 硬规则「Dock 预加载统一入口」；AGENTS_API.md 加「### Dock 预加载」小节（含最小接入示例）。
  - 架构审查（universal-arch-skill 模式 C）通过。lint 0 错误。
  - 状态栏任务项显示优化：`.status-bar-task-item` 字号 $font-size-xs(12px)→$font-size-2xs(10px)、.monitor-value 字重 semibold→normal（共享样式，所有任务项统一变小变轻）；statistics 状态栏文案简化（zh_CN/en_US）："正在刷新统计..."→"刷新中"、"统计刷新完成"→"刷新完成"、"统计刷新失败"→"刷新失败"。

## gitPush「提交规则检查」面板迭代：冗余消除 + 指定项目选择 + UI 优化
- 需求：审查冗余、新增指定项目单选过滤（用户确认：单选下拉 + 仅过滤展示，不改分析范围）、UI 微调。
- **冗余消除**：
  - `utils.ts` 新增纯函数 `analysisStatusText({analyzing, analyzed, analyzedAt, i18n, notRunKey})`，统一「分析中/上次分析/未分析」三元文案，`RuleCheckToolbar` 与 `AnalysisToolbar` 共用。
  - 新建公共组件 `components/common/CommitCountSelect.vue`（`COMMIT_COUNT_OPTIONS` + tooltip + `updateCount` emit，类 `.gp-count-select`）+ `styles/CommitCountSelect.scss`；两个工具条复用，删除 `.grc-count-select`/`.gpa-count-select` 死样式（顺带修正 `CommitAnalysisSettings.scss` 注释引用）。
  - 注：`.count-select` 样式实际在 4 处重复（grc/gpa/gls/bcl），本次仅迁移 grc/gpa 两处，LineStatsToolbar 与 BranchCommitList 未动。
- **项目选择（单选下拉 + 仅过滤展示）**：
  - `types/meta.ts` 新增 `RuleCheckPrefs { projectId: string }`；`types/storage.ts` 新增 `ruleCheckPrefs` 槽位（key `git-push-rulecheck-prefs`，默认 `{projectId:""}`）。
  - `useCommitAnalysis.ts` 新增 `ruleCheckProjectId` ref（""=全部项目）、`effectiveRuleCheckProjectId` computed（项目已删自动回退""，与 analysisStats 失效过滤一致）、`setRuleCheckProject(id)`（即时持久化）、`loadRuleCheckPrefs`（ensureAnalysis 内调用）；`commitRuleStats` 改为按 effective 过滤 entries 后再聚合。
  - 数据链路：gitPush/index.vue（`:projects` + `:project-id="effectiveRuleCheckProjectId"` + `@update-project="setRuleCheckProject"`）→ CommitRuleCheck/index.vue（新增 projects/projectId props + updateProject emit + `scoped` computed）→ RuleCheckToolbar（`@/components/Select.vue` size=small，首项「全部项目」）。
  - `runCore`/`runAnalysis`/缓存结构零改动，提交分析视图零影响。
- **UI 优化**：工具条改两行布局（第一行 项目下拉 flex:1 + 条数 + 按钮，第二行满宽状态文案）；`ViolationListSection` 新增 `scoped` prop，单项目模式隐藏每行重复的项目名 chip。
- i18n：zh_CN/en_US gitPush.json 各新增 `ruleCheckAllProjects`（全部项目/All Projects）、`ruleCheckSelectProject`（选择项目/Select Project）。
- 全部改动 lint 0 错误。用户需自行验证：`pnpm i18n:verify`、`pnpm lint`、`npx tsc --noEmit`。
- **追加修正（用户反馈"选择框窗体太大"）**：`.grc-project-select` 原 `flex:1` 会撑满工具条行（下拉面板 width:100% 跟随），且 Select 默认 maxHeight 200px。改为 `flex: 0 1 160px; min-width: 0`（固定紧凑宽度，仿 gpr-range-select 的 flex:0 0 88px 写法）+ Select 加 `:max-height="150"`。lint 通过。
- **追加修正 2（项目数 300+ 场景）**：用户自行将 `:max-height` 从 150 下调到 100（个人偏好）。项目下拉启用 `filterable`（`:filterable="projects.length >= 10"`）+ 复用 i18n `searchPlaceholder`（搜索项目...）作为 `filter-placeholder`，项目多时可直接输入过滤，解决 300+ 项目滚动查找困难。lint 通过。
- **追加修正 3（参考其他选择框）**：项目下拉尺寸 `size="small"`（28px 高）改为 `size="xsmall"`（22px 高），与 `VideoToolbar` 分类筛选、`apiDebugger` 方法选择等紧凑工具条 Select 用法保持一致，解决与同行条数选择/分析按钮高度不齐的问题；宽度 `flex: 0 1 160px`→`flex: 0 1 140px`。lint 通过。
- **追加修正 4（条数选项加 300/500/全部）**：`COMMIT_COUNT_OPTIONS` 从 `[30,50,100,200]` 扩为 `[30,50,100,200,300,500,"all"]`，新增导出类型 `CommitCount = number | "all"`。`getCommitLog`/`getCommitStatsLog` 底层已支持 `"all"`（省略 `-n`），runCore 中 `getCommitStatsLog` 对 `"all"` 传 `undefined`。连带改动：`commitCount` ref、`setCommitCount`、`CommitAnalysisCache`/`LineStatsCache.commitCount` 类型、4 个工具条/面板的 prop+emit 类型；`CommitCountSelect` 增加「全部」文案（i18n `analysisCommitsAll`/`analysisCommitsPerProjectAll`）与取值转换；`LineStatsToolbar` 顺带迁移到公共 `CommitCountSelect`（消除第 3 处 gls-count-select 重复，删除 `.gls-count-select` 死样式）。注：BranchCommitList(bcl) 仍保留自建原生 select 未动。lint 0 错误。
- **追加修正 5（项目下拉展开「别扭」= 面板过窄/项目名截断）**：用户确认是下拉面板过窄导致项目名截断。修复：(1) `.grc-project-select` 后代选择器覆盖 `.si-select__dropdown { min-width: 260px }`（默认 width:100% 跟随 210px 触发框）；(2) `.si-select__option { @include text-ellipsis }` 单行省略防换行撑高；(3) **全局 Select.vue** 两个 option（普通/分组）加 `:title="option.label"`，hover 显示完整标签。lint 0 错误。
