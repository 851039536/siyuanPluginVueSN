# 2026-08-29

## gitPush CommitRuleCheck 代码审查与修复

审查了 `src/features/gitPush/components/CommitRuleCheck/`（5 组件 + 依赖链路：commitRuleChecker.ts / useCommitAnalysis.ts / usePagedList.ts / CommitFixDialog.vue / types/meta.ts），五维度结论：逻辑漏洞 4 处、无内存泄漏、无死代码、无重复类型。

审查报告已保存至 `docs/gitpush-commit-rule-check-review.md`（doc-formatting-ex 排版，含扩展建议）。

### 已修复 4 处
1. **ViolationListSection.vue** 分页状态不重置：解构 `usePagedList` 的 `reset` + `watch(pagedSource, () => pagedReset())`，对齐 LogPanel 既有模式
2. **CommitFixDialog.vue + CommitRuleCheck/index.vue** saved 事件顺序依赖：`saved` 事件改为携带 `[projectId: string]` payload，`handleFixSaved(projectId)` 直接用事件参数，删除对 `editingViolation.value` 读取的注释契约
3. **commitRuleChecker.ts** 空白 scope 放行：`scope.trim() === ""` 判 invalidScope
4. **commitRuleChecker.ts** NaN 排序：`(Date.parse(x) || 0)` 兜底；启发式 `scopePart` 空格 scope 省略而非保留

### 扩展建议（报告 §九）
P0：一键启发式修复（复用 fixCommitMessageHeuristically）、规则可配置化（notChinese/type 白名单）、作者过滤；P1：批量修复、提交时实时校验、违规趋势；P2：导出报告、规则版本化。

lint 0 error（read_lints 验证），无新增 i18n 键，构建/tsc 由用户自行验证。
