# 2026-08-29

## gitPush CommitRuleCheck 代码审查与修复

审查了 `src/features/gitPush/components/CommitRuleCheck/`（5 组件 + 依赖链路：commitRuleChecker.ts / useCommitAnalysis.ts / usePagedList.ts / CommitFixDialog.vue / types/meta.ts），五维度结论：逻辑漏洞 4 处、无内存泄漏、无死代码、无重复类型。

审查报告已保存至 `docs/gitpush-commit-rule-check-review.md`（doc-formatting-ex 排版，含扩展建议）。

### 已修复 4 处
1. **ViolationListSection.vue** 分页状态不重置：解构 `usePagedList` 的 `reset` + `watch(pagedSource, () => pagedReset())`，对齐 LogPanel 既有模式
2. **CommitFixDialog.vue + CommitRuleCheck/index.vue** saved 事件顺序依赖：`saved` 事件改为携带 `[projectId: string]` payload，`handleFixSaved(projectId)` 直接用事件参数，删除对 `editingViolation.value` 读取的注释契约
3. **commitRuleChecker.ts** 空白 scope 放行：`scope.trim() === ""` 判 invalidScope
4. **commitRuleChecker.ts** NaN 排序：`(Date.parse(x) || 0)` 兜底；启发式 `scopePart` 空格 scope 省略而非保留

### 扩展建议（报告 §九）
P0：一键启发式修复（复用 fixCommitMessageHeuristically）、规则可配置化（notChinese/type 白名单）、作者过滤；P1：批量修复、提交时实时校验、违规趋势；P2：导出报告、规则版本化。

lint 0 error（read_lints 验证），无新增 i18n 键，构建/tsc 由用户自行验证。

## gitPush CommitRuleCheck P1 三项扩展（同日，第二轮）

用户选择：提交校验**硬阻止**、批量修复**手动勾选多选**、趋势**违规率折线+提交数柱状双指标**。已全部实现：

### 1. 提交时实时校验（硬阻止）
- `ListView/WorkingTreePanel.vue`：导入 `checkCommitRule` + `COMMIT_RULE_REASON_META`，新增 `validationReason` computed，textarea 下方红色提示（`.wt-commit-invalid`，复用 ruleCheckReason* i18n 键），提交按钮 `disabled` 追加 `!!validationReason`，`handleCommit` 加防御守卫
- `styles/WorkingTreePanel.scss` 新增 `.wt-commit-invalid`（`$font-size-2xs` + `$color-danger`）

### 2. 违规趋势双指标
- `types/meta.ts`：新增 `RuleViolationTrendDay` 类型 + `CommitRuleCheckStats.dailyTrend` 必填字段
- `commitRuleChecker.ts`：新增 `buildRuleViolationTrend(entries, days=30)` 纯函数（按 `formatLocalDate` 聚合每日 total/violations/compliant/rate），`analyzeCommitRuleCompliance` 内部填充 dailyTrend（单一构造点，useCommitAnalysis 无需改动）
- 新增 `components/CommitRuleCheck/RuleTrendSection.vue` + `styles/RuleTrendSection.scss`：SVG polyline 违规率折线（`preserveAspectRatio="none"` 覆盖）+ CSS 柱状提交数（`withBarPct`），挂载于 ReasonDistributionSection 之后
- i18n：ruleCheckTrendTitle/Total/Rate 中英同步

### 3. 批量修复（手动勾选多选）
- 新增 `composables/useBatchCommitFix.ts`：`selectedKeys`（Set，key=`projectId-hash-reason`）+ `toggle/selectAllVisible/clearSelection/fixSelected`；仅 `fixCommitMessageHeuristically` 返回非空才执行 `manager.rewriteCommitMessage(resolveValidPath(project), hash, fixed, true)`，串行执行，结果统计 `{fixed,skipped,failed}`
- `ViolationListSection.vue`：`inject(CARD_SERVICES_KEY)` 拿 manager，标题区加全选/批量修复按钮（`.grc-section-actions` 右对齐，不依赖未生效的 gp-grow），每行复选框，结果提示，`emit("batchFixed")`；数据源变化时 `pagedReset()` + `clearSelection()`
- `CommitRuleCheck/index.vue`：`@batch-fixed="emit('runAnalysis')"` 全量刷新
- `config/icons.ts`：注册 `checkboxMultipleOutline`（mdi:checkbox-multiple-outline）+ `pencilMultiple`（mdi:pencil-multiple）
- i18n：ruleCheckSelectAll/BatchFix/BatchFixTip/BatchResult 中英同步
- `styles/CommitRuleCheckPanel.scss` 追加 grc-section-actions / grc-item-check / grc-batch-result

### 验证
read_lints 0 error；新图标需 `pnpm validate:icons`；i18n 新增 7 键需 `pnpm i18n:verify`；lint/tsc 由用户自行验证。经验：gitPush 的 `gp-grow`（Form.scss）未被样式链 @use，实际不可用，右对齐用自带容器 margin-left:auto。

## 批量修复"点击没反应"修复（同日，第三轮）

用户反馈批量修复点击无反应。根因排查与修复：

1. **根因 A（主要）**：批量修复按钮 `:disabled="selectedKeys.size === 0 || fixing"`——未勾选任何违规时按钮禁用但 `vp-btn--ghost` 禁用态视觉不明显，用户直接点击毫无反馈
2. **根因 B**：选中集合用 `ref<Set<string>>`，Vue 3 对 Set 的响应式代理（size/迭代依赖收集）边界较多，存在勾选状态不可靠风险

修复：
- `useBatchCommitFix.ts` 改用**对象映射** `selectedMap = ref<Record<string, boolean>>` + `selectedCount` computed + 提供 `isSelected`；`fixSelected` 用 `violations.value.filter(v => selectedMap.value[violationKey(v)])` 直接匹配
- `ViolationListSection.vue`：按钮 `:disabled` 仅保留 `fixing`（不再因无选中禁用）；未勾选时点击显示引导提示 `batchNotice`（i18n 键 `ruleCheckSelectHint`，警告色样式 `.grc-batch-result--hint`）；watch 数据源变化时清空选中+提示+结果
- i18n 中英新增 `ruleCheckSelectHint`（"请先勾选要修复的违规项"）

read_lints 0 error。

## 移除违规趋势功能（同日，第四轮）

用户要求移除「违规趋势」（刚实现的 P1 三项之一，保留实时校验与批量修复）。已完整回滚趋势相关代码：

1. 删除文件：`components/CommitRuleCheck/RuleTrendSection.vue`、`styles/RuleTrendSection.scss`
2. `types/meta.ts`：移除 `RuleViolationTrendDay` 类型 + `CommitRuleCheckStats.dailyTrend` 字段
3. `commitRuleChecker.ts`：移除 `buildRuleViolationTrend` 函数 + analyzeCommitRuleCompliance 的 dailyTrend 填充 + 相关 import（RuleViolationTrendDay/formatLocalDate）
4. `CommitRuleCheck/index.vue`：移除 RuleTrendSection 挂载与 import
5. i18n：移除 `ruleCheckTrendTitle/Total/Rate` 键（zh_CN + en_US 分片；顶层 en_US.json 残留的 ruleCheckTrend* 键为构建产物，由 pnpm i18n:merge 自动清理，禁止手动改）

注意：用户对该批扩展的选择是**逐个功能试用后取舍**——趋势功能被否，实时校验（硬阻止）与批量修复（勾选多选）保留。

## 批量修复"全跳过"困惑改进（同日，第五轮）

用户反馈"选了 2 项批量修复，结果跳过 2 项"。根因：`fixCommitMessageHeuristically` 只能修格式类问题（首尾空白/分隔符/空 scope），语义类违规（missingType/invalidType/emptySubject/notChinese）无法确定性修复，属设计如此但缺少事前提示与原因解释。

改进：
1. `useBatchCommitFix.ts`：`BatchFixResult` 新增 `skippedReasons: CommitRuleReasonKey[]`（去重收集跳过原因）；新增导出 `isAutoFixable(v)` 辅助函数（复用 fixCommitMessageHeuristically 判断）
2. `ViolationListSection.vue`：
   - 每行 reason chip 后显示绿色 `mdi:auto-fix` 图标标记可自动修复项（勾选前可辨识）
   - 结果提示追加跳过原因文案（`skippedReasonText` computed，按 COMMIT_RULE_REASON_META 映射 i18n 拼接）
3. i18n 中英新增 `ruleCheckAutoFixable`（"可自动修复"）；SCSS 新增 `.grc-item-autofix`（`$color` 用 `var(--b3-theme-success)`）

read_lints 0 error。经验：批量修复的价值定位是"修格式类问题"，语义类需 AI/手动，列表标记 + 原因解释是必要配套。

## missingType 纳入批量修复（同日，第六轮）

用户确认：missingType **关键词智能推断补 type**；notChinese **维持中文强制**（英文描述仍违规，无法翻译则跳过+原因提示）。

实现（仅改 `commitRuleChecker.ts`）：
- `fixCommitMessageHeuristically` 增加 missingType 分支：正则无 type 前缀时，描述含中文则 `inferType(raw)` 推断 type 补全为 `type: 描述`；描述非中文仍返回空串（保持中文强制）
- 新增私有 `inferType(subject)`：关键词命中顺序即优先级——修复/解决/错误/异常/缺陷/崩溃/bug→fix；新增/添加/实现/支持/功能/feat→feat；重构/优化/精简/拆分/合并/提取/抽象/调整→refactor；文档/注释/说明/readme→docs；测试/用例/单测→test；样式/格式/布局/排版→style；默认 chore
- 副作用：missingType 项现可批量修复（isAutoFixable 返回 true、列表显示 ✨ 标记）；invalidType/emptySubject/notChinese 维持跳过

read_lints 0 error。i18n 无需新增。

## 汉字正则可读性改进（同日，第七轮）

用户指出 `/[一-鿿]/`（U+4E00–U+9FFF 字面字符范围）可读性差。已在 `commitRuleChecker.ts` 定义命名常量 `HAN_CHAR_REGEX = /[\u4E00-\u9FFF]/`（附注释"匹配 CJK 统一表意文字"）并替换 3 处使用（checkCommitRule 的 notChinese 判定 + 启发式修复 2 处）。项目其他模块均用标准转义写法（`\u4E00-\u9FA5` / `\u4e00-\u9fff`），仅此文件用了字面字符。read_lints 0 error。

## gitPush CodeReport 审查修复（同日，第八轮，计划 2bc84e9e6dbf48b28a2c282d771af07f）

对 CodeReport（9 组件 + useCodeReport/reportMetrics/useDebtInsights/types/report）按已确认计划执行三批次修复，19 文件 +223/−287（净减 64 行）：

### 逻辑 Bug 修复
1. `aggregateFileStats` lastModified 改 parseIsoMs 比较（FileAgg 增 lastMs 字段），修复 ISO 字符串字典序在混合时区误判
2. `calcMovingAverage7` 改本地日期构造 `new Date(y, m-1, d)` + setDate 回退，修复 UTC 负偏移时区窗口错位一天
3. `savePrefs` 丢 `debtMinModCount` → 该偏好链路整删（CodeReportPrefs 字段 + useCodeReport ref/钳位 + buildReportData 参数，回归 DEBT_MIN_MOD_COUNT 常量）
4. AuthorContributionSection netBarWidth 补净增 0 → "0%" 分支；删除无样式定义的 `gpr-net-bar--zero`
5. 报告重生成后复位交互态：AuthorContribution watch `props.authors`、TechDebtSection watch `props.report`（注意：AuthorContribution 的 props 无 report 字段，须 watch authors）

### 死代码删除（用户口径：直接删）
- `adviceKey` 全链路（HotspotFileRow 字段 + heatAdviceKey() + 6 条 reportHeatAdvice* 中英 i18n 分片键）
- CodeReportData.projectId/projectName（buildEmptyReport 去 project 形参，useCodeReport 初始构造去掉空 GitProject）
- useCodeReport 的 generatedAt 导出；fileDetailsMap 的 LOC 预读（六项指标不展示行数，纯浪费同步读盘）
- 注释漂移修正：「3 分区」→「4 分区」、debtFiles「升序」→「降序」

### 文件补丁懒加载（性能主修）
- ReportOps 新增异步 `getFileHistoryPatch(projectPath, file, since?)`（execGit 通道，since 对齐统计范围，5KB 截断迁至 ReportOps，10s 超时）+ GitPushManager 委托
- useCodeReport 暴露 `fetchFilePatch(path)`（失败返回空串）→ index.vue → CodeReport/index.vue → AuthorContributionSection → FileDetailModal（本地 patchContent/patchLoading + 请求序号防竞态，加载态复用 i18n.loading）
- 删除同步预取：fetchFileDiff（execFileSync 卡顿主因，作者数×3 次同步子进程）、DIFF_MAX_CHARS、FileStatRow.diffContent；fileDetailsMap 变零 IO 派生

### 去重与分层
- 顺序常量单一源：types/report.ts 由 META 键序派生 DEBT_SEVERITY_ORDER/HOTSPOT_LEVEL_ORDER（types/index.ts 补值导出），reportMetrics 私有 SEVERITY_ORDER/HOTSPOT_LEVEL_ORDER 删除，排序改 indexOf，热点占比改按等级名查找（pctByLevel）消除下标耦合；私有 heatLevel 改名 hotspotLevel（与 utils.heatLevel 同名不同义）
- **新建 `src/features/gitPush/debtInsights.ts`**（自 composables/useDebtInsights.ts 迁出，纯函数无 composable；buildCoupledMap → createCoupledIndex O(n) 建索引 + get(path) 按需取；CoupledFile 改 `Pick<DebtFileRow,…>`；SeverityDistItem 与 HotspotLevelSummary 按 Rule of Three 保留独立），旧文件已删
- currentProject 收归 useCodeReport 单一实现（父组件传 prop），AuthorContributionSection netClass 薄封装内联

### 渲染与清理
- CodeReport/index.vue：5 分区改 `v-if="visited.has(tab)"` + `v-show` 组合（visited Set 首次激活后挂载，保留切回不重算）
- TechDebtSection groups 单次分桶替代四次 filter；rankedFiles 过滤 isCodeFile 前置（减 statSync）
- CandlestickSection：scroll 监听清理 onUnmounted→onBeforeUnmount（Vue 3.5 卸载时序下 unmounted 时模板 ref 已置 null，原移除静默失效）；chart 工作底色边界判定改判 `chartArea` 对象本身（原 `!left` 把 0 误判缺失）

### 验证与遗留
- 工具层经验：**同文件多个 replace_in_file 并行存在竞态**（部分编辑被覆盖丢失，reportMetrics 曾出现一半编辑落盘导致引用悬空），必须同文件单编辑或分消息串行；`| cat` 管道在 PowerShell 报错，git 用 --no-pager
- read_lints：均 0 error；AuthorContributionSection:200 报 props.report 为 TS 服务陈旧诊断（实际已改 props.authors），以用户 `npx tsc --noEmit` 为准
- 用户需执行：`pnpm lint` / `pnpm i18n:verify` / `pnpm validate:icons` / `npx tsc --noEmit`；顶层 zh_CN.json/en_US.json 的 reportHeatAdvice* 残留由 `pnpm i18n:merge` 自动清除（禁手改）
- 遗留 TODO：reportMetrics.ts ~740 行、CandlestickSection.vue ~540 行超 500 行硬阈值，用户明确本次不拆

## CodeReport 下拉框收窄（同日，第九轮）
用户反馈报告面板顶部 2 个选择框太大。修改 `styles/CodeReportPanel.scss`：`.gpr-project-select` 由 `flex:1`（拉满整行）改为 `flex: 0 1 180px`（固定基准宽 + 窄容器可收缩，min-width:0 保留）；`.gpr-range-select` 由 88px 收窄为 72px。`.si-select__value` 自带 ellipsis，超长项目名不撑破。仅改 CSS，未动 Vue 与组件。

## CodeReport 下拉框高度收矮（同日，第十轮）
用户反馈高度不对。把 `components/CodeReport/index.vue` 两个 Select 的 `size="small"`（min-height 28px）改为 `size="xsmall"`（min-height 22px / padding 2px 6px / 字号 xs），与紧凑工具条协调。全局 Select 支持 xsmall 变体（Select.scss `--xsmall`），面板工具条非弹窗表单，不违反弹窗表单 `size="small"` 规则。lint 0 error。

## 批量修复结果弹窗（gitPush CommitRuleCheck）
用户要求"点击批量修复弹出可以看到修改了什么"。实现结果对照弹窗：
1. `useBatchCommitFix.ts`：新增 `BatchFixItemStatus`/`BatchFixItem` 类型（projectId/projectName/hash/reason/original/fixed?/status），`BatchFixResult.items` 收集逐项明细（fixed 记录 original+fixed、skipped 记录 reason、failed 记录状态）
2. 新增 `BatchFixResultDialog.vue` + `BatchFixResultDialog.scss`：参考 LogDetailDialog（Teleport + gp-dialog-fade 过渡自包含 + useDialogKeyboard + overlay/dialog/header/body/footer）；按状态分区块——已修复（绿色，原信息删除线→修复后绿色）、已跳过（黄色原因标签）、失败（红色边框）；标题栏 3 统计徽章；约 480px 宽
3. `ViolationListSection.vue`：`resultDialog` ref + `runBatchFix` 完成后 `items.length > 0` 即弹窗（快照数据，刷新后仍可查看）；`emit("batchFixed")` 不变
4. i18n 新增 6 键（batchFixResultTitle/Fixed/Skipped/Failed/Original/FixedLabel）中英同步。read_lints 0 error。要点：弹窗数据是快照、独立于 watch 清空逻辑。

## CommitFixDialog 提交时间策略改选择框 + 持久化（gitPush）
用户要求：修正弹窗的"保留原始提交时间/按当前时间提交"从保存时展开按钮改为常驻选择框，持久化选择，保存前选好、点保存直接执行。
改动：
1. `CommitFixDialog.vue`：移除 `dateChoiceVisible` 展开交互；新增 `preserveDate` ref + `dateOptions`（复用 ruleFixPreserveDate/ruleFixDefaultDate i18n 文案）+ 常驻 `Select`（size="small" 符合弹窗表单规则）；`onDateChoiceChange` 选择即 `manager.storage.commitFixPrefs.save({ preserveDate })` 持久化；`init()` 加载偏好恢复选择；`save()` 直接 `performSave(preserveDate.value)`，`performSave` 参数改名 preserve 避免与 ref 同名遮蔽
2. `types/meta.ts`：新增 `CommitFixPrefs { preserveDate: boolean }`
3. `types/storage.ts`：新增 `commitFixPrefs` TypedStorage 槽位（git-push-commitfix-prefs）+ `DEFAULT_COMMIT_FIX_PREFS = { preserveDate: true }`
4. `styles/CommitFixDialog.scss`：删除无用的 `.gp-fix-date-choice`/`.gp-fix-date-actions` 旧样式
read_lints 0 error。无新增 i18n 键。

## CommitFixDialog 选择框缩小 + 并行覆盖修复（gitPush，同日续）

用户反馈提交时间选择框太大。修复：
1. `CommitFixDialog.vue`：`size="small"` → `size="xsmall"`（min-height 22px）+ `class="gp-fix-date-select"`
2. `CommitFixDialog.scss`：新增 `.gp-fix-date-select { max-width: 240px }`（不占满整行）

**并行覆盖教训**：期间发现 storage.ts 的 `GitPushStorage` interface 中 `readonly commitFixPrefs` 声明被并行工作覆盖丢失（constructor 赋值与 import 保留），以及 `performSave` 函数体仍引用旧参数名 `preserveDate`（改参数名为 preserve 时函数体内未同步）。均已修复：补回 interface 声明、函数体改用 `preserve`。read_lints 0 error。教训：多会话并行时同文件编辑可能互相覆盖，改完必须 read_lints 验证全链路（含类型声明完整性）。

## CommitFixDialog 时间策略改 radio 勾选（gitPush，同日续2）

用户建议"改为勾选会不会更好"。下拉 Select → **radio 单选**（两个互斥选项直接可见，比展开下拉更直观）：
- 模板：Select → 两个 `<label class="gp-fix-date-option"><input type="radio" name="gp-fix-date-choice" :checked="preserveDate" @change="onDateChoiceChange(true|false)">文案</label>`，accent-color 复用全局 radio 样式
- script：移除 Select import + dateOptions；`onDateChoiceChange(preserve: boolean)` 直接接收 boolean
- SCSS：`.gp-fix-date-select` → `.gp-fix-date-options`（flex gap wrap）/`.gp-fix-date-option`
- 注意：初次修改时 onDateChoiceChange 的 replace 因写锁超时未落盘，模板已改而函数体仍是旧 `v === "preserve"` 逻辑（true/false 恒得 false），重新读取后已修正。教训：写锁超时的编辑必须重读确认再补，不能跳过。

read_lints 0 error。

## CommitRuleCheck 冗余审查与清理（同日，第十二轮，计划 87613ba2）

用户要求审查 `components/CommitRuleCheck` 冗余。经两轮澄清确定范围：「仅目录内清理，跨面板 SCSS 与 CommitAnalysis 不动，不建 BarListSection，不改 CommitRuleCheckStats 字段」。

### 审查发现（含本轮不改、记录在案）
- 高：`.grc-*` 与 `.gpa-*` 有 19 个类定义逐行重复（cards/card/value/label、section-title/count、bar-list/row/label/track/fill/num、item-msg/meta/project/hash/date）
- 高：条形区块模板 4 处重复（ReasonDistribution + ProjectRanking + AuthorType×2）
- 中：分析运行按钮 2 处重复（RuleCheckToolbar / AnalysisToolbar，Icon + gp-spin + 文案一字不差，仅图标名不同）
- 中：`projectCount` prop 与 `projects.length` 恒等；`violationCount` 与 `violations.length` 恒等（commitRuleChecker 保证）
- 低：RuleCheckOverview 死分支；useBatchCommitFix 的 `selectedMap` 无消费方；同模块两条 import
- 逻辑不一致：单条修正走 `runAnalysis(projectId)` 局部刷新，批量修复却 `runAnalysis()` 全量重跑

### 已修复（7 文件）
1. `useBatchCommitFix.ts`：`BatchFixResult` 新增 `projectIds`（Set 去重收集成功修复的项目）；移除无消费方的 `selectedMap` 导出
2. `useCommitAnalysis.ts`：`runAnalysis(projectId?: string | string[])`，空数组归一化为 undefined —— `runCore` 用 `projectIds?.length && !needNumstat` 判局部刷新，传空数组会误落全量分支
3. `CommitRuleCheck/index.vue`：删 `projectCount` prop（空项目判据改 `projects.length === 0`）；`runAnalysis` emit 载荷放宽为 `string | string[]`；`@batch-fixed="emit('runAnalysis', $event)"`
4. `ViolationListSection.vue`：新增 `ViolationRow` 接口 + `pagedRows` computed 预计算 `key`/`autoFixable`（原模板每行每次渲染都跑 `fixCommitMessageHeuristically` 正则，50 行分页）；条数徽章与分页总数统一取 `pagedSource.length`；`batchFixed` 携带 `projectIds`
5. `RuleCheckOverview.vue`：删不可达的 `totalCommits === 0 → 100` 分支（父层 `v-if` 已拦截）
6. `ReasonDistributionSection.vue` / `ViolationListSection.vue`：合并重复的 `../../types` import
7. `gitPush/index.vue`：删 `:project-count="projects.length"` 传参

刷新策略统一：单条修正与批量修复均走局部刷新（批量传受影响项目 id 数组）。

### 验证
read_lints（gitPush 目录）：仅 CodeReport 的 2 个既有错误（`AuthorContributionSection:237` / `TechDebtSection:103`，与本次无关）。用户需执行：`pnpm lint` / `pnpm i18n:verify` / `pnpm validate:icons` / `npx tsc --noEmit`。

要点：`selectAllVisible` 仍传原始 `pagedViolations`（composable 入参要求 `CommitRuleViolation[]`），增强行视图对象只用于渲染与 `isSelected`/`toggle(row.key)`。

## CodeReport UI 布局审查 + 性能/CSS 优化（同日，第十三轮，计划 dc5439a6）

用户要求审查 `components/CodeReport`（9 组件 + 关联 SCSS + 数据层）的 UI 布局，并优化性能与 CSS。22 文件 +425/−356。

### 修复的既有 Bug（重要）
- **`AuthorContributionSection.vue:237` 的陈旧 TS 诊断其实是真 Bug**：`netClass(a.netLines)` 少传 prefix（`utils.netClass(net, prefix, zeroSuffix?)` 需 2-3 参），运行时产出 `undefined--pos`，**净增正负着色一直失效**。改为本地薄封装 `netClass(n) => sharedNetClass(n, "gpr-cell", "")`（与 LineStats/ProjectLineDetail 的 `sharedNetClass` 包裹写法一致）。此前多轮记忆都把它记为"陈旧诊断"，实际是长期漏修。
- `.gpr-table-wrap` 只有 `overflow-y: auto` 无高度上限 → 三处 `position: sticky` 表头（`.gpr-row--head` / `.gpr-author-th`）**全部失效**。补 `max-height: 50vh` + `contain: paint`。
- `HotspotSection` 模板注释写"两栏布局"但 SCSS 是 `flex-direction: column`；`gpr-hot-left/right` 类名语义错误 → 改名 `gpr-hot-main/gpr-hot-summary`，`gap: $spacing-4` → `$spacing-3`。
- 热点 Tab 徽章用 `analyzedFiles`（全量，可达数百）但榜单只渲染 `HOTSPOT_LIMIT=12` 条 → 改 `hotspots.length`。

### 性能优化（三个高危项）
1. **蜡烛图 canvas 宽度无上限**：`minChartWidth = dailyStats.length × 30`，「全部」范围 5 年仓库约 54000px，逼近 Chrome canvas 65535 单维上限会白屏。新增 `reportMetrics.collapseDailyStats(list, MAX_CANDLES=180)` 按连续天数分桶（count 求和 / open 取桶首 / close 取桶末 / high-low 取极值 / date 取桶首日），再叠 `MAX_CHART_WIDTH` 硬上限兜底。节奏图与摘要卡片仍用原始 `dailyStats` 保持语义。
2. **diff 全文渲染无上限**：`FileDetailModal` 新增 `MAX_DIFF_LINES = 1500`，`parsedDiffLines`（全量解析，纯 JS 成本低）与 `diffLines`（截断渲染）分离，超出显示"还有 N 行未显示"；SCSS `.gpr-fm-dl` 加 `content-visibility: auto` + `contain-intrinsic-size`。
3. **模板内函数返回新对象**：`TechDebtSection` 的 `trendChipStyle(path)` 在 v-for 内逐行返回新 style 对象 → 父组件每次重渲染触发 N 次 inline style patch。下沉进 `groups` computed 的 `DebtGroupRow`（含 `trend`/`trendStyle`/`trendText`/`lastModifiedText`），删除 `trendMap`/`trendChipStyle`/`trendChipText`。

其他：scroll 监听改 rAF 节流（含 `cancelAnimationFrame` 清理）；`<Bar>` 加 `v-if="active"`（父传 active，切走卸载 canvas，切回 nextTick 恢复 savedScrollLeft）；`debtInsights.buildSeverityDist` 改单遍历计数；`createCoupledIndex.get` 加 Map memo；`FileDetailModal` keydown 改按 fileStat 开关注销。

### 拆分（超 500 行硬阈值）
`CandlestickSection.vue` 达 573 行 → 抽出 `src/features/gitPush/reportChart.ts`（chart.js 数据集/坐标轴/影线插件/formatHour/MIN_WIDTH_PER_DAY/MAX_CHART_WIDTH），组件降至 370 行。分工：`reportMetrics`=数据怎么算，`reportChart`=数据怎么画。README 目录树同步。
`maxOf(list, min)` 提至 `utils.ts`（CandlestickSection + AuthorContributionSection 共用，符合"2 个以上文件使用必须提取"），替代 `Math.max(...arr)` 爆栈风险写法。

### CSS 收敛
- 新增 `_mixins.scss` 的 `stat-card` / `stat-card-value` / `stat-card-label`，消除 `.gpr-card*` 与 `.gpc-card*` 逐行重复（视觉零变化：mixin 不含 ellipsis，由 TeamOverview 单独追加）
- `.gpr-net-bar-fill`：`transition: width` → `transform: scaleX(var(--gpr-net-fill))` + `transform-origin: left`（保持原从左增长视觉）+ 父级 `overflow: hidden`；JS 侧 `netBarWidth` → `netBarFill`(0~1)
- `FileDetailModal.scss` 的 `#28a745/#dc3545` → `var(--gp-diff-add-color)/var(--gp-diff-del-color)`（该文件已 `@include gp-diff-color-vars`）
- 新增 `types/report.ts` 的 `REPORT_CHART_COLORS` 作为 canvas 与 SCSS 的单一语义源（canvas 不能用 CSS var，SCSS 侧字面量加注释指向它防漂移）
- 热点表 `min-width: 420px` + 容器 `overflow-x: auto`（5 列定宽合计 280px，窄 Dock 下路径列仅剩约 60px）
- 债务表日期列 72px → 96px（"3 months ago"/"3 个月前" 被截断）

### 验证
read_lints（gitPush 目录）**0 error**（本轮把长期存在的 AuthorContributionSection:237 一并修掉）。i18n 新增 2 键中英同步（`reportChartAggregated` / `reportDiffTruncated`），只改分片。用户需执行：`pnpm lint` / `pnpm i18n:verify` / `pnpm validate:icons` / `npx tsc --noEmit`。

### 经验
- **`read_lints` 会返回陈旧诊断**：报的行号不随编辑移动时先别急着认，但也不能一律当陈旧——本次 `AuthorContributionSection:237` 连续多轮被记为"陈旧诊断"，实为漏参数真 Bug，修掉后 read_lints 才归零。判据：把该行号对应的代码读出来，确认调用参数个数是否真的匹配。
- 纯函数抽取的正当理由不止"重复 3 次"（Rule of Three），突破 500 行硬阈值同样是 AGENTS_ARCH.md 明示的拆分依据（"超过 500 行 → 应拆分逻辑与模板"）。
