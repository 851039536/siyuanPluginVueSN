# 2026-09-01

- 移除 gitPush 项目卡片拉取/推送按钮图标。修改 `src/features/gitPush/components/ListView/CardActionBar.vue`：移除 4 处 Icon（拉取主按钮 `mdi:arrow-down`/`mdi:loading`、拉取下拉项 `mdi:arrow-down`、推送主按钮 `mdi:arrow-up`/`mdi:loading`、推送下拉项 `mdi:arrow-up`），仅保留文字 + caret 图标。加载态旋转指示随之移除，active 高亮 class 仍保留。lint 通过。
