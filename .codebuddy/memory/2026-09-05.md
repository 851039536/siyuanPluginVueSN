# 2026-09-05

## gitPush 删除历史提交弹窗：展示 bundle 备份路径 + 点击打开文件夹

为 DropCommitDialog（ProjectCard LOG Tab 的删除历史提交）新增备份路径可见性：

1. **DropCommitDialog.vue**：
   - 新增 `backupPath` ref，`performDrop()` 中保存 `manager.createProjectBackup()` 返回的 .bundle 路径（此前被丢弃），并新增 `done` 状态
   - 删除成功后不再自动 emit close，停留展示「已备份删除前的完整历史」绿色信息条（含完整 bundle 路径 + 打开文件夹图标）；点击整条信息条或按 Enter 即经 `openLocalPath(dirname(bundle))` 打开备份所在文件夹（`bfg-backups/<项目名>/`）
   - footer 在 done 后把「取消/确认删除」切换为「完成」主按钮，用户主动关闭
   - 失败路径保留 backupPath 展示，便于用户自行定位恢复点
2. **ProjectCard.vue**：`handleDropSaved` 去掉 `droppingEntry.value = null`，仅刷新 LOG；关闭改由 DropCommitDialog 的「完成」/close 事件负责
3. **DropCommitDialog.scss**：新增 `.gp-drop-backup`（success 绿信息条，hover 加深，0.12s 过渡）与 label/path/open 子元素样式
4. **i18n**：zh_CN/en_US gitPush.json 新增 `dropCommitBackupDone` / `dropCommitDone` / `dropCommitOpenBackupTip` 三键

lint 通过。交互变化：删除历史提交成功后弹窗停留直到用户点「完成」。

## gitPush LOG 默认显示条数跟随选择框（替代固定 30 条）

问题：LOG Tab 首载/无参刷新调用 `WorktreeOps.getCommitLog(path)`（默认 `-n 30`），而 BranchCommitList 条数选择框默认 200，二者不一致。

修复（卡片级条数状态上移）：
1. **useCardData.ts**：新增 `logLimit` ref（默认 200）+ `setLogLimit()`；`loadLog(count?)` 缺省改走 `count ?? logLimit.value`，return 导出 logLimit/setLogLimit。ensureDetailsLoaded、onSignal("log")、无参 reloadLog 等一律按 logLimit 抓取
2. **ProjectCard.vue**：BranchCommitList 增加 `:initial-count="logLimit"`；新增 `handleLogCountReload(count)`（先 setLogLimit 再 reloadLog(count)）替代内联绑定
3. **BranchCommitList.vue**：新增 `initialCount?: number|"all"` prop，本地 `displayCount` 初始化 `props.initialCount ?? 200`（Tab 切换重建仍沿用用户上次选择）

lint 通过。无 i18n 变更。

## gitPush LOG 新增点击查看提交修改的文件

1. **WorktreeOps.ts**：新增 `getCommitFiles(projectPath, hash)` — 解析 `git -c core.quotepath=false show --name-status --format=` 输出为 FileChange[]（状态首字母映射 added/deleted/renamed/copied/unmerged/modified；R/C 行保留 oldPath，path 取末列即新路径）；merge/空提交返回空数组
2. **GitPushManager.ts**：委托 `getCommitFiles`
3. **BranchCommitList.vue**：行内新增文件按钮（mdi:file-document-outline，hover 显示，复用 bcl-tag-btn 同组样式）+ `viewFiles` emit
4. **新组件 CommitFilesDialog.vue + CommitFilesDialog.scss**（common/）：自包含注入 manager，按 target 拉取并展示 — 项目/hash/「共 N 个文件」元信息行 + 提交信息弱化单行 + 文件列表滚动区；状态标记复用 `.wt-file-status/.wt-s-*` 着色与 `.wt-file-path`（IconWrapper 处理 renamed/unmerged）；rename/copy 显示 ← 旧路径；merge 提交显示空态提示
5. **ProjectCard.vue**：`filesTarget` ref + `openCommitFiles(entry)` 构造 CommitFixTarget 打开弹窗
6. **i18n**：zh/en gitPush.json 新增 `commitFilesOpen/Title/Count/Empty/EmptyMerge/LoadFailed` 六键

lint 通过。

## gitPush LOG 文件按钮常显 + 点击文件查看提交内 diff（用户澄清后）

用户澄清交互：
1. **文件按钮常显，tag/删除仅行 hover 显示** — BranchCommitList.scss 把 `.bcl-files-btn` 从 hover 组拆出常显（opacity 0.7，hover 主色 1），`.bcl-tag-btn/.bcl-drop-btn` 保留原 hover 浮现
2. **点击文件行打开独立 diff 弹窗**：
   - WorktreeOps 新增 `getCommitFilePatch(path, hash, filePath)`（`git show --format= hash -- path`）+ GitPushManager 委托
   - 新组件 **CommitFileDiffDialog.vue/.scss**（common/）：自包含注入 manager；复用工作区 diff 视觉类（`.wt-diff-*`：header/legend/content/line 着色行号词级高亮），遮罩 `.gp-mask.cdf-overlay`（z-index 10001 + 重新 `@include gp-diff-color-vars`）；标题含 hash 徽标 + 增删统计 + 上下文件导航（← → 键 + Esc 关闭，watch file.path 重新加载）
   - CommitFilesDialog：文件行可点击（cursor/激活态），`diffFile` ref + `openDiff/navigateDiff`；Esc 在 diff 弹窗打开时只关 diff 不关列表（`!diffFile.value` 守卫）；diff 弹窗以 Teleport 兄弟节点叠加
   - i18n 新增 `commitFilesOpenDiff` 键（zh/en）

lint 通过。
