# 2026-09-09

## s3Backup 全量审查与修复（6 阶段计划执行完毕）

用户要求审查 `src/features/s3Backup`（逻辑漏洞/冗余/内存泄露/死代码/重复类型），按 P0→P1→P2→清理→拆分→解耦 6 阶段全部完成。

### 修复要点
- **P0-1 双日期 key**：`utils.ts` 新增 `splitBackupRelativePath` + `buildBackupUploadKey`（日期段取备份文件自身日期：`data-YYYYMMDD/` 目录名或文件名内嵌日期，无则 fallbackDate），`useFullS3Upload.makeS3Key` 与 `index.vue` 的手动上传 `buildUploadKey` 共用，消除双日期嵌套与两套 key 规则。
- **P0-2 ZIP 失败清理**：`BackupManager.finalizeAndSaveBackup` 改用 `stream.pipeline`（出错自动销毁两端，旧 `.pipe` 在源 error 时不销毁 dest → Windows unlink 必失败）；新增 `cleanupFailedZip`（unlink 失败改名 `.part`，防止损坏包被 `isArchiveFile` 当有效归档）；惰性流登记进 `lazyStreams` 失败时统一 destroy；可读性探测 `open+close` → `fs.access`。
- **P1 互斥空窗**：三入口运行标志改「进入即置位」（await selectWorkspacePath 之前），finally 复位；样板收敛为 `ensureWorkspaceReady()`。
- **P1 日志/存储**：`useBackupLogs.addLog` 落盘 `.catch` 兜底（消 unhandled rejection）；自动备份 busy 跳过日志 5 分钟节流（`BUSY_LOG_THROTTLE_MS`）；新增 `MAX_CHECKSUM_COUNT=100`/`MAX_UPLOAD_HOST_MAP=200` 截断。
- **P2**：manifest 上传失败与全量上传扫描失败补 addLog 再抛；`AutoBackupCard` backupTime 改草稿+失焦/回车提交（原逐键重启定时器）；`FileChecksumsCard` watch 加 deep（useChecksums 原地 splice 导致 watch 永不触发）；进度百分比去竞态（worker 本地 lastReportedPercent）；BackupListCard :key 用 `key||path||name`；`S3Backup.destroy` 补 `timers.clearAll()`。
- **清理**：`padNum` 去导出；`updateWorkspacePath` 移出返回值；`workspacePath` 字段标 @deprecated（与 workspaceRoot 恒等，仅旧数据兼容保留）；类型收敛：`WorkspaceFile=Pick<IncrementalFileEntry,...>`、`ManifestEntry=Omit<IncrementalFileEntry,...>`、`DropResult=Omit<FileChecksum,"time">`、新增 `toLocalBackupInfo()` 转换；`BackupLog.type` 新增 `s3IncrementalRestore`（增量还原不再混用 s3Download）+ i18n `logTypeS3IncrementalRestore`/`manifestUploadFailed`；README 同步。

### 拆分结果（行数）
- index.vue 855→228（BackupTab.vue 105 + useBackupOrchestrator 编排）
- BackupManager.ts 510→392（backupScanner.ts 120 外迁扫描）
- FileChecksumsCard.vue 421→63（checksums/DropVerifySection 211 + StoredChecksumsSection 164，manager 惰性缓存从模块级改实例级）
- useBackupOrchestrator.ts 453（>300 警戒线但 <500 硬阈值；二次拆出 useAutoBackupTrigger 76 + useLocalZipBackup 120）

### 解耦与状态收敛
- 新增 `instance.ts`：单例 getter/setter 唯一持有，断开 index.ts ↔ index.vue 循环依赖（仅剩 instance.ts 对 index.ts 的 type-only import，编译期擦除）。
- `index.ts` 删除 detectAndInitWorkspace/setWorkspacePaths/cachedWorkspaceRoot/getWorkspaceRoot（工作区路径以 useWorkspaceSettings.workspaceRoot 为唯一事实源：loadWorkspaceSettings 持久化兜底 → detectWorkspacePath API 检测已有则跳过）。
- `lastBackupTimestamp` 收敛为 S3Backup 实例唯一事实源（新增 getLastBackupTimestamp；useWorkspaceSettings 删除本地副本，buildWorkspaceSettings 经 `instance.getLastBackupTimestamp() ?? 0` 读取；markBackupCompleted 先写实例再落盘）。

### 经验
- 用户跑 vite build 报 4 个 MISSING_EXPORT 后补完 import 切换——**重构多文件 import 来源时应在同一轮内全量切完再交付**，不能改一半。
- `stream.pipeline` vs `.pipe`：源流 error 时 pipe 不销毁 dest，Windows 上 unlink 打开的文件必 EPERM——失败清理必须先 destroy 或用 pipeline。
- 非 deep watch 对「原地 splice/unshift 的同一数组引用」永不触发，props 数组清理逻辑必须 deep。

## 增量备份/还原审查修复 + 独立「增量」实验 Tab（完成）

**P1 修复（useIncrementalBackup.ts，397 行）**：
- 删除阶段：串行 for→`runWithConcurrency` 并发、try/catch→`withRetry` 重试；删除失败条目回填新 manifest（`oldManifest?.files[relativePath]` 显式守卫），下次 diff 仍进 toDelete 重删，消除永久孤儿对象；`failed = uploadFailed + deleteFailed` 统一计入结果
- 还原过滤：过滤条件提取为 `isUnsafeRelativePath` 纯函数；unsafePaths 非空时 console.warn 留痕 + 结果消息追加 `incrementalRestoreSkippedUnsafe`（"，已跳过 {count} 个不安全路径"）

**新增 useIncrementalPanel.ts（208 行）**：迁入 triggerIncrementalOnly/triggerIncrementalRestore/runIncrementalBackup/isIncrementalRunning/isIncrementalRestoring；修复 P1 静默返回（`!node` → `showMessage(MSG_DESKTOP_ONLY)`）；新增 manifestInfo（fileCount/createdAt/hostname 三字段投影）+ manifestLoadFailed + refreshIncrementalManifest + lastRestoreDir（还原成功后记录）+ openRestoreFolder（`@/utils/electronDialog` 的 openFolderInExplorer）。useBackupOrchestrator 503→476 行回到硬阈值内。

**关键设计：TDZ 规避**——panel 的 `isAnyTaskRunning` 用惰性 getter `() => isAnyTaskRunning.value` 注入（panel 实例化在 computed 定义之前，闭包运行时才求值）；panel 实例化位置在 useWorkspaceSettings/路径预览之后、canBackup/isAnyTaskRunning computed 之前。

**新 UI**：IncrementalTab.vue（172 行）四区块 = warning 实验横幅（思源变量 `--b3-theme-warning-lightest/lighter`，与 gitPush badge 同模式）+ 云端清单状态卡（三态：未配置/加载失败/无清单/信息网格，onMounted 自动加载一次）+ 操作卡（增量备份/增量还原/打开还原目录 v-if lastRestoreDir + 还原路径预览）+ 复用 BackupProgressSection。index.vue 第五 Tab（checksums 之后）带 `.s3-tab-badge`（$vp-mono + $font-size-2xs 大写）。ManualBackupCard 移除两增量按钮及 isIncrementalRunning/isIncrementalRestoring/isConfigured props（isConfigured 因此变成无引用 prop 顺带清理）。

**验证**：read_lints 全模块 0 错误；i18n zh/en 各 178 键对齐（Node UTF-8 解析校验——PowerShell ConvertFrom-Json 按 GBK 读 UTF-8 会乱码误报 JSON 错误，跨语言校验用 node）。配置 Tab 三开关未动。验证链留给用户：pnpm i18n:verify / tsc --noEmit / lint / build。

## superPanel「AI 配置档案」功能（完成，需求经澄清收敛）

澄清结论：①范围=通用配置档案（全供应商）；②切换=下拉选中不改字段，点「应用」按钮才整组回填并立即生效；③未保存内容直接覆盖无提示；④沿用"边改边自动保存"机制不改造。

### 改动清单
- `src/types/ai.ts` 新增 `AiProfileConfig`（name + provider/model/customModel/apiKey/customEndpoint/enableThinking/searchProvider/searchBochaApiKey 快照，全字段激活配置的仓库，不含 apiKeys map）
- `src/config/settings.ts`：`PluginSettings.aiProfiles: AiProfileConfig[]`（默认 []）；`encryptSensitiveFields/decryptSensitiveFields` 对每份档案的 apiKey/searchBochaApiKey 做 AES-GCM 加解密（decryptSetting 对无 `enc:` 前缀值原样返回，天然兼容旧数据）
- `superPanel/types/index.ts`（SuperPanelManager）：`buildAiSettingsProps` 透传 `profiles`（s.aiProfiles||[]）+ `onUpdate:profiles` → 新增 `handleUpdateAiProfiles`（按新旧数组长度差区分"保存/删除"提示语，经 `_updatePluginSettings` 走统一加密落盘）
- 新文件 `components/AiProfileManager.vue` + `styles/AiProfileManager.scss`：展示性子组件，内部持 selectedName + 名称输入，语义事件 `save(name)/apply(name)/delete(name)` 上抛；下拉选档案时同步填充名称输入便于同名覆盖；watch profiles 失效清理选中态
- `AiSettingsPanel.vue`：顶部新增「配置档案」SettingGroup；`profileList` ref 本地事实源；`handleProfileSave`（当前表单快照，同名覆盖替换）、`handleProfileApply`（整组回填 reactive settings 后**仅 emit 一次** update:settings，同步 apiKeys[provider]，空 key 删除）、`handleProfileDelete`（filter 后上抛）
- i18n `superPanel.json` zh/en 各 +9 键（aiProfileTitle/profileSelectPlaceholder/profileApply/profileDelete/profileSave/profileNamePlaceholder/profileManagerDesc/profileSavedMsg/profileDeletedMsg）；README 补充档案机制说明

### 设计经验
- 档案=「多份命名快照 + 一份激活态」：激活态仍由 aiApiProvider 等既有单组字段承载，运行期 `getApiConfigFromPlugin` 消费链零改动、其它 feature 零感知
- 应用档案必须原子整组赋值 + 单次 emit，禁止逐字段触发多次写盘；切供应商读 KeyMap 机制通过回写 `settings.apiKeys[provider]` 保持一致
- `AiProfileConfig` 放 `@/types/ai`（全局共享类型层）而非 superPanel/types，避免 `config/settings.ts` 反向 import feature 层

read_lints 全改动文件 0 错误。验证链留给用户：pnpm i18n:verify / tsc --noEmit / pnpm lint / pnpm vite build。

## superPanel 整套移除「功能状态标记」+ 模块内冗余清理（完成，需求经澄清收敛）

澄清结论：①用户原话「移除文档、待修复这些选项，没有什么意义」实指**整套状态标记功能无意义**——移除状态徽章（稳定/待修复/严重/需优化+清空弹窗）与顶部状态统计栏，并清 featureStatus 字段等连带项；②冗余审查仅限 superPanel 模块内部。

### 改动清单
- `superPanel/types/index.ts`：删 `FEATURE_STATUSES` 常量 + `FeatureStatus` 类型 + `Feature.status` 字段 + constructor 的 `onStatusFeature` 回调 + `handleStatusFeature()` 方法
- `config/settings.ts`：删 `PluginSettings.featureStatus` 字段与默认值（旧存档多余键由 loadSettings 合并天然忽略，无需迁移）
- `superPanel/index.vue`：删顶部状态统计栏模板、`statusStats/statusLabels` computed、`FEATURE_STATUSES` import、`statusFeature` emit 与 `:status-labels`/`:status-change` 绑定、features 里 status 取值；文件头注释改「功能搜索与功能卡片列表」
- `components/FeatureCard.vue`：删状态徽章+status-popover 模板、statusLabels prop、statusChange emit、状态菜单逻辑（statusBadgeRef/showStatusMenu/statusOptions/selectStatus/toggleStatusMenu）及 useClickOutside/`computed`/`ref`/FEATURE_STATUSES import；文件头注释改「开关、主题选择与快捷操作」
- `composables/useClickOutside.ts`：状态菜单唯一消费方移除后成孤儿，整文件删除
- styles：`variables.scss` 删 4 个 `$status-*` 色变量（grep 确认仅 superPanel 三处样式使用）；`index.scss` 删状态统计栏整块（40 行）；`feature-card.scss` 删 `$status-colors` map + 6 组 `.status-badge/.status-popover/.status-option(.dot)/.status-popover-*-transition`（约 110 行）
- i18n：`superPanel.json` zh/en 各删 statusStable/statusNeedsFix/statusCritical/statusMinor 4 键（对齐）；README 移除「状态标记」描述

### 冗余复查结果
- 全 src grep `FEATURE_STATUSES|FeatureStatus|featureStatus|statusFeature|onStatusFeature|handleStatusFeature|useClickOutside|\$status-` → 0 残留（顶层合并 JSON 的 status* 键为 i18n:merge 产物，下次构建自动清除，禁手改）
- 其它模块 `stats-*`/`status-*` 前缀（statistics/gitPush/s3Backup/docAnalysis 等）为各自独立语义，未误删；docNavigation 自有 useClickOutside 副本属跨 feature 各自实现，不在范围
- index.vue 240 行 / FeatureCard 149 行，均低于警戒线；read_lints 全 0 错误

验证链留给用户：pnpm i18n:verify / tsc --noEmit / pnpm lint / pnpm vite build。

## superPanel 移除顶部「刷新」按钮（完成）

`SuperPanelHeader.vue` 头部动作按钮由 AI配置/刷新/关闭 三枚减为 AI配置/关闭 两枚，同步清理整条刷新链路：
- `components/SuperPanelHeader.vue`：删 actions 中 refresh 项与 Emits `refresh`
- `index.vue`：删 `@refresh` 绑定与 Emits `refresh`
- `types/index.ts`（SuperPanelManager）：删 constructor 的 `onRefresh` 回调与 `handleRefresh()` 方法（close/open 供 toggle 继续使用）
- i18n：`superPanel.json` zh/en 各删 refresh/refreshing/refreshSuccess 3 键（对齐）
grep 全模块 refresh 0 残留；read_lints 0 错误。验证链同前。

## statistics 概览 Tab 移除「字数排行」WordRanking（完成）

用户 @ `overview/index.vue` 要求移除概览页字数排行区块。全链路清理：
- `overview/index.vue`：删模板 `<WordRanking>` 段 + import（`chart-section`/`section-title`/`BarChart`/`displayDailyStats` 全保留，BarChart 继续消费同一数据源）
- 删除 `components/overview/WordRanking.vue` + `styles/WordRanking.scss`（无其他消费者）
- i18n 分片 `statistics.json` zh/en 各删 `wordRanking` + `emptyText` 2 键（对齐；`emptyText` 确认统计模块内仅 WordRanking 消费，Select.vue/Chart.vue 的 emptyText 是 prop 名与统计 i18n 无关；`wordsUnit` 被 Trend/BarChart/NotebookWordPie 等多处复用保留）
- `styles/index.scss` 共享注释顺手去掉 WordRanking 字样
- 保留项：utils `barPct`（DocChangeSection 仍在用）/`formatNumber`（广泛复用）
- 残留 grep：源代码 0（仅顶层合并 JSON 旧键，构建期 i18n:merge 自动清除，禁手改）；read_lints 0 错误。验证链留给用户：pnpm i18n:verify / tsc --noEmit / dev 观察概览 UI。

## vite.config.ts 修复 Vite native configLoader 警告（完成）

用户贴出 Vite 警告：`configLoader: 'native'`（未来默认）下 `__dirname` 与 JSON import 无属性不受支持。修复 2 处：
- `import pluginInfo from "./plugin.json"` → 加 `with { type: "json" }`
- 43 处 `resolve(__dirname, ...)` → `resolve(import.meta.dirname, ...)`（alias 列表 + lib.entry 等全部）
前提确认：package.json `"type": "module"` + vite 8（Node ≥ 20.19，import.meta.dirname 可用）+ TS6 + @types/node 24（ImportMeta.dirname 类型）+ tsconfig.node.json module ESNext/moduleResolution bundler。残留 __dirname 0、read_lints 0 错误。未设 VITE_CONFIG_NATIVE_IGNORE_WARNING（那是压制非修复）。验证留给用户：pnpm dev / build 观察警告是否消失。

## resourceManager 分类功能审查 + 空分类可删除（完成）

需求经澄清收敛：①内置+自定义都可删（内置删除=隐藏持久化，可恢复）；②实现删除功能，入口在图片/文件页签分类筛选栏。

### 审查结论（分类实现现状）
- 分类实体 = 小写 key + 磁盘目录 `assets/<key>/`；内置 images/net/tool/other 硬编码 + 自定义（PluginStorage `resourceManager-customCategories`）双来源，无删除出口 → 分类只增不减、空目录磁盘残留。

### 改动清单
- `utils.ts`：新增 `BUILT_IN_CATEGORY_DEFS`（key+i18nKey 元数据，`BUILT_IN_CATEGORY_KEYS` 由它 derive）、`STORAGE_HIDDEN_KEY`（`resourceManager-hiddenBuiltIn`）、`categoryDirPrefix(key)`（统一小写前缀）、`resolveCategoryLabel(i18n,key)`；utils 需 type-import `ResourceManagerI18n`
- 新 `composables/useCategoryManager.ts`（约 200 行）：分类可见性组装（quickCategories/hiddenBuiltInCategories）、空分类删除（`deleteCategory` 返回 `ok|notEmpty|failed`，内容判定=内存缓存前缀 + readDir 磁盘兜底双保险，空目录 removeFile 清理）、内置恢复 `restoreBuiltIn`、自定义记录 `addCustomCategory`；持久化「先 save 成功后写内存」，异常回滚返回 failed
- `useResourceManager.ts`：分类/存储逻辑整体迁移到 manager（净减行数），`filteredAssets` 前缀改 `categoryDirPrefix`，新增 `handleDeleteCategory`（confirm+结果分型提示+active 复位）与 `handleRestoreBuiltIn`
- 新 `components/CategoryFilterBar.vue` + `styles/CategoryFilterBar.scss`：分类筛选 UI（待分类 chip + 分类 chip hover 右上角删除钮 + 「已隐藏 N」展开区恢复），index.vue 内联筛选栏替换为该子组件；`index.scss` 删除 `--category` 样式段
- types/i18n：`CategoryItem` 类型（key/label/builtIn）；zh/en resourceManager.json 各 +7 键（deleteCategory/deleteCategoryConfirm/categoryNotEmpty/deleteCategorySuccess/hiddenCategories/restore/restoreSuccess）；`ResourceManagerI18n` 同步
- README 更新（分类可删除规则 + 文件结构）
- read_lints 全模块 0 错误。验证链留给用户：pnpm i18n:verify / tsc --noEmit / dev 观察筛选栏删除交互。

### 交互改版：删除入口收敛为「分类设置」弹窗（用户二次反馈）
用户否掉 chip hover 删除 + 「已隐藏 N」展开区方案，要求改为「按钮 → 弹出分类设置」集中管理。改造：
- `CategoryFilterBar.vue`：回到纯 chip 切换（去掉 hover 删除/隐藏展开），行尾加「分类设置」按钮（`mdi:settings` 图标 + 文字，emit `manage`）；`CategoryFilterBar.scss` 同步精简
- 新 `CategorySettingsDialog.vue` + `CategorySettingsDialog.scss`：Teleport to body + mask（rgba(0,0,0,0.5) 禁 backdrop-filter、z-index 10000、fade+scale(0.98) 0.12s 对齐 gitPush 范式）；头部标题+close；规则说明 hint；可见分类行（label + 内置徽标 + delete 图标钮，mdi:delete 复用已注册图标）；下方「已隐藏 N」区行内恢复
- `useCategoryManager` 新增 `isCategoryEmpty(key)` 预检；`useRM.handleDeleteCategory` 先空判定（非空提示 categoryNotEmpty 直接拦，不弹 confirm）→ 确认 → 删除执行（内部二次校验兜底）→ 失败/成功分型提示 + 激活筛选复位
- index.vue 持 `settingsOpen` ref + 渲染弹窗（props 全来自 quickCategories/hiddenBuiltInCategories computed，删除/恢复实时联动）
- i18n zh/en +4 键（categorySettings/categorySettingsHint/categoryBuiltIn/noCategories）；类型同步；README 更新
经验：管理类功能集中弹窗优于散落 hover 入口；Teleport 弹窗样式作用域不受 Dock 面板 transform 影响，遮罩须 z-index 10000。read_lints 0；验证链留用户（i18n:verify / tsc / dev）。

## resourceManager UI 排版优化（plan 六步全部完成，380px 窄栏优先）

用户「审查当前功能UI，优化排版」，四区全选 + 允许适度结构调整。改动：
- **资源行两段式**（index.vue li 加 `rm-asset-item--panel` 变体，styles/index.scss 嵌套 &--panel）：信息行（缩略图 24 + 路径名 ellipsis）+ 操作行（`.rm-asset-item__action-group` 次要组定位/复制路径/复制MD/打开目录，`--primary` 组移动置右两端对齐），消除按钮挤压名称换行；DocAssetsSection 行同步改两段式
- **移动表单**：`rm-move-form__row` 改 grid（auto 1fr，label 2px padding 基线对齐，超长路径内容列自由折行）；分类区拆 `.rm-move-form__chips`（一排）+ `.rm-move-form__custom`（输入 flex1 + 应用）两行；操作区右对齐（确认移动 primary 置右）
- **页签/节奏**：rm-tabs item `flex:1 1 0; text-align:center` 等分收缩（overflow-x hidden）；rm-content padding `$spacing-2 $spacing-3 $spacing-3`；rm-asset-count 加 border-top 细分隔线分区
- **空/加载态**：新 `components/EmptyState.vue` + `styles/EmptyState.scss`（居中图标 20px opacity.55 + 文案，`spin` 布尔 prop 图标旋转 1.1s）；index.vue 图片/文件、丢失、未使用三区 + DocAssetsSection 四处状态全部替换；icons.ts COMMON_ICONS 新增 `inbox`(mdi:inbox)/`linkOff`(mdi:link-off)（注意 fileOutline 原本已注册，勿重复添加——曾误加后被去重）；index.scss 删除旧 .rm-empty 段
- README 同步（两段式说明 + 文件结构 + styles 清单）；read_lints 0；index.scss 约 446 行 / index.vue 约 480 行均在硬阈值内
验证链留用户：pnpm i18n:verify / tsc --noEmit / validate:icons（新图标）/ dev 观察。

## resourceManager 资源显示改「卡片网格（图库式）」（完成，经澄清：网格卡片 + 仅图片/文件页签）

- 新 `components/AssetGridList.vue` + `styles/AssetGridList.scss`：ul.rm-asset-grid 自适应列（minmax(104px,1fr)），卡片 = 正方形媒体区（aspect-ratio 1/1，图片 cover / 文件页或加载失败回退扩展名或 fileOutline 图标占位）+ 单行路径名 + hover 图标操作条（copy/linkVariant/folderOpen/magnify/folderMove，移动置亮 --primary）；thumbErrors/扩展名内部自管（watch items 清空），回调函数 props 注入（沿 DocAssets on* 惯例）
- 移动表单从行内提为区块级：index.vue 图片/文件 section 下 `<AssetGridList v-else .../>` + `v-if="movingAsset"` 独立 rm-move-form 卡（path→`activeMovePath` computed = movingAsset??''，应用/确认统一传 activeMovePath）
- index.vue 403 / index.scss 412 / AssetGridList.vue 142 行（500 阈值内；同时从 index.scss 迁出网格段、删除旧 rm-asset-item__thumb/preview 悬停大图样式；DocAssets 两段式 panel 与 missing/unused 行式列表保留）
- icons.ts COMMON_ICONS 新增 `magnify`(mdi:magnify)/`folderOpen`(mdi:folder-open-outline)（copy/contentCopy/linkVariant/folderMove/fileOutline 等复用既有键）
经验：模板行数逼近阈值时优先抽「自包含渲染子组件 + 随迁 SCSS」（单个入口 index.scss 拆分新 PascalCase scss 由子组件承载，双文件行数同时回落）；移动表单这类「列表内互斥展开」的状态可收敛为区块级单一表单 + activeMovePath 派生。read_lints 0；验证链留用户（validate:icons / i18n:verify / tsc / dev 观察图库卡片）。

## 卡片网格交互修复（用户反馈两问题）

1) **点击「移动」没反应**：根因为表单渲染在网格下方、超出滚动视口且无任何即时反馈。修复：index.vue 包 `handleStartMove`（startMoveAsset 后 nextTick + `moveFormRef.scrollIntoView({behavior:'smooth',block:'nearest'})`），移动表单挂 `ref="moveFormRef"`，点击即自动滚入可视区
2) **点击图片应放大显示**：网格化后曾删除行列表格的 hover 大图，现补点击放大：AssetGridList `img` 绑 `@click="onPreview(path)"` + img `cursor:zoom-in`；index.vue 新增 previewAsset ref + Teleport 弹层（`.rm-preview-mask/panel`，头部路径名 + close，body 大图 max 86vh/86vw，遮罩/关闭退出，`rm-preview-fade` 0.12s 过渡；样式放 index.scss 顶层（Teleport 移出 .resource-manager-panel 祖先，嵌套在 panel 内选择器不生效——教训：Teleport 顶层浮层样式必须文件顶层书写））
- 行数：index.vue 465 / index.scss 500（压回阈值内）；read_lints 0；验证链留用户（tsc / dev 观察点击移动滚入与图片预览）

## 移除「移动成功后自动全量重建索引」（用户认为无意义）

- `useResourceManager.ts` `handleMoveAsset`：删除 renameFile+updateAssetReferences 后的 `try { await fullReindexAssetContent() }` 块，补注释说明「思源按需维护资源索引，耗时操作留待重建索引页签手动触发」
- `fullReindexAssetContent` import 保留（handleRebuildIndex 手动入口仍在用）；README 无相关描述无需改
- read_lints 0；验证链留用户（dev 移动一个资源后确认列表正常、无报错）

## 移动交互改弹窗（用户「底部不友好，点击移动应弹窗体」）

区块级底部移动表单改为**居中弹窗**：
- 新 `components/MoveAssetDialog.vue` + `styles/MoveAssetDialog.scss`（146 行）：Teleport to body + mask；头部「移动」+ 关闭；body = 当前路径（只读）/ 新路径 input / 快捷 chips（emit applyCategory）/ 自定义输入行（update:custom）；底部确认(primary,disabled 空 newPath)/取消。props 双向协议：`newPath/custom` 只读值 + `update:newPath/update:custom` 回写父层（composable 仍是 moveNewPath/customCategory 唯一事实源），动作事件 confirm/cancel/applyCategory/applyCustom
- 移除了 index.scss 中 `.rm-move-form*` 全部样式（Teleport 内容命不中「嵌在 .resource-manager-panel 下」的选择器）；MoveAssetDialog.scss 自持按钮（`.rm-move-btn`，Teleport 场景不依赖主面板 .rm-btn）
- index.vue：删区块级表单 markup 与 handleStartMove/moveFormRef/nextTick，卡片 onMove 直接 `startMoveAsset`；挂 `<MoveAssetDialog v-if="movingAsset" ...>`（事件名一律 camelCase）
- 行数：index.vue 396 / index.scss 426；read_lints 0
经验重申：Teleport 弹层子组件样式必须自持（主面板 scoped 样式因祖先选择器/作用域不会命中 body 下节点）；弹层组件内按钮不沿用主面板 rm-btn，需组件内自定。验证链留用户（dev 卡片点移动弹窗、取消关闭、确认移动成功后自动关）

## MoveAssetDialog 改为「即时移动」（用户确认方案：保留即时移动，去掉确认按钮）

- 移除弹窗底部「确认移动」主按钮，仅保留「取消」（+ header 关闭/遮罩取消）；输入新路径按回车仍 emit confirm → handleMoveAsset 即时执行
- body 顶部加 `.rm-move-dialog__hint`（i18n 新键 `moveInstantlyHint` zh/en + types 同步）："选择分类、或在输入新路径后按回车，即立即移动该资源"，弱化小字防误触
- 移除 scss 中已无引用的 `.rm-move-btn--primary`
- README 交互描述同步；read_lints 0；MoveAssetDialog.vue 142 / scss 190。验证链留用户（dev：点分类即移动并关窗、回车移动、取消/失败保留弹窗）
- 用户反馈窗体太小：MoveAssetDialog.scss dialog 宽度 `min(88vw,340px)` → `min(92vw,420px)`

## statistics milestones 模块审查 + 直接修复（完成，plan 6 任务全执行）

审查 `components/milestones` 7 组件 + types/utils/composables/双 SCSS/i18n，交付审查报告 + 直接修复。

### 关键修复
- **稀有度/类型名直显键名 bug**：AchievementsTab/LevelConfigTab `{{ TIER_LABELS[tier] }}` 输出 i18n 键名（"tierCommon"）→ 新增本地 `textByKey`（命中译文/未命中空串，禁止键名直显与中文兜底）二次解析；AchievementsTab `MILESTONE_TYPES` 无 `label` 字段却被 `t.label`/`TYPE_LABEL_MAP.label` 使用 → 空文本，改 labelKey 查 i18n；`STAT_TYPE_DESCRIPTIONS` hint 直显键名 → 解析
- **AchievementsTab/LevelConfigTab 全量接 i18n**：启用既有死键（achEmptyHint/addCustomAchievement/statTypeLabel/levelHelp1-3/milestonePointsLabel/curveFormulaHint/saveLevelConfigBtn 等）；LevelConfigTab Props 补 `i18n` 声明（父早已传，先前被 attrs 吞掉）；删除确认统一 `confirmDeleteAchievement`
- **稀有度分配稳定化（核心）**：`utils/milestones.ts` 原 `tierOf(idx,total)` 按动态列表长度占比 → 达成里程碑使列表变长 → 已达成项 tier 降级 → 成就点倒退/等级抖动。改为 `tierByIndex(n)` 固定档位（n≤3 common、≤7 rare、≤12 epic、其余 legendary），生成时一次定档；`200` 上限提为 `MAX_MILESTONES_PER_TYPE` 常量（countMilestonesReached 默认共用）。**体验数值变化需告知用户**（稀有度分布从占比变为序号档位）
- 次要：`buildRows` 缺失类型以 `generateDefaultRules()` 兜底；`useMilestoneStorage` 拆分同步创建 storage + 首次 load（`initPromise` 可重入 await，save 前 `waitReady()` 防竞态丢写/回写覆盖）；`tierLabels/categories` 改 computed + textByKey 容错

### 冗余清理
- `milestones/index.vue` 11 字段 prop 钻孔 → 直传 `stats` 对象；MilestonesCard Props 收敛 `stats?: StatisticsData|null`（statCounts 内部映射）
- 抽 `MilestoneChip.vue`（折叠/展开 chip 复用）与 `AchievementCard.vue`（解锁/锁定卡合一）子组件
- `types/milestoneRules.ts` 新增 `TIERS` 常量（由 TIER_LABELS 键派生），LevelConfigTab 遍历改用（去掉本地 tierKeys computed）
- `THRESHOLD_ACHIEVEMENTS` 冗余 `prefix:"ach"` 字段去除（11 处），id 改 `${type}-${v}`，`getAchType` 解析同步（split[0]）
- i18n 清理死键 showAllMilestones，新增 `milestonesCategoryLabel`/`moreMilestonesHint`（zh/en 成对）；MilestoneCategoryList 区块标题/更多提示接 i18n（原中文硬编码）

### UI 排版
- 编辑弹窗 `.rule-editor-panel` 加 `max-width:calc(100vw-32px)`（窄屏/浮动窗防溢出）；≤640px 压缩左列 130→92px
- 危险色字面量 `rgba(207,34,46)`/`--stat-color-danger,#cf222e` 两处 → `stats.$color-danger`
- focus box-shadow 全部移除（Codex 禁 box-shadow），保留 border-color 高亮
- 遮罩统一 `rgba(0,0,0,0.5)` + z-index 10000；≤480px 压缩 Hero 图标/进度环与成就网格列

### 验证
read_lints 全模块 0 错误；验证链留给用户：pnpm i18n:verify / tsc --noEmit / pnpm lint / dev 观察里程碑 Tab（注意稀有度档位变化导致各里程碑稀有度显示不同）。

### 经验
- 弱类型 Record<string,...> 键收窄为 MilestoneTypeKey 需沿 MilestoneDef.type:string → statCounts 索引全链同步，本次未做（记入报告低优先级项）；模板可直接引用 defineProps 暴露的 prop 名（i18n 裸用合法）

