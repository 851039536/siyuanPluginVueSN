# 2026-09-10

## 新增 feature：componentPreview（组件预览）— plan 7 任务全部完成

用户需求：为 `src/components/` 共享 Codex UI 组件库做一个「组件预览」，展示各组件各种用法，方便查看与修改优化；视觉参考 `docs/codex-ui-reference.html`。

### 澄清结论（用户选择）
1. 承载形态：**插件内独立窗口**（addTab + openTab + openWindow 双形态），非静态 HTML / 非独立 dev 页面
2. 交互深度：**静态用法快照 + 源码复制**（不做交互式 props 调节面板）
3. 覆盖范围：**全部 14 个组件**
4. 示例来源：**结构化清单手写示例**（不做 @vue/compiler-sfc 自动解析 props）

### 交付内容

新增目录 `src/features/componentPreview/`：
- `index.ts`：registerComponentPreview（自挂载 `__componentPreview` + addCommand ⌃⌥V + addIcons `iconComponentPreview`）
- `index.vue`（150 行）：头部（isFloating 隐藏标题）+ 搜索 + NavSidebar + PreviewSection 遍历；滚动/点击双向同步导航高亮（suppressScrollSync 抑制平滑滚动期间抢高亮）
- `types/index.ts`（191 行）：PreviewManager（addTab 模型 + open/openFloating + mountPanel/unmountPanel + destroy）、I18n、PreviewExample、PreviewGroup（含 `importCode` 字段）
- `previewData/`（5 数据文件 + index 聚合）：button.ts(Button/IconWrapper)、input.ts(Input/FormField/Label)、control.ts(Select/Switch/Slider)、tagAvatar.ts(Tag/Badge/Avatar)、display.ts(Card/Chart/Loader)
- `components/`：PreviewSection.vue（分区 + 快照网格 + 单开代码展开）、CodeBlock.vue（等宽代码 + 复制反馈）、NavSidebar.vue（锚点列表 + 计数）
- `styles/`：index.scss / PreviewSection.scss / CodeBlock.scss / NavSidebar.scss
- `README.md`

注册 8 处全落地：`features/index.ts`(export+_Registered)、`src/index.ts`(import+DESTROYABLE_KEYS+registerFeatures)、`config/settings.ts`(enableComponentPreview 默认 true)、`config/icons.ts`(FEATURE_ICONS.componentPreview)、`features/config.ts`(FEATURE_CONFIG 条目 + openComponentPreview action)、`features/superPanel/types/index.ts`(ACTION_EVENT_MAP 映射)、`i18n/{zh_CN,en_US}/componentPreview.json`。

### 设计要点与经验

- **数据驱动渲染**：动态 `<component :is="group.component" v-bind="example.props">` + `slotText` 作默认插槽；`code` 字段与 props 同源手工维护。props 为 `Record<string, any>`，无编译期约束，需人工保证图标键为已注册 IconKey、v-model 组件用 `modelValue`。
- **Select/Input/Slider 组件会从 attrs 剥离 class/style**（`containerAttrs` 解构丢弃），因此**不能靠 props 传 style 控宽**，改由预览 SCSS 容器控制。
- **Loader 组件 height:100%** 依赖父容器显式高度，需单独给 stage `height: 160px`（`cp-card__stage--loader`）才能显示。
- **快捷键冲突检查方法**：`Select-String -Path src\features\*\index.ts -Pattern 'hotkey:'` 汇总已占用键位（本项目 ⌃⌥B/C/D/E/I/N/O/R/T/W 已用），⌃⌥V 空闲。
- **尺寸级固定像素**（图标按钮 22/24px、侧栏 180px、搜索框 200px）项目既有约定允许，但建议加 `// 无对应 Token` 注释对齐既有 18 处写法。
- 架构校验：`universal-arch-skill` 的 `validate-project-structure.py --lang vue --features componentPreview` 输出 **0 错误 0 警告**；read_lints 全模块 0 错误；无跨 feature 直接导入、无绕过统一入口调用。

### 待用户验证
`pnpm lint` / `pnpm i18n:verify` / `pnpm validate:icons` / `npx tsc --noEmit`，以及在思源内打开组件预览窗口观察实际渲染效果。

## 追加：预览卡片尺寸档位（用户需求「可以设置可选大小吗」→ 选定「预览卡片尺寸档位」）

用户四选项（卡片档位/浮动窗口尺寸/组件 size 切换/整体缩放）中只选了**预览卡片尺寸档位**（紧凑/舒适/宽松）。

### 实现
- **档位定义**：新增 `types/density.ts`（`PreviewDensity` 类型 + `DensityOption` + `PREVIEW_DENSITIES` 清单 + `DEFAULT_PREVIEW_DENSITY` + `isPreviewDensity` 守卫）。**关键：拆独立文件而非放 types/index.ts** —— 因 `types/index.ts` 运行时 import `../index.vue`（PreviewManager 挂载面板），若 composable 再从 types/index.ts 运行时导入常量会形成 `index.vue → composable → types/index.ts → index.vue` 运行时循环，有 TDZ 风险；`types/index.ts` 用 `export ... from "./density"` 转出保持对外入口不变。
- **持久化**：新增 `composables/usePreviewDensity.ts`（39 行），`PluginStorage` + `TypedStorage<PreviewDensity>`（key `component-preview-density`）+ `isPreviewDensity` 兜底损坏值；面板 `onMounted` 加载、`setDensity` 切换即落盘（不 await）。
- **UI**：头部 actions 内新增 `.cp-density` 分段切换器（S/M/L 短标签 + i18n tooltip，参照 codex-ui-reference 的 tabs 样式：surface 底 + active 用 background 凸出 + border）。
- **样式联动**：用 CSS 变量做单一数据源 —— `.cp-panel` 定义 `--cp-grid-min/--cp-stage-min-h/--cp-stage-pad/--cp-loader-h`（默认=舒适档），`.cp-panel--compact` / `.cp-panel--spacious` 覆盖（写在 `.cp-panel` 之后，同优先级后者胜）；`PreviewSection.scss` 的网格列宽、舞台高度/内边距、Loader 高度全部改为消费变量，避免三份重复规则。
  - 数值：紧凑 170px/72px/$spacing-2/120px；舒适 230px/96px/$spacing-4/160px；宽松 320px/140px/$spacing-5/220px。
- **i18n**：zh/en 各 +3 键（densityCompact/densityComfortable/densitySpacious），`I18n` 接口同步；键总数 19 对齐。

### 验证
read_lints 0 错误；结构校验 0 错误 0 警告；i18n 19 键对齐；存储键全项目唯一；行数最大 276（红线内）。验证链留用户：`pnpm lint` / `pnpm i18n:verify` / `npx tsc --noEmit`。

## 追加：移除 componentPreview 快捷键（用户「移除快捷键操作」）

**发现真实冲突**：`⌃⌥V` 早已被 **video 功能**占用（`src/features/video/types/index.ts:81`，`config.ts:105` 亦登记 `Ctrl+Alt+V`）。此前我核查快捷键时用了 `src\features\*\index.ts` 通配符，只覆盖到 feature 根目录的 index.ts，**漏掉了 `features/<name>/types/index.ts` 等子目录注册点**（video / formatAssistant / superPanel 的 hotkey 都在 types/index.ts 里）→ 误判 ⌃⌥V 空闲。

处理：移除 `index.ts` 中 `addCommand` 的 `hotkey: "⌃⌥V"`，保留 `addCommand`（仅作命令面板入口，不占默认快捷键）；注释与 README 同步说明冲突原因。i18n 键 `openComponentPreview` 保留（仍被 langKey 使用）。

验证：思源 `ICommand.hotkey?: string` 确认为可选（`node_modules/siyuan/siyuan.d.ts`），移除后类型安全；递归复查全项目 hotkey 占用表，componentPreview 已无 hotkey；read_lints 0；结构校验 0/0。

**顺带发现的既有冲突（非本次引入，未改动）**：`tableOfContents` 的 `⌃⌥I` 与 `ideaGenerator` 的 `⌃⌥I` 重复。

## 追加：componentPreview 集成到底部功能列表（用户「功能开关集成到底部功能列表」）

集成机制（`src/features/statusBar/featureRegistry.ts` 的 `buildFeatures` 数组 = 状态栏功能抽屉数据源，加一条即可）：

1. `featureRegistry.ts`：新增 `getI18nShard(plugin, "componentPreview")` + 条目 `{ id: "componentPreview", icon: "mdi:view-grid-outline", color: "#0ea5e9", title: componentPreviewI18n.title || "组件预览", pinnable: true, shortcut: { icon: "mdi:view-grid-outline", itemClass: "action-item component-preview-item" }, action: () => emitCustomEvent("openComponentPreview") }`
2. `statusBar/styles/index.scss`：按既有 `--status-color-*` 约定加变量 `--status-color-component-preview: #0ea5e9` + `.component-preview-item { color: ... }`

**自动获得的能力（零额外代码）**：
- **功能开关角标**：`toggleable` 由 `featureIdToSettingKey(id) in plugin.settings` 自动判定 → id `componentPreview` 推导出 `enableComponentPreview`（已在 settings.ts 定义），抽屉项自动带开关角标，点击经 `plugin.updateSettings` 持久化
- **pin 到状态栏快捷区**：`pinnable: true` + `shortcut` 字段；pin 后 `visibleShortcuts` 渲染 MonitorItem，点击走 `action`
- **自定义分类**：非 monitor 项自动参与 `useFeatureCategories` 分类归属

**事件链路验证**：`emitCustomEvent` 默认在 `window` 派发 CustomEvent（`src/utils/eventBus.ts`），`PreviewManager` constructor 里 `window.addEventListener("openComponentPreview", ...)` 正好接收 ✓。

验证：read_lints 0；结构校验（componentPreview + statusBar）0 错误 0 警告。

## 更正：「大小设置」是组件尺寸，不是卡片尺寸（推翻上一条卡片档位实现）

用户澄清原话：「大小设置是组件的尺寸，不是卡片的」。此前我在四选一中理解偏了（做了卡片/网格尺寸档位），实际需求是**组件自身 `size` prop 的档位切换**。

**重构内容（替换而非叠加）**：
- 删除 `types/density.ts` + `composables/usePreviewDensity.ts`（卡片档位）
- 新建 `types/size.ts`：`ComponentSize = "xsmall" | "small" | "medium" | "large"`、`SizeOption`、`COMPONENT_SIZES`、`DEFAULT_COMPONENT_SIZE = "small"`、`isComponentSize`
- 新建 `composables/usePreviewSize.ts`：storage key `component-preview-size`（旧 key 作废）
- `PreviewGroup` 新增 `sizeable?: boolean`；11 个支持 size 的组件标记为 true（Button/Input/FormField/Label/Select/Switch/Slider/Tag/Badge/Avatar/Card）；**Chart（size 为预设像素宽高，大档撑破卡片）/ IconWrapper（size 为像素数）/ Loader（无 props）不参与**
- `PreviewSection.vue` 新增 `size` prop + `resolveProps(example)`：`sizeable` 且示例**未显式指定 size** 时注入全局档位；显式指定 size 的示例（尺寸对比用例）保持原样，避免标题与实际渲染不符
- 回退卡片布局：`.cp-panel` 的 4 个档位 CSS 变量与 `.cp-panel--compact/spacious` 全部移除，网格列宽回 `minmax(230px,1fr)`、舞台回 96px/Loader 160px；切换器类名 `.cp-density*` → `.cp-size*`
- i18n：`densityCompact/Comfortable/Spacious` → `sizeXsmall/sizeSmall/sizeMedium/sizeLarge`（zh/en 20 键对齐）

验证：density 残留 grep 0；sizeable 标记 11 处；i18n 20 键对齐；read_lints 0；结构校验 0/0；行数最大 279。

**经验**：需求澄清阶段我给的选项描述里「组件 size 参数切换」与「预览卡片尺寸档位」区分度不够，导致用户误选；涉及"大小/尺寸"类需求时，应先用一句话确认是**被预览组件自身的尺寸**还是**预览界面/容器的尺寸**。

## 追加：组件 size 档位字号阶梯统一（用户「组件尺寸改到 xs，字体跟 s 一样大，这样不合理」）

用户从 componentPreview 切到 XS 档发现字体与 S 档完全相同，要求 XS 真正变小。澄清结论：XS 档统一 10px；范围＝全部 14 个组件（凡 XS 非 10px 一律对齐）；Card 标题全档改 10/12/14/16；Switch 标签新增四档变体 10/12/14/16。

### 改动（7 个 SCSS，纯字号声明）
| 文件 | 变更 |
|---|---|
| `Button.scss` | `&--xsmall` 字号 `$font-size-xs`(12px) → `$font-size-2xs`(10px) |
| `Input.scss` | `&--xsmall .si-input__field` 同上 |
| `Select.scss` | `&--xsmall` 的 trigger/option 改 2xs；并新增 `.si-select__filter-input / __empty / __group-label` 的 2xs 覆盖（XS 档下拉内部随选项联动）；L107 注释改为「与下拉选项默认档字号一致（XS 档在 &--xsmall 内单独覆盖为 10px）」 |
| `Avatar.scss` | `&--xsmall` 12px → 10px，删除过时注释「10px 无对应 Token」 |
| `Card.scss` | 标题 12/14/16/18 → **10/12/14/16**；副标题 10/12/12/14 → **10/10/12/14**（= 标题降一档、下限 10px，保证标题不小于副标题；XS 档同号靠 semibold 字重 + 弱化色区分） |
| `Switch.scss` | `&__label` 原固定 `$font-size-sm`(14px) → 基础声明改 `$font-size-xs` 兜底 + 四档反向选择器覆盖 `.si-switch--xsmall/small/medium/large &` = 10/12/14/16 |
| `Badge.scss` | 字号本已合规（XS 10px），仅清理「原 10px/11px 无对应 Token」过时注释，并补档位标注 |

未动：Tag / Label / FormField / Slider（XS 已 10px）、Chart（仅宽高）/ IconWrapper（像素数）/ Loader（无 props）。

### 文档同步
- `AGENTS_STYLE.md` § 字号层级：新增「### 组件 size 档位字号阶梯（两级字号制的唯一例外）」——四档 = `$font-size-2xs/xs/sm/base` = 10/12/14/16，明确这是两级字号制中**唯一**允许 10px 出现在控件正文的场景，且例外仅限 `src/components/styles/*.scss` 的档位变体，**不得**据此在 feature 业务样式把正文降到 10px；只改字号、不联动 padding/min-height/gap/图标尺寸。
- `src/features/componentPreview/README.md`：尺寸档位条目补充四档字号阶梯 + Card/Switch/Select 变更说明。

### 影响面（已向用户明示并获确认）
- 全插件 137+ 处 `size="xsmall"` 文字 12px → 10px。
- Card/Switch 默认档位是 `small` → 未显式传 size 的 Card 标题 14→12px、副标题 12→10px，Switch 标签 14→12px。
- 预览面板自身（切换器 / 持久化 / `resolveProps` 注入逻辑 / i18n）零改动。

### 验证
`universal-arch-skill` 模式 C 审查：`src/components/styles/*.scss` 中 `font-size:\s*\d` / `font-weight:\s*\d` / `line-height:\s*[\d.]+` 硬编码 grep **0 命中**；12 个 SCSS 的 `font-size` 声明全部取自 `$font-size-*` Token；read_lints 0 错误。剩余验证留用户：`pnpm lint` / `npx tsc --noEmit` + 预览面板逐档目视。

## 追加：S/M/L 档内边距各减 2px（用户「S M L 内间距缩一点点」）

澄清结论：范围＝全部含档位的组件；幅度＝水平与垂直各减 2px。

### -2px 的 Token 映射（关键经验）
全局间距 Token 为 4/8/12/16/20/24px，减 2px 后多数落在**非 Token 值**上。可用映射：
| 原值 | -2px 后 | 写法 |
|---|---|---|
| 4px `$spacing-1` | 2px | `$spacing-2px` |
| 8px `$spacing-2` | 6px | `m.$gap-xs`（_mixins 本地 6px） |
| 12px `$spacing-3` | 10px | `m.$spacing-2_5`（_mixins 本地 10px） |
| 16px `$spacing-4` | 14px | **无 Token**，硬编码 + `// 14px 无对应 Token` |
| 20px `$spacing-5` | 18px | **无 Token**，硬编码 + `// 18px 无对应 Token` |
| 10px `m.$spacing-2_5` | 8px | `$spacing-2` |
| 6px `m.$gap-xs` | 4px | `$spacing-1` |
| 5px（Badge 硬编码） | 3px | `$spacing-px`（=3px） |
| 14px（硬编码） | 12px | `$spacing-3` |
| 18px（硬编码） | 16px | `$spacing-4` |

### 逐组件改动
- **Button**：S `8px 12px`→`6px 10px`、M `8px 16px`→`6px 14px`、L `12px 20px`→`10px 18px`；同时 `gap` S 4→2px、M 8→6px、L 12→10px
- **Input** `.si-input__field`：S `4px 8px`→`2px 6px`、M `8px 12px`→`6px 10px`、L `12px 16px`→`10px 14px`
- **Select**：trigger 与 option 同步减 2px（S `2px 6px`/`2px 14px`、M `6px 10px`/`6px 14px`、L `6px 14px`/`6px 14px`）
- **Card**：header/footer/body 各减 2px（S `8px 10px`/`10px`、M `12px 14px`/`14px`、L `16px 18px`/`18px`）
- **Tag**：S `2px 8px`→`2px 6px`（纵向 2px 已到下限保留）、M `4px 10px`→`2px 8px`、L `6px 14px`→`4px 12px`
- **Badge**：仅水平（纵向本就是 0）——S 5→3px、M 6→4px、L 8→6px
- **Switch**：根 `gap` 12px→10px（轨道与标签间距，用户点名；该值非档位变体，故四档统一生效）

### 未改动的组件及原因
- **Slider**：档位差异只在轨道高度与滑块直径，**无内边距可缩**；`__value` 的 min-width 是数值预留宽度，缩小会挤字，故不动
- **FormField / Label / Avatar**：档位只控制字号 / 直径，无内边距
- **Chart（宽高）/ IconWrapper（像素数）/ Loader（无 props）**：不适用
- **XS 档**：用户只要求 S/M/L，XS 内边距保持不变

### 影响面
Button / Input / Select(trigger) / Card 有 min-height 兜底，纵向减 2px 后仍被 min-height 支配 → **高度不变，仅宽度各缩 2px**；Select 的 option 无 min-height → 下拉选项行高减 4px。所有组件默认 size 均为 `small`（已核对 11 个 .vue），故基础 `padding` 声明实际是未使用兜底。

验证：read_lints 0 错误；逐档 grep 复核 padding 值全部落位。

## 追加：Button 组件全面优化（用户「https://primevue.dev/button/ 组件UI参考，看下有什么可以优化的？给出建议」→ 选定「全部 11 项」）

对照 PrimeVue 5.0.1 Button 文档完成 11 项 + rounded。改动文件：`src/components/Button.vue`（104→177 行）、`src/components/styles/Button.scss`（189→311 行）、`src/components/styles/_mixins.scss`（+37 行）、`src/features/componentPreview/previewData/button.ts`（15→26 个示例）、`AGENTS_STYLE.md`（+32 行新章节 + 2 处表格行）。

### 5 项真实缺陷（均已核实为真 Bug，非推测）
1. **键盘焦点不可见**：`Button.scss` 只有 `outline: none` 无 `:focus-visible`（全项目 16 个组件 SCSS 中仅 Switch/Select 有）。修复：`&:focus-visible { outline: 2px solid var(--b3-theme-primary); outline-offset: 1px; }`。
   - **经验**：`_mixins.scss` 的 `focus-ring` mixin 只改 `border-color`，而 `.si-button` 的 border 是 `1px solid transparent`、实底变体不设 border-color → **单用该 mixin 在实底按钮上完全不可见**。焦点环要用 outline（同 Switch.scss），不能无脑复用 focus-ring。
2. **loading 时 spinner 压字**：`m.btn-spinner` 绝对居中 + 文案也居中 → 完全重叠。生产实证 `imageCompressor/index.vue:126`（`:loading` + 插槽"压缩中…"）。修复：图标条件由 `v-if="icon && !loading"` 改为 `v-if="icon"`，loading 时对 `__icon`/`__text` 用 `visibility: hidden`（保留占位 → **按钮宽度零跳变**，优于 PrimeVue 的「spinner 替换内容」会塌宽）。
3. **block 文案靠左**：`&__text { flex: 1 }` 吃掉全部剩余空间使 `justify-content: center` 失效，span 自身默认 `flex-start`。全项目 0 处用 `block`，仅预览暴露。修复：`&--block .si-button__text { justify-content: center }`（保留 flex:1 以维持 icon-right 贴右）。
4. **图标不随档位缩放**：spinner 已 12/14/16/18 但 `iconSize` 恒 16。修复：`iconSize` 默认改 `undefined` + `TIER_ICON_SIZE` 查表（12/14/16/18）；3 个文件 11 处显式传值（flashcardReading）优先级最高不受影响。
5. **硬编码色值 + 深色主题 active 失效**：`#ef4444`→`$color-danger-bright`（精确等值 hsl(0 84.2% 60.2%)）、`#dc2626`→`$color-danger`（精确等值 hsl(0 72% 51%)）、`rgba(239,68,68,*)`→`rgba($color-danger-bright, α)`；`--ghost:active` 的 `rgba(0,0,0,0.08)` 深色主题下不可见 → 改 `var(--b3-theme-surface-light, $color-surface)`。

### 6 项增强（零破坏）
6. **颜色轴 × 外观轴解耦**：新增 `severity`（6 色，覆盖 variant 推导色）+ `outlined`/`text` 布尔修饰。**关键设计**：新增类名一律加前缀（`--severity-*`/`--outlined`/`--text`），不复用不改写既有 `--primary`/`--secondary`/`--success`/`--danger`/`--ghost` 五个类 → 全项目 180+ 处调用渲染不变。
   - **颜色族用 CSS 自定义属性传递**：`_mixins.scss` 新增 `@mixin btn-color-family($theme-var, $fallback, $on-color)` 输出 `--btn-color`/`--btn-on-color`/`--btn-soft`/`--btn-soft-strong`；弱底色用相对颜色语法 `hsla(from var(--x, $y) h s l / 0.12)`（**同 Tag.scss 既有写法**，明暗主题自动正确）。这样 `--outlined`/`--text` 只需写一份通用规则，靠颜色块设的变量取色，避免 7 色 × 2 外观 = 14 个复合选择器。
   - **覆盖靠源码顺序**：SCSS 顺序 = 颜色块 → severity 块 → 外观修饰块，同特异性 (0,1,0) 靠后者胜；hover/active 均为 (0,4,0) 同样靠顺序。
   - **`--ghost` 刻意不设颜色族变量** → `interactive-soft` 回退到 `var(--b3-theme-surface-lighter)`，hover 与改动前**完全一致**（避免 104+ 处 ghost 按钮 hover 观感漂移）。
7. **交互反馈统一**：`interactive-raised` 新增 `$lift: false` 参数，**默认去掉 `transform: translateY(-1px)`**（消除密集工具栏 1px 抖动）；新增 `@mixin interactive-soft`（透明底按钮用主题感知弱底色，结构同 raised：0.12s + 无位移）→ danger/ghost 首次获得 transition。
8. **补 info / warning 变体**（`--b3-theme-info` / `--b3-theme-warning`）。
9. **补 `type` prop**（`button|submit|reset`，默认 button），替代硬编码 `type="button"`。
10. **无障碍命名**：新增 `title` / `ariaLabel` prop；`aria-label` **仅在纯图标场景**（`!slots.default && icon`）从 `title` 派生 —— 不覆盖文字按钮的可读标签（WCAG 2.5.3 考虑）；`import.meta.env.DEV` 下对两者皆缺的纯图标按钮 `console.warn`。
11. **`iconPosition` 扩为四向**：`--icon-top { flex-direction: column }` / `--icon-bottom { column-reverse }`，并覆盖 `&__text { flex: none }`（纵向下 flex:1 会撑高错位）。
12. **`rounded`**（`$radius-full`）+ 预览面板补 11 个新示例。

### 规范澄清（用户点名的「26/28px 对齐」）
`AGENTS_STYLE.md` 的「图标按钮固定 26×26」源自 `docs/codex-ui-reference.html` 的 feature 自建 `.icon-btn`（无档位）；共享 `<Button icon-only>` 是分档 API（22/28/36/44，默认 small=28 与文字按钮 min-height 对称）。**采用文档澄清路线**，不把 small 改成 26px（属破坏性视觉变更）。新增 `## 强制规则：按钮交互与无障碍（2026-09-10）` 章节固化 focus/loading/a11y/尺寸图标约定。

### 已知的既有语义变化（有意为之，需知悉）
- primary/secondary/success hover 不再有 1px 上浮（原设计抖动源）
- `variant="danger"` hover 底色由 `rgba(239,68,68,0.1)` → 主题 error 色 12% 透明；hover 不再加深边框（`#dc2626` 移除）
- `--secondary` 的 var fallback 修正（`$color-info`/`$color-bg` 误用 → `$color-bg`/`$color-fg`/`$color-border`），仅 var 缺失时生效

### 验证
- 架构审查（universal-arch-skill 模式 C）：Button.scss / _mixins.scss 硬编码色值（hex/rgba）与硬编码 font-size/weight **0 命中**；`Button.vue` 的 `<style>` 仅 `@use`；13 个产出类名全部在 SCSS 有对应规则（无悬空类名）；括号配平 48/48、20/20；read_lints 0 错误
- **行数**：Button.scss 311 行 —— 超 300 警戒线 3%，**远低于 500 硬阈值**，故未按计划拆分为 partial。理由：拆分需引入 `@use` 顺序依赖（外观修饰必须晚于颜色块），footgun 大于收益；且 AGENTS_STYLE 命名规则限定 `_` 前缀仅用于 mixins/变量 partial，纯规则 partial 无合规命名
- 剩余验证留用户：`pnpm lint` / `npx tsc --noEmit` / `pnpm validate:icons` + 预览面板逐项目视（尤其 loading 保宽、block 居中、outlined/text 取色）

## 追加：Button.vue 冗余与性能审查（用户「审查 Button.vue 是否有冗余，和性能问题」）

### 唯一真实缺陷：`isIconOnly` computed 依赖非响应式的 `slots`
**源码级证据（Vue 3.5.40）**：`node_modules/.pnpm/@vue+runtime-core@3.5.40/.../runtime-core.esm-bundler.js`
- `:5349` `const slots = instance.slots = createInternalObject()`
- `:4882` `const createInternalObject = () => Object.create(internalObjectProto)` → **普通对象，非 reactive**
- `:5341` `assignSlots` 原地 `slots[key] = children[key]`（`updateSlots` 复用同一对象）

→ `computed(() => !slots.default && !!props.icon)` 只在 `props.icon` 变化时重算；插槽增删不触发。而模板 `v-if="$slots.default"`（Button.vue:23）是渲染期实时读取 → **两者会不一致**：
- 初始无插槽（icon-only 方形固定宽高 28px）→ 后出现插槽 → 文字渲染了但 `si-button--icon-only` 还在 → **文字溢出方块**
- 反向：插槽消失但类名丢失 → 不再是方形

**影响面**：全项目**当前 0 处触发**（多行搜索 `<Button[^>]*>\s*<(template|span|IconWrapper|div)[^>]*v-if` 无命中）→ 属潜在缺陷。

**修复**：`buttonClasses` / `isIconOnly` / `accessibleName` 从 `computed` 改普通函数（渲染期必然重算，与模板 `$slots` 读取对齐）；computed 在此处的缓存是**有害的**（缓存了非响应式快照）。

### 冗余（2 项）
1. `disabled || loading` 重复 2 处（模板 `:disabled` Button.vue:7 与 `si-button--disabled` Button.vue:131）→ 可抽 `isDisabled`
2. `handleClick` 的 `!props.disabled && !props.loading` 守卫是**死代码**：原生 `:disabled` 不派发 click + `pointer-events: none`（Button.scss `--disabled`/`--loading`）→ 永不生效。保留作防御无妨

### 性能：无实际问题
`import.meta.env.DEV` 构建期静态替换 → 生产包无 `console.warn` 分支；`@click="handleClick"` 用稳定函数引用（非每次渲染新建箭头函数）；无 watcher / 定时器 / DOM 查询 / 深监听；`useSlots()` 只取引用无开销；`withDefaults` 已正确省略 `severity`/`iconSize`/`title`/`ariaLabel` 的冗余默认值。

### 复用结论
**Vue 的 `slots` 不是响应式**（见上源码证据）——凡「派生值需要跟随插槽增删变化」的场景，必须用普通函数或直接在模板读 `$slots`，不能用 `computed`。已同步到 MEMORY.md。

### 用户决定（2026-09-10）
审查发现的问题（`isIconOnly` computed 陈旧 + 2 项冗余）经确认 **暂不修改**，`Button.vue` 保持现状。属**已知并有意保留**的状态——后续会话不要主动重提或擅自"修复"这三点。

## 追加：AGENTS.md 新增「共享组件库使用规则（强制）」（用户「更新agent.md规则，使用通用组件时参考用法？」→ 选定「查阅 + 同步 + 优先复用」最完整一层）

### 背景缺口
改动前 `AGENTS.md` **完全没有**共享组件（`src/components/`）的使用规则，只在「关键文件速查」的目录树里提了一句「共享 shadcn-vue 组件（Button/Input/Select/Switch/Tag 等）」。而 `src/components/` 已被 180+ 处调用。

### 落地内容（全部在 `AGENTS.md`）
1. **新增 `## 共享组件库使用规则（强制）` 章节**（插在「统一入口原则」与「子组件数据流规则」之间，L158-209，53 行），三条主线：
   - **先查用法，禁止猜 props**：查询途径三级 —— 组件预览面板（推荐，真实渲染快照 + 可复制代码）→ `previewData/*.ts` 清单（props 与 code 同源）→ 组件源码 `interface Props`（最终事实来源）。另约束：图标只能传已注册 `IconKey`；尺寸档位统一 `xsmall/small/medium/large`（默认 small）
   - **优先复用，禁止 feature 自建同类控件**：12 类控件必须用共享组件；缺能力时**先扩展共享组件**（加可选 props 保持向后兼容）再消费。允许例外：纯展示局部布局容器、无档位的 26×26 `.icon-btn`
   - **组件清单表（14 个）**：逐组件列出职责 + 关键 props（Button/Input/Select/FormField/Label/Switch/Slider/Tag/Badge/Avatar/Card/Chart/IconWrapper/Loader），props 全部从各组件 `interface Props` 实读核对，非猜测
   - **改 API 必同步预览清单**：`previewData/*.ts`（props 与 code 一致）+ `componentPreview/README.md`
2. **「硬规则」新增 3 条 bullet**：共享组件先查后用 / 优先复用 / 组件 API 变更同步预览清单
3. **「规则分片索引」新增一行**：指向 `src/features/componentPreview/README.md`（使用场景：使用共享组件前查用法、或改 API 后同步清单）
4. **「关键文件速查」目录树**：`components/` 行改为「共享组件库 14 个（Button/Input/Select/Card/Chart 等）— 使用规则见「共享组件库使用规则」」

### 锚点链接校验
`./AGENTS_STYLE.md#强制规则按钮交互与无障碍2026-09-10`、`#核心规范速查表` —— 与文件内既有锚点风格（如 `#强制规则背景与过渡对齐-gitpush-范式2026-09-02`）一致：去标点、空格转连字符、英文小写。

## 追加：aiContentGenerator 共享组件合规审查 + 改造（用户「根据规则审查通用组件」→ 选定「审查 + 按规则改造」+ 四项范围全选）

### 产出
- 审查报告：`docs/ai-content-generator-component-audit.md`（违规清单含文件:行号 + 规则依据 + 图标/控件映射表 + 合规例外判定 + 本次不处理项 + 改造结果 + 验收清单）
- 改造范围：`src/features/aiContentGenerator`（26 个文件）+ `src/config/icons.ts` + i18n 分片 + `componentPreview`

### 改造前问题（量化）
| 维度 | 改造前 | 改造后 |
|---|---|---|
| 内联 `<svg><use xlink:href="#iconXxx">` | 30 处 | 0（`ReviewRadarChart` 图表 SVG 除外） |
| 原生 `<button>` | 13 处 | 1（`CollapsibleSection` 折叠头，合规例外） |
| 原生 `<select>/<input>/checkbox>` | 6 处 | 0 |
| 硬编码 UI 中文文案 | 约 62 处 | 0 |
| `MainContentArea.vue` | 375 行 | 281 行 |

### 关键动作
1. **图标体系统一**：`COMMON_ICONS` 新增 `minus`(mdi:minus) / `columns`(mdi:view-column)；`ACTION_META[].icon` 由 `string` 收敛为 `IconKey`；删除自建 `SvgIcon.vue`（与 `IconWrapper` 职责重叠、接受任意 `#iconXxx`）与 `modules/AIContentGenerator.ts` 的 `registerIcons()`（`plugin.addIcons` 注入自定义 `#iconColumns` sprite）
2. **表单控件**：模型下拉（`isGroup` 分组）+ 思考强度下拉 → `Select`；自定义模型名 → `Input`；联网/思考/审核 → `Switch`(xsmall + 默认 slot 放图标文字)；`SkillSection` 自建「触发器+下拉+搜索+外部点击关闭」整套（约 90 行）→ `Select` + `filterable`
3. **按钮**：分段切换（视图 Tab / Diff 模式 / 严重度筛选）用 `Button` 的 `text` 外观 + `variant` 在 `primary`/`ghost` 间切换表达选中态；分组容器保留（纯布局）；footer/快捷动作/纯图标按钮全部改 `Button`；`ReviewPanel` 的 `.subsection-toggle` 改为复用模块内 `CollapsibleSection`（消重复折叠实现）
4. **文案**：i18n 分片 26 → 90 键（中英对齐，`pnpm i18n:merge` 后顶层 1059 键）；`ACTION_META[].label` → `labelKey`（i18n 单一数据源）
5. **结构**：抽出 `ResultActionsBar.vue`；`.dot-flashing` 从 `MainContentArea.scss` 上移 `styles/index.scss`；删除全部死代码类（含无引用的 `ai-spinner-rotate` 关键帧）

### 扩展了共享 `Select`（按规则「缺能力时先扩展共享组件」+ 同步预览清单）
- 新增 `#selected` 作用域插槽（`{ option }`）：已选项富内容；不传时回退 `selectedLabel`。动机：`SkillSection` 需在选中项上显示"来源工具色点"，`Select` 原本只有纯文本 `selectedLabel` → 不加插槽就得丢功能
- `SelectOption` 新增 `keywords?: string`：`filterable` 的附加检索词，保留原自建下拉的多字段检索（名称+描述+工具）
- 同步：`previewData/control.ts` 新增「关键词筛选（keywords）」示例；`componentPreview/README.md` 新增「具名插槽」登记表（`PreviewExample` 只支持 `props` + `slotText`，**具名/作用域插槽无法在快照中渲染，必须在 README 登记**）

### 合规例外与遗留（已写入报告）
- `CollapsibleSection.vue` 折叠头保留原生 `<button>`：是「chevron + 图标 + 标题 + 状态点 + `headerRight` slot」复合布局容器，非标准按钮控件，全项目仅此一处（Rule of Three 未满足）；已补 `aria-expanded`/`aria-controls`（用 `useId()` 生成 bodyId）
- `ReviewRadarChart.vue` 自建 SVG 雷达图未迁移：共享 `Chart.vue`（chart.js）仅注册 line/bar/pie/doughnut/area 控制器，**不支持 radar**；图表类未纳入本次声明范围。后续若合规需给 `Chart.vue` 扩展 radar（注册 `RadarController`+`RadialLinearScale`）并同步 `previewData/display.ts`
- 改造后仍超 300 警戒线（未破 500）：`index.vue` 349 行（既有超标）、`BottomInputArea.vue` 319 行（因模板 props 展开而增长，改造前 251）。建议后续把「模型选择 + 思考开关 + 思考强度 + 审核开关」抽为 `GenerationOptionsBar.vue`

### 经验
- **删除前先做引用影响分析**：`SvgIcon` / `registerIcons` / `ACTION_META.icon` 三处删除前用 `search_content` 全项目核引用（`SvgIcon.vue` 全项目仅 1 份、`#iconColumns` 仅 1 处使用），确认零遗漏后再删
- **`<script setup>` 里 `export interface` 可被其他文件 `import type`**：项目内已有 24 处 `import type { SelectOption } from "@/components/Select.vue"` 先例，本次沿用
- **对象字面量 `as SelectOption` 断言会因缺必填字段报错**：分组对象（`{ isGroup, label, options }`）应声明为 `Array<SelectOption | SelectGroupOption>` 而非逐个 `as` 断言
- **`Select` 的 `@update:model-value` 载荷是 `string | number | boolean | null`**：直接 `$event as SomeLiteralUnion` 会因 `number` 不可比较而报错，须先 `String(...)` 再断言，或抽成具名 handler
- **扁平 i18n 键需先查跨分片重名**：新增键前用 `search_content` 在 `src/i18n/zh_CN/` 全量搜键名（本次发现 `copyTitle` 与 `flashcardReading.json` 重名 → 改名 `copyContentTitle`）
- **`pnpm i18n:merge` 可安全执行**（是生成步骤非验证步骤）；`pnpm vite build`/`pnpm lint` 仍禁止
- **Vue 模板内给无该 prop 的组件传 `title` 依赖 attrs fallthrough**：`Switch` 无 `title` prop 但根是单 `<button>`，fallthrough 生效；`npx tsc --noEmit` 不检查 `.vue` 模板，故不会被拦（`vue-tsc` 才是模板级检查器，项目未接入 npm script）

## 追加：新增第 15 个共享组件 Checkbox（用户「参考 primevue.dev/checkbox 新增组件」）

### 澄清结论（用户选择）
1. 功能范围 = **完整对齐 PrimeVue**：boolean 二元 + `trueValue`/`falseValue` 自定义值对 + `value` 数组分组模式 + `indeterminate` 半选 + `variant` outlined/filled + disabled/readonly/error
2. Props 风格 = **项目既有风格**：size 四档 `xsmall/small/medium/large`（默认 small）、用 `error`（不用 `invalid`）、`label`/`hint` 内建
3. 迁移范围 = **仅新增组件**，不替换 feature 内约 20+ 处原生 `<input type="checkbox">`

### 交付
- `src/components/Checkbox.vue`（251 行）+ `src/components/styles/Checkbox.scss`（156 行）
- `src/features/componentPreview/previewData/checkbox.ts`（12 个示例，`sizeable: true`）+ `previewData/index.ts` 聚合（置于 control 之后）
- 文档计数 14 → 15 共 7 处：`AGENTS.md`（L160/L168/L181/L440/L478 + 组件清单表加 Checkbox 行 + 复用规则控件清单加「复选框」）、根 `README.md`、`componentPreview/README.md`（L3/L8/L12/L13）
- **无 i18n 改动**（预览示例标题是数据文件内中文硬编码，非 i18n 键）

### 组件设计要点
- **原生 input 承载语义**：视觉隐藏的原生 `<input type="checkbox">`（`opacity:0` + 1px + `pointer-events:none`）放在 `<label>` 内，免费获得 Tab/Space 与表单语义；可视方框 `.si-checkbox__box` 纯展示。区别于 `Switch`（无原生等价元素故用 `button + role="switch"`）
- **模式自动解析**：`isGroup = !binary && Array.isArray(modelValue)`；二元模式用 `modelValue === trueValue` 严格相等；分组模式 `indexOf(value) > -1`
- **受控回写防漂移**：`handleChange` emit 后 `nextTick(syncNativeState)` 把 `checked`/`indeterminate` 写回 DOM —— 父级忽略更新时视觉自动回滚
- **`indeterminate` 只能写 DOM 属性**（无 HTML 属性可表达），用 `watch(() => props.indeterminate, ..., { flush: "post" })` + `onMounted` 同步；同时 `aria-checked="mixed"`
- **分组模式返回全新数组**（`filter` / `[...list, value]`），绝不原地 mutate
- **复用**：`FormField`（只传 hint/error，不传 label → 不渲染顶部 label，仅得提示行样式）、`IconWrapper`（`check`/`minus` 两个 **已注册** IconKey）
- **焦点环用 outline**：`&__input:focus-visible + &__box { outline: 2px solid ...; outline-offset: 2px; }`（禁用 `focus-ring` mixin，同 Switch 结论）
- **尺寸**：方框 14/16/18/20px（14/18 无 Token 已注释）、图标 10/12/14/16（`TIER_ICON_SIZE` 常量）、标签字号 10/12/14/16（四档阶梯）
- 新增 `required`（绑定原生 input，用于原生表单校验，不产生星号）；`labelBefore` 与 Switch 对齐

### 踩坑与经验
- **grep 漏检导致重复注册图标**：我首次用 `^\s+(minus|...)` 搜 `icons.ts` 得 0 命中，误以为 `minus` 不存在并注册了 `mdi:minus`，结果 `tsc` 报「对象文本不能具有多个名称相同的属性」（icons.ts:825 原有 `minus`）。**教训：改 `icons.ts` 前必须用宽松模式（如 `mdi:minus` 或 `minus:`）复核，或直接用 `lsp workspaceSymbol`；注册后立刻 `read_lints` 验证**
- `check`(L426) 与 `minus`(L822) 早已在 `COMMON_ICONS`，本次组件零图标新增
- SCSS 嵌套陷阱：`&__box` 内写 `.si-checkbox--xsmall &` 才会得到 `.si-checkbox--xsmall .si-checkbox__box`；若写成 `.si-checkbox--xsmall { .si-checkbox__box {} }` 会被 Sass 前置父选择器变成 `.si-checkbox .si-checkbox--xsmall .si-checkbox__box`（错误）。**沿用 Switch.scss 的 `.si-xxx--tier &` 反向选择器写法**
- 预览面板 `PreviewSection` 只透传静态 props、不接 `v-model` → 受控组件在预览中点击会即时回滚，属预期（静态快照定位）

### 验证
`read_lints` 0 错误（Checkbox.vue / checkbox.ts / previewData/index.ts / icons.ts）；`universal-arch-skill` 模式 C 审查：无硬编码颜色/字体三要素、`.vue` 内仅 `@use`、文件头注释齐备、行数远低于 300 警戒线、无跨 feature 导入、无绕过统一入口。
剩余验证留用户：`pnpm lint` / `pnpm i18n:verify` / `pnpm validate:icons` / `npx tsc --noEmit` + 在组件预览面板目视 Checkbox 分区与四档尺寸。

## 追加：新增共享组件 DatePicker（用户「参考 primevue.dev/datepicker」→ 同一模式）

### 澄清结论（用户选择）
1. 能力范围 = **常用子集**：`selectionMode` single + **range**、`view` 日/月/年、`dateFormat`、`minDate`/`maxDate`、`disabledDates`（数组/函数）、`disabledDays`、`showClear`、底部「今天/清除」；**不做** `showTime`/`timeOnly`/`multiple`/`inline`/`numberOfMonths`/`variant`
2. 值类型 = **Date 为主 + `valueFormat` 字符串输出**
3. 弹层 = **相对定位下拉（同 Select 范式，不用 Teleport）**
4. 迁移 = **仅新增组件**（项目 2 处原生 `<input type="date">` 不动）

### 重要发现：工作区存在并行变更
实施中发现 `src/components/` **已被并行加入 `ColorField.vue`**（并已把计数刷到「16 个」、写完 `previewData/colorField.ts`、更新了 AGENTS.md 与 componentPreview/README.md）。故 **DatePicker 是第 17 个组件**，计数从 16 → 17；同时修掉了 ColorField 遗留的 2 处陈旧「15 个」（`README.md:148`、`AGENTS.md:480`）。
**经验：改组件计数前必须先 grep 实测当前值，不能凭上一轮的记忆（上一轮结束时是 15，本轮已是 16）。**

### 交付（12 个新文件 + 5 处文档）
```
src/components/
├── DatePicker.vue                      338  # 公开入口（平铺，保持 @/components/X.vue 导入约定）
├── datePicker/                              # 私有实现子目录（禁止 feature 直接导入）
│   ├── types.ts                        158  # 类型 + 中文文案常量（WEEKDAY/MONTH/DEFAULT_ARIA_LABELS）
│   ├── formatUtils.ts                  241  # dateFormat 模板引擎：formatDate + parseDate
│   ├── utils.ts                        318  # 基础日期/网格/禁用判定/值形态转换
│   ├── useDatePicker.ts                331  # 入口逻辑（开合/定位/值适配/输入框交互）
│   ├── PickerPanel.vue                 310  # 面板（头部导航 + 视图主体 + 底部栏，自持视图状态）
│   ├── CalendarPanel.vue               270  # 日网格 + 42 格元数据 + 键盘导航
│   └── MonthYearPanel.vue              127  # 12 月宫格 / 10 年宫格
└── styles/  DatePicker.scss 187 / DatePickerPanel.scss 97 / DatePickerCalendar.scss 108 / DatePickerMonthYear.scss 71
```
预览：`previewData/datePicker.ts`（15 个示例，`sizeable: true`）+ `previewData/index.ts` 聚合。
文档：`AGENTS.md`（5 处计数 16→17 + 清单表加 DatePicker 行 + 复用控件清单加「日期选择」+ **新增「结构约定」段**）、根 `README.md`、`componentPreview/README.md`（L3/L8/L13/L14 + 具名插槽表加 `date`/`buttonbar` + 事件契约表加 DatePicker 行）。
零 i18n 改动（沿用 `Select.vue` 范式：组件内中文默认 prop 值 + `DEFAULT_ARIA_LABELS` 常量，调用方可用 `ariaLabels` 局部覆盖）。

### 架构要点
- **结构约定（本轮新增，已写入 AGENTS.md）**：公开组件平铺于 `src/components/`；组件的私有子部件与纯函数放同名小写子目录（`datePicker/`），子部件不计入 17 个组件清单
- **零第三方日期库**：全项目无 dayjs/date-fns/luxon，自建约 560 行纯函数（格式化引擎/网格/禁用/值转换）
- **值形态适配层**：输入侧 `Date`/字符串/数组自动归一，输出侧由 `valueFormat` 决定；内部一律 `Date` 运算
- **`isDateDisabled(date, options)` 单一入口**：min/max + disabledDays + disabledDates（数组或函数）四类约束收敛；数组预处理为 `Set<number>`（42 格逐格 `includes` 的 O(n) → O(1)）
- **禁用日期数组预处理**与 **42 格元数据一次性 `computed`**（模板只读，同时作 `date` 插槽 scope 载荷，一份数据两用）
- **`indeterminate` 之外的新坑：无时区字符串必须按本地时间解析**——`new Date("2026-09-10")` 按 UTC 解析，在西半球时区会退到前一天，与 `formatDate` 的本地输出无法往返一致。已加 `LOCAL_DATE_PATTERN` + `toLocalDate()` 显式按本地构造
- **dateFormat 令牌引擎**严格对齐 PrimeVue 表（`d/dd/o/oo/D/DD/m/mm/M/MM/y/yy/@/!` + `'字面文本'` + `''`）；`parseDate` 反向解析**只支持数值令牌**（`o/oo/D/DD/M/MM` 会返回 null），含溢出日期回读校验（2 月 31 日被判非法）
- **焦点管理三个真实缺陷（均已在实现中修复）**：① 关闭面板后 `inputRef.focus()` 会触发 focus 事件**立即重开面板** → 加 `skipFocusOpen` 抑制标记（`nextTick` 复位）；② 视图切换（月宫格→日视图）后原按钮卸载导致**焦点掉到 body、Esc 失效** → `focusActiveView()` 按目标视图把焦点交给网格或面板容器；③ 面板容器需 `tabindex="-1"` 才能程序化聚焦
- **SCSS 拆 4 份的必然性**：Vue scoped 样式只对子组件**根元素**生效，父组件的规则无法命中子部件内部元素 → 面板内部样式必须独立成 `DatePickerPanel.scss` 由 `PickerPanel.vue` 自己 `@use`（否则需要让子部件重复 `@use` 整份父样式，产生大量死规则）
- **档位 CSS 变量单点驱动**：根类 `.si-datepicker--{tier}` 定义 `--dp-cell-size/--dp-cell-font/--dp-input-min-height`，4 个 SCSS 跨文件消费，避免 4 份重复档位块
- **月/年宫格列数改为 4 / 5 列**（方案原文写 3×4）：让 12 月与 10 年都无残缺行、且与 7 列日历区等宽（月 1.75 格/年 1.4 格 × `--dp-cell-size`）

### 行数硬阈值触发的二次拆分（重要经验）
初版 `DatePicker.vue` **751 行**、`utils.ts` **557 行**，均破 500 硬阈值。拆分路径：
1. `utils.ts` → `utils.ts`(基础+网格+禁用+值转换) + **`formatUtils.ts`**(模板引擎)，注意 `formatUtils` **不能反向 import `utils` 的 `isValidDate`**（会形成循环），时间戳分支改用 `Number.isNaN(date.getTime())` 自校验
2. `DatePicker.vue` → 拆出 **`PickerPanel.vue`**（面板模板+自持视图状态，~310 行）与 **`useDatePicker.ts`**（入口逻辑 composable，~331 行），入口仅剩 ~338 行
- **composable 的依赖注入技巧**：不在 composable 里复用 SFC 的 `defineProps` 类型（避免依赖 Vue 宏的跨文件类型解析），而是在 composable 内声明**结构化子集接口**（`DatePickerSource` + `DatePickerEmitters` + `DatePickerPanelHandle`），SFC 直接传 `props` 与 emit 回调对象——类型安全且零宏魔法
- 最终 12 个文件**全部 < 500**；其中 4 个落在 300~500「需要关注」带（DatePicker.vue 338 / useDatePicker 331 / utils 318 / PickerPanel 310），已低于硬阈值

### 验证
`read_lints` 0 错误（全部 12 个新文件 + previewData 聚合）；`universal-arch-skill` 模式 C 审查全绿：
- 样式分离：4 个 `.vue` 的 `<style scoped>` **仅含 `@use`**
- 设计 Token：4 个 SCSS **硬编码颜色 0 命中**、`font-size/font-weight/line-height/border-radius` 硬编码 px **0 命中**；必要硬编码尺寸（14/18/20/22/26/28/32/36px）均带 `// 无对应 Token` 注释
- 档位字号阶梯：XS/S/M/L = `$font-size-2xs/xs/sm/base` = 10/12/14/16，**四档无同号**
- 文件头注释：8 个 `.ts`/`.vue` 全部齐备；4 个 SCSS 带 `// ========== Xxx.scss ==========` 头
- 统一入口：无存储/AI/事件/定时器/DOM 直调；唯一副作用（`document` click）`onMounted`/`onUnmounted` 成对
- 跨功能解耦：`src/components/` 内 **0 处 `@/features` 导入**
- 图标：`chevronDoubleLeft`/`chevronDoubleRight` 各注册 1 次无重复；无 emoji
- 死代码清理：删除未使用的 `addYears` 与 `DatePickerPlacement` 导出
- 结构校验：`si-datepicker` 前缀仅出现在本次新增文件；`datePicker/` 目录恰好 7 个文件无冗余

剩余验证留用户：`pnpm lint` / `pnpm i18n:verify` / `pnpm validate:icons` / `npx tsc --noEmit` + 在组件预览面板点击输入框展开日历目视交互。

## 追加更正：DatePicker 拆分后两处 import/类型问题（用户 build 报错）

用户 build 报 `[MISSING_EXPORT] "parseDate" is not exported by "src/components/datePicker/utils.ts"`。

**根因 1（导入来源未同步）**：`parseDate` 在拆分时随 `formatDate` 一并迁到 `formatUtils.ts`，但 `useDatePicker.ts` 仍把它留在 `./utils` 的导入里 → rollup 报 MISSING_EXPORT。修复：`parseDate` 与 `formatDate` 合并到 `./formatUtils` 导入。
**根因 2（ESLint 查不到的类型错误，同轮补修）**：`utils.ts` 的 `buildModelValue` 用 `list.map(d => valueFormat ? formatDate(...) : startOfDay(d))` 产出 `(string | Date)[]`，不可赋值给 `DatePickerValue`（TS2322）。修复：按 `valueFormat` 分支分别 map，各自返回同质数组。

**关键经验（已写入 MEMORY.md）**：三道验证各查不同问题，缺一不可——
- ESLint / `read_lints`：只查代码规范，**不查未导出成员，也不查类型**
- `npx tsc --noEmit`：查 `TS2614 无导出成员` 与 `TS2322 类型不匹配`（本次两处都能提前发现）
- `pnpm vite build`（rollup）：才查 `MISSING_EXPORT`
故**凡是改动模块导出边界（拆文件 / 移函数），必须本地跑一次 `npx tsc --noEmit` 并过滤新增文件路径**。该命令不在禁用清单内（禁用项仅 `pnpm vite build` 与 `pnpm lint`），只读无副作用。

**核查结论**：`npx tsc --noEmit | findstr datePicker` 已归零；全项目其余 tsc 报错（featureRegistry / features-index / s3FileManager / video 等）**均为既有噪声，与本轮无关，未触碰**；全仓库仅 `previewData/datePicker.ts` 引用 `@/components/DatePicker.vue` 公开入口，无任何文件深入 `datePicker/*` 内部（结构约定成立）。

## 追加：bookmarkMarker 审查 + 合规改造（用户「@src/features/bookmarkMarker 审查」→ 选定「审查 + 按规则改造」+ 四项范围全选）

### 产出
- 审查报告：`docs/bookmark-marker-component-audit.md`
- 改造范围：`src/features/bookmarkMarker`（全部）+ 共享层新增/扩展 + `generalSettings` 6 处 import 迁移 + i18n 分片

### 最重要的发现：原生取色器在思源里点不开
`RuleItem.vue` 的文字色/背景色用原生 `<input type="color">`，**思源 Electron 不弹取色器**（`generalSettings/components/ColorField.vue` 的文件头注释早已记录此环境限制，该组件正是为此自绘 32 色调色板）。修复路线：把该组件**提升**为 `src/components/ColorField.vue` + 迁移 SCSS + 改 6 处 generalSettings import + 删本地副本 + 同步预览清单（`previewData/colorField.ts`）+ `AGENTS.md` 组件数。
**同一缺陷扩散范围（本次未改，已写入报告）**：另有 8 个文件用原生 `<input type="color">` —— `prompts/CategoryManageModal`、`toolCollection/tools/colorPicker`、`imageCreation/{CoverDecorationSettings,CodeImageTab}`、`superPanel/FeatureCard`、`gitPush/{common/CategoryDialog,CommitAnalysis/AnalysisSettingsForm}`、`generalSettings/TabPinSettings`。建议单独立项批量替换。

### 量化结果
| 指标 | 前 | 后 |
|---|---|---|
| 原生 button / select / input / radio / range / label / chip | 3 / 1 / 6 / 7 / 1 / 9 / 2 | 全 0 |
| `any` 使用点 | 3 | 0 |
| props 直接突变点 | 7 | 0 |
| unhandled rejection 风险点 | 4 | 0 |
| `RuleItem.vue` | 422 行 | 202 行 |
| `styles/RuleItem.scss` | 318 行 | 59 行（拆 4 个 partial） |
| i18n 分片 | 1 顶层键（嵌套 32 键） | 34 扁平键 |

### 关键动作
1. **共享组件扩展**：①提升 `ColorField`（+新增 `change` 提交事件：`update:modelValue` 走实时值、`change` 走落盘信号）；②`Input` 新增 `borderless` prop（供 chips 类复合控件内嵌）。两者均已同步 `previewData` + `componentPreview/README.md` + `AGENTS.md`
2. **控件替换**：3 个自建按钮 → `Button`；间隔 `<select>` → `Select`（标签走 `label` prop，省掉自建 label）；8 处裸 `<label>` → `Label`；chips → `Tag(closable)`；内联输入/图标名 → `Input`；透明度 range → `Slider`（`show-value`+`formatValue` 直接产出百分比，删掉手写百分比 span）；7 个 radio 的两组 → `ModeGroupField`（`Button` text 外观 + variant 表达选中态）；颜色 → `ColorField`
3. **数据流契约**：`RuleItem` 不再 mutate props，改 `patch(payload)`（父级 `Object.assign` 到自有对象）+ `commit()`（落盘+通知+提示）+ `remove()`（按对象身份定位）三事件。顺带修掉「滑块拖动逐像素写盘 + toast 刷屏」
4. **运行时修复**：`applyMarkers` 整体 try-catch（`loadBookmarkCache` 查询失败只记日志、保持 `cacheLoaded=false` 让既有守卫跳过），**声明其永不 reject** → 各 fire-and-forget 点只 `void`；`setUpdateInterval` 等值短路；`hexToRgba` 支持 3 位 hex；SQL LIMIT 提常量
5. **类型收敛**：具名 `DisplayMode`/`MatchMode`；`BookmarkMarkerActionPayload` 判别联合取代 `(action: string, data: any)`；`BookmarkMarkerI18n` 接口（34 键）；`normalizeRules(unknown[])` + 字段白名单 + 类型守卫
6. **i18n 嵌套 → 扁平**：9 个键与他分片重名需重命名（`title`→`bookmarkMarkerTitle`、`displayMode`→`markerDisplayMode`、`updateInterval`→`markerUpdateInterval`、`interval*`→`markerInterval*`、`msgEnabled/Disabled`→`bookmarkMarkerMsg*`），新增 `panelCloseLabel`/`ruleRemoveLabel`
7. **结构**：`RuleItem.vue` 拆出 `components/ruleItem/{TagInputField,IconSelectField,ModeGroupField}.vue`；`v-for` key 由索引改为**规则对象身份**（`WeakMap` 生成稳定数字 key，避免调色板状态错位）

### 踩坑与经验（重要）
- **Vue scoped CSS 的父 scope 只传给子组件的「单根节点」**：拆出 `IconSelectField.vue`（**多根节点**）后，父组件 `RuleItem.scss` 里的 `.rule-row` 完全不生效 → 字段行丢失 flex 布局。修复：抽出 `styles/fieldRow.scss`，**每个渲染该 class 的组件各自 `@use`**，让 scoped 编译带上自身 `data-v`。**不要依赖父 scope 传递**
- **滑块类「实时 + 落盘」交互要分两个事件**：`patch(payload, persist=false)` + `@change="commit"`，否则拖动过程逐像素写盘并弹提示
- **`size="small"` 强制规则**（`AGENTS_STYLE.md`）：弹窗/表单里的 `Input`/`Select` 必须显式 `size="small"`（默认 medium 36px 过高）；chips 内联输入也曾误用 `xsmall`，已改回
- **i18n 嵌套结构是碰撞规避手段**，不是坏味道：`bookmarkMarker` 的 `title`/`updateInterval`/`interval*`/`msgEnabled` 等 9 键若扁平化必与他分片重名。`AGENTS_I18N.md` 明确两种结构都支持；本次按项目主流（扁平）统一并重命名，代价是 9 处键名变更
- **提升本地组件为共享组件时，import 组别要重排**：`ColorField` 从 `./ColorField.vue` 移入 `@/components/` 组时需插到该组字母序位置（`ColorField` < `IconWrapper`/`Input`），不能只改路径
- **组件数会随并行会话变化**：本次开始时 `src/components/` 已由另一轮加入 `Checkbox.vue`（15 个），故 ColorField 使其达到 **16** —— 改计数类文档前必须先 `list_dir src/components` 复核
- **【build 报 MISSING_EXPORT 复盘】新增预清单文件漏导出聚合数组**：`previewData/colorField.ts` 只写了 `export const colorFieldGroup: PreviewGroup`，漏了 `export const colorFieldPreviewGroups: PreviewGroup[]`，而 `previewData/index.ts` 按既有约定导入的是**聚合数组**（其余 6 个文件均同时导出「分组对象」+「分组数组」两项）。**教训：新增遵循既有模式的模块时，必须先把同目录同类文件的导出清单列出来对齐（`search_content '^export const'`），不能只照着「内容」写而忽略「导出契约」**。此为 `AGENTS.md`「重构 import 来源必须同轮全量切换」的同类错误

## 追加：compactMode 共享组件合规审查 + 改造（用户「@src/features/compactMode 根据规则审查通用组件」→ 选定「仅 compactMode 范围 + 审查报告并落地修复 + chip 换共享 Button 分组」）

### 改造前违规（20 项，5 类）
| 类别 | 违规 | 位置 |
|---|---|---|
| 共享组件 | 2 组原生 `<input type="radio">` 手写 chip（`.chip-option` 全项目唯一） | `CompactModeSettings.vue` L34-81 |
| 共享组件 | 3 处原生 `<label>` | 同上 L5 / L99 |
| 共享组件 | `plugin?: any` | L125 |
| 硬规则 | 内嵌 `<style scoped>` 107 行（且无 `lang="scss"`） | L236-343 |
| 硬规则 | 缺文件头注释 | 文件头 |
| 硬规则 | 硬编码 `font-size` 13/12/11/10px、`font-weight: 600`、`line-height: 1.4` | L245/246/258/261/288/312/314 等 |
| 硬规则 | 选中态用 `box-shadow: 0 0 0 1px` 描边 | L307 |
| 硬规则 | 装饰性 `letter-spacing: 0.5px` | L316 |
| 硬规则 | `transition: …0.12s, …` 逗号列举且无 `ease` | L295 |
| 硬规则 | 颜色只有 `var(--b3-theme-*)`，缺 `$color-*` 兜底 | 遍布 |
| i18n | 错别字 `compactMode: "紧洛模式"`（应为「紧凑模式」） | `zh_CN/compactMode.json:2` |
| i18n | 密度档位文案「适中/紧凑/极简」硬编码中文 | L141/145/149 |
| 结构/文档 | `areas` 5 键与 `ALL_AREAS` 重复；`getCompactModeState()` 零引用死代码；`index.ts` 注释档位 80/85/90/95/100 与实现不符；README 称样式经 `src/index.scss` 的 `@use` 引用（实为 `index.ts` 内 import，`src/index.scss` grep 0 命中） | — |

### 改造结果
- `CompactModeSettings.vue` **343 → 243 行**：radio chip → 共享 `Button` 分组（`variant` primary/ghost + `text` + `size="xsmall"` + `:aria-pressed`，循 `bookmarkMarker/ruleItem/ModeGroupField.vue` 先例）；原生 label → 共享 `Label`（`icon`/`icon-size` 承接原 `IconWrapper` + 文案组合）；`Switch` 沿用；补文件头注释；`<style>` 只留 `@use`
- 新增 `styles/CompactModeSettings.scss`：Token 化（`$spacing-*`/`$font-size-*`/`$radius-base`/`$vp-mono`/`$font-weight-*`/`$line-height-normal`），颜色双保险 `var(--b3-theme-*, $color-*)`，删 box-shadow 与 letter-spacing，过渡改 `0.12s ease`，卡片底色 `background → surface` 对齐 gitPush 范式
- `index.ts`：`ALL_DENSITIES`/`ALL_FONT_SCALES`/`ALL_AREAS` 改为 export（`ALL_FONT_SCALES` 由字符串数组改**数字数组**，消除 `String()` 转换）+ 新增 `CompactDensity`/`CompactArea` 字面量类型；抽出 `clearCompactClasses()` 让「先复位再置位」幂等；删死代码 `getCompactModeState()`；修注释档位
- i18n 分片：zh 错别字订正 + 中英各新增 `compactDensityModerate/Compact/Extreme`
- README：订正样式引入方式 + 补目录结构与「档位常量单一来源」说明

### 关键经验
- **`CompactModeSettings` 接口字段类型不能随手收紧**：`compactModeFontScale` 若改 `100|98|96|…` 字面量联合，`src/index.ts:171` 与 `main.ts:31` 传入的 `PluginSettings`（字段为 `number`）会立刻 TS2322；仅 `compactModeDensity` 因两侧同为同一字面量联合才可安全用 `CompactDensity`
- **组件 props 里的 plugin 类型**：思源 `Plugin` 基类无 `settings` 字段（`BalanceQuery.vue` 用 `as any` 迂回）；项目既有先例 `PronunciationDialog.vue` 用 `import type PluginSample from "@/index"`，`PluginSample.settings!: PluginSettings` 可直接读写，且 `import type` 被擦除不产生运行时循环
- **覆写共享组件内部样式要算特异性**：`Label.scss` 有 `.si-label:not(.si-label--inline) { margin-bottom: $spacing-1 }`（含 scoped 属性后为 (0,3,0)），父组件写 `.area-row .si-label` 会同特异性比顺序，故用 `.compact-mode-settings .area-row .si-label`（(0,4,0)）稳赢
- **`pnpm i18n:merge` 由 vite `buildStart` 自动执行**（`vite.config.ts` 的 `merge-i18n` 插件）：只改分片后合并 JSON 暂未同步属正常，用户下次 dev/build 自动补齐；`pnpm i18n:verify` 校验的是**合并文件**，故「新增键两侧都还没有」仍判定通过
- **原生 radio 遗留尚存 5 处**（`video/CompressDialog`、`wordQuery/WordQueryPanel`、`gitPush` 的 `SettingsDialog`/`CommitFixDialog`/`BatchFixDialog`），本次按用户选定范围未处理，可单独立项
- 同类问题在宿主 `generalSettings` 其余面板普遍存在（如 `CodeBlockSettings.vue` 的 `style-card` 自绘选择器、`border-row` 原生 label、`shadow-btn` 原生 button），本轮按范围未动

### 验证
`read_lints` 0 错误（组件 / `index.ts` / 新 SCSS）；残留 grep：`getCompactModeState` 0 命中、`compactDensity` 仅出现在两个分片。留用户验证：`pnpm lint` / `pnpm i18n:verify` / `pnpm validate:icons` / `npx tsc --noEmit` + 「常用设置 → 紧凑模式」面板目视。

## 追加：dataSnapshot 审查 + 合规改造，并新增第 20 个共享组件 ConfirmDialog（用户「@src/features/dataSnapshot 审查」→ 选定「含注册链与 API 层 + 审查报告并落地修复 + 确认弹窗提升为共享组件」）

### 新增能力：`src/components/ConfirmDialog.vue`（共享组件 19 → 20）
- 取三处 feature 先例（`s3FileManager/FmConfirmDialog`、`gitPush/common/ConfirmDialog`、`shortcut/ConfirmDialog`）的**能力并集**：props `visible`/`title`/`message`(`\n` 多行)/`confirmText`/`cancelText`/`danger`/`size`/`closeOnMask`/`confirmLoading`；emits `confirm`/`cancel`/`update:visible`；默认插槽覆盖消息区
- 三个设计决定：①**确认后不自动关闭**（父决定时机以容纳异步）；②**只监听 Esc，Enter 不绑定** —— 否则与聚焦按钮的原生 click 重复派发确认；③**打开时焦点给对话框容器而非确认按钮** —— 危险操作默认聚焦按钮会被 Enter 误触
- 样式 `styles/ConfirmDialog.scss`：遮罩 `rgba(0,0,0,0.5)` + `z-index: 10000`、卡片 background + 头脚 surface、`0.12s ease`（fade + scale 0.98），全部 Token 化
- 预览：`previewData/confirmDialog.ts`（4 示例，`sizeable: true`）+ 聚合接入；**新增「弹层类组件预览沙箱」机制** —— `.cp-card__stage` 设 `position: relative` 并把舞台内 `.si-confirm-mask` 覆盖为 `absolute; z-index: 1`（否则 fixed 遮罩会铺满整个预览窗口），已登记进 `componentPreview/README.md`
- 文档计数 19 → 20：`AGENTS.md` 5 处 + 组件清单表 + 复用清单 + 目录树 + 规则索引、根 `README.md`、`componentPreview/README.md`（3/8/13 + 具名插槽表 + 事件契约表）

### dataSnapshot 改造（量化）
| 指标 | 前 | 后 |
|---|---|---|
| `index.vue` | 358 行 | 228 行（拆 3 个子组件） |
| `styles/index.scss` | 365 行 | 232 行 |
| 原生 `<button>` / `<input>` | 10 / 1 | 0（全共享组件） |
| 自建控件样式段 | `.ds-btn*` + `.ds-create__input` + `.ds-confirm*` | 0 |
| `any`（i18n 取值 + API 断言） | 3 | 0 |
| 静默 `catch {}` | 2 | 0（补 `console.error`） |

- 控件映射：刷新（纯图标 `Button` + `aria-label`）、页签（`Button` text 外观 + `variant` 切选中）、创建（`Input size="small"` + `Button loading`）、查看/恢复/下载（`Button loading`）、删除云端标签（`Button variant="danger"` + 图标）、二次确认（共享 `ConfirmDialog`，恢复场景用默认插槽显示备注与时间）
- 结构：`components/{LocalSnapshotList,CloudSnapshotList,SnapshotDetail}.vue`；`styles/index.scss` 改为**共享基座**由 4 个组件各自 `@use`（顶层类，非嵌套在根下）；`v-for` 列表大小格式化改 `computed` Map 一次性计算
- i18n：新增 `types/i18n.ts`（键清单 → 类型 + 取值），新增 `restoreTitle`/`removeCloudTagTitle` 两键（zh/en 同步）；移除 `index.ts` 的 `|| "数据快照"` 硬编码兜底（`AGENTS_I18N.md` 强制规则）
- API 层：`getRepoSnapshotContent` 由裸 `fetch` 改走 `requestOrThrow`（保留「失败返回 `[]`」+ 日志）；`(snap as any).tag` → `snap.tag`

### 关键经验
- **i18n 类型化踩坑**：接口写成 `interface { xxx?: string }` 后，`snapshotTask.progress({ label: i18n.value.xxx })` 等 12 处立刻 TS2322。改为「键清单派生」：`const I18N_KEYS = [...] as const` + `Record<(typeof I18N_KEYS)[number], string>`，取值函数按清单逐键 `?? ""` 填充 → 调用方恒得 `string`，且新增键只需改清单一处
- **`npx tsc --noEmit` 再次证明必要**：本轮它抓出了 12 处 `string | undefined` 类型错误（ESLint / `read_lints` 全部静默通过）
- **fixed 弹层进预览会铺满窗口**：这是"提升弹层组件为共享组件"时才会暴露的问题，必须在预览舞台做沙箱覆盖
- 共享组件计数当天已被并行会话推到 19（+InputGroup/InputGroupAddon），落手前 `list_dir` 实测才拿到正确基数（19 → 20）

### 验证
- `universal-arch-skill` 模式 A：`validate-project-structure.py --lang vue --features dataSnapshot,componentPreview` → **0 错误 0 警告**
- `read_lints`：dataSnapshot 全目录 + `ConfirmDialog.vue` → 0 错误；`npx tsc --noEmit` 过滤 dataSnapshot/ConfirmDialog → 0 命中
- 行数：`ConfirmDialog.vue` 164 / `ConfirmDialog.scss` 85 / `index.vue` 228 / `useDataSnapshot.ts` 179 / 子组件 76~82（全部 < 300）
- 残留 grep：`ds-btn` / `ds-confirm__` / `ds-loading` / `ds-empty` / `import "./styles/index.scss"` → 0 命中
- 留用户验证：`pnpm lint` / `pnpm i18n:verify` / `pnpm validate:icons` / `npx tsc --noEmit`；目视「数据快照」Dock 面板 + 组件预览新增 ConfirmDialog 分区

### 不在本次范围（后续迁移清单）
其他 feature 的本地确认弹窗：`s3FileManager/components/FmConfirmDialog.vue`（+`useEscClose` LIFO 栈）、`gitPush/components/common/ConfirmDialog.vue`（用旧 `vp-btn` 类）、`shortcut/components/ConfirmDialog.vue`、`passwordVault/components/CategoryManagerModal.vue`（弹层但非确认框）—— 均可迁移到共享 `ConfirmDialog`，本次未动。

## 追加：新增共享组件 InputGroup + InputGroupAddon（用户「参考 primevue.dev/inputgroup 新增组件」）

### 澄清结论（用户选择）
1. 组件形态 = **双组件对齐 PrimeVue**：`InputGroup`（容器）+ `InputGroupAddon`（附加项），均为**平铺公开组件**（Addon 需被 feature 直接使用，不能放私有子目录）
2. 可拼接成员 = `Input` / `Button` / `Select` / `DatePicker`（**未选 Checkbox**）
3. 视觉 = **无缝拼接**（零间距、相邻边框 1px 重叠、仅最外侧保留圆角）
4. 范围 = **仅新增组件 + 预览**，不迁移任何 feature 的现有 flex 并排布局（`.path-input-group` / `.input-group` 等保持不动）

### 交付（4 新文件 + 4 改动 + 3 份文档）
- 新增：`src/components/InputGroup.vue`（30 行）、`InputGroupAddon.vue`（12 行，**无 `<script>` 块**，纯模板+样式）、`styles/InputGroup.scss`（150 行）、`styles/InputGroupAddon.scss`（23 行）
- 预览：新增 `previewData/inputGroup.ts`（6 个复合示例）+ `previewData/index.ts` 聚合（置于 inputPreviewGroups 之后）
- 预览框架扩展：`types/index.ts` 的 `PreviewExample` 新增 `render?: (props) => VNode | VNode[]`；`PreviewSection.vue` 增 `SlotRenderer`（defineComponent + `setup(props): () => VNode | VNode[]`）
- 文档：`AGENTS.md`（5 处计数 17→19 + 清单表加 2 行 + 复用控件清单加「输入框组合器」+ 结构约定补「配套组件也平铺」）、根 `README.md`、`componentPreview/README.md`（L3/L8/L13/L14 + 具名插槽表 2 行 + 清单扩展指南补 `render`）
- **零 i18n 改动**（预览示例标题是数据文件内中文）

### 关键设计（可复用结论）
- **成员边框都不在根元素上**：`Input` 在 `.si-input__wrapper`、`Select` 在 `.si-select__trigger`、`DatePicker` 在 `.si-datepicker__wrapper`、`Button`/`Addon` 在根 → 命中成员**内部**元素必须 `:deep()`
- **档位跨组件传递用 CSS 自定义属性**（非 provide/inject）：`InputGroup` 根类按档位输出 `--ig-addon-pad-y/pad-x/font/min-h`，Addon 用 `var(--ig-addon-*, 兜底)` 消费（沿用 `DatePicker.scss` 的 `--dp-*` 范式；Sass 中 CSS 自定义属性值**必须插值** `#{$var}`，否则原样输出变量名）
- **flex 分配**：`Input`/`DatePicker` 用 `flex: 1 1 0`（`flex-basis: 0` 会压制它们自带的 `width: 100%`，否则会撑破容器）；`Select` `0 1 auto` + `min-width: 6rem`；`Button`/`Addon` `0 0 auto`；所有子项 `min-width: 0`
- **`-1px` 重叠必须配 `z-index`**：`> *:hover, > *:focus-within { z-index: 1 }`，否则被重叠遮住的边框在聚焦时缺一段；`DatePicker` 组内 `outline-offset` 归一为 `-1px`、按钮 `:focus-visible` 同理
- **容器禁设 `overflow: hidden`**（`Select` 下拉是 wrapper 内相对定位、未用 Teleport，会被裁剪）；组内成员**禁带 `label`/`hint`/`error`**（FormField 会撑高错位）
- 高度对齐靠 `align-items: stretch` + 边框承载元素 `height: 100%`（`DatePicker` medium 档 min-height 32px ≠ `Input` 36px，必须拉伸）

### ⚠ 本轮最大的坑：插槽子组件**不带接收方的 scope 属性**（初版实现整体失效，已修正）
初版按「父组件 scoped 样式能作用于子组件根元素」写成 `.si-input-group > .si-input { … }`，**规则全部不匹配**。

- **机制**：Vue 的 scoped 属性在运行时由 `createBaseVNode` 取 `currentScopeId`（= 当前**渲染实例**的 `__scopeId`）写入。`<InputGroup><Input/></InputGroup>` 里的 `<Input/>` 由**调用方**组件的插槽函数创建，`withCtx` 会把 `currentRenderingInstance` 切回**插槽归属者**（调用方），故 Input 根元素带的是 `data-v-调用方`，**不是** `data-v-InputGroup`。官方文档侧证：插槽内容默认不受接收方 scoped 样式影响、须用 `:slotted()`，而 slot 内容拿到的是带 `-s` 后缀的 `data-v-xxx-s`，同样说明它没有接收方的裸 scope 属性。
- **正确写法（已实测编译结果）**：把「成员」部分整段放进 `:deep()`，用 `>` 保留直接子语义 —— `> :deep(.si-input)` 编译为 `.si-input-group[data-v-x] > .si-input`；成员**内部**的边框承载元素用后代形式 `:deep(.si-input__wrapper)` → `.si-input-group[data-v-x] .si-input__wrapper`（更耐受成员内部结构调整）。`> :deep(* + *)`、`> :deep(:first-child.si-button)`、`> :deep(:first-child .si-input__wrapper)` 均验证编译正确。
- **这条规则对所有「容器类共享组件包裹其他共享组件」的场景都成立**：只要子组件是**经插槽**进入的，就不能靠 `.容器 > .子组件` 的 scoped 选择器（`FormField` 等直接写在模板里的子组件不受影响）。
- **验证手段（可复用）**：`@vue/compiler-sfc` 的 `compileStyle({ source, filename, id, scoped: true })` 可离线查看 scoped 编译结果。⚠ 必须先经 Sass 展平再喂给 `compileStyle` —— 直接喂嵌套 CSS 会得到错误结论（scope 属性会被注入到内层选择器开头）。本次用 `node -e` + 自定义 importer（把 `@/` 映射到 `src/`、并回退 `_` 前缀 partial）把 `InputGroup.scss` 端到端编译成最终 CSS 逐条核对，输出全部锚定在 `.si-input-group[data-v-grp]` 上，确认无误。

### 预览框架的取舍
- 用 `<SlotRenderer>`（`defineComponent` 声明 props）而非让 `render` 返回整个 VNode 再 `<component :is="vnode">`：前者不依赖「VNode 作为 `is`」的隐式语义。代价是 SlotRenderer 定义在 `<script setup>` 内、每次渲染重建（预览面板静态、无状态损失，可接受）
- `props` 与 `render` 示例并存时 `resolveProps(example)` 会被调用两次（纯函数、开销可忽略），换取模板无局部变量

### 验证
`read_lints` 0 错误（6 个文件）；`npx tsc --noEmit` 新增文件**零新增报错**（全项目其余 42 行为既有噪声：`featureRegistry` 的 30 余条 `id 不存在于 FeatureRegistryEntry`、`*.vue` 无导出成员等）。剩余验证留用户：`pnpm lint` / `pnpm validate:icons` + 预览面板 InputGroup 分区逐档目视（重点看首末圆角、相邻边框无双线、聚焦边框完整、Select 下拉不被裁剪）。

## 追加：优化共享组件 Label（用户「https://primevue.dev/label/」→ 选定「仅优化共享 Label 组件」+ 对齐 Wrapper/禁用联动/required 无障碍 + 允许修失效行为）

### 交付（2 个组件文件 + 1 个预览清单 + 2 份文档，零 feature 改动）
- `src/components/Label.vue`（113 → 123 行）：新增可选 props `wrapper`（包裹模式：插槽内容直出、不套 `.si-label__text`，用于把控件包进 `<label>` 建立原生隐式关联）、`requiredText`（必填的 sr-only 替代文本）；`*` 标记补 `aria-hidden="true"`；根元素补 `:aria-disabled="disabled ? 'true' : undefined"`（只由 prop 驱动，不读 attrs）
- `src/components/styles/Label.scss`（123 → 137 行）：`&--wrapper`（`white-space: normal` + 字重回落到 `$font-weight-normal`，避免 medium 字重被相邻控件文本继承）；禁用态三入口（`&--disabled` / `&[data-disabled]:not([data-disabled="false"])` / `&:has(+ :disabled)`、`&:has(+ [data-disabled]:not([data-disabled="false"]))`）；`&__required-text` 裁剪式视觉隐藏（`clip-path: inset(50%)` + `$spacing-px` 尺寸/负边距）；align 修复；错误色 Token 修正 3 处；文件末尾 `.si-label.si-label--wrapper { margin-bottom: 0 }`
- `previewData/input.ts` 的 `labelGroup`：7 → 13 个示例（包裹控件用 `render` + `createTextVNode`、data-disabled 容器、disabled prop、required+requiredText、align 居中/右对齐）；**顺带修正旧示例「变体展示-primary」**——`variant` 只在 `--inline`（`tag !== "label"`）下可见，原示例未传 `tag="span"` 故渲染上与基础标签无异，已补 `tag: "span"`
- 文档：`componentPreview/README.md`（新增「表单标签能力」条目 + 具名插槽表加 `Label` 行 + 插槽表引导语改为「`props` + 默认插槽（`slotText` 或 `render`）」+ 清单扩展指南补 required 约定）、`AGENTS.md` 的 `Label.vue` 清单表行

### 两处真实缺陷（均已修）
1. **`align` 被架空**：`.si-label__text { flex: 1 }` 让根 `justify-content` 无自由空间可分 → align 三档此前完全无效。修法：**保留** `flex: 1`（它是「必填星号贴右 + 图标分列两侧」现有视觉的关键），把对齐落到文本层内部 `&--align-center/--align-right .si-label__text { text-align }`（flex 子项会被 blockify，`text-align` 生效）；根 `justify-content` 保留给无文本层的 wrapper 模式
2. **错误色 Token 无效**：`--b3-theme-destructive` **从未被定义**（`docs/codex-ui-reference.html` 明暗两套只定义 `--b3-theme-error`，暗色 `#f87171`；`AGENTS_STYLE.md:172` 同）→ 恒走 fallback `$color-danger` 且暗色偏暗。`Label.scss` 3 处改为 `var(--b3-theme-error, $color-danger)`。**同根因残留 6 处未修**（超出本次「仅 Label」范围）：`Tag.scss`(3) / `Slider.scss`(2) / `Badge.scss`(1)

### 关键结论（可复用）
- **禁用联动只能做到「半自动」**：`:has(+ :disabled)` 仅命中「禁用态就在根元素上」的兄弟（`Switch` 的原生 `button[disabled]`、原生 `input`）；`Input`/`Select`/`DatePicker` 的禁用态加在内部元素（`.si-input__field`/`.si-select__trigger`/`.si-datepicker__wrapper`），根元素无 `disabled` → **命中不到**，必须由包装层显式加 `data-disabled`。刻意只取相邻兄弟 `+` 而非通用兄弟 `~`，避免同容器下方禁用控件误伤上方标签
- **禁用联动依赖属性自然透传** → `Label.vue` **不得**加 `inheritAttrs: false`
- **不输出 `aria-required`**：该属性属输入类角色，放在 `<label>` 上是无效 ARIA；`required` 只负责视觉标记 + SR 替代文本，控件侧需自行声明 `required`/`aria-required`（与 PrimeVue 文档口径一致）
- **`wrapper` 必须用显式 prop 而非自动检测插槽内容**：Vue 的 `slots` 非响应式（既有结论），且文本/控件判定不可靠
- PrimeVue 官方 Label 页 API 区块为客户端渲染、抓取为空；`raw.githubusercontent.com` 与 `primevue.dev/label/label.md` 分别 fetch failed / 内容被截断 → 只能确认「唯一功能性 prop 是 `for`」与 Basic/Required/Wrapper/Disabled 四段语义，新增 props 命名（`wrapper`/`requiredText`）为本项目自定、不与官方冲突

### 验证
`read_lints` 0 错误；`npx tsc --noEmit` 过滤 `Label` / `previewData/input` **零报错**；`--b3-theme-destructive` 残留 grep 仅剩 `Tag`/`Slider`/`Badge`（预期清单）；硬编码 `font-size/font-weight/line-height/hex/rgba/border-radius/padding/margin` 数字在 `Label.scss` **0 命中**；`Label.vue` 123 行 / `Label.scss` 137 行 / `previewData/input.ts` 295 行（均在 300 警戒线内）；Sass + `@vue/compiler-sfc compileStyle({scoped:true})` 端到端编译核对，`:has()` 兜底与 `[data-disabled]` 选择器最终形态正确。5 个既有调用点（`PronunciationDialog` / `CompactModeSettings` / `RuleItem` / `ModeGroupField` / `IconSelectField`）均未使用 `align`/`state`/`variant`/`for`/`disabled`/`required` → 修复零回归；`CompactModeSettings` 的 (0,4,0) 覆盖未受影响。剩余验证留用户：`pnpm lint` + 预览面板 Label 分区目视。

## 追加：新增共享组件 Listbox（用户「https://primevue.dev/listbox/」→ 选定「新增共享组件 Listbox」+ 多选/复选/勾选/筛选/三状态 + 不做键盘矩阵与虚拟滚动 + 仅新增组件与预览）

### 澄清结论（用户选择）
1. 目标 = **新增共享组件 `Listbox`**（内联列表选择，与下拉形态 `Select` 互补）；排除「给 Select 加 multiple」与「仅出报告」
2. 能力 = 多选 + checkbox 模式、勾选指示（checkmark）、筛选 + 三种状态（disabled / optionDisabled / invalid）；**未勾选「字段映射 + 分组」** → 无 `optionLabel`/`optionValue`/分组
3. 键盘 = **只做基础键**（Tab / ↑↓ / Enter / Space），不做完整矩阵、不做虚拟滚动
4. 范围 = **仅新增组件 + 预览**，不动任何 feature

### 交付（3 新文件 + 4 改文件，零 feature 改动）
- `src/components/Listbox.vue`（303 行）+ `styles/Listbox.scss`（187 行）
- `src/features/componentPreview/previewData/listbox.ts`（140 行，10 个示例）+ `previewData/index.ts` 聚合（置于 controlPreviewGroups 之后）
- 计数 **20 → 21** 共 7 处：`AGENTS.md` 160/168/181/448/486 + 组件清单表追加 `Listbox.vue` 行 + 177 行「优先复用」清单追加「列表选择」；根 `README.md:148`；`componentPreview/README.md` L3/L8（覆盖清单）/L14（sizeable 清单）+ 新增「内联列表选择」能力条目 + 具名插槽表登记 `option` 插槽
- **零 i18n 改动、零图标新增**（`check` / `magnify` 均已在 `COMMON_ICONS`）

### 关键决策（可复用）
- **选项类型复用 `Select.vue` 导出的 `SelectOption`**：无字段映射需求 → `option.disabled` 直接可用（无需 `optionDisabled` 字段名 prop），`keywords` 让筛选天然支持别名检索
- **指示器自绘、不复用 `Checkbox`（唯一复用例外）**：`role="option"` 内不得嵌套可交互元素 —— `Checkbox` 是 `<label>` + 原生 `<input type="checkbox">`，嵌进 option 会「option 里又套一个 checkbox」双重语义，且 label 包裹结构会与 option 点击双触发；只有勾选图标复用 `IconWrapper`（`check`）。该例外已写入组件头注释与预览 README
- **校验态用 `error?: string` 而非 `invalid?: boolean`**：项目统一约定（Input/Select/DatePicker/Checkbox 均用 `error`），一个 prop 同时负责描红外框与文案
- **筛选框复用共享 `Input`**（`borderless` + `prefix-icon="magnify"` + `clearable`）：`Select` 自建筛选输入是因为它在下拉浮层内无法承载 `FormField`；Listbox 是内联形态无此约束 → 必须复用
- **键盘走 `aria-activedescendant` 单点表达**（容器持 tabindex），不给每个 option 加 tabindex（避免 Tab 序列膨胀）；点选项后把焦点交回列表以便键盘连续操作（用 `:focus-visible` 画轮廓，鼠标点击不闪）
- **面板可安全 `overflow: hidden`**（内联形态无绝对定位浮层，与 Select/DatePicker 相反），但列表必须自带 `overflow-y: auto`
- 多选命中用 `Set`（O(1)），toggle 返回**全新数组**（非 deep watch 对原地 splice 不触发）

### 踩坑与经验
- **`.ts` 文件不能 `import type { SelectOption } from "@/components/Select.vue"`**：tsc 不解析 `.vue` 导出 → 报 `TS2614: Module '"*.vue"' has no exported member 'SelectOption'`（**项目已有两处同类既有噪声**：`useCodeImageGenerator.ts`、`statusBar/featureRegistry.ts`）。`.vue` 文件内这样导入不会暴露（vue 文件不经 tsc 检查）。故预览数据改为结构化字面量（其 `props?: Record<string, any>` 本就不受类型约束），错误归零
- **`FormField` 必须把控件放进默认插槽**，不能写成自闭合 sibling —— FormField 是多根组件，模板顺序为 label → slot → hint → count，控件若放成兄弟节点会导致提示文案排到控件上方（Input/Select 都是把 wrapper 放进 slot）
- **`previewData/control.ts` 已 297 行**，故 Listbox 另建 `previewData/listbox.ts`（两个导出：`listboxGroup` + `listboxPreviewGroups`，遵循目录内导出契约）
- **`option` 作用域插槽无法在预览快照中构造**：预览框架的 `render` 只能提供**默认插槽**内容（外层组件仍是 `group.component`），无法注入具名插槽 → 未造该示例，改为在 `componentPreview/README.md` 具名插槽表登记用法（遵循项目既有约定）
- `Listbox.vue` 303 行，**超 300 警戒线 1%、远低于 500 硬阈值**，沿用 `Button.scss`（311 行）的既有判断不拆分（拆分需引入 `listbox/` 私有子目录，结构复杂度大于收益）

### 验证
`read_lints` 0 错误（3 个文件）；`npx tsc --noEmit` 过滤 `Listbox`/`previewData` **零报错**（修掉上述 TS2614 后）；`Listbox.scss` 硬编码 `font-size/font-weight/line-height/hex/border-radius/padding/margin` 数字 **0 命中**；Sass + `@vue/compiler-sfc compileStyle({scoped:true})` 端到端编译核对，四档变体与 `&--checkbox &__check`、`&--error &__panel` 等后代选择器全部锚定在带 `data-v` 的元素上、无永不匹配项；计数 grep 三份文档全为 21；`previewData/index.ts` 已聚合。剩余验证留用户：`pnpm lint` + 预览面板 Listbox 分区目视（四档尺寸、单选/多选/两种指示器、筛选、空态、单项禁用、整体禁用、错误态）。

## 追加：新增共享组件 RadioButton（用户「参考 https://primevue.dev/radiobutton/，新增组件」）

### 澄清结论（用户选择）
1. 标签形态 = **内置 `label` prop + 默认插槽**（与 Checkbox/Switch 一致），同时保留 `inputId` 供外部 `<label for>`
2. 尺寸 = **项目 4 档** `xsmall/small/medium/large`（默认 small），圆框 14/16/18/20px
3. 校验态 = **`error?: string` + 内嵌 `FormField`**（与 Checkbox 完全一致，含 `hint`）
4. 范围 = **仅新增组件**，不迁移 feature 内 5 处原生 radio

### 交付（2 新文件 + 1 新预览文件 + 3 份文档，零 feature 改动）
- `src/components/RadioButton.vue`（~205 行）+ `src/components/styles/RadioButton.scss`（~150 行）
- `src/features/componentPreview/previewData/radioButton.ts`（12 个示例，`sizeable: true`）+ `previewData/index.ts` 紧随 `checkboxPreviewGroups` 聚合
- 计数 **21 → 22**：`AGENTS.md` 160/168/181/450/488 + 组件清单表追加 `RadioButton.vue` 行 + 177 行「优先复用」清单追加「单选框」；根 `README.md:148`；`componentPreview/README.md` L3/L8（覆盖枚举）/L16（sizeable 枚举）/L17（四档字号说明）+ 新增「单选组的快照局限」条目
- **零 i18n 改动、零图标新增**（**不引入 `IconWrapper`** —— 选中圆点是纯 CSS 实心圆 `scale(0)→scale(1)`）

### 组件设计要点
- **结构完全同构 Checkbox**：`FormField` 包裹 → `<label class="si-radiobutton__control">`（整行可点击）→ 视觉隐藏原生 `<input type="radio">`（`opacity:0` + 1px + `pointer-events:none`）+ 可视圆框 `__box` + CSS 圆点 `__dot` + `__label`
- **保留原生 radio 分组语义、不自造键盘逻辑**：同组传同一 `name` 时，Tab / ←→↑↓ / Space 与 ARIA 单选组语义由浏览器原生提供（PrimeVue 同思路）
- **`binary` 二值模式**沿用 PrimeVue 语义；**不做数组分组模式**（radio 天然单选）；**无 `indeterminate`**（无半选态）
- `checked = binary ? !!modelValue : modelValue === value`；`handleChange` 只 emit `props.binary ? true : props.value`（**单选项不可取消**，与 Checkbox 的核心差异）+ `nextTick(syncNativeState)` 防受控漂移
- `readonly` 用 `handleClick`/`handleKeydown` 的 `preventDefault()`；`disabled` 交给原生属性。**只读拦截必须覆盖方向键**（`READONLY_BLOCKED_KEYS = [" ", "ArrowUp/Down/Left/Right"]`）—— 同组 radio 的原生方向键会直接改选并移动焦点，只拦 Space/click 会漏（Checkbox 无此问题，是 RadioButton 特有）
- 新增 `stringValue` computed：原生 `input.value` 只接受 string/number（对象等复杂值不落 DOM，分组由受控 v-model 表达）
- 焦点环用 `&__input:focus-visible + &__box` 的 outline（`outline-offset: 2px`），**复用 Checkbox 的既有结论**：实底变体 border 为 transparent，`focus-ring` mixin 不可见
- 圆点尺寸用 **50% 等比**（随圆框 14/16/18/20 → 7/8/9/10px），省掉一套档位规则

### 预览清单的取舍
`RadioButton` 与 `Checkbox` 同属「多实例互相约束」组件，**单卡片快照只能渲染一个实例**，故未使用 `render`（`render` 提供的是**默认插槽内容**，会落进 RadioButton 的标签区，不能用来并排放多个选项）→ 组用法只体现在 `code` 模板（「单选组（同 v-model + 同 name）」示例）并在 `componentPreview/README.md` 登记局限说明。

### 验证
`read_lints` 0 错误（RadioButton.vue / radioButton.ts / previewData/index.ts）；计数 grep 全项目 `21 个组件` 类表述 **0 残留**，`AGENTS.md`/`README.md`/`componentPreview/README.md` 均为 22；`AGENTS.md` 组件清单表行数实测 = 22（与计数一致）。剩余验证留用户：`pnpm lint` / `npx tsc --noEmit` + 预览面板 RadioButton 分区目视（四档圆框与标签、选中圆点放大、实底变体、错误描红、禁用只读、焦点环）。

## 追加：优化共享组件 Select（用户「https://primevue.dev/select/」）

### 澄清结论（用户选择，Q0 为单选）
1. 范围 = **仅 ARIA 无障碍补齐**（未勾选「校验与状态扩展」→ 本次不做 `error` / `loading` / `variant="filled"` / `fluid`）
2. 键盘 = **基础补全**（Esc 焦点返还、打开定位已选项、ArrowUp 收起态打开、激活项滚动入视野）；不做 Home/End/PageUp/PageDown、不做 type-ahead
3. 大能力 = **全部不纳入**（multiple / checkbox 多选 / chips / 虚拟滚动 / editable / `optionLabel`-`optionValue`），多选继续用 `Listbox`（两者共用 `SelectOption` 类型契约）

### 影响面基线（code-explorer 子代理实测，先于改动）
- `import ... from "@/components/Select.vue"` 的文件 **45 个**（约 39 个真正渲染）；此前用 `<Select` 计数得到 ~73 是**大小写不敏感**造成的假象（误命中泛型 `computed<SelectOption[]>` 与原生 `<select>`）
- 具名插槽仅 **1 处**使用（`aiContentGenerator/components/SkillSection.vue` 的 `#selected` + `#option`）
- `SelectOption`/`SelectGroupOption` 有 **24 个文件**从 `@/components/Select.vue` 导入 → **必须保持该导出路径**
- `defineExpose` 的 open/close/focus/blur **零外部消费者**
- 外部样式覆写仅 5 处：2 处 `.si-select__dropdown { min-width: 260px }`、`.si-select { flex: 1 }`、`.si-select { width: 100% }`、`InputGroup.scss` 的 `:deep(.si-select__trigger)` 圆角归零 → 均不受本次改动影响
- 显式传 `maxHeight` 的有 4 处（200×2 / 420 / 240），为 flex 化后的优先回归点

### 交付（1 个新私有目录 5 文件 + 4 处改动，零 feature 改动）
```
src/components/
├── Select.vue                    # 改：ARIA + 键盘 + 单一 activeIndex（573 → 约 492 行）
├── FormField.vue                 # 改：+可选 labelId（:id 打在 .si-form-field__label）
├── select/                       # 新：组件私有实现（禁止 feature 直接导入）
│   ├── types.ts                  # 选项类型单一数据源（SelectOption/SelectGroupOption/OptionType/SelectSize）
│   ├── navigation.ts             # 平铺构建 buildFlatItems / 落点 resolveAnchorIndex / isGroupOption
│   ├── utils.ts                  # 匹配 / 筛选 / 选中查找 / 分组判定 / v-for key
│   ├── useSelectNavigation.ts    # useId 派生 id 工厂、WeakMap 下标映射、activeIndex、activeDescendantId、选项 ref 与滚动
│   └── useSelectKeyboard.ts      # 键盘控制器（收起/展开态方向键、Enter/Space、Esc、Tab + 筛选框按键委托）
└── styles/Select.scss            # 改：下拉 flex 列化 + 列表弹性高度
```
预览与文档：`previewData/control.ts`（+「无障碍命名（无可见标签）」示例）、`previewData/input.ts`（+「标签关联 id（labelId）」示例）、`componentPreview/README.md`（+「下拉选择的无障碍与键盘」条目 + FormField labelId 说明）、`AGENTS.md`（Select 行补 3 props、FormField 行补 labelId）。零 i18n 改动、零图标新增。

### 关键设计（可复用）
- **单一 `activeIndex`**：原 `hoveredIndex` + `hoveredGroupKey` 双状态合并为一个平铺下标（渲染顺序 = 导航顺序 = DOM 顺序），删除 4 个函数 2 个 ref，且 `aria-activedescendant`、高亮类、滚动三处同源
- **对象 → 下标用 `WeakMap`**：模板是嵌套渲染（分组 → 子项），用 `computed` 一次性建 `WeakMap<SelectOption, number>`，模板里 `optionId(domIndex(option))` 直接打 id；`optionId` 对负下标返回 `undefined` 使属性整体不渲染
- **滚动沿用 Listbox 方案**：`:ref` 回调数组 + `scrollIntoView({ block: "nearest" })`，不用 `getElementById` 全局查询
- **可靠焦点语义**：`closeDropdown({ restoreFocus })` —— Esc 与键盘选中传 true（焦点收回触发器），**外部点击 / Tab / 鼠标选中一律 false**（否则用户点别处会被抢焦点）；顺带修掉「`handleClickOutside` 在面板已关闭时也狂发 `visible-change(false)`」（改为 `wasOpen` 才派发）
- **筛选框按键必须单独处理**：不能复用 `handleKeydown`，否则 `Space` 被 `preventDefault` → **筛选框里打不出空格**。`handleFilterKeydown` 只接管 ↑↓/Enter/Esc/Tab
- **筛选框渲染条件去掉 `filteredOptions.length > 0`**：原来输入一个不匹配字符即**卸载输入框、焦点掉到 body**，用户无法改查询词（真实缺陷）
- **空态从 `.si-select__options` 的兄弟改为其子节点**，列表容器恒常渲染 `role="listbox"`，否则无匹配时 `aria-controls` 指向不存在的 id
- **分组用 `role="group"` + `aria-labelledby` 指向可见分组标题的 id**（`groupId(index)`），而非 `aria-label` + `aria-hidden`（后者会让 `groupId` 变成死代码）
- **清除按钮**：`role="button"` + `tabindex="-1"`（保持在 Tab 序列外，与 PrimeVue 一致，仅虚拟光标可达）+ `aria-label=clearLabel`，图标 `aria-hidden`
- **`.si-select__wrapper` 从 `FormField` 的兄弟位置移入其默认插槽**：原来与 `Input.vue` 的写法不一致，导致 `hint` 会渲染在控件**上方**（全项目 0 处给 Select 传 `hint`，故零回归）；对齐后 hint 正常排到控件下方

### ⚠ 本轮最有价值的经验：`.ts` 无法从 `.vue` 导入类型（TS2614）的**新解法**
旧结论是「`.ts` 里只能用结构化字面量」，本次找到更好的方案：
1. 把选项类型下沉到纯 TS 模块 `src/components/select/types.ts`（`navigation.ts`/`utils.ts`/composable 全部直接引用真实类型，**彻底去掉泛型噪音**）
2. `Select.vue` 的 `<script setup>` 里做**类型别名导出**保住对外路径：
   ```ts
   import type { SelectOption as SelectOptionShape } from "./select/types"
   export type SelectOption = SelectOptionShape
   ```
   （不能写 `export type { SelectOption } from "./select/types"` 之外的形式，也不能 `export interface SelectOption extends …` —— 别名写法与既有 `export interface` 同属 type-only 导出）
3. **已用项目实际编译器实测**：`@vue/compiler-sfc@3.5.40` 的 `parse` + `compileScript` 对 `export interface` / `export type X = Y` / `export type { X } from` **三种写法均通过**
4. 同法还做了一个**端到端 SFC 编译校验**（`compileScript` 后取 `bindings` 喂 `compileTemplate`），一次性验证「脚本宏 + 模板 + 模板引用绑定」全部可编译 —— 这是 tsc（不解析 `.vue`）与 ESLint（不做类型检查）都覆盖不到的一层，**改动 `.vue` 模板/宏时值得复用**

### 两个工具坑
- `Get-Content | Measure-Object -Line` **会漏掉空行**（Select.vue 报 557，实际 616）→ 统计行数要用 `(Select-String -Path X -Pattern '^<style').LineNumber` 之类锚点，或 `(Get-Content X).Count`
- 用 sass 离线编译组件 SCSS 时，**自定义 importer 无法解析项目的 `@/variables.scss` 别名**（`findFileUrl` / `canonicalize`+`load` 两种写法都试过仍报 Can't find stylesheet）→ 该 harness 不可用；判据是**未改动的 `FormField.scss` 同样失败**。本次退回「括号配平 + 关键声明存在性」的轻量校验

### 验证
- `read_lints` 0 错误（Select.vue / FormField.vue / select 下 5 个文件 / control.ts / input.ts）
- `npx tsc --noEmit` 过滤本次路径 **零新增报错**（仅剩既有噪声 `useCodeImageGenerator.ts` 的 TS2614，与本次无关）
- `@vue/compiler-sfc` 端到端编译 Select.vue / FormField.vue **全部通过**（Select bindings 76 个，模板引用标识符全部就位）
- `universal-arch-skill` 模式 A：`--features componentPreview` → **0 错误 0 警告**
- 私有目录隔离：feature 中 `components/select/` 引用 **0 命中**；`Select.vue` 的 `<style>` 仅 `@use`；`Select.scss` 硬编码颜色/字号 **0 命中**
- 行数：`Select.vue` 约 492（**回到 500 硬阈值内**，原 573）、`navigation.ts` 61 / `utils.ts` 67 / `useSelectNavigation.ts` 103 / `useSelectKeyboard.ts` 105 / `types.ts` 32、`Select.scss` 247
- SCSS：括号配平 0/0，5 条改动声明均落到最终规则，`max-height: inherit` 已移除
- 留用户验证：`pnpm lint` / `npx tsc --noEmit` + 目视回归（优先 4 处显式 `maxHeight`：`SkillSection`（唯一插槽用户 + placement=top + 240）、`RepoCleanToolbar` / `RuleCheckToolbar`（filterable + 200 + 外部 min-width）、`apiDebugger`（分组 + 420））

## 追加：优化共享组件 Slider（用户「https://primevue.dev/slider/」）

### 澄清结论（用户选择）
1. 范围 = **仅修复 5 项真实缺陷**（Q0 为单选）：① `showMinMax` 是死 prop；② `readonly` 形同虚设；③ 档位只改拇指、轨道伪元素恒 6px；④ 错误色用了不存在的 `--b3-theme-destructive`；⑤ 可见 label 未与滑块建立无障碍关联。**未勾选**「进度填充」与「显式 `ariaLabel`/`ariaLabelledby` props」→ 本次不做
2. 大能力 = **都不做**：保持原生 `input[type=range]` 架构（键盘 ←→↑↓/Home/End/PgUp/PgDn 与 `role="slider"` + `aria-valuemin|max|now` 全由浏览器原生提供）；不做 `range` 双手柄、不做 `orientation="vertical"`
3. `showMinMax` 展示形式 = **轨道下方两侧**（min 靠左 / max 靠右，不挤压轨道宽度）

### 交付（2 个文件改动 + 3 处文档，零 feature 改动）
- `src/components/Slider.vue`：172 → 234 行；补文件头注释（此前**完全缺失**，违反硬规则）
- `src/components/styles/Slider.scss`：236 → 233 行（档位去重后净减）
- `previewData/control.ts` 的 `sliderGroup`：7 → 10 个示例（新增「显示极值范围」「极值 + 当前值」「只读」），summary 同步
- `componentPreview/README.md`：新增「滑块能力」条目 + 四档字号说明段补 Slider
- `AGENTS.md`：`Slider.vue` 行职责改写 + 关键 props 补 `disabled`/`readonly`/`label`/`hint`/`error`
- 零 i18n 改动、零图标新增、零新增公开 props

### 5 项缺陷的具体修法
1. **`showMinMax` 死 prop**（全项目 grep：`showMinMax` 只出现在 Slider.vue 自身、**0 消费者**，模板里根本没渲染）→ 模板新增 `.si-slider__minmax` 一行（min/max 两个 span），用一个 `__field-column` 列容器把 `<input>` 与极值行包起来（这样极值才与轨道等宽、左右端对齐），`__value` 仍留在轨道右侧
2. **`readonly` 形同虚设**（原生 range 不支持 `readonly`，此前只改了个 cursor）→ ①`handleInput`/`handleChange` 开头 `if (readonly) { syncNativeValue(); return }`；②`syncNativeValue()` 把 `el.value` 写回 `String(nativeValue.value)` —— **必须同步回滚**，因为 `modelValue` 未变化时 Vue 不会重新 patch `value`；③`handleKeydown` 拦截 8 个改值键（`ArrowUp/Down/Left/Right`、`Home`、`End`、`PageUp`、`PageDown`，**Tab 必须放行**）；④补 `aria-readonly`
3. **档位对轨道无效** → 根因是 `::-webkit-slider-runnable-track` / `::-moz-range-track` 写死 `height: 6px`，而 4 个档位块**只覆盖 `.si-slider__field` 的 height 与两个 thumb 伪元素**，漏了 track 伪元素 → WebKit 下实际渲染的轨道高度永远是 6px。修法：改为 **CSS 自定义属性单点驱动**（`--si-slider-track-h` / `--si-slider-thumb-size`，4 个伪元素统一消费，档位块只赋值），并**带 fallback**（`var(--x, 兜底)`）——伪元素是否继承自定义属性是标准行为，但若不继承则优雅退出为基础档而**不会让拇指消失**
4. **错误色 Token** → `var(--b3-theme-destructive, $color-danger)` ×2 改 `var(--b3-theme-error, $color-danger)`；本项目该 Token 残留**由 6 处降到 4 处**（`Tag.scss`×3 / `Badge.scss`×1）
5. **label 未关联** → `const labelId = `${useId()}-label``，传给 `FormField`（本轮刚加的可选 prop）并让 `<input>` 用 `aria-labelledby` 指向它；无 `label` 时不渲染该属性

### 同批修掉的同款问题（已在计划中向用户说明）
- **`Slider.vue` 也把控件写在 `FormField` 的兄弟位置**（与本轮 `Select.vue` 同款）→ `hint`/`error` 会渲染到控件**上方**（预览「错误状态」示例原本就是这个错误排布）。已移入默认插槽；全项目 **0 处**给 Slider 传 `hint`/`error`，故零回归
- `Slider.vue` **缺文件头注释**（硬规则）→ 已补

### 明确不纳入（避免蔓延）
- 进度填充（已选段主题色）
- 显式 `ariaLabel` / `ariaLabelledby` props（用内部 `useId` + `FormField.labelId` 即可闭环）
- `range` 双手柄 + `minStepsBetweenHandles`、`orientation="vertical"`
- **`--b3-theme-primary-rgb` 的非法 fallback**：`rgba(var(--b3-theme-primary-rgb, $color-danger), 0.2)` 中 fallback 是 `hsl()`，`rgba(hsl(...), 0.2)` 语法无效 → 该声明在变量缺失时会被整条丢弃。但该变量由 `src/features/themeColor/index.ts:176` 运行时 `style.setProperty` 写入，且全项目 60+ 处同样写法（属既有约定）→ **本次不动，仅记为潜在问题**

### 影响面（实测）
- `Slider` 仅被 **6 个文件**引用（`imageCreation` 3 处 / `floatingToolbar/QRCodeDialog` / `bookmarkMarker/RuleItem` / 预览数据），且只传 `v-model`/`:label`/`:min`/`:max`/`:step`/`size`/`showValue`/`class`；**无任何调用点传 `hint`/`error`/`readonly`/`showMinMax`**
- `features` 中 `si-slider` 样式覆写 **0 命中** → 内部 DOM 调整不被外部选择器影响

### 验证
- `read_lints` 0 错误（Slider.vue / control.ts）
- `npx tsc --noEmit` 过滤 `Slider` **零报错**
- `@vue/compiler-sfc` 端到端编译 `Slider.vue` **通过**（41 个 bindings，`minLabel`/`maxLabel`/`nativeValue`/`labelId`/`handleKeydown` 等模板引用全部就位）
- `universal-arch-skill` 模式 A：`--features componentPreview` → **0 错误 0 警告**
- SCSS 核对：括号配平 0/0；`var(--si-slider-track-h, 6px)` 在 field + 两个 track 伪元素共 **3 处**（缺陷 ③ 的修复点）；`--si-slider-thumb-size` 9 处；`__field-column` / `__minmax-value` 已落地；`--b3-theme-destructive` **归零**；`&__field` 已无 `flex: 1`；硬编码 `font-size/weight/line-height` **0 命中**
- 硬编码颜色仅剩 4 处**既有** thumb 阴影 `rgba(0,0,0,*)`（与 `Switch.scss` 同款「保留为设计需要」写法，已带注释）+ 2 处既有焦点环，均非本次引入
- 行数：`Slider.vue` 234 / `Slider.scss` 233（均远低于 300 警戒线）
- 留用户验证：`pnpm lint` / `npx tsc --noEmit` + 预览面板 Slider 分区**逐档确认轨道粗细真的随档位变化**（本次核心修复，同时验证 CSS 变量确实继承进伪元素）、极值标签排布与「极值 + 当前值」组合、只读示例拖不动且方向键无位移、错误态明暗配色；再抽查 6 个调用点布局

## 追加：修 Slider 轨道不可见 + 让预览面板受控示例真正可交互（用户「为什么看不到横线，只看得到圆形的，调整数字也不会变」）

### 现象 1「看不到横线」= 真实对比度缺陷（已修）
- 预览卡片 `.cp-card { background: var(--b3-theme-background) }`，`.cp-card__stage` 无自有 background → 舞台底色 = **`#ffffff`**（浅色）
- 轨道原为 `var(--b3-theme-surface, $color-surface)` = **`#f7f7f5`** → 与白底只差 ~3% 灰度，**等同不可见**；暗色 `#2c2c2e` vs `#1c1c1e` 才看得出，所以此前一直没暴露
- 修法：`Slider.scss` 三处（`&__field` 自身 + `::-webkit-slider-runnable-track` + `::-moz-range-track`）改为 `var(--b3-border-color, $color-border)`（浅色 `#e0deda` / 暗色 `#3a3a3c`）
- **教训（已写进 MEMORY.md）**：`--b3-theme-surface` 与 `--b3-theme-background` 是「卡片 / 面板」相邻色对，**不能用来画需要与面板区分开的细线**；同类偏弱：`Switch.scss` 的轨道用 `surface-lighter`

### 现象 2「调整数字也不会变」= 预览面板机制限制（根因非 Slider）
`PreviewSection.vue` 原为静态透传 `<component :is="group.component" v-bind="resolveProps(example)">` —— 没有 `v-model`、没有 `@update:model-value` 监听。拖拽时浏览器会改原生 `input` 的 DOM 值（圆点会动），但组件 emit 出的 `update:modelValue` **无人接收** → `modelValue` 恒为初始值 → `showValue` 数字 / `showMinMax` 极值永远不变。`Input`/`Select`/`Switch`/`Checkbox`/`RadioButton`/`DatePicker`/`Listbox` 在预览里全都一样。

用户选定「改预览框架，受控组件可交互」。

#### 实现（只改 `PreviewSection.vue`，+62 行；不新增文件、不改任何组件本体）
新增内联组件 `PreviewStage`：
- `componentProps` 经 `toRef` 包装、**每次渲染重新解析** → 切档位即时生效；`modelValue` 单独存本地 `ref` → **切档位不会覆盖用户已改的值**
- 目标组件「声明了 `modelValue`」才注入 `modelValue` + `onUpdate:modelValue`（写回本地 ref）；否则**一个额外属性都不注入**
- 新增 `hasSlot` prop：仅当示例确有 `render` 或 `slotText` 时才转发默认插槽。**必须如此** —— 否则 `$slots.default` 恒真会改变组件内部分支（`Button.isIconOnly`、`Checkbox`/`Switch` 的 `v-if="label || $slots.default"`、`RadioButton` 会多渲染空 label 并撑出 8px gap）

#### 识别判据（关键）
```ts
const declared = (component as unknown as { props?: Record<string, unknown> }).props
return !!declared && Object.prototype.hasOwnProperty.call(declared, "modelValue")
```
`defineProps<Props>()` 会被编译成运行时 `props` 选项，判断可靠。**已实测**（`compileScript` 后扫 props 块）：`Checkbox / ColorField / DatePicker / Input / Listbox / RadioButton / Select / Slider / Switch` = YES；`Avatar / Badge / Button / Card / Chart / ConfirmDialog / FormField / IconWrapper / InputGroup / Label / Tag` = no；`InputGroupAddon / Loader`（无 `<script>` 块 → `props` 为 undefined）同样安全跳过。**为什么必须判断**：给未声明该 prop 的组件注入 `modelValue`/`onUpdate:modelValue` 会落进 attrs，**多根组件（`FormField`）会报 extraneous attrs 警告**。

#### 行为变化（有意）
- `Checkbox`「选中/未选中」、`Switch`「开/关」、`RadioButton` 选项等示例**点击会改变状态**（原为固定展示）
- `Slider` 拖动时数字与极值实时更新；只读示例仍纹丝不动
- `render` 复合示例内部的子组件仍是静态 props（`code` 展示的仍是最初静态组合），已在 `componentPreview/README.md` 新增「受控示例可交互」条目说明

#### 作废历史结论
2026-06-23 日志中「预览面板只透传静态 props、不接 `v-model` → 受控组件点击会即时回滚，属预期（静态快照定位）」**已作废**。预览面板定位更新为「**可交互 demo + 可复制代码**」。

### 验证
- `read_lints` 0 错误（`PreviewSection.vue` / `Slider.scss`）
- `npx tsc --noEmit` 过滤 `PreviewSection|Slider|componentPreview` **零报错**
- `@vue/compiler-sfc` 端到端编译 `PreviewSection.vue`（bindings 16）+ `Slider.vue`（bindings 41）**均通过**
- `universal-arch-skill` 模式 A：`--features componentPreview` → **0 错误 0 警告**
- SCSS：`background: var(--b3-border-color, $color-border)` 落在 39（field）/94（webkit track）/102（moz track）；轨道规则已无 `--b3-theme-surface`；`--b3-theme-destructive` 仍 0；括号配平 0/0
- 行数：`PreviewSection.vue` 191 / `Slider.vue` 208 / `Slider.scss` 197（均远低于 300）
- 留用户验证：`pnpm lint` + 预览面板（逐档看轨道粗细、拖 Slider 时数字实时变、只读示例拖不动、勾选/开关可点、**22 个分区无 extraneous attrs 警告**、切档位不重置已改的值）

## 追加：修 Slider 拇指与轨道不同心（用户「功能正常，但是 UI 没有居中，滑动按钮和进度条不是垂直的」+ 截图）

截图显示轨道线已可见 ✓、功能正常 ✓，但**圆点掉在横线下方**，整体视觉重心偏下（用户同时描述为「没居中」）。

**根因（原生 range 的经典坑）**：Chromium 的 `::-webkit-slider-thumb` **默认与轨道「顶」对齐、不是垂直居中**。轨道只有 4px 而拇指 14px，于是拇指从轨道顶部往下画 → 圆心比轨道中心低 5px；又因为布局盒（= 输入框高度 = 轨道高 4px）在 flex 里居中，而拇指只向下溢出，视觉质量整体偏下 → 两个现象同一个根因。

**修法（1 行）**：给 `::-webkit-slider-thumb` 补
```scss
margin-top: calc((var(--si-slider-track-h, 6px) - var(--si-slider-thumb-size, #{$spacing-4})) / 2);
```
- 四档算得：xsmall (3-10)/2 = -3.5px、small (4-14)/2 = -5px、medium (6-16)/2 = -5px、large (8-18)/2 = -5px
- **只加 WebKit**：Firefox 的 `::-moz-range-thumb` 由浏览器自动与轨道同心，加了反而会歪
- 效果：拇指与轨道同心 ✓，且溢出变为上下对称（-5 / +9 相对布局盒，圆心仍落在盒中心）→ 视觉居中 ✓
- 代价：拇指相对改动前上移 5px，上方多出 5px 溢出；FormField 标签与控件之间有 4px 间隙，实测影响不可察。若后续在紧凑行里出现「拇指碰到上方标签」，可改用「输入框高度 = 拇指高 + 轨道用 margin-top 居中」的变体

**经验（已写进 MEMORY.md）**：用原生 `input[type=range]` 自绘滑块时，**只要给轨道伪元素设了高度，就必须同时给 WebKit 拇指补半个高度差的负 margin**，否则永远不同心；`margin-top` 与 `--thumb/--track` 两个变量写法可复用同一套档位变量。

### 验证
- SCSS：括号配平 0/0；`margin-top` 仅出现在 `::-webkit-slider-thumb`（`::-moz-range-thumb` 无）；`var(--b3-border-color, $color-border)` 仍在 field + 两个 track；行数 238
- `read_lints` 0 错误
- 留用户验证：预览面板逐档确认**拇指与轨道同心、整体垂直居中**（xsmall/small/medium/large 四档都要看）

## 追加：修复 `pnpm dev` 起不来（用户贴出 `Cannot find module 'livereload'` 报错）

### 根因：依赖包被「半个解压」破坏，与代码改动无关
`rollup-plugin-livereload` 加载不到自己的依赖 `livereload`。查下来：
- `.pnpm/livereload@0.9.3/node_modules/livereload/` **只剩 `svg` 与 `test` 两个子目录**，`package.json` / `lib/` / `dist/` / `bin/` 全都不见了 → `require('livereload')` 必然失败
- 根 node_modules 与 lockfile 本身**完全正常**（`node_modules/.pnpm/livereload@0.9.3` 存在、插件里也有指向它的 junction）

### 一条可复用的诊断命令（扫全量损坏包，很有用）
```js
// node -e "…"：遍历 .pnpm/*/node_modules/<pkg>，检查 package.json 是否缺失
const fs=require('fs'),path=require('path');const root='node_modules/.pnpm';
let broken=[],total=0;
for(const e of fs.readdirSync(root)){const nm=path.join(root,e,'node_modules');
  if(!fs.existsSync(nm))continue;
  for(const s of fs.readdirSync(nm)){if(s.startsWith('.'))continue;
    if(s.startsWith('@')){const sd=path.join(nm,s);
      for(const i of fs.readdirSync(sd)){total++;if(!fs.existsSync(path.join(sd,i,'package.json')))broken.push(e+'/'+s+'/'+i)}}
    else{total++;if(!fs.existsSync(path.join(nm,s,'package.json')))broken.push(e+'/'+s)}}}
console.log('检查包目录数:',total,'| 缺 package.json 的:',broken.length,'\n'+broken.join('\n'));
```
本次结果：**1358 个包目录里只有 2 条异常**，且都指向同一个包（第 2 条是 `rollup-plugin-livereload@2.0.5/livereload` 这个 junction，穿透后同样缺 package.json）→ **单点损坏，排除面性损坏**。

### 修复过程（两步，均已执行）
1. **`pnpm install --force`**：从 store 重新解压，`livereload` 恢复完整（`package.json`/`lib`/`dist`/`bin` 齐备，`require('livereload').createServer` 为 function ✓），`node_modules/.bin/vite.CMD` 就位 ✓。代价：`--force` 会重下全部包（本次 540 个，镜像很慢，耗时长）。
2. 但 `--force` 触发了 **`ERR_PNPM_IGNORED_BUILDS`**，并**自动生成了 `pnpm-workspace.yaml`**（占位内容：`'@parcel/watcher': set this to true or false` 等 3 条）→ install 以报错收尾。已把三处补成 `true` 并重跑 `pnpm install`：三个 postinstall（`@parcel/watcher` / `esbuild` / `vue-demi`）全部 `Done`，2.9s 干净收尾 ✓。

### 关键判断依据（值得记住）
- **esbuild / @parcel/watcher 不需要构建脚本也能工作**：它们的二进制来自 optional 平台包（`.pnpm/@esbuild+win32-x64@0.28.2`、`.pnpm/@parcel+watcher-win32-x64@2.5.6` 均在位）✓
- **`vue-demi` 的 postinstall 必须允许**：它用于把 `lib/index.*` 切换到 Vue 3 入口，而本项目的 `vue-demi` 解析为 `0.14.10(vue@3.5.42)`；跳过会让它停在 Vue 2 入口 → 有运行时风险
- 注意 `.pnpm/<pkg>/node_modules/<dep>` 多为 **junction**：手动 `Remove-Item -Recurse` 有「跟随 junction 删掉目标真包」的风险，**修复一律走 pnpm 自身命令**，不要手删目录

### 遗留与建议
- `pnpm-workspace.yaml` 目前是 **untracked（`??`）**：它决定后续 install 是否复现该报错，**建议纳入版本控制**（否则换机器/换同事仍会踩）
- `pnpm dev` 属 `vite build --watch`，按项目规则**由用户自行运行验证**

### 验证
- `livereload` 可 require 且 `createServer` 为 function ✓
- `node_modules/.bin/vite.CMD` 存在 ✓
- `pnpm install` 2.9s 干净收尾、三个 postinstall 全部 Done ✓

## 追加：新增共享组件 Textarea（用户「参考 https://primevue.dev/textarea/ 新增通用组件」）

### 澄清结论（用户选择）
1. 组件定位 = **独立组件，旧用法保留**：新建 `Textarea.vue`，`Input.vue` 的 `type="textarea"`（16 文件 19 处）**原样不动、不做迁移**（零回归优先，接受两套实现短期并存）
2. 能力范围 = `autoResize` 自动增高 + `variant` 描边/实底 + `fluid` 占满宽；**未选**拖拽调整高度手柄
3. 命名 = **项目风格，吸收 PrimeVue 语义**：`Textarea.vue`；用 `autoResize`（不用旧 `autosize`）、`variant`、`error`（不用 `invalid`）、四档 `size`

### 交付（3 新文件 + 3 份文档 + 1 聚合改动，零 feature 改动）
- `src/components/Textarea.vue`（~310 行）+ `src/components/textarea/autoResize.ts`（~70 行，私有纯函数）
- `src/components/styles/Textarea.scss`（166 行）
- `src/features/componentPreview/previewData/textarea.ts`（11 个示例，`sizeable: true`）+ `previewData/index.ts` 聚合（紧随 `inputPreviewGroups`）
- 计数 **22 → 23**：`AGENTS.md` 160/168/181/450/488 + 组件清单表在 `Input.vue` 后插入 `Textarea.vue` 行 + 177 行「优先复用」清单追加「多行文本域」；根 `README.md:148`；`componentPreview/README.md` L3/L8（覆盖枚举）/L19（sizeable 枚举）/L20（档位内边距说明）+ 新增「多行文本域」能力条目 + 事件契约表加 `Textarea` 行
- **零 i18n 改动、零图标新增**

### PrimeVue API 结论（Context7 查得，与首轮网页抓取互补）
- Props：`modelValue` / `defaultValue` / `name` / `autoResize`（bool，默认 false，"height changes as being typed"）/ `size`（only `small|large`）/ `invalid`（bool）/ `variant`（`outlined|filled`）/ `fluid`（"spans 100% width of the container when enabled"）/ `formControl` / `dt` / `pt` / `ptOptions`
- `rows` / `cols` / `wrap` 是**原生属性透传**（attr fallthrough），组件本身不声明这两个 prop
- PT 只有 `root` 一节 → **根元素就是原生 `<textarea>`**（本项目为承载边框/焦点环而多包了一层 wrapper，属有意的结构差异）
- 无障碍：`<label for>` + `id`，或 `aria-labelledby` / `aria-label`；键盘仅 Tab 移入；**无具名插槽**

### 关键设计决策
- `fluid` **默认 `true`**（与项目其他控件默认 `width:100%` 一致）；`:fluid="false"` 时根 + 字段改 `width: auto`，由原生 `cols` 决定固有宽度（字段若保持 `100%` 会与内容宽度循环依赖）。**该默认值与 PrimeVue 相反，已在文档中显式标注**
- `autoResize` 时 `rows` 兼任**初始高度与 `minRows` 兜底**；`maxRows` 不传则**不设上限**（对齐 PrimeVue），超上限转内部滚动。计算沿用 `Input.adjustHeight` 的「行高 × 行数 + 上下内边距」公式
- **不照搬 `Input.scss` 的 `min-height: 80px / max-height: 200px`**：硬编码会与 `rows` / `autoResize` 语义打架，高度改由 `rows` + 行数约束决定
- `variant` 复用 `Checkbox` / `RadioButton` 的 `outlined|filled` 字段与实现（实底 = `--b3-theme-surface-lighter` 填充 + `border-color: transparent`）
- **实底聚焦必须用内嵌 `outline`（`-1px`）**：实底 border 为 transparent，`focus-ring` mixin 只改 border-color 会完全不可见（复用 Checkbox / RadioButton / Slider 的既有结论）
- 校验态用根类 `.si-textarea--error .si-textarea__wrapper:focus-within` 覆盖实底聚焦规则 —— **刻意不加 `__wrapper--error` 元素修饰类**（它特异性只有 (0,1,0)，会被 `.si-textarea--filled .si-textarea__wrapper`(0,2,0) 压掉；`Input.scss` 里正是靠 `!important` 才生效）
- `aria-labelledby` 走 `useId()` 生成 `labelId` 传给 `FormField`（复用 `Slider` 先例），外部显式 `ariaLabelledby` 优先
- `fieldStyle` 在 `autoResize` 时返回 `{}`，把 `resize` / `overflow` 交给 `--autoresize` 类 —— **否则 Vue 重渲染会把手写计算的 `overflow-y` 覆盖回 `hidden`**（`textareaStyle` 与 JS 直写 DOM 样式会互相打架）
- 高度算法抽到 `src/components/textarea/autoResize.ts`（`applyAutoResize` / `clearAutoResize`）：与 Vue 响应式无关的 DOM 测量单元，抽出后 `Textarea.vue` 由 364 → ~310 行。**纯函数抽取的正当理由不止 Rule of Three，突破 300 警戒线且具内聚语义同样是依据**

### 架构审查（universal-arch-skill 模式 C）与修项
- 修 1：XS 档 `padding` 的 `6px` 硬编码 → `m.$gap-xs`（`_mixins.scss` 的 6px 本地 Token，与 `Input.scss` small 档写法统一）
- 修 2：抽出 `textarea/autoResize.ts`（见上）并把 4 处重复的 `if (autoResize) nextTick(adjustHeight)` 收敛为 `remeasure()` 单一入口
- 其余全绿：`.vue` 的 `<style scoped>` **仅含 `@use`**；颜色全部 `var(--b3-theme-*, $color-*)` 双保险；`font-size` 全取 Token（10/12/14/16 四档无同号）；`--b3-theme-destructive` **0 命中**；无 emoji 图标；无跨 feature 导入；文件头注释齐备

### 验证
- `read_lints` **0 错误**（5 个新增/改动文件）
- `npx tsc --noEmit` 过滤 `Textarea|textarea|autoResize` → **零新增报错**（全项目其余均为既有噪声：`featureRegistry` 的 `id 不存在于 FeatureRegistryEntry` 30 余条、`*.vue` 无导出成员等）
- **`@vue/compiler-sfc` 端到端离线校验**（`parse` + `compileScript` 取 `bindings` 喂 `compileTemplate`）：parse errors 0 / template errors 0；style 块确认为 `scss/scoped=true` 且内容仅 `@use`
  - ⚠️ 项目根**无法直接 `require("@vue/compiler-sfc")`**（pnpm 不提升），须用绝对路径 `node_modules/.pnpm/@vue+compiler-sfc@3.5.42/node_modules/@vue/compiler-sfc/dist/compiler-sfc.cjs.js`
- **Sass 离线编译成功**（3228 字节）：`findFileUrl` importer 把 `@/` 映射到 `src/` 可用（此前记忆里「自定义 importer 解析不了 `@/variables.scss`」在本轮**已被推翻**，该写法本次实测可行）；关键选择器展开确认为 `.si-textarea--filled .si-textarea__wrapper:focus-within` / `.si-textarea--error .si-textarea__wrapper:focus-within` / `.si-textarea:not(.si-textarea--fluid) .si-textarea__field`，与特异性分析一致
- 行数：`Textarea.vue` ~310 / `Textarea.scss` 166 / `autoResize.ts` ~70 / `previewData/textarea.ts` 126（均 < 500 硬阈值；`Textarea.vue` 略超 300 警戒线，与 DatePicker 338 / Listbox 330 同带）
- 计数 grep 三份文档均为 23，全项目无残留「22 个组件」表述（仅 `docs/ai-content-generator-component-audit.md` 的「22 个 .ts/.vue」属另一语境）
- 留用户验证：`pnpm lint` / `pnpm i18n:verify` / `pnpm validate:icons` + 预览面板 Textarea 分区目视（四档尺寸、描边/实底、聚焦环、自动增高输入时长高、`fluid=false` 的固有宽度、字数统计、禁用只读错误态）

## 追加：新增共享组件 ToggleButton（用户「https://primevue.dev/togglebutton/」）

### 澄清结论（用户选择）
1. 组件形态 = **独立组件，内部复用 `Button`**（`variant`/`severity`/`outlined`/`block` 随状态切、`:aria-pressed` 透传）→ 零样式复制，天然继承四档尺寸、图标体系、`focus-visible`、无障碍命名
2. 能力 = `onLabel`/`offLabel` 文案切换 + `onIcon`/`offIcon` 图标切换 + `fluid`；**未选 `readonly`** ⇒ 不实现
3. 存量 = **不迁移**现有 6 处 `Button` + `:aria-pressed`（那是「多选项互斥分段组」语义，与「单按钮布尔开关」不同）

### 交付（3 新文件 + 4 改动，零 feature 改动、零 i18n、零图标新增）
- `src/components/ToggleButton.vue`（186 行）+ `src/components/styles/ToggleButton.scss`（**仅 14 行**，只承载容器 `inline-flex/column` 与 `--fluid` 的 `width:100%`）
- `src/features/componentPreview/previewData/toggleButton.ts`（9 个示例，`sizeable: true`）+ `previewData/index.ts` 聚合
- 计数 **23 → 24**：`AGENTS.md` 160/168/181/450/488 + 清单表在 `Button.vue` 后插入 `ToggleButton.vue` 行 + 177 行复用枚举加「按钮式开关」并写明边界；根 `README.md:148`；`componentPreview/README.md` L3/L8/L19 + 新增「按钮式开关」能力条目 + 事件契约表加一行

### 核心设计（可复用结论）
- **外观映射（零新增按钮样式）**：`variant="ghost"` 恒定 + `:severity="error ? 'danger' : (pressed ? 'primary' : undefined)"` + `:outlined="!pressed"` ⇒ 未按下=中性描边透明底 / 按下=主色实底 / 错误=红色系（描边或实底）
  - ⚠️ **关键坑**：`--severity-*` 块会设置 `--btn-color`，而 `--outlined` 的 `color`/`border-color` 取 `var(--btn-color, 中性兜底)` → **只要传了 severity，未按下就变成「主色描边」而不是中性描边**。要拿到 PrimeVue 那种「中性 → 主色」的过渡，**未按下必须不传 severity**（传 `undefined` 走 Button 的无默认值分支）
  - 依赖的 Button.scss 源码顺序：颜色块(77-132) → severity 块(134-174) → 外观修饰 `--outlined`/`--text`(176-212)，同特异性靠后者胜
- **无文案时必须不传默认插槽**：`Button.isIconOnly = !slots.default && !!icon`。若写成 `<Button>{{ currentLabel }}</Button>`，即使文案为空串，`$slots.default` 仍恒真 → icon-only 档位失效（不是方形），且 `.si-button__text` 的 `flex:1` + 容器 `gap` 会让图标偏心。⇒ 采用 **`v-if="currentLabel"` / `v-else` 双分支 + `v-bind="buttonProps"` 共用参数**，无文案时自动退化为方形纯图标按钮
- **`onLabel`/`offLabel`/`onIcon`/`offIcon` 均无默认值**：PrimeVue 默认 `yes`/`no`，但项目禁止硬编码 UI 文案 ⇒ 不设默认，解析为空时切到图标分支；两条 DEV 告警（对齐 `Button.vue` 既有做法）：①文案与图标同时为空；②「可见文案/图标随状态变化」而 `ariaLabel`/`ariaLabelledby`/`title` 全缺 —— 落实 PrimeVue 无障碍章节的「**始终提供不随状态变化的 aria 标签**」红线
- **新增 `tabindex` prop（偏离计划的一处）**：计划写「`tabindex` 走 attr fallthrough 到 Button 根」，但 ToggleButton 的根是 `<div>`（FormField 需要容器），`inheritAttrs` 默认会把 attrs 落在 div 上 → 故把 `tabindex` 提升为显式 prop 再透传给 Button。其余 attrs（`class`/`style`）仍落根 div，与 Input/Slider/Select 行为一致
- `error` 除切 `severity="danger"` 外还输出 `aria-invalid="true"`；`hint`/`error` 经 `FormField`（**控件必须放默认插槽**）；无 `label` prop（按钮自身文案即标签，故不需要 `useId()`/`labelId` 关联）
- emits：`update:modelValue` + `change`（沿用 `Switch` 的单布尔控件双事件约定）+ `click`；`disabled` 时三者均不派发
- `defineExpose({ focus, blur })` 用**结构化子集接口** `ButtonHandle` 承接内部 Button 的 expose（沿用 DatePicker composable 的「结构化子集接口」先例，避免依赖组件实例类型推导）
- `fluid` 默认 **`false`**（按钮天然内容宽；与 `Textarea.fluid` 默认 `true` 相反，已在 AGENTS 清单表注明）
- **不做**：`readonly`（用户未选）、`defaultValue`（非受控模式）、`variant`/`severity`/`outlined`/`text`/`rounded` 等外观透传 props（PrimeVue ToggleButton 无这些轴，避免 scope creep）

### 踩坑
- **预览清单里的图标必须写已注册的语义 `IconKey`**：初版写了 `mdi:lock` / `mdi:star`（原始 Iconify 名），而 `IconWrapper` 走 `getIconConfig(key)` 按**键名查表** → 不会渲染。已改为 `eye`/`eyeOff`、`star`/`starOutline`（均在 `COMMON_ICONS`）。另注意项目无通用 `lock`/`lockOpen` 键（只有语义化的 `pageLock`/`encryption`）
- PrimeVue 官网本页与 Textarea 一样：正文只有章节骨架，**完整 API 表格需用 Context7 补**（`/websites/primevue` 可直接查到 props 全集与默认值）

### 验证
- `read_lints`：**0 错误**（4 个新增/改动文件）
- `npx tsc --noEmit` 过滤 `ToggleButton|toggleButton` → **零报错**
- `@vue/compiler-sfc` 端到端离线编译：parse 0 / template 0 错误；bindings 34 个（`buttonProps`/`currentLabel`/`currentIcon`/`severity`/`pressed`/`hasError`/`toggleClasses` 等模板引用全部就位）；style 块 `scoped=true` 且仅 `@use`
- Sass 离线编译成功（294 字节），输出恰为 `.si-togglebutton` 与 `.si-togglebutton--fluid`
- 硬编码检查：`ToggleButton.scss` 的 hex/rgba/font-size/font-weight/line-height/padding/border-radius 数字 **0 命中**（全文件仅 14 行、4 条声明）
- 行数：`ToggleButton.vue` 186 / `ToggleButton.scss` 14 / `previewData/toggleButton.ts` 119（全部远低于 300 警戒线）
- 计数 grep：三份文档均为 24，全项目无残留「23 个组件」表述
- 零改动确认：`Button.vue` / `Button.scss` / `Switch.vue` 一字未动，6 处 `Button` + `:aria-pressed` 调用点未动
- 留用户验证：`pnpm lint` / `pnpm i18n:verify` / `pnpm validate:icons` + 预览面板 ToggleButton 分区目视（未按下中性描边 → 点击变主色实底、文案与图标随状态切换、纯图标示例为方形、`fluid` 铺满、错误态红色、禁用不可点、四档尺寸），并抽查既有 6 处分段组按钮观感未变

## 追加：新增共享组件 SpeedDial（用户「https://primevue.dev/speeddial/」）

### 澄清结论（用户选择）
1. **定位方式** = 「组件自带角落定位档位」：四档 `position` + `offset`，**默认 `position: fixed`** 悬浮视口角落（PrimeVue 自己不管定位，靠调用方传 style）
2. **轨迹与交互** = 「完整展开轨迹」（`type` 四档 + `radius`）。该题是**单选项**，用户未勾选 `mask` 与 `hideOnClickOutside` ⇒ **`mask` 不实现**（与官方默认 false 一致）；`hideOnClickOutside` 因官方默认 `true` 且属浮层基本可用性，**保留为默认开启并提供同名 prop**（已在方案中显式标注）
3. **附加能力** = **四项全选**：动作文字气泡 / 主按钮图标旋转 / 具名插槽（item·button·icon）/ `buttonProps`·`actionButtonProps` 透传

### 交付（4 新文件 + 1 私有目录 3 文件 + 6 处改动，零 feature 改动、零 i18n、零图标新增）
```
src/components/
├── SpeedDial.vue                  295  # 公开入口（平铺）
├── speedDial/                          # 私有实现（禁止 feature 直接导入）
│   ├── types.ts                    96  # 字面量类型 + 8 向角度表 + 档位边长表 + 默认常量
│   ├── geometry.ts                 95  # 轨迹偏移纯函数 + 几何→CSS 变量映射
│   └── useSpeedDial.ts            194  # 开合 / 外部点击 / 键盘 / 焦点 / id 生成
└── styles/SpeedDial.scss           86  # 位置档位 + 开合动效 + 图标旋转
```
- 预览：`previewData/speedDial.ts`（203 行，12 个示例，`sizeable: true`）+ 聚合接入
- **预览沙箱**：`PreviewSection.vue` 追加 `cp-card__stage--speeddial` 判定；`PreviewSection.scss` 追加 `.si-speeddial { position: absolute }` 覆盖 + `--speeddial { height: 260px }`
- 计数 **24 → 25**：`AGENTS.md` 160/168/181/450/488 + 清单表加 `SpeedDial.vue` 行 + 177 行复用枚举；根 `README.md:148`；`componentPreview/README.md` L3/L8/L19 + 能力条目 + **具名插槽表 3 行**（item/button/icon）+ **事件契约表 2 行**

### 核心设计（可复用结论）
- **`position` 用类输出而非内联样式**：`.si-speeddial--{corner} { right/bottom: var(--si-speeddial-offset) }` + `offset` prop 只写 CSS 变量（不传时样式内取 `$spacing-4`）→ ①预览沙箱能用**一条规则**覆盖成 `absolute`；②调用方可直接覆写
- **轨迹几何抽为纯函数** `resolveActionOffset(...)`：8 向 → 屏幕角度（0° 右、顺时针、y 向下为正）；`linear` 沿方向单位向量步进；`circle` 从方向角均分 360°；`semi-circle` / `quarter-circle` 以方向角为中心左右各展 90° / 45°；`total <= 1` 统一退化为「方向单位向量 × 半径」避免除零；`radius < 按钮边长` 时抬到按钮边长避免全塌到主按钮
  - 数学上「项目左上角 + `translate(R cos, R sin)`」恰好等价于「项目中心落在以主按钮中心为心、半径 R 的圆上」，故无需额外半宽偏移
- **几何 → CSS 变量**由 `toItemStyle(offset, index, delayStep)` 承担（`--si-speeddial-x/y/delay`），样式侧不按项数生成规则；组件里只留一行 `itemStyle(index)`
- **收起态用 `visibility: hidden`**（自动移出 Tab 序列与无障碍树，无需 `aria-hidden`/`tabindex` 手工管理）；开合过渡的 `transition-delay` **必须写三段**（opacity/transform 逐项递增、visibility `0ms`）—— 否则展开瞬间元素仍 hidden 不渲染，淡入动画会被吞掉
- **键盘触发判别用 `event.detail === 0`**（原生 button 的键盘 click 其 detail 为 0）→ 只有键盘操作才把焦点移入首项，鼠标点击不抢焦点（沿用 `Select` 的 `restoreFocus` 结论：仅 Esc 返还焦点）
- **受控 + 内部兜底**：`visible` prop 通过 `watch` 同步到内部 `isOpen`，既支持 `v-model:visible`，也能在不传时独立开合 —— 这一条让**预览卡内的示例真正可点击开合**（`PreviewStage` 只注入 `modelValue`，不注入 `visible`）
- **跨组件命中 Button 内部元素必须 `:deep()`**：图标旋转写 `&--rotate &__trigger :deep(.si-button__icon)`（Button 内部元素带的是 Button 自己的 scope 属性）；编译产物确认为 `.si-speeddial--rotate .si-speeddial__trigger :deep(.si-button__icon)`
- **透传 props 的合并顺序**：`buttonProps` / `actionButtonProps` 作 spread 在前，显式 props 在后；`ariaLabel` 等**用条件展开**（`...(x ? { ariaLabel: x } : {})`）避免「传 `undefined` 把透传值覆盖掉」
- 明确不做：`mask`、`unstyled`/`pt`/`dt`、`itemicon` 插槽（该项 scope 含 `class` 直通，与「图标一律走 `IconKey`」冲突）、`MenuItem` 的 `url`/`target`/`separator`/子菜单

### 踩坑与修项
- **`linear` 起算位置真实缺陷（已修）**：初版写 `index × (按钮边长 + 间隙)` ⇒ **`index 0` 距离为 0，第一个动作与主按钮完全重叠**。改为 `(index + 1) × (按钮边长 + 间隙)`。该缺陷由我加的**纯函数数值断言**抓出，非目视发现
- **预览沙箱的 `overflow: hidden` 陷阱**：`.cp-card` 是 `overflow: hidden`，`position: top-*` 的示例若沿用默认 `direction: up`，动作会向上越出舞台被裁掉 ⇒ 该示例必须配 `direction: down`（已在示例内加注释）
- **数组 props 的预览写法**：`props.model` 用模块级常量数组（与 `code` 里的 `:model="actions"` 对应），避免把同一份数组抄 12 遍
- **行数收口**：入口初版 301 行（超 300 警戒线 1 行）⇒ 把「几何 → CSS 变量」映射从组件移到 `geometry.ts`（更内聚 + 省 6 行）→ 295 行

### 验证
- `read_lints`：**0 错误**（8 个新增/改动文件）
- `npx tsc --noEmit` 过滤 `SpeedDial|speedDial` → **零报错**
- **`@vue/compiler-sfc` 端到端离线编译**：`SpeedDial.vue` 与 `PreviewSection.vue` 均 parse 0 / template 0 错误；`SpeedDial.vue` 的 style 块 `scss/scoped=true` 且仅 `@use`
- **SCSS 离线编译成功**（1609 字节）：`--bottom-right` / `--top-left` / `__list` / `--open .si-speeddial__item` / `--rotate .si-speeddial__trigger` 五个关键选择器全部存在，`destructive` 0 残留
- **轨迹几何数值断言全部通过**（用 `esbuild@0.28.2` 把 `geometry.ts` bundle 成 CJS 后逐项求值）：`linear/up/28+8` → `(0,-36) (0,-72) (0,-108)`；`linear/left` → `(-36,0) (-72,0)`；`circle/up` 4 项 → 四项到原点距离**恒为 72**，极角 270/0/90/180；`semi-circle/up` 3 项 → 180/270/360°；`quarter-circle/up` 3 项 → 225/270/315°；单项 circle → `(0,-72)`；`radius=0` → 抬到 `(28,0)`
- 架构审查（universal-arch-skill 模式 C）全绿：`.vue` 内仅 `@use`；`SpeedDial.scss` 硬编码颜色/字体三要素/padding/margin/border-radius **0 命中**、`--b3-theme-destructive` 0；5 个 `.ts`/`.vue` 文件头注释齐备；**feature 未导入 `components/speedDial/`**、components 内 `@/features` **0 命中**；无存储/AI/事件总线/定时器直调（`document` 点击监听 `onMounted`/`onBeforeUnmount` 成对）；无新增图标
- 行数：295 / 96 / 95 / 194 / 86 / 203（全部 < 300 警戒线）
- 计数 grep：三份文档均为 25，全项目无残留「24 个组件」表述
- 零改动确认：`Button.vue` / `Button.scss` / `IconWrapper.vue` 一字未动
- 留用户验证：`pnpm lint` / `pnpm i18n:verify` / `pnpm validate:icons` + 预览面板 SpeedDial 分区（**重点确认预览卡内不会因 fixed 铺满窗口**、四档轨迹形态、8 向、气泡方向、图标旋转、开合过渡与逐项延迟、点击外部收起、Esc 返还焦点、四档尺寸、左上角示例向下展开）

## 追加：产出《共享组件库迁移到普通 Vue 3 项目》文档（用户问「复制到别的项目就行吗，还是会有其他问题」）

澄清结论：目标 = **普通 Vue 3 项目（非思源）**；交付 = **复制清单 + 迁移说明文档**。产出 `docs/components-vue3-migration-guide.md`（8 节 + 复核命令附录），**未改任何源码**。

### 实测基线（用 code-explorer 全量核验 + PowerShell 复核，全部可复现）
- `src/components/` **72 文件**：根 26（25 `.vue` + `chart.types.ts`）/ `datePicker` 7 / `select` 5 / `speedDial` 3 / `textarea` 1 / `styles` 30
- **组件零业务耦合（关键结论，可直接迁普通 Vue 3）**：`src/components/**` 内 **0 处** i18n / plugin 实例 / `siyuan` 包 / store / `@/features` 导入；跨目录依赖**仅两项** —— `@/config/icons`（14 文件，其中 `IconWrapper.vue` 是唯一运行时消费方，其余 `import type { IconKey }`）与 Sass `@/variables.scss`（30 个 scss）
- 第三方仅 4 个：`vue`（28 文件 = 25 `.vue` + 3 composable）/ `@iconify/vue` / `chart.js` + `vue-chartjs`（**仅 Chart.vue**）。组件只用 `mdi:`，故 `@iconify-json/ph` 可省
- 组件消费 **20 个 `--b3-*` 变量**；组件自身硬编码 **15 个 IconKey**（minus/check/x/close/eye/eyeOff/calendar/chevronUp/Down/Left/Right/chevronDoubleLeft/Right/magnify/plus）

### 三个非思源环境降级点（全库唯一硬依赖宿主处）
1. 样式全走 `var(--b3-theme-*, fallback)`，fallback 是硬编码 Codex 亮色 → 不做桥接则**恒定亮色**
2. `Chart.vue:175-180` 的 `isSiYuanDarkMode()` 判 `html[data-theme-mode]`/`.theme-dark`/`.b3-theme-dark` → `theme="auto"` 恒判亮色
3. `SpeedDial.vue:206` 的 `itemClass` 输出思源内置 `b3-tooltips b3-tooltips__{side}` → 非思源无气泡（`aria-label` 仍在）

### 迁移期的两个易错点（写入文档，避免目标项目照抄复现）
- **`*-rgb` 变量必须是逗号分隔**：组件按 `rgba(var(--b3-theme-primary-rgb, 201, 122, 93), 0.1)` 消费（`Input.scss:180` / `Card.scss:256` / `Select.scss` / `Slider.scss` / `Chart.scss:53`）→ 桥接时写现代的空格分隔会让**整条声明失效**
- **`ColorField.scss` 有 4 处 `var(--b3-theme-outline)` 完全不带 fallback**（16/31/50/65 行）→ 变量未定义时该 `border` 声明在计算值阶段失效、边框退 `currentColor`，桥接必须补 `--b3-theme-outline`

### 环境/工具经验
- **自检命令要按实际 import 写法设计**：`Chart.vue` 的 `import { ... } from "chart.js"` 是**多行写法**，用 `^import .* from "..."` 会漏掉 chart.js / vue-chartjs → 改为 `from "(@/...|[a-z@]...)"` 才完整
- 提取「三元表达式里的图标键」(`(isOpen ? 'chevronUp' : 'chevronDown') as IconKey`) 用 `'(\w+)'\)?\s*as IconKey` 会漏掉非末位键 → 正确做法是**先按行匹配 `as IconKey`，再对该行 `[regex]::Matches($_.Line, "'(\w+)'")` 取全部**；`name="close"` 这类无捕获组的备选分支会让 `$_.Groups[1]` 静默丢失 → 用 `[regex]::Match($_.Line, '"(\\w+)"')` 统一取第一个引号串
- 文档基线数字**必须实测**：本轮核出 3 处偏差（`vue` 引用 29→28 文件、图标表引用 15→14 文件、图标表 854→853 行）

> ⚠️ 上表基数为 **25 个公开组件** 时的快照；同日又新增 `Paginator`（第 26 个）后需重算：`src/components/` **78 文件**（根 26 `.vue` + `chart.types.ts` / `datePicker` 7 / `select` 5 / `paginator` 3 / `speedDial` 3 / `textarea` 1 / `styles` 31）。组件自身硬编码 IconKey 清单需补 `chevronDoubleLeft` / `chevronDoubleRight`（原本已在列）

## 追加：新增共享组件 Paginator 并把三处自建分页全量迁移（用户「https://primevue.dev/paginator/」）

### 澄清结论（用户选择）
1. **迁移范围** = 「新增 + 三处全迁移」（imageCompressor / flashcardReading / skillLearning）
2. **组成控制** = 「项目式显式 props」（`showFirstLast` / `showPageLinks` / `pageLinkSize` / `showReport` / `rowsPerPageOptions` / `showJumpInput` / `alwaysShow`），**不采纳** PrimeVue v5 的 `template` 字符串
3. **状态模型** = 「项目风格 `page`(1 基) / `rows` / `total`」，**不采纳** PrimeVue 的 `first`(零基行偏移) / `totalRecords`

### PrimeVue API 要点（Context7）
`totalRecords` / `rows` / `first` / `pageLinkSize`(5) / `rowsPerPageOptions` / `template`（字符串或按 max-width 的断点对象）/ `currentPageReportTemplate`(`({currentPage} of {totalPages})`) / `alwaysShow`(true)；emits `update:first`·`update:rows`·`page`；slots `start`·`end`·`container` + 各图标槽。**v5 已无 `showPageLinks` / `showFirstLastIcon` / `showJumpTo*` 等布尔开关**（全由 `template` 控制）——用户选择回到显式 props 路线。

### 交付（4 新文件 + 1 私有目录 3 文件 + 预览 + 6 处迁移改动 + 4 份文档）
```
src/components/
├── Paginator.vue              291  # 公开入口（平铺）
├── paginator/                      # 私有实现（禁止 feature 直接导入）
│   ├── types.ts                69  # 字面量类型 + DEFAULT_LABELS + 载荷/占位符类型
│   ├── pageLinks.ts            67  # 页码窗口折叠纯函数
│   └── state.ts                74  # 状态推导 + 载荷构造 + 报告格式化（纯函数）
└── styles/Paginator.scss       60  # 布局 + 档位字号单点驱动 + 报告/省略号
```
- 预览：`previewData/paginator.ts`（171 行，13 个示例，`sizeable: true`）+ 聚合接入
- 计数 **25 → 26**：`AGENTS.md` 160/168/181/450/488 + 清单表加 `Paginator.vue` 行 + 177 行复用枚举加「分页」；根 `README.md:148`；`componentPreview/README.md` L3/L8/L19 + 能力条目 + 事件契约表 1 行
- **零 i18n 改动、零图标新增**（4 个 chevron 均已注册）

### 迁移明细
| 位置 | 迁移前 | 迁移后 |
|---|---|---|
| `imageCompressor/index.vue` | `<template v-if="totalPages>1">` + 首页/上一页/`x / y`/下一页/末页（`SiButton`）+ 每页条数 `SiSelect`（内联「切条数回第 1 页」） | 单个 `<Paginator v-model:page v-model:rows :total :always-show="false" :show-page-links="false" :rows-per-page-options="pageSizeOptions" :labels="paginationLabels" size="xsmall" />`；因在工具栏 flex 行内，**去掉外层 wrapper** |
| `flashcardReading/index.vue` | `v-if="viewMode==='list' && totalPages>1"` + 上一页/`x / y`/下一页（`Button variant="secondary"`） | 保留 `.pagination` 作**纯居中容器**；`totalPages` computed **整块删除**，可见性条件改 `filteredCards.length > CARD_CONFIG.PAGE_SIZE`（等价） |
| `skillLearning/components/SkillListView.vue` | `v-if="paginated"` + **原生 `<button>`** 的 `&laquo;` / `x / y` / `&raquo;` | `<Paginator v-model:page :rows="pageSize" :total :show-page-links="false" size="xsmall" />`；`.skill-list-view__pagination` 退化为纯居中容器（删掉 `#94a3b8` 硬编码色与整个 `button {}` 块） |
| `useFilteredCards.ts` | — | 返回值**增补 `pageSize`**（接口 + return，纯增量、不影响 `FlashcardView`）；**删除已成死代码的 `totalPages` / `paginated`** |
| `useImagePagination.ts` | — | **删除死代码 `totalPages` / `resetPagination`**（后者在本轮之前就已无人引用） |
| 各自 `styles/index.scss` | `.pagination-controls`(+`.page-info`) / `.pagination`(+`.page-info`) / `&__pagination` 的按钮样式 | 只保留**纯布局容器**（居中 / 间距），控件样式全部交还共享组件 |

### 关键设计
- **越界页码自动收敛并回派 `update:page`**：`currentPage` 是对 `props.page` 的收敛投影，`watch([() => props.page, totalPages])` + `onMounted` 在**真正不一致时**才回派（与父级现值比较，避免回环）；否则 `total` 变小后会停在空页并显示「第 3 / 2 页」
- **`rows <= 0` 兜底为 1**：否则 `Math.ceil(total / 0)` 得 `Infinity`，总页数直接爆掉
- **`rows` 变更自动回第 1 页**（与被迁移的 imageCompressor 原内联逻辑一致）；同一次交互先派 `update:rows`、必要时再派 `update:page(1)`、最后派一次 `change`
- **页码窗口** `resolvePageLinks(page, totalPages, pageLinkSize)`：以当前页为中心取窗口再回拉保证窗口满，首末页恒显
- **`rowsPerPageOptions` 取 `Array<number | SelectOption>`**：`imageCompressor` 的 `pageSizeOptions` 返回 `{ value: number, label }`（label 由 i18n 模板 `"{num}/页"` 生成）→ 原样透传即保住本地化文案，也避免 i18n 键变孤儿
- **`labels` 承载导航 aria-label**（中文默认值 + 可逐项覆盖）：三处迁移把既有 i18n 文案经 `labels` 注入 ⇒ 用户可见文本零变化、**零 i18n 分片改动、无孤儿键**
- **跳页输入用共享 `Input`**：`Input` 无 `ariaLabel` prop（`aria-label` 会落到其根 `div` 上）⇒ 用 `placeholder` 兼作可见提示与可访问名称（**不改 `Input.vue`**，遵守计划硬约束）
- **不做**：PrimeVue 的 `template` 字符串、各图标槽、`start`/`end` 具名插槽（三处迁移都不需要，也省掉预览 README 的插槽登记）、`pt`/`dt`/`unstyled`

### 踩坑与修项
- **真实缺陷（数值断言抓出）**：页码窗口与末页之间**只隔 1 页时仍跳号**（窗口 3、总 7 页、当前第 4 页 → 渲染 `1 … 3 4 5 7`，漏第 6 页且无省略号提示）。修法：**只隔 1 页就直接补上该页**，间隔 ≥2 页才折叠；左右对称（左侧 `start === 3` 时补第 2 页）
- 这类窗口算法必须用「贴左 / 居中 / 贴右 / 窗口 ≥ 总页数 / 窗口 = 1 / 非整除」组合做数值断言，**光看代码看不出跳号**
- **行数收口**：入口初版 323 行（超 300 警戒线）⇒ 把「状态推导 + 载荷构造 + 报告格式化」整体下沉到 `paginator/state.ts`（纯函数、更内聚）→ 291 行
- **预览局限已登记**：组件用 `v-model:page`，而预览框架 `isControlledComponent()` 只识别 `modelValue` ⇒ 示例是**静态快照**（点页码不改高亮），故示例特意用不同 `page` 值覆盖贴左/居中/贴右/折叠形态；已写入 `componentPreview/README.md`
- **审计发现的超范围项（未动，仅记录）**：`SkillListView.vue` 仍残留原生 `<input>`（搜索框 第 5 行）与原生 `<button>` + `×` 字符（删除卡片 第 36 行），与本次分页迁移无关，属既有违规，建议单独立项

### 验证
- `read_lints`：**0 错误**（新组件 5 文件 + 三处迁移 feature + previewData 聚合）
- `npx tsc --noEmit` 过滤 `Paginator|paginator|useFilteredCards|useImagePagination|SkillListView` → **零报错**
- **`@vue/compiler-sfc` 端到端离线编译 4 个 `.vue` 全 PASS**（`Paginator` / `imageCompressor` / `flashcardReading` / `SkillListView`），parse 与 template 错误均为 0
- **纯函数数值断言 19 项全 PASS**（esbuild 转 CJS 逐项求值）：`resolvePageLinks` 9 例、`resolvePaginatorState` 6 例（空数据 / 末页 / 上下界收敛 / `rows=0` 兜底 / `total<0` 兜底 / `NaN`）、`normalizeRows` / `formatReport`（未知占位符保留）/ `toChangePayload`
- **Sass 离线编译 4 个 `.scss` 全 PASS**（`Paginator.scss` 1115 字节 + 三处迁移的 feature 样式）；`--b3-theme-destructive` 0 残留
- 架构检查：`page-info` / `pagination-controls` / `&laquo;` / `&raquo;` 全项目 **0 残留**；feature 只导入**公开入口** `@/components/Paginator.vue`、**0 处触及 `components/paginator/` 私有目录**；`src/components/**` 内 `@/features` **0 命中**；`Paginator.scss` 硬编码颜色/字体三要素 **0 命中**
- 行数：291 / 69 / 67 / 74 / 60 / 171（全部 < 300 警戒线）；计数 grep 三份文档均为 26
- 留用户验证：`pnpm lint` / `pnpm i18n:verify`（同时确认无孤儿键）/ `pnpm validate:icons`；目视回归三处分页（图片压缩工具栏含每页条数切换、卡片列表视图、技能列表）与预览面板新增 Paginator 分区（四档尺寸、省略号折叠、`alwaysShow:false` 单页隐藏、禁用）
