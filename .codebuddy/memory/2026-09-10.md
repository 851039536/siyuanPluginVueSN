# 2026-09-10

## 新增 feature：componentPreview（组件预览）— plan 7 任务全部完成

用户需求：为 `src/components/` 共享 Codex UI 组件库做一个「组件预览」，展示各组件各种用法，方便查看与修改优化；视觉参考 `docs/codex-ui-reference.html`。

### 澄清结论（用户选择）
1. 承载形态：**插件内独立窗口**（addTab + openTab + openWindow 双形态），非静态 HTML / 非独立 dev 页面
2. 交互深度：**静态用法快照 + 源码复制**（不做交互式 props 调节面板）
3. 覆盖范围：**全部 14 个组件**
4. 示例来源：**结构化清单手写示例**（不做 @vue/compiler-sfc 自动解析 props）

### 交付内容

新增目录 `src/features/componentPreview/`：
- `index.ts`：registerComponentPreview（自挂载 `__componentPreview` + addCommand ⌃⌥V + addIcons `iconComponentPreview`）
- `index.vue`（150 行）：头部（isFloating 隐藏标题）+ 搜索 + NavSidebar + PreviewSection 遍历；滚动/点击双向同步导航高亮（suppressScrollSync 抑制平滑滚动期间抢高亮）
- `types/index.ts`（191 行）：PreviewManager（addTab 模型 + open/openFloating + mountPanel/unmountPanel + destroy）、I18n、PreviewExample、PreviewGroup（含 `importCode` 字段）
- `previewData/`（5 数据文件 + index 聚合）：button.ts(Button/IconWrapper)、input.ts(Input/FormField/Label)、control.ts(Select/Switch/Slider)、tagAvatar.ts(Tag/Badge/Avatar)、display.ts(Card/Chart/Loader)
- `components/`：PreviewSection.vue（分区 + 快照网格 + 单开代码展开）、CodeBlock.vue（等宽代码 + 复制反馈）、NavSidebar.vue（锚点列表 + 计数）
- `styles/`：index.scss / PreviewSection.scss / CodeBlock.scss / NavSidebar.scss
- `README.md`

注册 8 处全落地：`features/index.ts`(export+_Registered)、`src/index.ts`(import+DESTROYABLE_KEYS+registerFeatures)、`config/settings.ts`(enableComponentPreview 默认 true)、`config/icons.ts`(FEATURE_ICONS.componentPreview)、`features/config.ts`(FEATURE_CONFIG 条目 + openComponentPreview action)、`features/superPanel/types/index.ts`(ACTION_EVENT_MAP 映射)、`i18n/{zh_CN,en_US}/componentPreview.json`。

### 设计要点与经验

- **数据驱动渲染**：动态 `<component :is="group.component" v-bind="example.props">` + `slotText` 作默认插槽；`code` 字段与 props 同源手工维护。props 为 `Record<string, any>`，无编译期约束，需人工保证图标键为已注册 IconKey、v-model 组件用 `modelValue`。
- **Select/Input/Slider 组件会从 attrs 剥离 class/style**（`containerAttrs` 解构丢弃），因此**不能靠 props 传 style 控宽**，改由预览 SCSS 容器控制。
- **Loader 组件 height:100%** 依赖父容器显式高度，需单独给 stage `height: 160px`（`cp-card__stage--loader`）才能显示。
- **快捷键冲突检查方法**：`Select-String -Path src\features\*\index.ts -Pattern 'hotkey:'` 汇总已占用键位（本项目 ⌃⌥B/C/D/E/I/N/O/R/T/W 已用），⌃⌥V 空闲。
- **尺寸级固定像素**（图标按钮 22/24px、侧栏 180px、搜索框 200px）项目既有约定允许，但建议加 `// 无对应 Token` 注释对齐既有 18 处写法。
- 架构校验：`universal-arch-skill` 的 `validate-project-structure.py --lang vue --features componentPreview` 输出 **0 错误 0 警告**；read_lints 全模块 0 错误；无跨 feature 直接导入、无绕过统一入口调用。

### 待用户验证
`pnpm lint` / `pnpm i18n:verify` / `pnpm validate:icons` / `npx tsc --noEmit`，以及在思源内打开组件预览窗口观察实际渲染效果。

## 追加：预览卡片尺寸档位（用户需求「可以设置可选大小吗」→ 选定「预览卡片尺寸档位」）

用户四选项（卡片档位/浮动窗口尺寸/组件 size 切换/整体缩放）中只选了**预览卡片尺寸档位**（紧凑/舒适/宽松）。

### 实现
- **档位定义**：新增 `types/density.ts`（`PreviewDensity` 类型 + `DensityOption` + `PREVIEW_DENSITIES` 清单 + `DEFAULT_PREVIEW_DENSITY` + `isPreviewDensity` 守卫）。**关键：拆独立文件而非放 types/index.ts** —— 因 `types/index.ts` 运行时 import `../index.vue`（PreviewManager 挂载面板），若 composable 再从 types/index.ts 运行时导入常量会形成 `index.vue → composable → types/index.ts → index.vue` 运行时循环，有 TDZ 风险；`types/index.ts` 用 `export ... from "./density"` 转出保持对外入口不变。
- **持久化**：新增 `composables/usePreviewDensity.ts`（39 行），`PluginStorage` + `TypedStorage<PreviewDensity>`（key `component-preview-density`）+ `isPreviewDensity` 兜底损坏值；面板 `onMounted` 加载、`setDensity` 切换即落盘（不 await）。
- **UI**：头部 actions 内新增 `.cp-density` 分段切换器（S/M/L 短标签 + i18n tooltip，参照 codex-ui-reference 的 tabs 样式：surface 底 + active 用 background 凸出 + border）。
- **样式联动**：用 CSS 变量做单一数据源 —— `.cp-panel` 定义 `--cp-grid-min/--cp-stage-min-h/--cp-stage-pad/--cp-loader-h`（默认=舒适档），`.cp-panel--compact` / `.cp-panel--spacious` 覆盖（写在 `.cp-panel` 之后，同优先级后者胜）；`PreviewSection.scss` 的网格列宽、舞台高度/内边距、Loader 高度全部改为消费变量，避免三份重复规则。
  - 数值：紧凑 170px/72px/$spacing-2/120px；舒适 230px/96px/$spacing-4/160px；宽松 320px/140px/$spacing-5/220px。
- **i18n**：zh/en 各 +3 键（densityCompact/densityComfortable/densitySpacious），`I18n` 接口同步；键总数 19 对齐。

### 验证
read_lints 0 错误；结构校验 0 错误 0 警告；i18n 19 键对齐；存储键全项目唯一；行数最大 276（红线内）。验证链留用户：`pnpm lint` / `pnpm i18n:verify` / `npx tsc --noEmit`。

## 追加：移除 componentPreview 快捷键（用户「移除快捷键操作」）

**发现真实冲突**：`⌃⌥V` 早已被 **video 功能**占用（`src/features/video/types/index.ts:81`，`config.ts:105` 亦登记 `Ctrl+Alt+V`）。此前我核查快捷键时用了 `src\features\*\index.ts` 通配符，只覆盖到 feature 根目录的 index.ts，**漏掉了 `features/<name>/types/index.ts` 等子目录注册点**（video / formatAssistant / superPanel 的 hotkey 都在 types/index.ts 里）→ 误判 ⌃⌥V 空闲。

处理：移除 `index.ts` 中 `addCommand` 的 `hotkey: "⌃⌥V"`，保留 `addCommand`（仅作命令面板入口，不占默认快捷键）；注释与 README 同步说明冲突原因。i18n 键 `openComponentPreview` 保留（仍被 langKey 使用）。

验证：思源 `ICommand.hotkey?: string` 确认为可选（`node_modules/siyuan/siyuan.d.ts`），移除后类型安全；递归复查全项目 hotkey 占用表，componentPreview 已无 hotkey；read_lints 0；结构校验 0/0。

**顺带发现的既有冲突（非本次引入，未改动）**：`tableOfContents` 的 `⌃⌥I` 与 `ideaGenerator` 的 `⌃⌥I` 重复。

## 追加：componentPreview 集成到底部功能列表（用户「功能开关集成到底部功能列表」）

集成机制（`src/features/statusBar/featureRegistry.ts` 的 `buildFeatures` 数组 = 状态栏功能抽屉数据源，加一条即可）：

1. `featureRegistry.ts`：新增 `getI18nShard(plugin, "componentPreview")` + 条目 `{ id: "componentPreview", icon: "mdi:view-grid-outline", color: "#0ea5e9", title: componentPreviewI18n.title || "组件预览", pinnable: true, shortcut: { icon: "mdi:view-grid-outline", itemClass: "action-item component-preview-item" }, action: () => emitCustomEvent("openComponentPreview") }`
2. `statusBar/styles/index.scss`：按既有 `--status-color-*` 约定加变量 `--status-color-component-preview: #0ea5e9` + `.component-preview-item { color: ... }`

**自动获得的能力（零额外代码）**：
- **功能开关角标**：`toggleable` 由 `featureIdToSettingKey(id) in plugin.settings` 自动判定 → id `componentPreview` 推导出 `enableComponentPreview`（已在 settings.ts 定义），抽屉项自动带开关角标，点击经 `plugin.updateSettings` 持久化
- **pin 到状态栏快捷区**：`pinnable: true` + `shortcut` 字段；pin 后 `visibleShortcuts` 渲染 MonitorItem，点击走 `action`
- **自定义分类**：非 monitor 项自动参与 `useFeatureCategories` 分类归属

**事件链路验证**：`emitCustomEvent` 默认在 `window` 派发 CustomEvent（`src/utils/eventBus.ts`），`PreviewManager` constructor 里 `window.addEventListener("openComponentPreview", ...)` 正好接收 ✓。

验证：read_lints 0；结构校验（componentPreview + statusBar）0 错误 0 警告。

## 更正：「大小设置」是组件尺寸，不是卡片尺寸（推翻上一条卡片档位实现）

用户澄清原话：「大小设置是组件的尺寸，不是卡片的」。此前我在四选一中理解偏了（做了卡片/网格尺寸档位），实际需求是**组件自身 `size` prop 的档位切换**。

**重构内容（替换而非叠加）**：
- 删除 `types/density.ts` + `composables/usePreviewDensity.ts`（卡片档位）
- 新建 `types/size.ts`：`ComponentSize = "xsmall" | "small" | "medium" | "large"`、`SizeOption`、`COMPONENT_SIZES`、`DEFAULT_COMPONENT_SIZE = "small"`、`isComponentSize`
- 新建 `composables/usePreviewSize.ts`：storage key `component-preview-size`（旧 key 作废）
- `PreviewGroup` 新增 `sizeable?: boolean`；11 个支持 size 的组件标记为 true（Button/Input/FormField/Label/Select/Switch/Slider/Tag/Badge/Avatar/Card）；**Chart（size 为预设像素宽高，大档撑破卡片）/ IconWrapper（size 为像素数）/ Loader（无 props）不参与**
- `PreviewSection.vue` 新增 `size` prop + `resolveProps(example)`：`sizeable` 且示例**未显式指定 size** 时注入全局档位；显式指定 size 的示例（尺寸对比用例）保持原样，避免标题与实际渲染不符
- 回退卡片布局：`.cp-panel` 的 4 个档位 CSS 变量与 `.cp-panel--compact/spacious` 全部移除，网格列宽回 `minmax(230px,1fr)`、舞台回 96px/Loader 160px；切换器类名 `.cp-density*` → `.cp-size*`
- i18n：`densityCompact/Comfortable/Spacious` → `sizeXsmall/sizeSmall/sizeMedium/sizeLarge`（zh/en 20 键对齐）

验证：density 残留 grep 0；sizeable 标记 11 处；i18n 20 键对齐；read_lints 0；结构校验 0/0；行数最大 279。

**经验**：需求澄清阶段我给的选项描述里「组件 size 参数切换」与「预览卡片尺寸档位」区分度不够，导致用户误选；涉及"大小/尺寸"类需求时，应先用一句话确认是**被预览组件自身的尺寸**还是**预览界面/容器的尺寸**。

## 追加：组件 size 档位字号阶梯统一（用户「组件尺寸改到 xs，字体跟 s 一样大，这样不合理」）

用户从 componentPreview 切到 XS 档发现字体与 S 档完全相同，要求 XS 真正变小。澄清结论：XS 档统一 10px；范围＝全部 14 个组件（凡 XS 非 10px 一律对齐）；Card 标题全档改 10/12/14/16；Switch 标签新增四档变体 10/12/14/16。

### 改动（7 个 SCSS，纯字号声明）
| 文件 | 变更 |
|---|---|
| `Button.scss` | `&--xsmall` 字号 `$font-size-xs`(12px) → `$font-size-2xs`(10px) |
| `Input.scss` | `&--xsmall .si-input__field` 同上 |
| `Select.scss` | `&--xsmall` 的 trigger/option 改 2xs；并新增 `.si-select__filter-input / __empty / __group-label` 的 2xs 覆盖（XS 档下拉内部随选项联动）；L107 注释改为「与下拉选项默认档字号一致（XS 档在 &--xsmall 内单独覆盖为 10px）」 |
| `Avatar.scss` | `&--xsmall` 12px → 10px，删除过时注释「10px 无对应 Token」 |
| `Card.scss` | 标题 12/14/16/18 → **10/12/14/16**；副标题 10/12/12/14 → **10/10/12/14**（= 标题降一档、下限 10px，保证标题不小于副标题；XS 档同号靠 semibold 字重 + 弱化色区分） |
| `Switch.scss` | `&__label` 原固定 `$font-size-sm`(14px) → 基础声明改 `$font-size-xs` 兜底 + 四档反向选择器覆盖 `.si-switch--xsmall/small/medium/large &` = 10/12/14/16 |
| `Badge.scss` | 字号本已合规（XS 10px），仅清理「原 10px/11px 无对应 Token」过时注释，并补档位标注 |

未动：Tag / Label / FormField / Slider（XS 已 10px）、Chart（仅宽高）/ IconWrapper（像素数）/ Loader（无 props）。

### 文档同步
- `AGENTS_STYLE.md` § 字号层级：新增「### 组件 size 档位字号阶梯（两级字号制的唯一例外）」——四档 = `$font-size-2xs/xs/sm/base` = 10/12/14/16，明确这是两级字号制中**唯一**允许 10px 出现在控件正文的场景，且例外仅限 `src/components/styles/*.scss` 的档位变体，**不得**据此在 feature 业务样式把正文降到 10px；只改字号、不联动 padding/min-height/gap/图标尺寸。
- `src/features/componentPreview/README.md`：尺寸档位条目补充四档字号阶梯 + Card/Switch/Select 变更说明。

### 影响面（已向用户明示并获确认）
- 全插件 137+ 处 `size="xsmall"` 文字 12px → 10px。
- Card/Switch 默认档位是 `small` → 未显式传 size 的 Card 标题 14→12px、副标题 12→10px，Switch 标签 14→12px。
- 预览面板自身（切换器 / 持久化 / `resolveProps` 注入逻辑 / i18n）零改动。

### 验证
`universal-arch-skill` 模式 C 审查：`src/components/styles/*.scss` 中 `font-size:\s*\d` / `font-weight:\s*\d` / `line-height:\s*[\d.]+` 硬编码 grep **0 命中**；12 个 SCSS 的 `font-size` 声明全部取自 `$font-size-*` Token；read_lints 0 错误。剩余验证留用户：`pnpm lint` / `npx tsc --noEmit` + 预览面板逐档目视。

## 追加：S/M/L 档内边距各减 2px（用户「S M L 内间距缩一点点」）

澄清结论：范围＝全部含档位的组件；幅度＝水平与垂直各减 2px。

### -2px 的 Token 映射（关键经验）
全局间距 Token 为 4/8/12/16/20/24px，减 2px 后多数落在**非 Token 值**上。可用映射：
| 原值 | -2px 后 | 写法 |
|---|---|---|
| 4px `$spacing-1` | 2px | `$spacing-2px` |
| 8px `$spacing-2` | 6px | `m.$gap-xs`（_mixins 本地 6px） |
| 12px `$spacing-3` | 10px | `m.$spacing-2_5`（_mixins 本地 10px） |
| 16px `$spacing-4` | 14px | **无 Token**，硬编码 + `// 14px 无对应 Token` |
| 20px `$spacing-5` | 18px | **无 Token**，硬编码 + `// 18px 无对应 Token` |
| 10px `m.$spacing-2_5` | 8px | `$spacing-2` |
| 6px `m.$gap-xs` | 4px | `$spacing-1` |
| 5px（Badge 硬编码） | 3px | `$spacing-px`（=3px） |
| 14px（硬编码） | 12px | `$spacing-3` |
| 18px（硬编码） | 16px | `$spacing-4` |

### 逐组件改动
- **Button**：S `8px 12px`→`6px 10px`、M `8px 16px`→`6px 14px`、L `12px 20px`→`10px 18px`；同时 `gap` S 4→2px、M 8→6px、L 12→10px
- **Input** `.si-input__field`：S `4px 8px`→`2px 6px`、M `8px 12px`→`6px 10px`、L `12px 16px`→`10px 14px`
- **Select**：trigger 与 option 同步减 2px（S `2px 6px`/`2px 14px`、M `6px 10px`/`6px 14px`、L `6px 14px`/`6px 14px`）
- **Card**：header/footer/body 各减 2px（S `8px 10px`/`10px`、M `12px 14px`/`14px`、L `16px 18px`/`18px`）
- **Tag**：S `2px 8px`→`2px 6px`（纵向 2px 已到下限保留）、M `4px 10px`→`2px 8px`、L `6px 14px`→`4px 12px`
- **Badge**：仅水平（纵向本就是 0）——S 5→3px、M 6→4px、L 8→6px
- **Switch**：根 `gap` 12px→10px（轨道与标签间距，用户点名；该值非档位变体，故四档统一生效）

### 未改动的组件及原因
- **Slider**：档位差异只在轨道高度与滑块直径，**无内边距可缩**；`__value` 的 min-width 是数值预留宽度，缩小会挤字，故不动
- **FormField / Label / Avatar**：档位只控制字号 / 直径，无内边距
- **Chart（宽高）/ IconWrapper（像素数）/ Loader（无 props）**：不适用
- **XS 档**：用户只要求 S/M/L，XS 内边距保持不变

### 影响面
Button / Input / Select(trigger) / Card 有 min-height 兜底，纵向减 2px 后仍被 min-height 支配 → **高度不变，仅宽度各缩 2px**；Select 的 option 无 min-height → 下拉选项行高减 4px。所有组件默认 size 均为 `small`（已核对 11 个 .vue），故基础 `padding` 声明实际是未使用兜底。

验证：read_lints 0 错误；逐档 grep 复核 padding 值全部落位。

## 追加：Button 组件全面优化（用户「https://primevue.dev/button/ 组件UI参考，看下有什么可以优化的？给出建议」→ 选定「全部 11 项」）

对照 PrimeVue 5.0.1 Button 文档完成 11 项 + rounded。改动文件：`src/components/Button.vue`（104→177 行）、`src/components/styles/Button.scss`（189→311 行）、`src/components/styles/_mixins.scss`（+37 行）、`src/features/componentPreview/previewData/button.ts`（15→26 个示例）、`AGENTS_STYLE.md`（+32 行新章节 + 2 处表格行）。

### 5 项真实缺陷（均已核实为真 Bug，非推测）
1. **键盘焦点不可见**：`Button.scss` 只有 `outline: none` 无 `:focus-visible`（全项目 16 个组件 SCSS 中仅 Switch/Select 有）。修复：`&:focus-visible { outline: 2px solid var(--b3-theme-primary); outline-offset: 1px; }`。
   - **经验**：`_mixins.scss` 的 `focus-ring` mixin 只改 `border-color`，而 `.si-button` 的 border 是 `1px solid transparent`、实底变体不设 border-color → **单用该 mixin 在实底按钮上完全不可见**。焦点环要用 outline（同 Switch.scss），不能无脑复用 focus-ring。
2. **loading 时 spinner 压字**：`m.btn-spinner` 绝对居中 + 文案也居中 → 完全重叠。生产实证 `imageCompressor/index.vue:126`（`:loading` + 插槽"压缩中…"）。修复：图标条件由 `v-if="icon && !loading"` 改为 `v-if="icon"`，loading 时对 `__icon`/`__text` 用 `visibility: hidden`（保留占位 → **按钮宽度零跳变**，优于 PrimeVue 的「spinner 替换内容」会塌宽）。
3. **block 文案靠左**：`&__text { flex: 1 }` 吃掉全部剩余空间使 `justify-content: center` 失效，span 自身默认 `flex-start`。全项目 0 处用 `block`，仅预览暴露。修复：`&--block .si-button__text { justify-content: center }`（保留 flex:1 以维持 icon-right 贴右）。
4. **图标不随档位缩放**：spinner 已 12/14/16/18 但 `iconSize` 恒 16。修复：`iconSize` 默认改 `undefined` + `TIER_ICON_SIZE` 查表（12/14/16/18）；3 个文件 11 处显式传值（flashcardReading）优先级最高不受影响。
5. **硬编码色值 + 深色主题 active 失效**：`#ef4444`→`$color-danger-bright`（精确等值 hsl(0 84.2% 60.2%)）、`#dc2626`→`$color-danger`（精确等值 hsl(0 72% 51%)）、`rgba(239,68,68,*)`→`rgba($color-danger-bright, α)`；`--ghost:active` 的 `rgba(0,0,0,0.08)` 深色主题下不可见 → 改 `var(--b3-theme-surface-light, $color-surface)`。

### 6 项增强（零破坏）
6. **颜色轴 × 外观轴解耦**：新增 `severity`（6 色，覆盖 variant 推导色）+ `outlined`/`text` 布尔修饰。**关键设计**：新增类名一律加前缀（`--severity-*`/`--outlined`/`--text`），不复用不改写既有 `--primary`/`--secondary`/`--success`/`--danger`/`--ghost` 五个类 → 全项目 180+ 处调用渲染不变。
   - **颜色族用 CSS 自定义属性传递**：`_mixins.scss` 新增 `@mixin btn-color-family($theme-var, $fallback, $on-color)` 输出 `--btn-color`/`--btn-on-color`/`--btn-soft`/`--btn-soft-strong`；弱底色用相对颜色语法 `hsla(from var(--x, $y) h s l / 0.12)`（**同 Tag.scss 既有写法**，明暗主题自动正确）。这样 `--outlined`/`--text` 只需写一份通用规则，靠颜色块设的变量取色，避免 7 色 × 2 外观 = 14 个复合选择器。
   - **覆盖靠源码顺序**：SCSS 顺序 = 颜色块 → severity 块 → 外观修饰块，同特异性 (0,1,0) 靠后者胜；hover/active 均为 (0,4,0) 同样靠顺序。
   - **`--ghost` 刻意不设颜色族变量** → `interactive-soft` 回退到 `var(--b3-theme-surface-lighter)`，hover 与改动前**完全一致**（避免 104+ 处 ghost 按钮 hover 观感漂移）。
7. **交互反馈统一**：`interactive-raised` 新增 `$lift: false` 参数，**默认去掉 `transform: translateY(-1px)`**（消除密集工具栏 1px 抖动）；新增 `@mixin interactive-soft`（透明底按钮用主题感知弱底色，结构同 raised：0.12s + 无位移）→ danger/ghost 首次获得 transition。
8. **补 info / warning 变体**（`--b3-theme-info` / `--b3-theme-warning`）。
9. **补 `type` prop**（`button|submit|reset`，默认 button），替代硬编码 `type="button"`。
10. **无障碍命名**：新增 `title` / `ariaLabel` prop；`aria-label` **仅在纯图标场景**（`!slots.default && icon`）从 `title` 派生 —— 不覆盖文字按钮的可读标签（WCAG 2.5.3 考虑）；`import.meta.env.DEV` 下对两者皆缺的纯图标按钮 `console.warn`。
11. **`iconPosition` 扩为四向**：`--icon-top { flex-direction: column }` / `--icon-bottom { column-reverse }`，并覆盖 `&__text { flex: none }`（纵向下 flex:1 会撑高错位）。
12. **`rounded`**（`$radius-full`）+ 预览面板补 11 个新示例。

### 规范澄清（用户点名的「26/28px 对齐」）
`AGENTS_STYLE.md` 的「图标按钮固定 26×26」源自 `docs/codex-ui-reference.html` 的 feature 自建 `.icon-btn`（无档位）；共享 `<Button icon-only>` 是分档 API（22/28/36/44，默认 small=28 与文字按钮 min-height 对称）。**采用文档澄清路线**，不把 small 改成 26px（属破坏性视觉变更）。新增 `## 强制规则：按钮交互与无障碍（2026-09-10）` 章节固化 focus/loading/a11y/尺寸图标约定。

### 已知的既有语义变化（有意为之，需知悉）
- primary/secondary/success hover 不再有 1px 上浮（原设计抖动源）
- `variant="danger"` hover 底色由 `rgba(239,68,68,0.1)` → 主题 error 色 12% 透明；hover 不再加深边框（`#dc2626` 移除）
- `--secondary` 的 var fallback 修正（`$color-info`/`$color-bg` 误用 → `$color-bg`/`$color-fg`/`$color-border`），仅 var 缺失时生效

### 验证
- 架构审查（universal-arch-skill 模式 C）：Button.scss / _mixins.scss 硬编码色值（hex/rgba）与硬编码 font-size/weight **0 命中**；`Button.vue` 的 `<style>` 仅 `@use`；13 个产出类名全部在 SCSS 有对应规则（无悬空类名）；括号配平 48/48、20/20；read_lints 0 错误
- **行数**：Button.scss 311 行 —— 超 300 警戒线 3%，**远低于 500 硬阈值**，故未按计划拆分为 partial。理由：拆分需引入 `@use` 顺序依赖（外观修饰必须晚于颜色块），footgun 大于收益；且 AGENTS_STYLE 命名规则限定 `_` 前缀仅用于 mixins/变量 partial，纯规则 partial 无合规命名
- 剩余验证留用户：`pnpm lint` / `npx tsc --noEmit` / `pnpm validate:icons` + 预览面板逐项目视（尤其 loading 保宽、block 居中、outlined/text 取色）

## 追加：Button.vue 冗余与性能审查（用户「审查 Button.vue 是否有冗余，和性能问题」）

### 唯一真实缺陷：`isIconOnly` computed 依赖非响应式的 `slots`
**源码级证据（Vue 3.5.40）**：`node_modules/.pnpm/@vue+runtime-core@3.5.40/.../runtime-core.esm-bundler.js`
- `:5349` `const slots = instance.slots = createInternalObject()`
- `:4882` `const createInternalObject = () => Object.create(internalObjectProto)` → **普通对象，非 reactive**
- `:5341` `assignSlots` 原地 `slots[key] = children[key]`（`updateSlots` 复用同一对象）

→ `computed(() => !slots.default && !!props.icon)` 只在 `props.icon` 变化时重算；插槽增删不触发。而模板 `v-if="$slots.default"`（Button.vue:23）是渲染期实时读取 → **两者会不一致**：
- 初始无插槽（icon-only 方形固定宽高 28px）→ 后出现插槽 → 文字渲染了但 `si-button--icon-only` 还在 → **文字溢出方块**
- 反向：插槽消失但类名丢失 → 不再是方形

**影响面**：全项目**当前 0 处触发**（多行搜索 `<Button[^>]*>\s*<(template|span|IconWrapper|div)[^>]*v-if` 无命中）→ 属潜在缺陷。

**修复**：`buttonClasses` / `isIconOnly` / `accessibleName` 从 `computed` 改普通函数（渲染期必然重算，与模板 `$slots` 读取对齐）；computed 在此处的缓存是**有害的**（缓存了非响应式快照）。

### 冗余（2 项）
1. `disabled || loading` 重复 2 处（模板 `:disabled` Button.vue:7 与 `si-button--disabled` Button.vue:131）→ 可抽 `isDisabled`
2. `handleClick` 的 `!props.disabled && !props.loading` 守卫是**死代码**：原生 `:disabled` 不派发 click + `pointer-events: none`（Button.scss `--disabled`/`--loading`）→ 永不生效。保留作防御无妨

### 性能：无实际问题
`import.meta.env.DEV` 构建期静态替换 → 生产包无 `console.warn` 分支；`@click="handleClick"` 用稳定函数引用（非每次渲染新建箭头函数）；无 watcher / 定时器 / DOM 查询 / 深监听；`useSlots()` 只取引用无开销；`withDefaults` 已正确省略 `severity`/`iconSize`/`title`/`ariaLabel` 的冗余默认值。

### 复用结论
**Vue 的 `slots` 不是响应式**（见上源码证据）——凡「派生值需要跟随插槽增删变化」的场景，必须用普通函数或直接在模板读 `$slots`，不能用 `computed`。已同步到 MEMORY.md。

### 用户决定（2026-09-10）
审查发现的问题（`isIconOnly` computed 陈旧 + 2 项冗余）经确认 **暂不修改**，`Button.vue` 保持现状。属**已知并有意保留**的状态——后续会话不要主动重提或擅自"修复"这三点。

## 追加：AGENTS.md 新增「共享组件库使用规则（强制）」（用户「更新agent.md规则，使用通用组件时参考用法？」→ 选定「查阅 + 同步 + 优先复用」最完整一层）

### 背景缺口
改动前 `AGENTS.md` **完全没有**共享组件（`src/components/`）的使用规则，只在「关键文件速查」的目录树里提了一句「共享 shadcn-vue 组件（Button/Input/Select/Switch/Tag 等）」。而 `src/components/` 已被 180+ 处调用。

### 落地内容（全部在 `AGENTS.md`）
1. **新增 `## 共享组件库使用规则（强制）` 章节**（插在「统一入口原则」与「子组件数据流规则」之间，L158-209，53 行），三条主线：
   - **先查用法，禁止猜 props**：查询途径三级 —— 组件预览面板（推荐，真实渲染快照 + 可复制代码）→ `previewData/*.ts` 清单（props 与 code 同源）→ 组件源码 `interface Props`（最终事实来源）。另约束：图标只能传已注册 `IconKey`；尺寸档位统一 `xsmall/small/medium/large`（默认 small）
   - **优先复用，禁止 feature 自建同类控件**：12 类控件必须用共享组件；缺能力时**先扩展共享组件**（加可选 props 保持向后兼容）再消费。允许例外：纯展示局部布局容器、无档位的 26×26 `.icon-btn`
   - **组件清单表（14 个）**：逐组件列出职责 + 关键 props（Button/Input/Select/FormField/Label/Switch/Slider/Tag/Badge/Avatar/Card/Chart/IconWrapper/Loader），props 全部从各组件 `interface Props` 实读核对，非猜测
   - **改 API 必同步预览清单**：`previewData/*.ts`（props 与 code 一致）+ `componentPreview/README.md`
2. **「硬规则」新增 3 条 bullet**：共享组件先查后用 / 优先复用 / 组件 API 变更同步预览清单
3. **「规则分片索引」新增一行**：指向 `src/features/componentPreview/README.md`（使用场景：使用共享组件前查用法、或改 API 后同步清单）
4. **「关键文件速查」目录树**：`components/` 行改为「共享组件库 14 个（Button/Input/Select/Card/Chart 等）— 使用规则见「共享组件库使用规则」」

### 锚点链接校验
`./AGENTS_STYLE.md#强制规则按钮交互与无障碍2026-09-10`、`#核心规范速查表` —— 与文件内既有锚点风格（如 `#强制规则背景与过渡对齐-gitpush-范式2026-09-02`）一致：去标点、空格转连字符、英文小写。
