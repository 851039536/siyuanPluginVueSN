# 2026-09-11

## 全项目硬编码合规审查（按规则，4 类全量）

用户诉求：「根据规则审查项目硬编码」。澄清后口径：范围 = 全项目 `src/`；类别 = SCSS 设计 Token / i18n 文案 / 业务常量 / 图标与 emoji 四类全选；交付 = **报告 + 修复计划（不改代码）**；判定 = **务实口径**（排除规范明示例外与历史约定）。

### 交付物（本次新增 3 个，未改动任何 `src/**`）

| 产物 | 规模 | 说明 |
|---|---|---|
| `scripts/audit-hardcode.mjs` | 981 行 | 只读扫描器。31 条规则，四类分组；内建 15 类例外白名单；`--dry` 只扫不写、`--top N` 打明细。已知超出 500 行警戒线（脚本非 `src/`，未拆分） |
| `docs/hardcode-audit.md` | 542 行 | 审查报告：结论摘要 / 口径与 15 类例外 / 31 条规则矩阵 / P0-P3 明细 / 复核校正记录 / 9 批次修复计划 / 不建议修清单 |
| `docs/hardcode-audit.data.json` | 1585 KB | 机器可读基线，供修复后复跑 diff 做回归守卫 |

### 定稿基线

扫描 1292 文件 / 212915 行（**`src/features/` 实际 1222 文件**，远多于 IDE 早期快照的 596——`gitPush` 209、`statistics` 90、`toolCollection` 75、`docAnalysis` 50）。

**命中 3613 处（P0 0 / P1 1422 / P2 2076 / P3 115），按例外排除 1495 处。**

| 类别 | 命中 | 文件 | 分布 |
|---|---|---|---|
| SCSS 设计 Token | 2633 | 251 | P1 510 / P2 2020 / P3 103 |
| i18n 文案 | 609 | 95 | P1 609 |
| 业务常量 | 124 | 74 | P1 68 / P2 56 |
| 图标与 emoji | 247 | 84 | P1 235 / P3 12 |

### 三个高价值结论

1. **9 个 `.vue` 内联样式块共 402 行**（违反样式分离强制规则）：`formatAssistant/index.vue` 256 行最重、`unitConverter/ASCIIConverter.vue` 69、`video/VideoPlayerDialog.vue` 38、`App.vue` 15，另 5 个 1~13 行。多数内联块**同时含硬编码字号/字重/圆角/间距**（双重违规）。
2. **232 处图标名未在 `src/config/icons.ts` 注册**（`mdi:` 201 + `ph:` 31，分布 77 文件）。它们**能正常渲染**（`iconifySetup` 已离线预加载 MDI + Phosphor，`validate:icons` 只校验注册表条目），因此从未被告警，但违反「只能传已注册 IconKey」硬规则。
3. **`src/features/globalRelations/styles/index.scss` 单文件犯五类硬编码**（字号 15 / 字重 5 / 行高 3 / 圆角 7 / 间距 29），整文件未 Token 化，单文件重写性价比最高。

### 安全性结论

**P0 = 0，无硬编码凭据。** 独立交叉验证 `sk-` / `apiKey` / `Bearer` / `secret` 模式后确认无泄漏。`src/utils/settingsCrypto.ts` 用的是「内置于代码的应用层加密密钥材料 + PBKDF2 派生」（文件头已声明「防偶然浏览，不防反汇编」），属**既定设计，不应被当作硬编码密钥「修复」**。

### 扫描器自身的两个坑（写同类脚本必防）

1. **对象字面量键覆盖**：`ctx = { file, line: i + 1, line, trimmed }` —— 后写的 `line`（源码文本）覆盖了行号，导致 `findings[].line` 存的是文本。表现极隐蔽：只有自定义处理器（不走 `r.pattern`）的规则行号是对的，其余全错，且**没有任何报错**。修法：原名改 `raw`，`line` 只存数字；同时必须同步修正所有 `ctx.line` 消费点（漏改一处会让 `RegExp.exec(数字)` 静默无匹配，规则集体失效）。
2. **用 `m[0]` 做前缀判定会让规则整体失效**：`icon/unknown-name` 的 `ICON_SET_PREFIX.test(ctx.match)`，而 `ctx.match = m[0]` 含首尾引号（`'"mdi:x"'`）→ 恒为 false → 该规则**零命中且在 `excluded[]` 里假装是「非 Iconify 图标名」**。必须用捕获组 `m[1]`。

另修正 3 类初版误报：`$spacing-2px` 等 `$var` 引用被 `mapLengths` 当字面量、`|| ""` 空串类型兜底被计为 i18n 兜底、`disk-browser-settings` 中的 `sk-` 被误判为 API Key 前缀。

### 务实口径的 15 类例外（已代码化进扫描器常量）

`src/_variables.scss` / `src/index.scss` / `src/utils/timerRegistry.ts`；组件库 `&--xsmall..--large` 档位字号；行内 `// 无对应 Token`；`var(--b3-*, fallback)` 内字面量；`--b3-theme-primary` 的 `$color-danger` fallback；`rgba(0,0,0,0.5)` 遮罩；`Slider.scss` 焦点环 fallback（已知问题勿单独修）；`icons.ts` 已注册 IconKey；非网络请求 URL；AI 端点/模型集中定义文件；集中数据表（`aiModels`/`providers`/`pricing`/`docAnalysis/types`/`gitPush/types/meta`/`shortcut/data`）；`src/components/**` 与 `componentPreview/previewData/**` 的 i18n 豁免（组件库刻意零 i18n 依赖）；emoji 内容预设与生成模板；领域数据表（`apiDebugger/types`、`gitConfigDesc`、`coverStyles*`、`skillLearning/data`）；注释行。

补充：`--b3-theme-destructive` **属真违规**（该变量从未定义，应为 `--b3-theme-error`），残留 4 处 = `Tag.scss`(3) + `Badge.scss`(1)。

### 未做（第一轮审查阶段）

未改动任何 `src/**`；未执行 `pnpm lint` / `pnpm vite build`（按项目约定由用户执行）；未改动 `package.json`（建议后续可加 `"audit:hardcode"` 脚本）。

---

## 修复第一轮（同日，用户「开始执行」后）

按报告 § 8.2「首轮动手顺序 1–4 项」执行，改 5 个文件 + 新建 1 个：

1. `src/components/styles/Tag.scss`(3) + `Badge.scss`(1)：`--b3-theme-destructive` → `--b3-theme-error`
2. `src/features/aiContentGenerator/components/styles/SkillPreviewModal.scss`：删弹窗主样式 `box-shadow`（已有 border 兜底）；遮罩 `0.45` → `0.5`
3. `src/features/globalRelations/styles/index.scss`（387 行）：**整体 Token 化重写**。关键点：原文件**完全没有 `@use`**，须补 `@use "@/variables.scss" as *;` 才能用全局 Token
4. `src/features/formatAssistant/index.vue`：311 行内联 `<style>` 迁出 → `@use "./styles/index.scss"`
5. `src/features/formatAssistant/styles/index.scss`：**新增**，承接并 Token 化

### 命中变化（同口径复跑）：3613 → 3288（−325）；P1 1422→1139 / P2 2076→2034 / P3 115→115

- `scss/undefined-token` **4 → 0**（已清）
- `scss/font-weight` **5 → 0**
- `scss/in-vue-inline` 402 → 146（−256，文件数 9→8）→ **9 个内联样式文件还剩 8 个**
- `spacing` 1139→1110、`border-radius` 132→125、`font-size` 28→13、`line-height` 58→55、`transition-duration` 133→131、`color-functional` 167→165、`color-literal` 288→287、`box-shadow` 37→36
- `globalRelations` 的 SCSS 命中全清（仅剩 `index.ts:15` 的 1 处 i18n 兜底）；新增样式文件 0 命中

### 两个可复用的做法

- **无对应 Token 的值不散落注释，改为文件顶部集中声明局部变量**：`$gr-space-10: 10px; // 无对应 Token`、`$fa-font-11: 11px; // 无对应 Token`。好处：①单一来源、②注释集中在最显眼处、③扫描器的 `scssLiteralExempt` 靠行内 `无对应 Token` 判定，定义行能被正确豁免。已在两个文件落地
- **跨文件成对常量要保留并注明**：`$gr-accent: #06b6d4` 必须与 `icons.ts:328` 的 `globalRelations` 图标色一致，且全局 `$color-*` 无语义青色档位 → 标注 `// 无对应 Token` 保留，不做「Token 化」
- 同类：`$fa-preview-bg: #fff`（预览区须固定白底对齐公众号阅读态，无 Token）；`border-radius: 50%`（圆形几何，非圆角 Token 场景）保留

### 顺带修正的非硬编码项（对齐 AGENTS_STYLE）

- 8 处 `border: 1px solid var(--b3-theme-surface-lighter)` → `var(--b3-border-color)`（该变量落在「相邻色陷阱」区间，不应用于画分隔线；作为 **背景填充** 的 3 处保留）
- `.close-btn:hover` 背景 `rgba(0,0,0,0.08)` → `var(--b3-list-hover)`
- `.input-textarea` 字体栈 → `$vp-mono`；根容器补 `font-size: $font-size-xs`（审查检查点 1）

### 验证记录（本机可复用的三条命令）

| 验证 | 命令/方式 | 结果 |
|---|---|---|
| IDE 诊断 | `read_lints` | 6 文件 0 错误 |
| Sass 离线编译 | `node -e` + `sass.compileAsync(file, { importers: [{ findFileUrl(u){ if(!u.startsWith('@/')) return null; return new URL('file:///'+root+'/src/'+u.slice(2)) } }] })` | 两文件均 OK（**注意**：`import sass from 'sass'` 已废弃，改用 `import * as sass`） |
| SFC 结构 | 正则计数 `<template>/<script>/<style>` 各恰好一对 + 行数 | `formatAssistant/index.vue` 439 行，结构完整 |

**`@vue/compiler-sfc` 不可用**：它是 `@vitejs/plugin-vue` 的传递依赖，未提升到根 `node_modules`，`node -e "import('@vue/compiler-sfc')"` 会 `Cannot find package`。要离线校验 SFC 需另装或走 `vue-tsc`（用户执行）。

### 踩到的两个操作坑

1. `node scripts/audit-hardcode.mjs --dry` **不写盘**，若随后用 JSON 查询会读到**上一轮旧基线**（曾据此误判「formatAssistant 未修复」）。核对前必须先跑一次不带 `--dry` 的命令落盘
2. PowerShell 下 `node -e` 里含 `lang="ts"` 这类**双引号字面量会被外层引号吞掉**导致语法错误 → 改用正则字面量或避免双引号

### 下一轮

**B1 SCSS 间距 Token 化**（剩 1110 处 / 164 文件；头部 `statistics/MilestoneRuleEditor.scss` 68、`gitPush/styles/index.scss` 44、`htmlViewer/styles/index.scss` 42）。**前置检查**：逐文件确认是否已 `@use '@/variables.scss' as *;` —— Sass 变量不跨文件传递，缺导入直接替换必编译失败（`globalRelations` 就是这种情况）。

---

## 修复第二轮（同日「继续」）：B5 样式分离收尾 —— 内联样式全清零

改 8 个 + 新增 8 个文件，`scss/in-vue-inline` **146 → 0**。

| 文件 | 要点 |
|---|---|
| `src/styles/app.scss`（新） + `src/App.vue` | App.vue 的 `.plugin-app-main` 宿主布局（纯布局无硬编码）。**新建了 `src/styles/` 顶层目录**承接无 feature 归属的样式，保留 scoped 语义 |
| `unitConverter/styles/{BaseConverter,BaseUnitConverter,ASCIIConverter}.scss`（新）+ 对应 `.vue` | 全部 Token 化；`.vue` 用**双行导入**（组件专属 + `index.scss`） |
| `video/styles/{VideoDownloadDialog,VideoPlayerDialog}.scss`（新）+ 对应 `.vue` | 同上 |
| `rssReader/styles/index.scss` + `index.vue` | `.loading-icon` 内联规则移入既有 index.scss（紧邻既有 `@keyframes spin`） |
| `htmlViewer/components/SnippetLibrary.vue` | srcdoc 内嵌 `<style>` 提为具名常量 `PREVIEW_DOC_STYLE` |
| `unitConverter/styles/index.scss` | **删除模块本地 `$mono` 字体栈**，7 处 `font-family` 改用全局 `$vp-mono`（AGENTS_STYLE 禁止各模块重复声明 `$vp-mono`） |

### 关键判定：`ASCIIConverter` 的 `@include index.label-style`

原 `.vue` 用 `@use "../styles/index.scss";`（**不带 `as *`**）是为了拿 `index` 命名空间调 `index.label-style` mixin。迁出到 `styles/ASCIIConverter.scss` 后必须写 `@use "./index" as index;` 才能继续用该 mixin，同时 `.vue` 保留 `@use "../styles/index.scss";` 以维持原 CSS 发射范围。**迁移内联样式前必须检查是否有 `@include 命名空间.mixin` 依赖**，忽略会直接编译失败。

### 两个工具修正

1. **`scanVueStyleBlocks` 误报修正**：原正则扫全文件，把 `SnippetLibrary.vue` 里 `iframe srcdoc` 字符串中的 `<style>` 当成组件内联样式。改为**只扫 SFC 顶层 `<style>`**（先 `blankOut` 掉 `<script>` / `<template>` 区域，保留行号）。该类「隔离文档内嵌样式」（srcdoc / 导出产物 / 生成内容模板）属**合法例外**，不应计入
2. **`scss/z-index` 口径偏差修正**：实现里对任意 `≠10000` 都报，而报告 § 三 写的是「≥1000 且 ≠10000」。改为 `v >= 1000 && v !== 10000`，命中 84（66 文件）→ **39（32 文件）**。顺带说明：`z-index: 4` 这类局部层叠本就不该报

### 累计战果：3613 → **3098**（−515）；P1 1422→993 / P2 2076→2034 / P3 115→71

已归零规则：`scss/in-vue-inline`(402→0)、`scss/undefined-token`(4→0)、`scss/font-weight`(5→0)。
`scss/font-family` 仍是 120 处：`unitConverter` 的 `$mono` 已并，但 `statistics/*` 的 **121 处 `stats.$font-mono`** 待处理 —— **改定义处一处即可**（与 `$mono` 同型，性价比最高）。

### 迁移内联样式的标准动作（复用清单）

1. 读 `.vue` 的 `<style>` 块，记下是否有 `@use ... as X` / `@include X.mixin` 依赖
2. 新建 `styles/<ComponentName>.scss`，**首行显式 `@use "@/variables.scss" as *;`**（不依赖链式传递）
3. 有 mixin 依赖时补 `@use "./index" as index;`
4. 无对应 Token 的值集中声明为 `$<缩写>-*` 局部变量 + `// 无对应 Token`
5. `.vue` 的 `<style>` 改为**双行导入**（组件专属在前、`index.scss` 在后）
6. 逐文件 `sass.compileAsync` 离线编译 + `read_lints` + 扫描器复跑

---

## 修复第三轮（同日「开始吧，全部」）：B1 间距 Token 化全量执行

### 路线决策：采纳 A（我推荐的），新增两个全局档位

```scss
// src/_variables.scss（沿用 $spacing-2px 的「值即名称」写法）
$spacing-6px: 6px;
$spacing-10px: 10px;
```
并把 `src/components/styles/_mixins.scss` 的**重复字面量收敛为单一来源**：
`$gap-xs: $spacing-6px;` / `$spacing-2_5: $spacing-10px;`（原为 `6px` / `10px` 字面量；`_mixins.scss` 本身就有 `@use '@/variables.scss' as *;`，`$spacing-10px` 与本地 `$spacing-2_5` **不同名故无遮蔽冲突**——这正是选 `$spacing-10px` 而非 `$spacing-2_5` 作为全局名的原因）

### 执行方式：脚本化 codemod（`scripts/codemod-spacing-tokens.mjs`）

151 文件 × 1202 处逐一手改不可靠且不可复核。脚本特性：等值替换、**默认干跑**、`--write` 落盘、`--report` 输出 JSON 变更清单、**幂等**（复跑 0 改动）。

**结果**：目标 164 文件 → 改动 151 文件 / 1202 处替换 / 977 行；离线编译 **151/151 通过 0 失败**；`scss/spacing` **1110 → 239**（消除 79%）；总命中 3098 → **2227**。

### 本轮最大的坑（务必记住）

codemod 需要判断「某文件能否直接用裸 `$spacing-*`」。**初版只匹配 `@/variables.scss` 这一种拼写**，判定「133 个文件需补导入」——全是误判，因为项目里有三种等价形态：
1. `@use "@/variables.scss" as *;`（绝对别名）
2. `@use "../../../variables" as *;`（**相对路径**，`aiContentGenerator` / `statistics` 在用）
3. `@use "./variables" as *;` 而 `superPanel/styles/variables.scss` **`@forward '@/variables.scss'`**（转发出全局 Token）

若照初版插入，会**重复加载同一模块**导致 Sass 编译失败。正确判定必须按 Sass 语义：
- `resolveScss(fromFile, url)`：支持 `@/` 前缀（**要补回 `src/`**，我一开始漏了 → 全部判 false）、相对路径、partial 候选（`_x.scss` / `x.scss` / `x/_index.scss` / `x/index.scss`）
- `hasGlobalTokens(file)`：只认 `@use ... as *`，且**递归只能沿 `@forward` 链走**——`@use` **不传递**成员（Sass 的 `@use` 非传递），沿 `@use` 链递归会把「仅 `@use` 了 index 的文件」误判为已具备
- 修正后「需补导入」= **0**（全部文件已具备）

### 两处刻意保守处理（保证「零语义变化」严格成立）

1. **rem 不做 rem → px 映射**：`0.125rem`(2px) / `0.1875rem`(3px) / `0.375rem`(6px) / `0.625rem`(10px) 共 8 处（全在 `compactMode/styles/index.scss`）。`$spacing-2px` / `$spacing-6px` 是 **px** 值，映射过去在「根字号 ≠ 16px」时会变结果 → **保持 rem 字面量**，同声明内的 rem 值照常 Token 化。做法：修正映射表 → `git restore` 该文件 → 用修正后的 codemod 重新生成该文件（比逐行手改 8 处更可靠，因为其中有 4 组重复文本无法唯一定位）
2. **负值与 1px/5px/7px/14px 等无 Token 档位保持原样**，不做「就近取 Token」近似（会改视觉）

### 遗留：239 处真·无 Token 档位（需设计决策）

`1px`(82，发丝线/边框合并，含 `margin:-1px`)、`5px`(54)、`14px`(46，组件库「原值减 2px」档)、`7px`(11)、`18px`(7)、`0.375rem`(6)、`28/30/9/36px` 等零散档、负值 3 处。三选项：补全局 Token / 就近取 Token（改视觉）/ 标注 `// 无对应 Token` 保留。建议只对反复出现的 `5px`/`14px` 考虑补 Token。

### `git diff` 假警报（避免误判）

`git diff --shortstat` 报 155 文件 / 21072 删除行，**其中 20090 行来自 `docs/hardcode-audit.data.json`**（1.1 MB / 48k 行、每次扫描重写、且被暂存为 `AM` 状态 → 与暂存版差异巨大）。真实源码改动仅 ~987 行 / 151 个 SCSS。**核查方法**：`git diff --numstat` 按文件排序看头部，而不是只看 shortstat 总数。抽样 `Tag.scss` 为 3/3，行尾符仍为 CRLF（未被破坏）。

### 下一轮

B2（圆角 + 字体族，252 处 / 70 文件）→ B3（色值，455 处，须先剔除 6 类整体豁免）。`statistics/styles/index.scss` 的 `$font-mono: $vp-mono;` 说明那 121 处 `stats.$font-mono` **已经是全局 Token 的别名**，真正待改的是 `htmlViewer`(2) / `MarkdownEditor`(1) / `apiDebugger`(1) 的硬编码字体栈。

---

## 修复第四轮（同日「继续」）：B2 圆角 + 字体族

### 圆角：新增两档全局 Token（与 B1 同型决策）

`scss/border-radius` 分布暴露缺口：**2px(38) + 3px(32) = 70/125**，圆角 Token 表只有 4/6/8/12/16/24px。新增：
```scss
$radius-2px: 2px;   // 沿用 $spacing-2px 的「值即名称」写法
$radius-px: 3px;    // 沿用 $spacing-px 的写法
```
**不要命名为 `$radius-1_5`** —— 要保持与 `$spacing-2px`/`$spacing-px` 一致的命名族。

**结果**：52 文件 / 110 处替换 / 107 行，编译 52/52 通过；`border-radius` 125 → **16**（消除 87%）。剩余 16 处是 10px/20px/1px/7px/14px 零散档（真无 Token）。

### 字体族：120 处里有 115 处是误报

`scss/font-family` 从 120 → **0**，但**大部分是修扫描器不是改代码**：
1. **`stats.$font-mono` 这类带命名空间的变量引用被当成硬编码** —— 原清洗正则 `/\$[\w.-]+/g` 只剥掉 `$font-mono`，留下 `stats.` 判为非空。改为 `/[\w.-]*\$[\w.-]+/g`（含命名空间）→ 115 处消失（`statistics/*` 21 文件用的 `stats.$font-mono` 本就指向 `$vp-mono`）
2. **`$vp-mono !important`** —— 剥变量后剩 `!important` 判为非空。补 `.replace(/!\s*(?:important|default)/gi, ' ')` → 1 处消失
3. 真正硬编码只有 **5 处**：`htmlViewer`(2) / `MarkdownEditor`(1) / `apiDebugger`(1) → `$vp-mono`；`skillLearning`(1 系统字体栈) → `$font-zh`

**注意**：那次 `!important` 的 `replace_in_file` 报了「success」但**内容没变**（复扫仍报同一处）。**改扫描器后必须回读源码或复扫验证，不能只信工具返回值。**

### codemod 泛化

`codemod-spacing-tokens.mjs` → **`codemod-hardcode-tokens.mjs`**（旧文件已删）。加 `--rules scss/spacing,scss/border-radius`，用 `FAMILIES` 表驱动（声明正则 + px/rem 两张映射表）。泛化后对 B1 复跑仍 0 改动（幂等性保持）。

### 第 3 次踩「@use 命名空间」坑 —— 已加守卫

有文件写 `@use "variables" as *;`（**裸包名**），Sass 会优先解析到**同目录的本地 `_variables.scss`**。给它们再补 `@use "@/variables.scss" as *;` → **两个 `*` 命名空间成员同名 → 编译失败**。

新增 `canSafelyAddStarImport(file)`：若已存在「解析不到全局」或「无法解析」的 `as *` 导入 → **整文件跳过**（不替换、不写盘、进报告）。本轮 2 个文件被跳过：
- `flashcardReading/styles/StatisticsView.scss`：补 `@use "@/variables.scss" as g;` + 改 `g.$radius-2px`（2 处）
- `floatingToolbar/styles/_mixins.scss`：复用已有的 `as g` 命名空间（1 处）

**当文件已有本地变量文件占用 `*` 时，加带命名空间的 `@use ... as g;` 再用 `g.$token` 是零冲突的解法。**

**顺带发现（本轮未动）**：`floatingToolbar/styles/_variables.scss` 的 `$radius-md: 6px`（全局 8px）、`$radius-full: 50%`（全局 9999px）——**同名不同值**，建议后续收敛。

### 累计：3613 → **1998**（−1615，消除 44.7%）

P1 993 / P2 934 / P3 71。已归零：`in-vue-inline` 402→0、`undefined-token` 4→0、`font-weight` 5→0、**`font-family` 120→0**；`spacing` 1110→239；`border-radius` 125→16。

---

## 修复第五轮（同日）：B3 色值可行性分析 —— 结论是**不适用机械替换**

### 三个决定性事实

1. **`$color-*` 不是 UI 色值的替换目标**。`src/_variables.scss` 开篇注释写明其用途是「作为 `--b3-theme-*` 的 fallback，基本不实际生效」。全局 Token 是 **hsl 语义色**（`$color-danger: hsl(0 72% 51%)`），项目散落的是 **Tailwind 色阶**（`#ef4444`）——**无等值关系**。改 `var(--b3-theme-*)` 必然改变色相/明度，而 B1/B2 能放心跑 codemod 全靠「等值替换、零视觉变化」。
2. **相当比例的硬编码是刻意设计**：`color: #fff` **37 处**几乎全是彩色徽章上的白字；`rgba(0,0,0,α)` 28 处是遮罩/阴影；还有 Gitea `#609926` / CNB `#00a8e8` 品牌色、金银铜等级色 `#d4a017`/`#8e9aa8`/`#b87333`、macOS 装饰色 `#ff5f56` 等 —— 改了反而错。
3. **项目已有集中调色板模式**（`--gp-diff-*`、`--stat-color-*`、`--status-color-*`、`$color-tier-*`），说明「模块内定义语义调色板再引用」是既有范式，不是所有色值都要收敛到 `--b3-theme-*`。

### 剔除豁免后的真实分布（265 处 / 8 族）

white 54（37 处是徽章白字→保留）| red 39 | green 38 | gray 32 | other 29（品牌/等级/装饰→保留）| black 28（遮罩→保留）| amber 24 | purple 12（无主题变量→保留）| blue 9

「可主题化」合计 **142 处**（red/green/amber/blue/gray → `--b3-theme-error/success/warning/info/on-surface-variant`），但每处**都会改变视觉**（是修复不是回归：深色主题下写死的 `#ef4444` 本就有对比度问题）。

### 本轮实际改动：只改扫描器，未动业务代码

新增 `colorLiteralExempt`：`--x-color:` CSS 自定义属性声明、`$color-*` 声明处 → 判为「调色板定义，合法」。**调色板的职责就是集中声明字面量，使用点才需要引用变量**。`scss/color-literal` 287 → **260**，总命中 1998 → **1971**。

### 决策点（已写入报告，等用户拍板）

A 保守（只写白名单）/ **B 推荐（改语义色 142 处，视觉会变）** / C 激进（B + `#fff` 37 处逐处判断）。

`rgba(239,68,68,α)` 这类低透明度变体建议**保留**：无干净等价物，`--b3-theme-error-rgb` 走 `rgba(var(...), α)` 会踩项目既有的 `rgba(hsl(...), α)` 语法坑。

### 新增隐患记录

`statistics/styles/index.scss` **重新定义了 `$color-danger: #cf222e` 等与全局同名的变量**（全局是 `hsl(0 72% 51%)`），两者值不同，混用会导致 fallback 行为不一致。

---

## 修复第六轮（同日）：B3 按选项 B 执行 —— 58 处语义色主题化

用户选 B。逐条审阅后从「142 处族总量」筛出真正可安全改的 **58 处 / 14 文件**，编译 14/14 通过。

**映射表**（目标是 `var(--b3-theme-*)`：CSS 变量，**无需补 SCSS 导入**）：
| 族 | 色值 | 目标 |
|---|---|---|
| red | `#ef4444` `#dc2626` | `var(--b3-theme-error)` (16) |
| green | `#10b981` `#22c55e` `#16a34a` | `var(--b3-theme-success)` (12) |
| gray 文字 | `#94a3b8` `#64748b` `#6b7280` | `var(--b3-theme-on-surface-variant)` (18) |
| amber | `#f59e0b` `#d97706` | `var(--b3-theme-warning)` (8) |
| blue | `#3b82f6` `#0ea5e9` `#2563eb` | `var(--b3-theme-info)` (3) |
| gray 底/框 | `#f0f0f0` | `var(--b3-theme-surface)` / `var(--b3-border-color)` (2) |

**142 → 58 的差额**：rgba 低透明度变体 45（无干净等价物）+ 图表色 15（带 `// REPORT_CHART_COLORS.*`、`// chartConfig *` 注释）+ 语义徽章 8（`heading-badge-h1~h6`、`lang--*`、`--gitee`）+ 配套配色 6。

**三套必须整体保留的配套配色**（单独改任一色都会破坏设计完整性）：
- AntD success 提示框四色：`#f0fff4` 底 / `#b7eb8f` 边 / `#135200` 字 / `#52c41a`（htmlViewer）
- slate 深色卡片三色：`#1e293b` 底 / `#e2e8f0` 字 / `#334155` 边（skillLearning 的 SkillDialog / _shared）—— 只改字色会在深底上对比度不足
- 暗色编辑器 `#e0e0e0`（MarkdownEditor）—— 改后在浅色主题下对比度反转

**同时修正扫描器**：CSS 自定义属性声明一律算调色板定义（正则从 `^--[\w-]*colou?r[\w-]*\s*:` 放宽到 `^--[\w-]+\s*:`），因为 `--gp-badge-untracked-bg` 这类不含 color 字样的也是调色板。

**结果**：1971 → **1904**，`scss/color-literal` 260 → **197**。累计 3613 → 1904（−1709，消除 47.3%）。

### 踩坑：58 处替换全生成了非法值，但编译全过

替换脚本为构造正则用 `from.slice(1)` 剥掉 `#`，匹配 `ef4444` 后换成 `var(--b3-theme-error)`，拼出 **`#var(--b3-theme-error)`** —— 58 处全部中招。

**Sass 离线编译 14/14 全部通过**（Sass 不校验 CSS 属性值合法性，`color: #var(...)` 被当普通值原样输出）。靠抽查产物内容才发现，随后补一次全库 `#var(` 扫描清理干净，并复查 B1/B2 产物未受同类影响。

**教训：编译通过 ≠ 正确。机械替换后必须抽查产物内容。**

### 关键认知（决定后续还有什么能机械替换）

**B1/B2 能放心跑 codemod，靠的是「值即名称」Token 可以按需新增**（`$spacing-7px`、`$radius-2px`）→ **等值替换**。
而 `line-height` / `transition-duration` 是**语义档位 Token**，值域固定，替换必然改变表现：
- `line-height` 55 处：Token 只有 1.25/1.5/1.75，实际值却是 1.4(13)/1.6(8)/1.7(6)/1.3(3)/1.65/2；`1`(17) 是图标居中归一化应保留
- `transition-duration` 131 处：规范要求 0.12s，实际是 0.15s(40+)/0.2s(20)/0.3s(8)/0.6s(4)；`width 0.6s` 进度条动画改 0.12s 会明显変快

**所以还能等值扩 Token 的只剩 `scss/spacing`(239) 与 `scss/border-radius`(16)。** 建议下一轮 B4：spacing 剩余深度分析 + **边框宽度误报修正**（剩余 TOP 值 `1px` 82 处大概率是 `border: 1px solid` 的边框宽度，应走边框 Token 而非间距 Token）。

---

## 修复第七轮（同日「继续」）：B4 间距收尾 —— spacing 1139 → 28

### 我上一轮的猜测是错的

猜「`1px` 82 处是 `border: 1px solid` 的边框宽度误报」→ **错**。实际是 `padding: 1px` 62 + `gap: 1px` 8 + `margin-*: 1px` 12，都是真间距。**动手分析前别下结论。**

### 三个真收获

1. **`em` 单位应豁免（约 37 处）**：剩余项里 `margin: 0.3em 0`、`padding-left: 1.4em` 全在 Markdown 排版（`:deep(p)`/`:deep(ul)`）。em 是相对字号的排版单位，转 px 会破坏文本缩放。给 `scss/spacing` 加豁免：**值全为 em（非 rem）时不计违规**。
2. **补 5 个高频 px 档位，覆盖 190 处**：此前 px Token 只有 2px/3px/6px/10px，而剩余 TOP 值是 `1px`(82)/`5px`(53)/`14px`(39)/`7px`(10)/`18px`(6) —— **没 Token 才是它们一直没被替换的真因**（不是缺 `@use`，这些文件都有 `@use`）。新增 `$spacing-1px/5px/7px/14px/18px`，沿用「值即名称」族，**只补 `$spacing-1~16` 未覆盖的非 4 倍数**。
3. **收尾 23 处过时注释**：B1/B2/B4 新增 Token 后，一批 `// 无对应 Token` 变得自相矛盾（如 `padding: $spacing-6px; // 水平 6px 无对应 Token`）。**会误导维护 + 让扫描器误豁免**，已统一改「已收敛为全局 Token」。

### 结果

codemod **86 文件 / 201 处 / 185 行**，`需补 @use` **0**，编译 **86/86 通过**。产物抽查无可疑写法（`padding: $spacing-14px $spacing-18px` 形式正确）。

`scss/spacing` **1139 → 28**（消除 **97.5%**）；总命中 1904 → **1693**。累计 **3613 → 1693（−1920，消除 53.1%）**，P1 993 / P2 629 / P3 71。

剩余 28 处是 `0.375rem`(6)/`28px`(3)/`30px`(2)/`36px`(2)/`9px`(2) 等零散值与负值偏移，不值得为零星几处新增档位。

### 下一轮（B5）

1. `border-radius` 16 处收尾 —— 等值扩 10px/20px/1px/7px/14px 五档即可
2. 模块局部变量收敛：`globalRelations` 的 `$gr-space-1/5/6/7/10/14`、`unitConverter` 的 `$ac-space-6/10/14`、`formatAssistant` 的 `$fa-space-6/10` 现在都有全局 Token 了，可改为委派（`$gr-space-6: $spacing-6px;`）
3. `superPanel` 本地 `$spacing-xs/sm/md/lg` 更名 `$sp-*`

---

## 新增共享组件 Timeline（第 27 个公开组件，同日）

用户诉求：「根据 https://primevue.dev/timeline/ 新增通用组件」；澄清后口径 = **仅组件库新增**（不改动任何 feature）、**核心对齐 + 裁剪**（去掉官方 `layout` 水平布局与 `connector` 插槽）、**纯展示无交互**。

### 交付物（新增 4 个源码 + 修改 2 个预览框架文件 + 6 处文档）

| 产物 | 说明 |
|---|---|
| `src/components/Timeline.vue`（96 行） | 单根 `<ol>` + 逐项 `<li>` 三段式（对侧 / 分隔列（节点 + 连接线）/ 内容）；props = `value`（必填）/ `align`（`left` 默认 / `right` / `alternate`）/ `size`；插槽 = `content` + `opposite` + `marker`，作用域 `{ item, index }`；**无 emits** |
| `src/components/timeline/types.ts`（19 行） | `TimelineAlign` / `TimelineSize` / `TimelineSlotProps`；`.vue` 用 `export type X = XShape` 别名转出（沿用 Paginator 写法 —— `<script setup>` 允许 `export type` 声明，不允许 `export` 语句） |
| `src/components/styles/Timeline.scss`（106 行） | 全部短名 Token；`--si-timeline-font` 单点驱动四档字号；节点 10px 空心圆 + 连接线 2px `--b3-border-color` |
| `src/features/componentPreview/previewData/timeline.ts`（233 行） | 9 例：基础 / 对侧日期 / align=right / alternate / marker 序号（Avatar）/ marker 图标（IconWrapper `check`）/ Card 富内容 / size=large / 单条事件 |
| `componentPreview/types/index.ts` + `PreviewSection.vue` | **预览框架扩展（关键前置）**：`PreviewExample.slots` + `PreviewStage.namedSlots` |

### 必须记住的三件事

1. **预览框架原先不支持具名/作用域插槽**（只能默认插槽 `slotText` / `render`）⇒ 任何「渲染全靠插槽驱动」的组件放进预览都会变成空壳。本次新增 `PreviewExample.slots: Record<string, (slotProps) => VNode | VNode[]>`，`PreviewStage` 把每个工厂包成插槽函数注入 `h()` 的 children（返回值非数组则包成数组；children 为空传 `undefined`）。**工厂按列表项被多次调用 ⇒ 必须在工厂内部新建 VNode**，顶层构造后复用同一实例会触发 Vue 告警。`hasSlot` 语义**保持「仅默认插槽」不变**（否则多根组件会出现 extraneous attrs）。
2. **`opposite` 容器恒渲染**（即使未传该插槽）：官方 Basic 观感是两侧 `flex: 1` 等宽、竖线稳定居中；若按 `$slots.opposite` 条件渲染，切到 `alternate` 时布局会突变。`alternate` = 奇数索引 `flex-direction: row-reverse`（等价官方 `nth-child(even)` 反向）；对侧文本朝竖线侧对齐（基类 `text-align: right`，反向行覆盖为 `left`），内容一律左对齐。
3. **纵向留白必须放在两侧列（`padding-bottom`）而不是行上**：分隔列 `align-items: stretch` + 连接线 `flex: 1` 才能贯通整行；若把间距写成 `&__event` 的 `padding`/`gap`，连接线会在每行之间断开。末行用 `&__event:last-child > &__opposite, > &__content { padding-bottom: 0 }` 收口。

### 其他落地细节

- 官方 `align` 另有 `top` / `bottom`（仅水平布局有意义），插槽另有 `connector` 与 `icon` 作用域参数（`icon` 源自其内部图标解析）→ 本项目**均裁剪**，图标由调用方在插槽内渲染 `IconWrapper`（示例用已注册的 `check`）。
- 四档 `size` **只驱动字号**（10/12/14/16），节点直径与线宽恒定 —— 不给 Timeline 开「几何随档位」的口子（Slider / DatePicker 属已登记特例）。
- 组件库零 i18n ⇒ 本次**零 i18n 分片改动**；默认节点是纯 CSS 空心圆，组件内无任何文案。
- 文档计数 26 → 27 全量同步：`AGENTS.md`（5 处计数 + 组件清单表新增 Timeline 行 + 复用枚举补「时间线」+ 目录树注释）、根 `README.md`、`componentPreview/README.md`（计数 + 新功能条 + sizeable 清单 + 具名插槽表新增 3 行 + 清单扩展指南补 `slots` 字段 + 尺寸细则）、`kit/README.md`、`components/docs/components-vue3-migration-guide.md`（含**基线文件数 78 → 86**（实测值，原基线已漂移）+ 私有子目录清单补 `timeline/`）。
- 示例内复用共享组件（`Avatar` 序号节点、`IconWrapper` 图标节点、`Card` 富内容）以免为示例新增 SCSS，也避免 inline style 硬编码。

### 验证记录

| 验证 | 结果 |
|---|---|
| `read_lints`（5 个源文件） | 0 错误 |
| `npx tsc --noEmit` 过滤 `Timeline|previewData|componentPreview` | 0 命中（总输出 44 行全为历史噪声：未使用声明 + `TS2614`） |
| 离线 Sass 编译 + **产物抽查** | 选择器正确（如 `.si-timeline__event--reverse .si-timeline__opposite`）—— 编译通过 ≠ 正确，故打印了 CSS 核对 |
| 行数 | 96 / 19 / 106 / 233，均低于阈值（预览数据 300 行拆分线） |

### 追加：横向布局（用户当日追问「怎么只有竖向的，横向的呢」）

上次澄清时用户选了「裁剪掉 horizontal」，交付后立刻追问要横向 ⇒ **澄清选项里的「裁剪」不等于用户最终不要，交互式选项的排除项要留一句确认口**。补 `layout` 支持：

- `TimelineLayout = "vertical" | "horizontal"`（默认 `vertical`）；`TimelineAlign` 扩展为 `left | right | alternate | top | bottom`（竖向用 left/right，横向用 top/bottom，`alternate` 两向通用）。组件公共 API 与类型契约的变更**没有破坏兼容**（新增可选 prop + 联合类型加值）。
- **一根 `--reverse` 机制复用两个方向**：`isReversed(index)` 分向判断（竖向看 `right`、横向看 `bottom`，`alternate` 都看奇数索引），CSS 侧只换方向值——竖向 `row-reverse`、横向 `column-reverse`。**反向选择的依据必须按轴判断，否则横向写 `row-reverse` 只会把三块左右翻转**。
- 横向 CSS：根 `<ol>` 变 `display: flex; flex-direction: row`（事件等分宽度），`<li>` 变 `column`；分隔行 `flex-direction: row`（节点在左、连接线向右 `flex: 1` + `height: 2px` + `width: auto` 覆盖竖向的 `width: 2px`）；两侧块的 `padding-bottom` 归零、`text-align` 统一左对齐（竖向的「贴线对齐」在横向无意义）。
- **横向的连线对齐是这套实现的唯一软肋**：列高由各列内容决定（`align-items: stretch` 保证列等高），两侧块各占列高一半 ⇒ 分隔行落在垂直中线、节点跨列对齐；但若某列 `opposite` 更高（换行行数不同），该列会被自己内容顶高，连线呈阶梯状（**与官方实现同病**）。示例特意用等长短文案（`COMPACT_EVENTS`），并在 `componentPreview/README.md` 写明「需要严格水平对齐时让 `opposite` 等高或留空」。想彻底解决只能上 `display: contents` + 跨列 grid/subgrid，代价是 `<li>` 语义与可访问性风险，故不做。
- 示例 9 → 13 例：新增「横向 - 内容居下（`align="top"`）/ 内容居上（`align="bottom"`）/ 上下交错（`alternate`）/ 自定义节点」。`previewData/timeline.ts` 339 行（与既有 `control.ts` 339 行持平，未超同类文件规模）。组件 111 行 / 样式 145 行。
- 同步的文档：`AGENTS.md` 组件清单行的 Timeline 描述与「关键 props」列（补 `layout`）、`componentPreview/README.md` 的时间线功能条（补 `layout` 两向语义 + 横向对齐 caveat）与具名插槽表两行（「竖线」→「线（竖向为左/右、横向为上/下）」）。

### 未做（按项目约定由用户执行）

`pnpm lint` / `pnpm i18n:verify` / `pnpm validate:icons` / `npx tsc --noEmit`；以及插件内**目视回归** —— 预览面板是唯一目视入口，重点看 Timeline 分区的 13 个示例（竖向 9 + 横向 4，尤其两个方向的 `alternate` 交错与末项连接线收口）。

---

## 增强 Card：对齐 PrimeVue Card 插槽契约（同日）

用户只发了一个 URL（`https://primevue.dev/card/`）⇒ 澄清后确定为「**增强现有 Card**（不是新建）」+「**插槽 + 容器钩子**」。改 5 个文件、**无新增文件**。

### 官方契约（npm 类型声明 + Context7 双查，两侧一致）

**无任何功能性 props**（只有 `dt`/`pt`/`ptOptions`/`unstyled`）、**5 个无作用域参数的具名插槽**（`header`/`title`/`subtitle`/`content`/`footer`）、**无自定义事件**、PT 元素含 `caption`（官方只经 PT 暴露，无同名 prop）。官方典型用法是「footer 内放按钮组」，且**不内置按钮排列样式**（示例自备 `flex gap-4`）。

### 落地要点

- `title`/`subtitle`：`<h3 v-if="title || $slots.title"><slot name="title">{{ title }}</slot></h3>` —— **用插槽默认内容承载 prop**，避免 `v-if/v-else` 双分支；插槽天然优先于 prop（官方语义）。
- `content`：`<slot name="content"><slot /></slot>` —— 嵌套插槽实现「具名未传时回落默认插槽」，**不引入任何 `$slots` 判断**（也规避了 `$slots` 非响应式的坑）。
- **不用 computed 判断 `$slots`**：`useSlots()` 的 slots 对象非响应式，写进 `computed` 会缓存失效；`$slots.x` 写在模板里则每次渲染重新读取 ✓。
- 容器钩子：`contentClass` → `.si-card__body`、`captionClass` → `.si-card__header-content`；**不新建 `__content` 层** —— 否则直接打断 3 处既有 `:deep(.si-card__body)` 定制（`flashcardReading/{SingleCardView,StatisticsView}/styles/*.scss` 与 `flashcardReading/styles/index.scss:319`），属于为对齐而制造回归。
- 顺带修 1 处既有缺陷：`__header` 渲染条件原为 `$slots.header || title`，**漏判 subtitle**（只传副标题时整块标题区不渲染、内容被静默丢弃），已放宽为 header/title/subtitle 三者任一。
- 顺带补 `Card.vue` 缺失的**文件头注释**（项目强制规则，修改既有文件时补齐）。

### 演示类必须可视化（可复用经验）

类名钩子在快照里若传「无样式的类名」，渲染与默认完全一致 = 没演示。故在 `PreviewSection.scss` 加两个**仅演示用**的类，并踩到并记录一个特异性事实：**`.si-card__body` 的 padding 由 `.si-card--{tier} .si-card__body`（0,2,0）给出，演示类（0,1,0）改 padding 会被压制** ⇒ 演示类只演示不被覆盖的属性（边框 / 底色 / 圆角），并在注释里写明原因，避免读者误以为钩子没生效。

### 文档同步（含补登记历史欠账）

`componentPreview/README.md` 的具名插槽表此前**从未登记 Card 的 4 个既有插槽**（header / header-extra / cover / footer），本次一并补齐 7 行，并显式登记**三处与官方的语义差异**（`header` 是带下边框的标题栏≠官方通栏、官方通栏对应本项目 `cover`、`footer` 是独立分区带上边框且官方在 body 内）；事件契约表补上既有但未登记的 `Card.click`（仅 `clickable` 且非 disabled/loading 时派发）。`AGENTS.md` 的 Card 行同步补 3 插槽 + 2 钩子。**组件总数仍 27，其余计数文档无需改动**。

### 验证记录

| 验证 | 结果 |
|---|---|
| `read_lints`（Card.vue / display.ts / PreviewSection.scss） | 0 错误 |
| `npx tsc --noEmit` 过滤本次路径 | 0 命中（44 行输出仍为历史噪声） |
| 离线 Sass 编译预览样式表 + **产物抽查** | `SASS OK bytes=2521`，`.cp-hook-caption` / `.cp-hook-content` 均在产物中 |
| 行数 | Card.vue 192 / display.ts 273（<300）/ PreviewSection.scss 155 |

**零视觉变化**：4 处调用点（`scriptLauncher/ScriptList`、`flashcardReading/{StatisticsView,SingleCardView,CardList}`）只用 `variant`/`size`/`class` + 默认插槽，新插槽与新 props 均未传即输出与改动前一致；既有 8 张示例卡片亦不变。仍需用户在预览面板目视新增 3 例（具名插槽 / 底部工具栏 / 容器钩子）。
