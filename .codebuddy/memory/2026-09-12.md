# 2026-09-12

## 组件预览：移除各分区的「尺寸」示例卡（尺寸演示收敛到头部档位切换）
- 背景：头部已有 XS / S / M / L 全局档位切换（`index.vue` → `usePreviewSize` → `PreviewSection.resolveProps` 注入），分区卡片再摆「尺寸」卡属重复入口。
- 删除 **27 张**尺寸卡，覆盖 21 个 `previewData/*.ts`：`button`(4)、`tagAvatar`(2)、`megaMenu`(2)、`sidebar`(2)、`checkbox`/`radioButton`/`control`(Switch)/`datePicker`/`textarea`/`toolbar`/`toggleButton`/`paginator`/`timeline`/`tabs`/`speedDial`/`tooltip`/`dialog`/`drawer`/`confirmDialog`/`confirmPopup`/`fileUpload` 各 1。每张连同 `props`、`slotText`/`slots`、`code` 整块删除。
- 插槽内子控件去固定 size：`panel.ts`（icons / togglebutton / footer 共 4 个 Button）、`display.ts`（Card footer 2 个 Button）、`timeline.ts`（`renderIndexMarker` 的 Avatar ×1，垂直与横向两处 code 镜像同步）。
- 保留（动态跟随档位，删了会让复合示例档位失效）：`toolbar.ts` 的 `size: p.size`、`dialog`/`drawer` 的 `footerButtons(exampleProps.size)`、`confirmDialog`/`confirmPopup`/`fileUpload`/`sidebar` demo 包装的 `props.size`、`input.ts` 的 Switch `p.size`、`inputGroup.ts`；IconWrapper 像素 size、Splitter 的 `sizes`/「尺寸持久化」也不属档位演示，未动。
- 要点迁移：Toolbar「**容器档位不注入插槽**，内部控件需自行传同档 size」写进 `toolbar.ts` 的 `summary` 与文件头注 2，并在 `componentPreview/README.md` 的 Toolbar 段保留说明；`README.md` 档位段改写为「档位切换是唯一尺寸演示入口」；`PreviewSection.vue` 的 `resolveProps` JSDoc 同步（`base.size !== undefined` 兜底分支**保留**，仅改注释）。
- 验证：全目录搜 `size[=:]"(xsmall|small|medium|large|xlarge)"` 归零；受改动分区均仍 `sizeable: true`（25 处）；`read_lints` 0 诊断；`npx tsc --noEmit` 无 componentPreview 相关新增报错（既有报错集中在 statusBar/featureRegistry 等并行在改模块）。
- 意外发现：`previewData/sidebar.ts`（Sidebar / SidebarMain）是扫描之后新增的分区文件，含 2 张尺寸卡，已按同一口径清理 —— 说明并行会话会持续新增清单文件，收尾验证必须重新全目录搜。

## 新增共享组件 Message（参考 PrimeVue Message）
- 需求与拍板：参考 https://primevue.dev/message/ 新增通用组件。`severity` **照搬官方取值**（`secondary`/`success`/`info`/`warn`/`error`/`contrast`，与库内 `Button.severity` 的 `warning`/`danger` **有意不统一**）；做 `closable` + `life`，**不做** `variant`（outlined/simple）；本次只新增组件，不顺带迁移 feature 内既有自建提示。
- 交付：`src/components/Message.vue`（161 行）+ `styles/Message.scss`（约 150 行）+ `previewData/message.ts`（13 张示例卡，`sizeable: true`）+ `previewData/index.ts` 聚合（`...messagePreviewGroups` 插在 tagAvatar 之后）。
- 关键实现决策：
  1. **不自动隐藏**：点击关闭与 `life` 到期都只派发 `close(MouseEvent | null)`，是否移除由调用方 `v-if` 决定（同 `ConfirmDialog`「confirm 后不自动关闭」范式）—— 已登记进 AGENTS.md 组件行与 componentPreview README「事件契约」表。
  2. **关闭按钮复用共享 `Button`**（icon-only，覆写 `--button-size` 为 18/20/22/24）⚠️ **必须同时写 `min-height: 0`**：Button 档位类自带的 `min-height: 28/36/44px` 会反过来决定高度，只改 `--button-size` 完全无效（下文有实例）。
  3. 插槽名用 **`closebutton`**（作用域 `{ closeCallback }`，经 computed + `v-bind` 传递，同 `Dialog`）而**非官方 `closeicon`** —— 依 AGENTS.md 已登记的「同类语义全库只有一个名字，优先于字面对齐官方」。
  4. `life` 用组件内原生 `window.setTimeout`（库内 `tooltip`/`megaMenu`/`sidebar`/`tieredMenu` 同范式），**不用 `@/utils/timerRegistry`**（组件库需可整目录外迁、零业务耦合）；三条清理路径 = 到期 / 手动关闭 / `onBeforeUnmount`。
  5. `icon` prop 与 `icon` 插槽都不传则**不渲染图标**（与官方一致：`severity` 不自动附带图标，预览必须显式传 `icon` 才看得到）。
  6. 预览清单里的 `icon` 插槽示例**不给 `IconWrapper` 传 `size`** ⇒ svg 取 `1em` 随消息档位字号自动缩放，从而**不必在 previewData 复制组件内部的档位图标尺寸表**（避免同一常量两处定义）。
- 文档同步（实测基线：**47 公开组件 / 54 个 styles（含 2 partial + index）/ 15 私有目录 + 3 支撑目录 / 32 个 previewData / 155 个文件**）：`AGENTS.md`（4 处总数 + TieredMenu/Sidebar 行内埋藏数字 + 新增组件行 + 复用清单补「内联消息提示」）、`componentPreview/README.md`（总数 + 覆盖清单 + sizeable 清单 + 档位段 + 2 行插槽 + 1 行事件 + 新增功能段）、`kit/README.md`、`kit/theme.ts` 注释、根 `README.md` 目录树、迁移指南（3/6/62-64/109/147，并补全 7 个漏登记的组件与 4 个漏列的私有目录）。
- ⚠️ **【待修】既有潜在 bug（本次未改，爆破半径控制）**：`styles/FileUpload.scss` 的 `&__remove.si-fileupload__remove.si-button--icon-only` 只覆写 `--button-size`（20/22/24/26）而**没清 `min-height`** ⇒ 实际高度仍被 Button 档位类的 28/36/44px 撑住，移除按钮呈「宽 ≠ 高」的椭圆。修法与 Message 相同：补 `min-height: 0`。
- 验证：`read_lints` 0 诊断；`npx sass --no-source-map src/components/styles/Message.scss` 编译通过且选择器符合预期（`.si-message__close.si-message__close.si-button--icon-only[data-v]` (0,4,0)、(0,5,0) 稳压 Button 的 (0,3,0)）；`npx tsc --noEmit` 无 Message 相关新增报错（仅既有 `s3FileManager` TS6133）；`pnpm i18n:verify` 通过（4460 键对齐）；`pnpm validate:icons` 通过。
- 环境坑（新增）：IDE **拦截**未带 `-Encoding` 的 PowerShell `Get-Content` 命令（防 GBK/UTF-8 解码错乱）⇒ 行数/内容统计改用工具读文件，或换成单条 `Select-String` 式命令。

## 修复 FileUpload 移除按钮（顺带发现，**此前诊断有误，已纠正**）
- 我上一轮报的「只覆写 `--button-size` 没清 `min-height` ⇒ 呈椭圆」**只对了一半**。真正根因更深：`fileUpload/FileList.vue` **恒定**把 `fileremoveicon` 插槽出口渲染进 `<Button>`，使 Button 的 `$slots.default` 恒真 ⇒ `isIconOnly` 为假 ⇒ **`si-button--icon-only` 类从未输出**，整个 `--button-size` 覆写块是**死代码**（按钮实际是 ~36×28 的带框文本按钮 + 一个空的 `.si-button__text`）。推测是后加 `fileremoveicon` 插槽时引入的回归。
- 关键验证（临时脚本 `tmp-slot-compile-check.mjs`，已删）：用 `vue/compiler-sfc` 的 `compileTemplate` 实测 `<template v-if="$slots.x" #default>` 的编译产物 → `createSlots({_:2}, [ cond ? { name: "default", fn: withCtx(...) } : undefined ])`，即**插槽是条件注册的**，不传时 `slots.default` 真的不存在。
- 修法：
  1. `FileList.vue`：插槽出口改为 `v-if="$slots.fileremoveicon"` **条件转发**（恢复纯图标形态；提供插槽时才走文本形态）。
  2. `FileUpload.scss`：把几何量直接锁在 `.si-fileupload__remove.si-fileupload__remove` 上（`width/height: var(--si-fu-remove)` + `min-height: 0` + `padding/gap: 0`），不再依赖 `si-button--icon-only`；新增档位变量 `--si-fu-remove`（root 兜底 22px、四档 20/22/24/26，与文档数字一致，放在既有档位变量块内）；另加 `:deep(.si-button__text){justify-content:center; &:empty{display:none}}` 处理文本形态下的空文本层与居中。
  3. 文档同步：`AGENTS.md`（FileUpload 行）与 `componentPreview/README.md`（FileUpload 段 + 具名插槽表 `fileremoveicon` 行）补上「清 `min-height`」「尺寸锁自有类」「插槽须条件转发」三条。
- 作用域核实（易错点）：`styles/FileUpload.scss` 被 **3 个** `.vue` 同时 `@use`（`FileUpload.vue` / `fileUpload/FileList.vue` / `fileUpload/ChooseArea.vue`），各自带自己的 scope attr ⇒ 规则能命中子部件模板里的元素（`.si-fileupload__remove` 带的是 `data-v-filelist`）。
- 验证：`npx sass` 编译通过（`.si-fileupload__remove.si-fileupload__remove{...}` 与 `:deep()` 规则均按预期输出）；`read_lints` 0；`previewData/fileUpload.ts` 未使用该插槽 ⇒ 预览快照走纯图标形态，档位切换可目视回归。

## 组件计数再对齐（并行会话引入 Toast）
- 发现：并行会话已新增 `Toast.vue` + `styles/Toast.scss` + `styles/ToastMessage.scss` + `toast/` 私有目录 + `previewData/toast.ts` 并接入 `PREVIEW_GROUPS`，且已把 `AGENTS.md`(160/168/181/476/514) 与 `componentPreview/README.md`(3/8/37) 的**头部计数**改成 48、补了 Toast 组件行与功能段；但 `TieredMenu` / `Sidebar` 行内的「与全库 N 个组件一致」等**行内计数**仍是我写的 47 ⇒ 全库出现「头部 48 / 行内 47」。
- 本次对齐（10 处）：`AGENTS.md` 行内 2 处、`componentPreview/README.md` 行内 1 处、`kit/README.md`、`kit/theme.ts`、根 `README.md`、迁移指南 5 处（含「155 → 160 个文件」「IconWrapper 依赖 17 → 18」，并把 `Toast` 按字母序补进组件清单：`Timeline / Toast / ToggleButton`）。
- 当前实测基线：**48 公开组件 / 56 个 styles scss / 19 个小写起始目录（15 私有 + `styles` `kit` `docs`）/ 33 个 previewData ts / 递归 160 个文件**（已写进 MEMORY.md）。
- 教训：组件总数分散在**至少 12 处**（含表格单元格内嵌、注释、迁移指南），多会话并行时「各改一半」必然产生内耗 ⇒ 改计数后一律用 `\d+ 个` 全量复扫，并优先在**同一次改动内**改完所有位置。

## 组件预览面板 UI 排版优化（审查报告落地：P0/P1 + 舞台登记表）
- 触发：用户「审查当前UI排版，是否有优化」→ 先出 15 项分级报告 → 用户「根据建议」⇒ 落地第 1-10 项 + 第 14 项。**未纳入**：11 超宽屏 max-width、12 窄屏导航替代、13 import 常驻改 chip、15 搜索框 aria-label（已核实 `Input.vue` 用 `useAttrs()` 把 attrs 全量透传到**容器 div**（仅剥离 class/style）⇒ `aria-label` 对内部 `<input>` 无效，要修必须先给共享组件加 prop，超出「仅面板」范围）。
- 改动文件 10 个：`index.vue` / `components/{PreviewSection,CodeBlock,NavSidebar}.vue` / `styles/{index,PreviewSection,CodeBlock,NavSidebar}.scss` / `types/index.ts`（`I18n.sizeLabel`）/ `componentPreview/README.md` + 两个 i18n 分片（`sizeLabel`）。
- 关键决策：
  1. **网格 `align-items: start`** 取代默认 `stretch` —— 此前 MegaMenu(340px)/TieredMenu(300)/Dialog(300) 会把同排所有卡片拉到同高、多出的高度全灌进 `flex:1` 的舞台；展开代码块也会撑高邻居。
  2. **宽卡片跨列**：`.cp-card--wide { grid-column: span 2 }` 包在 `@media (min-width:720px)` 内（<720px 单列时跨列会横向溢出），只给 dialog/drawer/megaMenu/tieredMenu。
  3. **舞台特例收敛为登记表**：`PreviewSection.vue` 的 `STAGE_CLASS_BY_GROUP`（id→修饰类）替换 6 条布尔判断；**并行会话新增的 `toast` 已并入**（改动期间它出现了，说明该文件正被多会话同时编辑 ⇒ 编辑前必须重读）。
  4. **三处自建按钮改共享 `Button`**：档位切换器（`role="group"` + `sizeLabel` + 选中 `primary`/未选中 `ghost`+`text`+`:aria-pressed`，仅留 `$ff-mono` 字形 class）、卡片代码按钮与代码块复制按钮（icon-only，`size="xsmall"` = 22px，必传 `ariaLabel`；复制成功用 `:severity="copied ? 'success' : undefined"` —— 未复制不传 severity 以免污染取色）。
  5. **无障碍补强**：导航项 `aria-current` + 内嵌 `:focus-visible`（侧栏 `overflow-y:auto`，外扩环会被裁）；`<aside>` 加 `aria-label`；复制结果加 `.cp-sr-only` 的 `aria-live="polite"` 播报（项目此前无 sr-only 工具类，首个）。
  6. **字号**：示例名 10px→12px + `on-surface`；代码块 10px→12px 且 `word-break: break-all` → `overflow-wrap: break-word`（原写法会把标识符断在中间）；分区摘要 `-webkit-line-clamp: 2` + `:title`（Toolbar 的长告警不再平铺）；导航行高 4→6px、计数去 `opacity`。
  7. **边框令牌统一** `--b3-border-color`（6 处），`--b3-theme-surface-lighter` 只保留 hover/激活底色（NavSidebar 2 处、PreviewSection 1 处 demo hover）。
- 顺手清理：删除 `.cp-card:hover`（卡片整体不可点，只有页脚按钮可点，hover 高亮属误导）、`.cp-card__code-btn` / `.cp-codeblock__copy--copied` 自建样式块。
- 验证：`pnpm i18n:merge` + `i18n:verify` 通过（4461 键对齐）；`read_lints` 0 诊断；`npx tsc --noEmit` 过滤 `componentPreview` **无命中**。用户侧待跑 `pnpm lint` 与 `pnpm dev` 目视。

## 组件预览面板第二轮（计划已出，**待用户确认后执行**）：舞台分档 + 内存治理
- 用户新增需求：**进入组件预览页，内存每几秒 +100MB，峰值 1GB+**；补充线索「**关闭页签后内存恢复正常**」⇒ 内存被**活的组件树持有**，可排除跨页面全局泄漏（模块级缓存 / `window` 监听器泄漏 —— 那类关页也会留着）。三种互斥可能：①自激循环或定时器持续分配（实例数/DOM 数稳定）②DOM 节点泄漏（节点数同步涨）③实例被反复创建且旧实例被引用（实例数同步涨）。
- 已排除的嫌疑（勿重复排查）：`Loader` 纯 CSS；`Message.vue:121-145` 与 `toast/ToastMessage.vue:162-208` 的 `life` 定时器都有句柄 + `onBeforeUnmount` 清理；**Chart 用 `vue-chartjs` 的 `<Bar>/<Line>/<Pie>/<Doughnut>` 子组件**（`Chart.vue:7-30`，实例生命周期由库管理）；`Chart.vue` 的 `MutationObserver` 有 `disconnect`（:185-200）；`previewData/toast.ts` 的 `ToastDemo` 宿主只是受控回写，无累积。
- 用户已拍板的方案：**懒挂载 + 远区卸载**（IO `root=contentRef`、`rootMargin: 800px`、卸载时用实测高度占位、锚点跳转与搜索命中强制挂载，常驻实例 ~290 → ~20-40）；**定位方式 = 交付 DevTools Console 脚本 + 二分法，用户自测回传**（AI 不运行插件）；**布局细节同轮交付**（分层舞台：容器类满宽+顶对齐 `--fill`、多变高度顶对齐 `--top`、原子控件紧凑 `--compact` 64px、`--chart` ≥190px；列宽 230→260px；跨列名单追加 toolbar/paginator）。
- 41 个分区的宽度特性实测（关键结论）：根自带 `width:100%` = Toolbar/Input/Textarea/InputGroup/Slider/Listbox/DatePicker/ColorField/Timeline/Tabs/Splitter/Divider/Loader；**根无宽度会被舞台行 flex 收缩为内容宽（真失真）= Card/Panel/Message/Paginator(inline-flex)/Chart(固定 200×150)**；内容宽即正确 = Button/IconWrapper/ToggleButton/Switch/Checkbox/RadioButton/Tag/Badge/Avatar/Label/未带 label 的 Select。
- 计划文件另含：`PreviewCard.vue` 抽取（`resolvedProps` 只解析一次，消除模板里 `resolveProps(example)` 的两次调用）+ `PreviewStage` 的 **modelValue 同值写回守卫**（截断 `emit→写回→重渲染→emit` 自激）+ `docs/component-preview-memory-diagnosis.md`（三个 Console 脚本 + 判定树 + 四步二分法）。
- ⚠️ 执行提醒：`componentPreview/` 正被并行会话同时编辑（本轮已遇到两次：新增 `toast` 分区、`sidebar` 数据文件）⇒ 每次编辑前先重读文件，收尾复扫分组数与舞台修饰类名单。
- ✅ **本轮已执行完毕**（用户「开始」）：
  1. **舞台分档**（`PreviewSection.scss` + 登记表）：新增 `--compact`(64px) / `--fill`(满宽+顶对齐，`> * { width:100% }`) / `--top`(顶对齐) / `--chart`(≥190px)；`STAGE_CLASS_BY_GROUP` 改 **`Record<string, string[]>`**；列宽 230→260px；跨列名单 + toolbar/paginator；新增 `.cp-section__placeholder`（兜底 min-height 96px）。
  2. **卡片拆分**：新增 `components/PreviewCard.vue`（卡片三段 + `resolvedProps` computed 单次解析 + `SlotRenderer`）与 `components/PreviewStage.ts`（渲染函数组件，隐藏 DOM，含 `isControlledComponent` 与**同值写回守卫** `isSameValue`：`Object.is` + 数组浅比较）；`PreviewSection.vue` 精简为分区外壳 + 网格 + `showCode` 状态（保留「同分区同时只展开一个代码块」）。
  3. **懒挂载 + 远区卸载**：`index.vue` 单 IO（`root=contentRef`、`rootMargin: 800px 0px`）进区 `forceMount` / 出区 `scheduleUnmount`（400ms 延迟，卸载前记录网格 `offsetHeight` 到 `sectionHeights`）；`mountSectionsInViewport()` 首帧几何同步挂载防白屏；`watch(filteredGroups)` 搜索命中强制挂载 + `nextTick` 后 `observeSections()` 重建观察；`onBeforeUnmount` 断连 IO 并清全部定时器。
  4. **诊断手册**：新增 `docs/component-preview-memory-diagnosis.md`（脚本①内存/DOM/实例计数、脚本②定时器与监听器净增量、脚本③DOM 变更频率；判定树；四步二分法；回传模板；已排除嫌疑清单）。
  5. **文档同步**：README 新增「分区懒挂载与远区卸载」段 + 舞台分档表格 + 列宽/跨列说明（并把 `STAGE_CLASS_BY_GROUP` 描述更新为数组）。
- 验证：`read_lints` 0 诊断；`npx tsc --noEmit` 过滤 `componentPreview` 无命中；`npx sass` 单文件编译**不可用**（`@/variables.scss` 别名需自定义 importer，属已知环境限制，非代码问题）⇒ SCSS 语法最终由用户 `pnpm dev` 验证。用户侧待跑 `pnpm lint` + 采样脚本回传读数。
- 已知遗留（未做，避免扩大改动面）：`PreviewCard.vue` 与 `PreviewSection.vue` 都 `@use` 了同一份 `styles/PreviewSection.scss`（CSS 会重复输出一次，与既有 `index.scss` 被 4 个组件重复引入同性质）；若要消除，可把卡片/舞台样式拆成 `styles/PreviewCard.scss`。

