# 2026-09-14

## shortcut 面板「点击删除没反应」修复

- **根因**：`ShortcutManager` 是模块级单例（`new ShortcutManager()` 存模块变量），内部 `shortcuts` 数组**非响应式**；`index.vue` 的 `filteredShortcuts / totalCount / tabs / getTabCount` 用 `computed(() => manager.getAllShortcuts())` **直接读 Manager** ⇒ computed 没有任何响应式依赖，值被永久缓存。删除时 Manager 内部 `splice` + `saveAll` 落盘都成功（数据真的删了），但列表与计数不会重算 ⇒ 卡片不消失、看着像"没反应"（新增、编辑同理不刷新）。
- **事件链核对无误**（不是事件没接上）：`ShortcutCard` emit `delete(shortcut.id)` → `ShortcutGrid` 转发 → `requestDelete` → 共享 `ConfirmDialog`（`@confirm`/`@cancel` 与当前 API 一致，`2814fef8` 迁移到共享组件时改对了；遮罩 `position: fixed` 与旧的本地实现一致）。
- **修法（3 文件）**：
  1. 新增 `src/features/shortcut/utils.ts` —— 纯函数 `searchShortcuts(list, keyword)`（大小写不敏感，空关键词返回 `[]`）
  2. `manager.ts` —— `search()` 改为委托 `searchShortcuts`（消除复制，行为不变）
  3. `index.vue` —— 新增响应式镜像 `const allShortcuts = ref(manager.getAllShortcuts())` + `refreshShortcuts()`；`totalCount / tabs / getTabCount / filteredShortcuts` 全部改读该 ref；`confirmDelete()` 与 `addShortcut()` 成功后调用 `refreshShortcuts()`；`onMounted` 末尾（await 收藏/最近之后）再刷一次，覆盖「面板先于 `initShortcutData` 异步载入时挂载」的时序。
- **验证**：`read_lints` 0 条；`pnpm typecheck`（vue-tsc）全仓库 0 error。
- **可复用结论**：任何「面板 computed 直接读单例 Manager」的模块都有同一隐患；正确模式 = 视图层 `ref` 镜像 + 变更后 `refresh()`（gitPush `useProjectCrud.loadProjects()` 即此模式）。

## shortcut 模块完全重构（UI 排版 + 数据持久化）

- **需求**：用户要求「完全重构功能：UI 显示排版，和数据持久化」；确认后锁定范围 = 紧凑列表 UI + 三键分离持久化 + 导入导出/重置 + 冲突检测。**不做**：预置隐藏/编辑、平台过滤、分页与虚拟滚动。
- **变更规格**：`docs/shortcut-refactor-spec.md`（AC-01~AC-14 验收清单、迁移算法、冲突判定口径、任务分解）。
- **UI**：2 列卡片网格 → 单列紧凑行式列表。`ShortcutRow`（108px 按键徽章列固定宽保证纵向对齐 + 名称/描述 + 分类 Tag + 冲突警示 + 操作按钮，按钮默认半透明、悬停/聚焦显形）；`ShortcutList`（分组 + 吸顶组头 + 会话级折叠 + 空态走 `IconWrapper`）；`PanelHeader`（两行共享 `Toolbar`：搜索/分类/新增 + 收藏·最近·冲突/计数/导入·导出·重置）；`ShortcutDialog` 改用共享 `Dialog`（字段级必填校验）。**删除** `ShortcutGrid.vue` / `ShortcutCard.vue` 及其 SCSS。
- **持久化三键分离**：`plugin-shortcuts-custom`（唯一可写）＋ `plugin-shortcuts-favorites` ＋ `plugin-shortcuts-recent`；旧键 `plugin-shortcuts-all` 仅作一次性迁移源。迁移守卫用「新键 `exists()`（含空数组）」而非「新键非空」——否则用户删光自定义项后旧数据会复活；**写新键成功才删旧键**，失败保留待下次重试。预置判定改为 id 集合 `isPreset`（视图不渲染编辑/删除 + Manager 拒绝写入，双层设防）。
- **新增能力**：导入导出（`dataTransfer.ts` 纯逻辑；载荷 `type/version/exportedAt/shortcuts`，导入按「与预置 id 冲突忽略 / 同 id 覆盖 / 新 id 追加」合并并提示数量）；重置（`clearUserData` 写三个空数组以保留迁移标记）；冲突检测（`normalizeShortcutKeys` 归一化 + `buildConflictMap` 单遍 O(n)，行内警告高亮 + 「冲突」筛选）。
- **分层**：`utils.ts`（纯函数）/ `dataTransfer.ts`（导入导出纯逻辑）/ `manager.ts`（预置只读 + 自定义可写双段）/ `types/storage.ts`（三键 + 迁移 + 剪枝）/ `composables/useShortcutData.ts`（响应式镜像与副作用）/ `composables/useShortcutFilter.ts`（筛选管道）/ `index.vue` 收敛为编排层（316 → 217 行）。
- **踩坑（可复用）**：① `manager.getPresetIds()` 返回的 Set 会被 `loadFrom()` **整体替换**，composable 在创建时直接持有旧引用 ⇒ 面板早于数据初始化挂载时误判「全是自定义项」，改为 `ref` 镜像并在 `refresh()` 同步；② 筛选按钮按下态**不要**用 `:class="is-pressed"` 覆写共享 `Button`——ghost 的 hover 规则特异性高达 (0,5,0)（`.si-button--ghost:hover:not(...):not(...)[data-v]`），打不赢 ⇒ 改用组件自带 `variant="primary" + :outlined`；③ 面板内弹层不被 `overflow: hidden` 裁剪的前提是遮罩 `position: fixed` 且祖先未创建包含块 ⇒ **不要在面板根上写 `container-type`**（会毁掉 fixed 定位与裁剪语义）。
- **验证**：`pnpm typecheck` 0 error；`read_lints` 0；`pnpm i18n:verify` 通过（13 个新键 `sc*` 双侧对齐 4478 leaf keys）；`pnpm validate:icons` ✅；离线编译模块 5 个 SCSS 全通过；架构技能结构校验 0 错误 0 警告；自检无 >500 行文件、无内联样式、无硬编码字号/字重/圆角。

### 追加：列表改 2 列卡片（CR-002，同日）

- `ShortcutList` 分组内容区改 `grid-template-columns: repeat(2, minmax(0, 1fr))`（`align-items: stretch` 同行卡片等高）；`ShortcutRow` 由横向 4 列行式改为**纵向两行卡片**：上行 = 按键徽章 + 行内操作，下行 = 名称 + 平台标签 + 冲突警示 + 描述（与名称同行、次级色省略）。
- **关键手法**：操作按钮用 `visibility: hidden`（不参与布局 ⇒ **恒定预留空间**）+ `opacity` 淡入 ⇒ 悬停时卡片零抖动；但 `visibility: hidden` 的元素不可聚焦，必须保留一个始终可见的可聚焦入口（按键徽章按钮）来触发 `:focus-within`，键盘用户才能 Tab 进操作按钮。
- 卡片内不再重复与组头同名的工具标签（`showToolBadge(shortcut, group.name)` 做标签与组名比对——预置数据的 `group` 与工具名完全一致）；收藏卡把徽章底色改回 `--b3-theme-background`，否则与卡底同色不可辨。
- 规格与文档同步：`docs/shortcut-refactor-spec.md` 追记 CR-002；模块 README 的 UI 段改为 2 列卡片，并说明 `ShortcutRow` 未复用共享 `Card` 的原因（紧凑卡片需逐项覆写，会与 Card 自身 scoped 样式打 (0,2,0) 特异性官司 ⇒ 属规范允许的「纯展示的局部布局容器」例外）。
- 验证：`read_lints` 0、`pnpm typecheck` 0 error、模块 5 个 SCSS 离线编译全通过。

### 追加：移除「收藏」功能（CR-003，同日）

- 按用户要求移除收藏：卡片星标按钮、工具栏「收藏」筛选按钮、收藏态卡片样式全部删除；`ShortcutFilterMode` 去掉 `"favorite"`、`ShortcutQuery` 去掉 `favoriteIds`、`filterShortcuts` 去掉收藏分支；`ShortcutStorage` 去掉 `favorites` 槽位（`pruneUserState` → `pruneRecent`，`clearUserData` 只清自定义 + 最近使用）；`useShortcutData` 去掉 `favorites` / `toggleFavorite`（`persistUserState` → `persistRecent`）。
- **修正上文**：持久化现为「自定义 + 最近使用」两份用户数据（不再是三键分离），筛选只剩「最近 / 冲突」。
- **i18n 键 `favorite` / `unFavorite` / `filterFavorite` 刻意保留**：合并后的命名空间是全局扁平的，其他模块会跨分片读取同名顶层键（`minimalBrowser/components/Toolbar.vue` 就在用 `i18n.favorite`）⇒ 删键有跨模块风险，故只删代码不删键。
- 旧键 `plugin-shortcuts-favorites` 不再读写，留在用户数据目录但不参与业务（不影响运行）。
- 验证：`read_lints` 0、`pnpm typecheck` 0 error、模块 5 个 SCSS 编译通过。

### 新增 agent 规则：禁止创建临时校验脚本（用户要求）

- 落点：`AGENTS.md`（验证链条）+ `AGENTS_BUILD.md § 构建与验证` + `MEMORY.md`（环境与边界）。
- 规则内容：禁止新建 `.tmp-*.mjs` / `.tmp-*.js` / `scripts/tmp-*.mjs` 这类一次性脚本（含「为校验 SCSS 能否编译而临时写一个 `findFileUrl` importer 脚本」），也不允许以「用完即删」方式绕过；验证只走 `read_lints` + `pnpm typecheck` / `pnpm i18n:verify` / `pnpm validate:icons`，SCSS 编译与 `pnpm lint` / `pnpm vite build` 由用户执行；样式改动自查改为「对照 `src/components/kit/variables.scss` 核对 Token 名」；确实需要可复用检查能力时在 `scripts/` 下正式命名落地并在文档登记。
- 背景：本次重构中先后三次创建 `.tmp-scss-check.mjs`（离线编译 SCSS）与 `.tmp-audit.mjs`，虽已删除，但产生了未跟踪文件且验证口径与用户实际执行的命令不一致。

### 追加：移除 4 类预设快捷键（CR-004，同日）

- 按用户要求删除 `data/siyuan.ts`(9 条) / `data/plugin.ts`(3 条) / `data/vscode.ts`(16 条) / `data/cmd.ts`(19 条)，共移除 47 条；`presets.ts` 收窄为 NPM(10) + NVM(9) + Visual Studio(23) = **预置 89 → 42 条**（实测 `data/` 下 `id:` 计数 42）。
- **只删数据、不删标识**：`ShortcutCategory` / `CATEGORY_LABEL_I18N_KEYS` / `TOOL_CATEGORIES` / i18n 文案（`siyuanShortcuts` 等）全部保留 —— 与「`openspec` 有标识无数据」的既有状态一致；面板分类下拉由数据驱动（`countByCategory(allShortcuts)`），无数据不会出现空选项。恢复方式已写进 `presets.ts` 头注释：补回 `data/<分类>.ts` 并在 `presets.ts` 引入即可。
- 影响面：预置不落盘 ⇒ 用户自定义数据不受影响；被移除分类的 id 若残留在「最近使用」中，由启动时 `pruneRecent` 自动剪枝。
- 文档同步：README 概览改为「42 条预置」+ 示例计数 `12 / 42`；`docs/shortcut-refactor-spec.md` 追记 CR-004，并声明 CR-001 边界「不改动 89 条预置数据」按本次要求失效。
- 验证：`read_lints` 0、`pnpm typecheck` 0 error、`data/` 目录仅剩 npm/nvm/visualStudio/presets 四个文件。

### 修复：新增窗体点一下即消失（共享弹层外壳潜伏 bug）

- **现象**：点击「+ 新增」打开 `ShortcutDialog` 后，点进任意输入框的第一下弹窗就关闭。
- **根因**：`src/components/overlay/useOverlay.ts` 的遮罩点关判定只比较「`mousedown` 目标 === `mouseup` 目标」，**没有校验目标是否为遮罩本体**。弹层内部任意点击（点输入框、点文案）两个目标都是同一个内部元素 ⇒ 被判成「点了遮罩」⇒ `onDismiss()` ⇒ `update:visible(false)`。长期潜伏的两个原因：`Dialog.dismissableMask` 默认 `false`（本次为表单开了它才踩到），而 `ConfirmDialog` 里任何点击本来就会关闭弹窗。
- **连带发现（更严重）**：`ConfirmDialog` 的确认按钮也会走这条路径 —— `mouseup` 先于 `click` 触发遮罩点关 ⇒ 先 emit `cancel`（调用方 `cancelDelete()` 把 `deleteConfirmId` 清成 null）⇒ 再 emit `confirm` 时读到 null 直接 return ⇒ **删除完全失效**。这解释了用户最初的「点击删除没反应」：除已修的「列表非响应式」外，还有这条**确认回调被提前清空状态**的硬不执行。
- **修法**：`handleMaskMouseDown` 仅当 `event.target === event.currentTarget`（即遮罩本体）时记录目标；`handleMaskMouseUp` 同样要求抬起点落在遮罩本体上。三个消费方（`Dialog` / `ConfirmDialog` / `Drawer`）都把这两个 handler 挂在遮罩 div 上，故 `currentTarget` 恒为遮罩 ✓；行为变化仅限「弹层内部点击不再误关」，`dismissableMask: false` 的用法完全不受影响。
- **文档核对**：`componentPreview/README.md`（对话框段）早已写明「需在遮罩上按下并抬起才算数，弹层内按下、遮罩上抬起不误关」—— 该表述原先与实现不符，修复后才真正成立，无需改文档。
- 验证：`read_lints` 0、`pnpm typecheck` 0 error。

### 追加：表单「分组」改为下拉选择（CR-005，同日）

- 需求：「分组应该是可以选的下拉分类」。
- 做法：`ShortcutDialog` 的「分组」由 `Input` 改为共享 `Select`（`filterable`）；选项 = 默认分组（`customShortcuts` 文案）+ 现有分组（`utils.listGroups(allShortcuts)`，去重升序）+ 当前值，末尾追加「新建分组…」哨兵项 `__new_group__`（仅在下拉内部流转、不写入表单数据）⇒ 选中后切换为 `Input`，旁置「从列表选择」按钮切回（切回时回落默认分组）。
- **关键点**：选项里必须显式带上「默认分组」与「当前值」——否则「尚无自定义项」与「编辑一个用旧分组名的条目」两种情况下下拉会空白（`listGroups` 只反映数据里真实存在的分组）。
- 新增纯函数 `utils.listGroups` 并从模块入口导出；i18n 新增 `scNewGroup` / `scPickFromList`（中英双侧）。
- 验证：`read_lints` 0、`pnpm typecheck` 0 error、`pnpm i18n:verify` 通过（4480 leaf keys，新增 2 键已合并）。

### 修复：分组下拉「选什么都不生效」（CR-005 遗留）

- **现象**：新增/编辑对话框里分组下拉选任何项，触发器文案与再次展开时的勾选态都还是「自定义」。
- **根因**：共享 `Select` 是**纯受控**组件（`selectedOption = computed(() => findSelectedOption(props.options, props.modelValue))`，自身不持有值），我上一版只写了 `:model-value` + `@update:model-value="handleGroupSelect"`，而 handler 里只改了 `form.group`、**没回写 `groupSelectValue`** ⇒ 显示层永远停在初始值。
- **修法**：`handleGroupSelect` 在写入表单的同时回写 `groupSelectValue`（哨兵「新建分组…」分支除外，那一支会切换到输入框）。`backToGroupList()` 原本就回写了，故只有选中路径受影响。
- **澄清**：底层数据一直是正确的（`form.group` 确实被写入并入库），只有弹窗显示没更新 —— 因此此前用该下拉保存的条目其实已落在所选分组下，无需数据修复。
- **同类自查**：`PanelHeader` 的搜索框与分类下拉都是 `:model-value` + handler，但经由 index.vue 的 `v-model:xxx` 回写 ✓ 无此问题；模块内只此一处漏回写。
- 验证：`read_lints` 0、`pnpm typecheck` 0 error。

### 修复：新建分组「没有效果」（CR-005 遗留）

- **现象**：选「新建分组…」后，无论怎么操作，保存的条目还是落在默认「自定义」分组。
- **根因（两个叠加）**：① 选哨兵项后 `Select` 被 `v-if` 换成 `Input`，**焦点没有移交** ⇒ 用户直接敲键盘**落空**（输入框实际为空）；② `buildCustomShortcut` 对空分组**静默回落**默认分组 ⇒ 用户的显式选择被无声忽略，表现为"新建分组没效果"。
- **修法**：`handleGroupSelect` 改 async，切到新建态后 `await nextTick()` 再调 `newGroupInputRef.value?.focus()`（`Input` 已 `defineExpose({ focus })`）；`handleConfirm` 增加「新建态下分组名必填」校验，为空时在输入框上就地报错并阻断确认，不再静默回落；`backToGroupList` 与输入时清空该错误。
- **可复用结论**：`v-if` 切换控件必须手动移交焦点；**用户的显式选择（如"新建"）不应静默回落，缺值要给就地校验** —— 静默兜底会把"没生效"伪装成功能 bug。
- 验证：`read_lints` 0、`pnpm typecheck` 0 error。

### 追加：移除「最近使用」与「冲突检测」+ 卡片显示优化（CR-006，同日）

- **移除最近使用**：筛选按钮、卡片圆点、复制即计入最近使用的逻辑、存储读写全部删除；`plugin-shortcuts-recent` 不再使用（旧数据留在用户数据目录，不参与业务）。
- **移除冲突检测**：`normalizeShortcutKeys` / `buildConflictMap` / `isConflicting` / 冲突高亮气泡 / 「冲突」筛选全部删除；`ShortcutFilterMode`、`ShortcutQuery.filter`、`ShortcutConflictMap` 一并下线 ⇒ **筛选管道只剩「关键词 + 分类」**，工具栏第二行仅剩 计数 + 导入/导出/重置。
- **存储与类型瘦身**：`ShortcutStorage` 只剩「自定义 + 旧键迁移」（删掉 recent/favorites 槽位、`pruneRecent`/`clearUserData`/`pruneIds`/`sanitizeStringArray`）；`useShortcutData` 不再需要 `plugin` 入参（无用户态要读）与 `RECENT_LIMIT`；`resetAll` 直接 `manager.clearCustom()`。
- **卡片显示优化**：下行改 `flex-wrap` + 描述 `flex-basis: 120px` ⇒ **名称短时描述同行、名称长时描述自动折到第二行**（不用在密度与可读性之间取舍）；卡片统一中性边框 + `$r-base` 圆角、按键徽章 `$r-sm` 圆角；悬停/焦点反馈统一为「surface 底 + 主色描边」（取代原收藏态、冲突态两套着色）；按键徽章 hover 加深描边。
- i18n 键 `filterRecent` / `scFilterConflict` 等**保留未删**（与 CR-003 同策略：合并后命名空间全局扁平，删键有跨模块风险）。
- 验证：`read_lints` 0、`pnpm typecheck` 0 error、`pnpm i18n:verify` 通过（4480 leaf keys）。

### 追加：内容显示自适应重构 + 模块样式审查（CR-007，同日）

- **需求**：「继续重构：修改快捷键的显示，内容可能是快捷键，可能是命令行，可能是其他方便快捷复制的，优化 ui 审查 css 冗余」。确认口径：主显 `copyContent || keys`、**纯自动判定**形态（不加表单类型选项）、长内容**单行省略 + 悬停看全文**、CSS 审查**仅限模块 5 个 scss**。
- **显示模型（纯函数，视图与复制同源）**：新增 `isKeyCombo(text)`（按 `,` 拆序列 → 按 `+` 拆 token → 逐 token 白名单：修饰键 / `F1`–`F12` / 命名键 / 单字母数字 / 单字符符号；序列上限 3）与 `resolveShortcutDisplay(item)` → `{ content, kind, hotkey? }`。`content = copyContent || keys`；`kind = isKeyCombo(content) ? "keys" : "code"`；`hotkey` 仅在「有独立 copyContent + 与 keys 不同 + keys 是按键组合」时给出。**拆序列只在 `kind === "keys"` 时进行** ⇒ 含逗号的命令不会被拆散（旧 `splitKeySequences` 直接按逗号拆会误伤）。
- **判定方向（偏保守）**：宁可把按键渲染成代码芯片（观感退化），也不把命令渲染成徽章（误导用户）。实测样本覆盖：`F5` / `Ctrl+Alt+Shift+N` / `Ctrl+K, Ctrl+C` / `Ctrl+\, E`（含反斜杠与空格，靠 `token.trim()` + 符号白名单通过）/ `npm install -g`（含空格 → 非按键 ✓）。
- **卡片**：内容行 = 主内容芯片（整块点击复制，`:focus-visible` 焦点环，`aria-label` 形如「复制：npm install」）+ 弱化次显快捷键徽章（`flex-shrink: 0`，不参与压缩）+ 悬停操作；命令芯片刻意用 `--b3-theme-surface-lighter` 而非 `surface`（避免与悬停卡底仅差约 3% 灰度而融为一体）。名称行保持 `flex-wrap` 自适应。
- **表单**：「按键组合」→「内容（必填 → `copyContent`）」+「快捷键（可选 → `keys`，留空写内容副本）」；`ShortcutFormData` 增 `content`；校验错误从 `errors.keys` 改为 `errors.content`。
- **零迁移**：`ShortcutInfo` 结构不变；`sanitizeShortcutArray` 加「`keys` 缺失回退 `copyContent`」防御（外部导入只填内容也不丢）；`searchShortcuts` 覆盖 `copyContent`（按命令也能搜到）。
- **样式审计（code-explorer 子代理）**：结论是**废代码 0 条**（未使用选择器 0 / 未引用局部变量 0 / 已删功能残留 0 / Token 违规 0）——今天的删减已做干净；实际动作都是「等价改写」：三处近似芯片（`--code` / `__key` / `__hotkey`）的公共几何合并为同族规则、省略号三连抽成四选择器分组（`__hotkey` 刻意排除，否则短文本被截断）、`0.12s` 收敛为 `$motion-fast`、为两处依赖共享组件类名的选择器（`:deep(.si-toolbar__end)`、`.si-tag`）补锚点注释。
- **⚠️ 合并时踩到的顺序陷阱（已修）**：`.shortcut-row__content`（重置 `padding: 0 / border: none`）与其修饰类 `--code` / `--keys` **特异性相同（均为单类）**，同特异性靠源码顺序覆盖 ⇒ 重置必须排在骨架与修饰类**之前**，否则代码芯片的内边距与描边被压掉。合并重复规则时"先声明公共骨架"是错的，必须按「重置 → 公共骨架 → 形态修饰」排序。
- 验证：`read_lints` 0、`pnpm typecheck` 0 error、`pnpm i18n:verify` 通过（4486 leaf keys，+6 新键）、`pnpm validate:icons` ✅、架构技能结构校验 0 错误 0 警告；文档同步：规格就地更新 §一/§三 + 追加 CR-007（AC-15~AC-22，并明示 AC-07/AC-11/AC-13 废止）、README 新增「内容显示模型」节与样式审查结论。

### 追加：清理 CLI 预置的编造快捷键（CR-008，同日）

- **起因**：用户指着 `npm.ts` 里 `keys: "Ctrl+Alt+N"` 问「npm 为什么会有快捷键？」。查证：**npm / nvm 是 CLI，没有原生快捷键**，这些 `Ctrl+Alt+X` 是 `keys` 既必填又是主显示字段时代被塞入的**编造数据**（`visualStudio.ts` 的 23 条才是真键位）；CR-007 后它们仍以弱化徽章显示，会误导用户以为真能触发命令。
- **用户已自行删除一条**（`npm install` 的 keys）⇒ 触发 `TS2741: Property 'keys' is missing ... required in type 'ShortcutInfo'`（`keys` 当时为必填）。注意：`read_lints`（IDE 诊断）当时返回 0，**权威结论要靠 `pnpm typecheck`**。
- **处理（用户选推荐方案）**：`ShortcutInfo.keys` 改**可选**（`keys?: string`）⇒ `data/npm.ts`(10) 与 `data/nvm.ts`(9) 删光伪键、只留 `copyContent`；`visualStudio.ts` 不动。必须同时补的 2 处兜底（否则改可选会引入新 bug）：`utils.searchShortcuts` 的 `s.keys.toLowerCase()` → 可选链（undefined 会抛，搜索会整体挂掉）、`ShortcutDialog` 回填 `keys: value.keys ?? ""`（`ShortcutFormData.keys` 仍为必填 string）。
- 其余路径天然兼容：`resolveShortcutDisplay` 用 `(item.keys || "")`、`sanitizeShortcutArray` 用 `readString(item.keys) ?? copyContent`（自定义/导入只填内容也不丢）、`buildCustomShortcut` 表单路径恒非空。
- **数据文件头注释同步修正**：npm.ts / nvm.ts 原文「用于首次使用时 seed 到本地持久化存储」是 CR-001 前的过期说法（预置已不落盘）⇒ 改为「预置不落盘（代码即真源）」并写明不写 `keys` 的原因。
- 验证：`pnpm typecheck` 0 error、`read_lints` 0；`data/` 下仅 `visualStudio.ts` 仍有 23 条 `keys`。文档：规格追加 CR-008、README 数据模型节补「真键位 vs 命令/文本」两种预置形态说明。

### 追加：shortcut 整体迁入工具合集（CR-009，同日）

- **需求**：「把 shortcut 完整迁移到 toolCollection/index.vue」。澄清后确认：按既有先例（`unitConverter` / `wordQuery`）**整体搬入 + 摘除旧入口**、栅格**自适应列数**、存储键**顺便改名**（用户明确选了改名而非零迁移）。注意 `toolCollection/index.vue` 只是容器（README 明确「新增工具无需修改 index.vue」），真正要动的是 `tools/registry.ts`。
- **搬迁**：`git mv src/features/shortcut src/features/toolCollection/tools/shortcut`（23 文件全部 R 重命名，历史保留，旧目录无残留）；`tools/registry.ts` 加 `{ id: "shortcut", component: ShortcutTool }` + `TOOL_LABEL_KEYS.shortcut = i18n.shortcuts ?? "Shortcuts"`（第 10 个工具）。
- **入口摘除**：删 `registerShortcut` / `addShortcutDock`（`iconKeymap` / `shortcut-panel-dock`）；`src/index.ts` 的 import 与 `if (s.enableShortcuts)` 调用、`features/index.ts` 导出、`settings.ts` 的 `enableShortcuts`、i18n `enableShortcuts(Desc)`（中英）均按先例留「已迁移至 …」注释或直接删除；**额外发现并处理**：`vite.config.ts` / `tsconfig.json` 的 `@shortcut` 别名（先例中 `@unitConverter` / `@wordQuery` 已删除）+ `AGENTS_API.md` 别名清单条目。i18n 分片 `shortcuts.json` 保留 → `pnpm i18n:merge` 后 4486 → **4484** 键（正好 -2）。
- **初始化改为幂等 bootstrap**：新增 `tools/shortcut/bootstrap.ts` 的 `ensureShortcutData(plugin)`（模块级 Promise 缓存；失败不缓存以便下次挂载重试），`index.vue` 的 `onMounted` 改为 `await ensureShortcutData(props.plugin)` 后 `init()`。原因：工具合集**切换工具即销毁重建组件**，若每次挂载都读盘会覆盖内存态。该工具的 Props 中 `plugin` **必填**（不像 wordQuery 那样可选）。
- **存储键改名 + 两代迁移**：`plugin-toolCollection-shortcut-custom`（唯一可写）；迁移源按代次 `plugin-shortcuts-custom` → `plugin-shortcuts-all`（后者需过滤预置 id），逐代读取**命中即停**（旧键存在但为空数组 ⇒ 视为用户已清空，不回落更早的键）；守卫仍是新键 `exists()`（含空数组）；写新键成功才删已消费的旧键。
- **⚠️ 前置修复的三个容器级集成风险（影响所有工具，非仅 shortcut）**：
  1. **弹层被裁剪**：共享弹层不用 Teleport（遮罩 `position: fixed; inset: 0`），而 `.tool-collection-panel` 常驻 `transform: translateX(-50%) scaleY(1)` + `contain: layout paint` ⇒ 面板成为 fixed 后代的包含块、弹层被关进面板并裁掉。修法：移除 `contain` 与常驻 transform，居中改 `left/right: 0` + `margin-inline: auto`，过渡改 `scaleY(0)`（tab-mode 补 `right: auto` / `margin-inline: 0`）。
  2. **键盘抢键**：`useToolNavigation.handleKeydown` 已排除输入类元素，但弹层内按 Esc 会连带关闭整个工具合集 ⇒ 增加 `document.querySelector('[aria-modal="true"]')` 让路判定（共享 `Dialog` 非模态时 `aria-modal="false"`，`ConfirmDialog` 恒 true ⇒ 只拦真模态）。
  3. **双层滚动**：`.tool-collection-content` 自身 `overflow-y: auto` + `padding: $s-3`，靠 `.shortcut-panel { height: 100% }` 填满其内容盒、滚动交给内部 `.shortcut-list` 独占 ⇒ 无第二条滚动条（样式注释已写明该契约）。
- **栅格**：`repeat(2, minmax(0, 1fr))` → `repeat(auto-fill, minmax(220px, 1fr))`。
- **验证**：`pnpm typecheck` **0 error**、`read_lints` 0、`pnpm i18n:verify` 通过（4484 键）、`pnpm validate:icons` ✅、架构校验脚本（toolCollection）**0 错误 0 警告**；全仓库旧引用 grep 只剩规格文档里的历史记录；registry 10 条。⚠️ 既有超 500 行文件 3 个属其他工具（`base64Image/index.vue` 917、`unitConverter/utils/units.ts` 690、`wordQuery/styles/codeUtils.scss` 530），本次未触碰；shortcut 工具最大文件 271 行。
- **文档**：规格就地更新（表头加「模块现路径」、§一 A/B 行与注册链边界、§二存储键表与迁移步骤）+ 追加 CR-009 + AC-23~AC-30（并注明 AC-01~AC-22 的 Dock 场景改由工具合集承载）；随迁 README 重写入口/存储键/目录树/API 路径；toolCollection README 工具表加「快捷键」行；`AGENTS.md` / `AGENTS_API.md` 两处过期的「在 index.vue 的 tools computed 添加条目」改为 registry 注册说明。

## gitPush 工作区差异弹窗：新增/未跟踪文件无内容修复 + 弹窗优化

- **根因**：`git diff [--cached] -- <file>` 对**未跟踪文件恒为空**（该文件不在 index 中），数据层又用硬编码中文「（无差异）」兜底 ⇒ 点开 `?` 未跟踪新增文件只见那行字。
- **兜底命令实测**（本机只读验证）：`git diff --no-index -- /dev/null <file>` 输出与普通 diff 同格式（`diff --git` / `new file mode` / `index ` / `--- /dev/null` / `+++ b/…` 均命中既有 `DIFF_META_PREFIXES`）；**但退出码为 1**（有差异即 1，等同 `--exit-code` 语义）。
- **数据层修法**：`GitExecutor.execGit` 的 `options` 增 `allowExitCodes?: number[]`（仅 `typeof error.code === "number"` 且命中才走 `resolve(stdout…)`，ENOENT/超时/maxBuffer 仍 reject）；`WorktreeOps.getFileDiff` 去掉中文兜底（失败返回空串，文案交视图层 i18n）；常规 diff 为空时新增私有 `isFileInHead`（`git ls-tree HEAD -- <path>`，实测已跟踪非空 / 未跟踪空，无提交或命令失败按 false）⇒ 不在 HEAD 才回退 `--no-index`；常态路径不增子进程。
- **状态归属修正**：porcelain 解析由 `xy.includes("M")` 改为 **X 位优先、Y 位回退**（AM/RM 原被吞成「已修改」，且连带跳过 rename 的 `-> ` 路径拆分），并显式识别 unmerged 组合（DD/AU/UD/UA/DU/AA/UU）；`FileChange` 新增可选 `unstaged`（与 `staged` 同真 = 暂存区与工作区各一份差异）。丢弃逻辑只判 `untracked`/`staged`，故该修正是纯展示改进。
- **弹窗四项优化（用户全选）**：① 加载态（`useCardData.diffLoading` 与 `fileDiffs` **同键同构** `Record<string, boolean>`，`finally` 删除；命中缓存不亮）② 标题全路径 tooltip + 状态徽章 + 重命名旧路径 ③ 差异范围切换（仅 `staged && unstaged` 显示；共享 `Button` 分组 + `:aria-pressed`；另一 scope 由 `WorkingTreePanel.prefetchOtherScope` 后台预取）④ 复制差异全文/文件路径（`copyToClipboard` + 2 秒对勾反馈）⑤ Esc/←→ 改**捕获阶段** + `stopImmediatePropagation`（对齐 CommitFileDiffDialog）。另修 `fileIndex` 暂存翻转瞬间的 -1（精确匹配失败时按路径回退）。
- **新增 4 个 i18n 键**（`diffScope`/`copyDiff`/`copyPath`/`copied`）+ `diffEmpty` 文案微调；`utils.diffCacheKey(path, staged)` 统一 `s::`/`u::` 键生成（原 useCardData 与弹窗各拼一次）。
- **验证**：`pnpm typecheck` 0 error、`read_lints` 0、`pnpm i18n:verify` 4484 键对齐 0 重复、`pnpm validate:icons` ✅。
- **遗留（已告知用户，未擅自动）**：`FILE_STATUS_META.title` 是硬编码中文（状态徽章与列表 tooltip 同源）⇒ en_US 下该处仍中文，彻底修需把 title 改 titleKey 并让消费方从视图层取 i18n；`WorktreeOps.ts` 563 行 / `utils.ts` 822 行超 500 行硬阈值（前者本次 +37，计划明确「不新增文件」故未拆）。

## 遗留两项修复（用户点名：文件状态文案 i18n 化 + 超 500 行拆分）

### ① 文件状态文案彻底 i18n 化

- `types/meta.ts`：`FILE_STATUS_META` 由 `Record<string, { icon, title }>`（中文 title）改为 **`Record<FileChangeStatus, { icon, titleKey }>`**（键集完整性由类型约束，新增状态会编译报错）；`import type { FileChangeStatus }` 与既有 `GitProject` 同源（类型环无运行时影响）。
- 新增 `utils/fileStatus.ts` 的 **`fileStatusText(file, i18n)`**（titleKey → i18n，未知状态回退状态码避免空标签）与 `fileStatusTitle(file, i18n)`；4 个消费方改为传 i18n：`WorkingTreePanel` / `StashSection` / `CommitFilesList`（tooltip）与 `WorkingTreeDiffDialog`（状态徽章）。
- `CommitMsgGenerator.buildCommitFileListSection` 原用 `FILE_STATUS_META[...].title` 拼中文进 AI 提示词 ⇒ 改为写机器可读的 `${f.status}`（提示词不参与 i18n，也避免再次依赖展示层元数据），并从 import 中摘掉该常量。
- 新增 7 个 i18n 键（`fileStatusModified/Added/Deleted/Renamed/Untracked/Copied/Unmerged`，中英各 7），运行 `pnpm i18n:merge` 后 **4494 叶键**对齐、0 重复。
- ⚠️ 顺带发现：`src/i18n/{zh_CN,en_US}.json`（合并产物）由构建/dev 自动再生，编辑分片后**必须 `pnpm i18n:merge`**（否则运行时读到的合并文件缺新键）——本次合并前合并文件缺 7 个 fileStatus 键，合并后才齐。

### ② 超 500 行文件拆分（两个）

- **`utils.ts` 822 行 → `utils/` 目录 12 文件**（最大 `diffText.ts` 206 行）：`index.ts` 为**显式汇聚导出**，导出面与拆分前逐条对齐 ⇒ **所有消费方 `"../utils"` import 零改动**（`vue-tsc` 的 TS2305 会兜住漏导出，实测 0 error）。域划分：project / platform / fileStatus / diffText / gitOutput / analysis / format / metrics / search / runtime / errors。删除原 `utils.ts`（同一路径若文件与目录并存会优先命中文件）。
- **`managers/WorktreeOps.ts` 563 → 464 行**：把 5 个纯文本解析（porcelain 状态、stash list、commit log、name-status 提交文件、分支）抽到 `utils/gitOutput.ts`（含我新加的状态归属修正与 unmerged 组合识别），类内只留 execGit 调用 + 兜底 try/catch。
- 文档同步：`gitPush/README.md` 目录树补 `utils/` 12 行 + 共享常量节补 `FILE_STATUS_META`（titleKey 口径）；`AGENTS.md` 分层表 `utils.ts` 行补「超 500 行拆 `utils/` + 汇聚 index.ts」；`AGENTS_ARCH.md § 单文件行数上限` 新增「常用拆分模式」表（utils 目录化 / Manager 抽纯函数 / 巨型组件三式）。
- ⚠️ **仍超 500 行的 gitPush 文件**：`types/meta.ts` **672 行**（本次新发现，未拆，已告知用户）；`utils.ts` 与 `WorktreeOps.ts` 已达标。
- 验证：`pnpm typecheck` 0 error、`read_lints`（gitPush 全目录）0 条、`pnpm i18n:verify` 4494 键对齐、`pnpm validate:icons` ✅。

## 审查（越界 + bug）与三项审查修复

- **审查结论**：核心修复无回归；2 处轻度越界已披露并保留（AGENTS*.md 规范文档改动、CommitMsgGenerator 提示词状态改机器 id）；发现 2 真 bug + 1 UI 风险并修复。
- **修复 ①（既有 bug）`discardFile` 对已暂存新增文件必失败**：`reset HEAD` 后 `checkout -- <file>` 对不在 HEAD 的文件必报 pathspec 错误 ⇒ 整体抛错、文件残留未跟踪。修法：staged 分支先 `isFileInHead` 分流——新增类（A/AM/AD）reset 后走 `clean -f`，修改/删除类维持 reset+checkout（原行为不变）。
- **修复 ②（本次修复的边角）`git rm --cached` 场景差异不可见**：该命令后 porcelain 出两行同路径（`D ` 已暂存删除 + `??` 未跟踪），未跟踪条目 `git diff` 为空但文件仍在 HEAD ⇒ 旧判据 `isFileInHead` 把兜底挡掉。修法：**兜底判据按比较基线区分**——staged=false 用新私有 `isFileInIndex`（`git ls-files --error-unmatch`，不命中退出码非 0）；staged=true 维持 `isFileInHead`。常态仍只在 diff 为空时多跑 1 次子进程。
- **修复 ③（UI 风险）弹窗头部窄窗口裁剪**：操作区约 370px，独立浮动窗口 90vw < ~420px 时按钮会被 `overflow: hidden` 裁掉 ⇒ `.wt-diff-header { flex-wrap: wrap }` + `.wt-diff-header-actions { margin-left: auto }`（`.wt-diff-header` 被 CommitFileDiffDialog 共享，wrap 对两弹窗均为良性）。
- ⚠️ **过程教训**：给既有私有方法补新方法时，先读原方法当前落点——本次把新 `isFileInHead` 追加在新 `getFileDiff` 之后，与文件后方的原定义重复（TS2393），靠 typecheck + read_lints 兜住后删除旧定义。通用规则：**编辑前先读目标方法周边，勿凭记忆假设落点**。
- 验证：`pnpm typecheck` 0 error、`read_lints` 0、`WorktreeOps.ts` 485 行（<500）、`isFileInHead`/`isFileInIndex` 各 1 处定义；逻辑自查表 ①A 丢弃 reset+clean ②M 丢弃 reset+checkout 同旧 ③未跟踪整文件新增 ④rm --cached 未跟踪整文件新增 ⑤tracked 真无差异空态。

## gitPush/ListView 控件规则审查 + 全量整改（报告 + 16 文件迁移）

- **产出**：`docs/gitPush-listview-controls-review.md`（违规清单含行号证据 + 例外登记表 + 假阳性排除 + 整改记录 §七）。
- **共享库新增能力（先落地后消费）**：`Button` 新增 `dense?: boolean`（`si-button--dense`，**仅与 `size="xsmall"` 协同**：`min-height:0` / padding 2px 5px / gap 3px / 圆角 4px / 纯图标 20px；独立使用无效果）；`ToggleButton` 同步透传 `dense`（紧凑开关需要）；`COMMON_ICONS` 补登 23 个 IconKey（validate:icons 232 个全过）；`AGENTS.md`（Button 行 + 复用清单）/ `AGENTS_STYLE.md`（按钮尺寸章）/ `componentPreview/README.md`（新增「紧凑按钮修饰」条目）+ previewData/button.ts 2 条示例。
- **迁移口径**：`vp-btn--ghost|primary|danger --sm` → `<Button variant="…" size="xsmall" dense>`；`gp-btn-danger` 悬停变红改由 `.git-push-panel .gp-btn-danger.si-button:hover:not(:disabled)`（(0,4,0)+）承担，**删掉 Shared.scss 里的 `!important` 思路**。
- **CardTabs 重构为 Tabs 五件套外壳**：`TabPanel` 改**具名插槽**（`#worktree/#log/#stash/#tag`）由 `ProjectCard` 下发 ⇒ 获得**真实 tabpanel 语义**（aria-controls 不再悬空），`lazy` 保持「未激活面板不挂载」（等价原 `v-if`）；`ProjectCard` 四个 `v-if` 面板收进插槽，事件契约零改动。
- **两个弹窗迁 `Dialog`**（`dismissableMask` 显式开、`closable=false` 保留自有关闭钮、**header 插槽必须把作用域 `headerId` 打到自己的标题元素上**否则 `aria-labelledby` 悬空）；`WorkingTreeDiffDialog` 的 ←/→ 保留捕获阶段 `stopImmediatePropagation`，**Esc 交还 Dialog 内建**（删掉自建 keydown）；遮罩 / z-index / 过渡全部随自绘 overlay 删除而消失（backdrop-filter 与 z-index:9999 一并清零）。
- **样式特异性策略（可复用）**：覆写共享组件的 feature 规则统一抬 (0,3,0)～(0,4,0)（`.git-push-panel` / 直接容器类作前缀），**优先删 `!important`**；同特异性 (0,3,0) 会与 `--dense.si-button--xsmall[data-v]` 打平 ⇒ 涉及 padding/圆角的覆写必须到 (0,4,0)。
- **i18n**：新增 5 键（`cardTabChanges/Log/Stash/Tag` + `commitLogCountLabel`），merge 后 4499 叶键对齐；`CardTabs` 的 CHANGES/LOG/STASH/TAG 与 `BranchCommitList` 的硬编码中文 title 清零。
- **验证**：`pnpm typecheck` 0 error、`read_lints` 0（ListView 全目录 + 共享库 + styles）、`i18n:verify` 通过、`validate:icons` 通过；ListView 内 `vp-btn` 0 处、裸 select/input/textarea 0 处、裸 `<button>` 仅剩 18 处（全部在例外登记的下拉浮层与行内确认内）。
- **范围外残留（登记未动）**：`Buttons.scss` 的 0.15s 过渡与 `.gp-toggle-chip`（使用方在 ListView 之外）；`.gp-caret-icon` 仍被 CardHeaderActions 双图标触发钮使用（Button `__text` 已有 4px gap，多 1px 属可接受偏差）。
- **⚠️ 教训**：删掉 `import { Icon } from "@iconify/vue"` 前必须全模板搜 `<Icon`——三个文件（ConflictSection/StashSection/TagPanel）模板里仍有展示图标，删导入后 `read_lints` 与首次 `pnpm typecheck` 都没报（组件名落进全局 shim），靠 grep 才发现；**删导入后必须再跑一次 typecheck 并 grep 模板标签**。

### 用户反馈修复：卡片分类选择框「外框套内框」

- **现象**：用户报「卡片里面的分类选择框被包了」。根因 = **原生 `<select>` 时代的 `.gp-cat-select` 样式在迁移后落到了共享 `Select` 的根元素上**：`border/padding/max-width:60px/opacity` 加在 `.si-select` 根，而可视外壳是内部的 `.si-select__trigger`（自带 1px 边框 + `background: var(--b3-theme-background)` **实底**）⇒ 出现「外框套内框」+ 卡片 surface 底上多出一块白底方块。
- **修法**：外观全部改写到 trigger 上（`.git-push-panel .gp-cat-select.si-select--xsmall .si-select__trigger`，(0,4,0) 才压得住 `--xsmall` 档的 `min-height/padding`，hover 描边需 (0,6,0) 压组件 scoped 的 hover 取色）；根只留 `max-width: 60px`。
- **连带发现**：根宽被限死后，下拉面板沿用 `width: 100%` 会被裁字 ⇒ `.gp-cat-select`（min-width 140px）与 `.bcl-count-select`（76px）的下拉都补 `width: auto + min-width`。
- **可复用规则**：**迁移到共享组件时，旧的原生元素样式必须整体「下沉」到组件内的可视元素上**（`Select`→`.si-select__trigger`、`Input`→`.si-input__wrapper`、`Textarea`→`.si-textarea__field`、`Tag`→`.si-tag`）；留在根上的 `border/background/padding` 会与组件自身外壳叠加成双层。判据：类名挂在组件根时，凡「本来就是画在这个元素上的框」都要重新落位。

### 用户反馈修复②：点生成/提交时右侧控件抖动

- **确认的真 bug**：`.wt-output-close` 原 `position: absolute` 被共享 `Button` 根类的 `position: relative`（scoped (0,2,0)）压过（我的单类只有 (0,1,0)）⇒ 关闭按钮退回正常流、撑高输出框。修法：`.git-push-panel .wt-commit-output .wt-output-close.si-button` 抬到 (0,4,0)。**这是「单类选择器覆写共享组件根属性」的第二例，与 `.gp-cat-select` 同源**。
- **抖动机制（已按此加固）**：卡片内多个 `overflow-y: auto` 容器（`.wt-body` 420px / `.wt-files` 200px / `.bcl-list` 160px / `.gp-stash-list` / `.gp-tag-list`）在内容增减时就地切换滚动条 ⇒ 内容宽度来回变化 ⇒ 右侧控件（尤其分类下拉框）被挤压/回弹。修法三处：① 上述滚动容器统一加 `scrollbar-gutter: stable`（槽位常驻，Chromium 94+ 支持）；② `.gp-card-actions > * { flex-shrink: 0 }`（顶栏控件一律不参与收缩）；③ `.wt-summary-actions { flex-shrink: 0 }`。
- **审计结论**：把 ListView 内所有共享组件根上的覆写按特异性过了一遍——只有 `.gp-cat-select` 与 `.wt-output-close` 两处低于 (0,3,0)；其余（`.si-tag`/`.si-divider`/`.si-select`/`.si-input`/`.si-textarea`/`.si-panel`/`.si-dialog`/`.si-button` 系列）都已在 (0,3,0)～(0,6,0)。**规则：凡覆写组件根自身声明的属性（position/display/flex/gap/padding/border/background/color/transition），一律 ≥ (0,3,0)；只设组件未声明的属性（margin/flex-shrink/opacity）才可用单类。**

### 用户反馈调整③：提交表单「类型按钮 + 模板下拉」合并同一行

- 用户反馈「选择模板和上面按键好像是一样的，合并起来」——迁移后模板下拉与类型按钮都是「细描边小圆角框」，分成两行像重复控件。
- 做法：删掉 `.wt-template-row` 容器，把 `<Icon mdi:file-document-outline>` + `Select` 并入 `.wt-commit-types`（`flex-wrap: wrap; align-items: center`）；图标给 `margin-left: auto` 把「图标 + 下拉」整组推到行尾 ⇒ **放得下同一行、放不下整组换行到第二行右端**（不需要媒体查询）。
- 下拉面板必须**右对齐触发钮并向左生长**（`left: auto; right: 0; width: auto; min-width: 100%; max-width: 220px`）：控件贴行尾，右向展开会被 `.git-push-panel { overflow: hidden }` 裁掉。**通则：贴面板右缘的 Select 一律让 dropdown 反向生长。**

### 用户反馈调整④：移除模板下拉，类型按钮改中文标签

- 需求：「移除下拉选择框，把中文合到那些按键中，点击赋值时是标准单词（fix）」。
- 做法：删掉提交表单里的模板 `Select`（连同引导图标、`selectedTemplateId` / `templateOptions` / `handleTemplateChange` / `handleSelectTemplate`、`commitTemplates` prop、`Select`/`Icon` 导入，`ProjectCard` 侧同步摘掉 `:commit-templates` 与 `const commitTemplates`）；类型按钮改为显示中文。
- **中文标签零新增 i18n 键**：直接复用已有的 `COMMIT_ANALYSIS_TYPE_META[ct].labelKey`（`commitTypeFeat/Fix/Chore/Docs/Style/Refactor/Test` 早已存在），视图层函数 `commitTypeLabel(ct)` 解析、缺失回退 `ct` 本身。**`commitType` 的值与 `updateCommitMessage()` 拼出的前缀始终是标准单词（`fix: …`），中文只影响按钮显示**；按钮加 `:title="ct"` 让悬停可见标准单词。
- **遗留（已告知用户，未擅自清理）**：`commitTemplates` 现在**没有任何 UI 消费方**了（数据源仍在：`manager.getCommitTemplates` / `CommitMsgGenerator` 的 storage 槽位仍被 AI 生成逻辑使用），但 `useGitTagsConflicts.commitTemplates` + `loadCommitTemplates()`（`index.vue:750`）+ `index.vue:415/993` + `types/cardServices.ts:28` 已成死管线 —— 属 ListView 之外，需用户确认后再清。

### 用户反馈修复③：类型按钮「点击没有赋值到窗体」

- **根因**：`updateCommitMessage()` 原本在 `commitMessage` 为空时**故意 return**（旧注释「如果为空，不自动填充（等用户点生成）」）⇒ 空框点类型按钮毫无反应。模板下拉移除后这个按钮成了唯一快捷入口，行为立刻暴露。
- **新语义**：始终写入 / 替换前缀 —— 空输入 → `fix: `；已有前缀 → 只换类型单词（正则 `^([A-Za-z]+)((?:\([^)]*\))?!?):\s*` **保留 scope 与 `!`**，`feat(ui)!: x` → `fix(ui)!: x`）；有正文无前缀 → 补前缀。写完后 `await nextTick()` + `textareaEl.focus()` 把焦点交回输入框。
- **连带修的实时校验噪音**：新增 `INCOMPLETE_COMMIT_RE = /^(?:[A-Za-z]+(?:\([^)]*\))?!?:\s*)?$/` 判定「尚未填写完成」（空串 / 只有前缀）⇒ ① `validationReason` 在该状态下返回 null（否则刚点按钮就闪「首尾空白」/「缺少类型」红字）；② 提交按钮改由 `isCommitIncomplete` 兜底（否则仅前缀也能提交）；③ `checkCommitRule` 改吃 `trim()` 后的文本（提交时本就 trim，消除「敲一个空格就闪违规」）。顺带修掉旧缺陷：**空框曾恒定显示「缺少类型前缀」**。
- **可复用结论**：**「输入为空时静默不动作」在只有一个入口时是不可接受的行为**——入口收敛后必须让点击总有可观察结果；同时要给出「未填写完成」这个第三态，避免把「还没写」当成「违规」。

## gitPush 工作区面板：MM 状态可见性修复

**用户报障**：暂存 1 个文件后再修改它，面板不显示「未暂存 1」；其他 git 软件会同时显示已暂存 1 + 未暂存 1。

**审查结论（不是解析 bug）**：`parseWorktreeStatus` 对 porcelain `MM`/`AM` 本就是双计数（X 位与 Y 位各自 `!== " " && !== "?"`），刷新后摘要条会显示 `●1 ●1`。真因是**无自动刷新**——工作区状态只在 4 个时机刷新（卡片首次点击 / Tab 切回 worktree / 手动刷新按钮 / 面板内 git 操作后），外部编辑器改完切回来仍是旧快照。数据安全无风险（commit 只提交 index 快照；discard 传陈旧 staged/status 最坏是「以为丢了实际没丢」或 pathspec 报错抛出），风险在**认知误导**：用户以为全部改动都会进提交。

**修复（3 文件）**：
1. `WorkingTreePanel.vue` 自动刷新：`window focus`（从编辑器/其他应用切回）+ 根元素 `@pointerdown`（双窗口并列不切焦点）共用 `handleAutoRefreshTrigger`，防抖 800ms；`watch(refreshingWorkingTree)` 记录 `lastRefreshStartedAt`，2s 内不重复起子进程；`gitOpLoading || refreshingWorkingTree` 期间跳过；`TimerRegistry` 托管定时器、`onUnmounted` 移除 window 监听。**未采用 plan 里的 panel focusin**——Electron 下切回窗口时 DOM 焦点未变，点非聚焦区域也不触发 focusin，不可靠。
2. MM 行级标记：`file.staged && file.unstaged` 时在路径后渲染 `.wt-partial-mark`（6px 圆点，`--b3-theme-warning` 实心 + `--b3-theme-primary` 描边，用 `$s-px6`/`$s-px1`/`$r-full` Token），title = 新 i18n 键 `stagedAndUnstagedTip`。
3. MM 行不弱化：`.staged { opacity: 0.5 }` 会把标记一起压暗，且「已暂存 = 已完成」的弱化语义对 MM 文件是误导 ⇒ 新增 `.staged.partially-staged { opacity: 1 }`（(0,3,0) 压过 (0,2,0)）。

**验证**：read_lints 0、`pnpm typecheck` 0、`pnpm i18n:merge` + `i18n:verify` 4500 leaf keys 对齐。

**顺带**：MEMORY.md 从 11422 字符压到 ~6700（原体积超限被截断），§1–4 防回归要点保留，§5–9 精简。
