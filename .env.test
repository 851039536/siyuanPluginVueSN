# Siyuan workspace path for test env
# Loaded by `pnpm dev:test` (vite build --watch --mode test)
# Keys defined here override `.env`; undefined keys inherit from `.env`
# The workspace contains `data` folder
VITE_SIYUAN_WORKSPACE_PATH=E:/siyuan_test
