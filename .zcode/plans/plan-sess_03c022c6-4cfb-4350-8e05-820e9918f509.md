# aiContentGenerator 冗余重复修复计划

模块：`src/features/aiContentGenerator/`（35 文件 / 5673 行）。审查发现的硬违规 4 处 + 模块内冗余 7 处 + i18n 冗余，修复范围仅限本模块（跨模块共享化项仅记录，不改动其他 feature）。

## A. 规则硬违规修复

1. **剪贴板统一入口**：`composables/useEditOperations.ts:74` 的 `navigator.clipboard.writeText` 改为 `copyToClipboard`（`@/utils/domUtils`），保留原 try/catch 与失败提示。
2. **定时器统一入口**：`index.vue:392-393` 防抖的裸 `window.setTimeout`/`clearTimeout` 改走 `TimerRegistry`（`@/utils/timerRegistry`，句柄类型 `TimerHandle`），组件 `onUnmounted` 时 `clear`；实现时按该文件实际 API（单例/实例化）选用。
3. **代码高亮统一通道**：删除 `index.vue:83-84` 的 `import hljs` 与 `highlight.js/styles/github.css`、删除 `applyCodeHighlighting`（:236-249）及其 watch；改在 `utils.ts` 的 `renderMarkdown` 调 `parseMarkdown` 时传 `codeHighlight: true`（共享内联样式通道，同时消除与 `main.ts` 全局 github-dark.css 的双主题冲突）。保留 XSS 转义扩展不动。
4. **文件头注释**：`components/DiffPreview.vue:1` 补简要功能说明注释。

## B. 模块内冗余消除

5. **评级枚举收敛**：`types/index.ts` 新增 `RATING_VALUES: readonly ReviewRating[] = ["优秀","良好","需改进"]`；`modules/AIContentGenerator.ts:61` `validateRating` 改用它；`:302` 硬编码 `"需改进"` 改用已有 `RATING_NEEDS_FIX`；审核 prompt 中 `"优秀|良好|需改进"`、`"高|中|低"`（:251/:253）改为由 `RATING_VALUES`/`SEVERITY_LEVELS` join 生成。`ReviewPanel.vue` 的 `RATING_CLASS_MAP` 是评级→样式映射（职责不同），保留。
6. **EditActionKey 三表合一**：`types/index.ts` 新增 `ACTION_META: Record<EditActionKey, { label; icon; prompt; reviewLabel }>`，合并 `BottomInputArea.vue:230-237` quickActions 与 `index.vue:253-270` actionPrompts/ACTION_REVIEW_LABELS（文案原样迁移，不做 i18n 改造）；两处消费方改为读表。
7. **删重复 escapeHtml**：`utils.ts:62-69` 本地 `escapeHtmlText` 删除，改 `import { escapeHtml } from "@/utils/stringUtils"`（已确认无循环依赖；`&#x27;` 与 `&#039;` 行为等价）。
8. **deepseek 魔法串收敛**：`AIContentGenerator.ts:266` 等处与 `useGeneration.ts:99` 的 `"deepseek-v4-pro"`/前缀判断，改引 `@/config/aiModels.ts:31` 已有权威定义（按其导出形态引用）。
9. **设置加载合规**：`index.vue:374` 的 `load()` + null 跳过改为 `loadOrDefault()`（`TypedStorage` 已有 API），默认值来源唯一化为 `DEFAULT_AI_SETTINGS`；save/load 的字段组装系 Vue 响应式所需，保留。
10. **样式超限拆分**：`styles/index.scss` 558 行超 500 硬阈值——按类名归属拆出独立 partial（如 `styles/_sections.scss`，仅 `_` 前缀文件可用 partial 名）或组件专属 scss，类名不变；`ReviewPanel.scss` 389 行若有自然拆分边界则顺带，否则仅记录（超警戒线非硬阈值）；删除 1 行空壳 `styles/ContentAreaEmpty.scss` 并移除 `ContentAreaEmpty.vue` 中对应 `@use`。`components/styles/` 与 `styles/` 双目录并存列为记录项不强制归并。

## C. i18n（已确认纳入）

11. **死键清理**：对探明的 58 个零引用键逐键 grep 双查（字面引用 + `i18n[...]` 动态值引用模式，如 `sevKeyMap`）后，从 `src/i18n/{zh_CN,en_US}/aiContentGenerator.json` 同步删除；跑 `pnpm i18n:merge` 重新生成大文件 + `pnpm i18n:verify` 校验对齐（二者非 lint/build，允许执行）。
12. **Dock 标题走 i18n**：分片新增 `dockTitle`（zh: "AI信息生成" / en 对应现有英文名），`AIContentGenerator.ts:101` 改读 `this.plugin.i18n.aiContentGenerator.dockTitle`。

## D. 仅记录不改（跨模块共享化，待后续单独处理）

- `extractJsonFromText`（本模块健壮版）与 wordQuery `parseJsonResponse`（正则版）同职责，可上移 `@/utils`。
- 流式消费样板（AbortController+disposed+onChunk）在 useGeneration / gitPush AiErrorAnalysisDialog / ideaGenerator useIdeaDetail 三处，达 Rule of Three 可提取共享 `useAiStream`。
- 暗色主题探测两份（`DiffPreview.vue` vs `themeColor.isDarkMode()`）。
- `SkillSection` 自写带搜索下拉、`BottomInputArea` 裸 `<select>`×2 与自写 toggle×3 未用共享 Select/Switch（属重复造轮子候选，改造有 UI 回归风险，本轮不动）。

## 验证分工

- 我执行：i18n:merge、i18n:verify、`npx tsc --noEmit`（类型层面自检）。
- 用户执行：`pnpm lint`、`pnpm build`、以及 Dock 面板人工回归（重点：代码高亮样式是否正常、快捷动作六按钮、审核面板、复制/插入文档功能）。

预期效果：消除全部统一入口违规；模块内枚举/元数据单源化（types/index.ts 增约 40 行，index.vue 减约 40 行并降到 ~400 行）；i18n 分片瘦身约 55%；styles/index.scss 降到 500 行以下。