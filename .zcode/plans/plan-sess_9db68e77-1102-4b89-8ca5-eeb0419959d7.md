深度审查与重构 diskBrowser 和 apiDebugger 的 TS/JS 逻辑（严格保证业务不变）：

### apiDebugger
1. `useApiDebugger.ts`：
   - 修复 `syntaxHighlight` 的 XSS 漏洞：当前未匹配正则的文本未转义，`v-html` 会执行；改为逐段构建，所有文本均 escapeHtml。
   - `sendRequest` 错误路径提取 errMsg，消除重复调用 getErrorMessage。
   - 删除从未在模板使用的 `selectedEndpoint` 死状态。
   - 文件头注释移至文件顶部。

2. `index.vue`：
   - 移除两处内联 style，提取到 SCSS 类。
   - 移除所有 i18n 硬编码兜底（符合 AGENTS.md）。
   - 用模块级 Map 缓存预设端点，`handleEndpointChange` O(1)；`methodOptions` 从 `HTTP_METHODS` 派生；删除冗余 `endpointSelectValue`。
   - 添加文件头注释。

3. `types/index.ts` / `types/storage.ts` / `index.ts`：补充文件头注释。

4. i18n 分片：补 `copyFailed` 中英 key；`pnpm i18n:merge` 重新生成大文件。

### diskBrowser
1. `useDiskBrowser.ts`：
   - 文件头注释移至文件顶部。
   - 移除所有 i18n 硬编码兜底。
   - `toggleFavorite` 改为 await 保存，失败回滚 UI 并提示。
   - 异步 fire-and-forget 调用加 `void` 明确语义。
2. `utils/index.ts`：
   - `formatSize` 增加单位越界保护。
   - `buildPath` 删除死代码 replace。
   - `formatDate` 提取 formatYmd 消除重复日期格式化代码。
3. 组件模板移除 i18n 硬编码兜底。
4. i18n 分片补齐 `totalSpace`/`usedSpace`/`freeSpace`/`disks`/`clickToBrowse`/`noFavorites` 中英 key（之前只改了大文件，现修正为改分片 + merge）。
5. `types/index.ts` / `types/storage.ts` / `index.ts` 补充文件头注释。

验证：`npx eslint`、`npx vue-tsc` 过滤相关文件、`pnpm i18n:verify`、`pnpm i18n:merge`。