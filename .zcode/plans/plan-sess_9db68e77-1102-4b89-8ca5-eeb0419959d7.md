优化 diskBrowser 首页（未选中磁盘时的视图）UI：

1. `index.vue` 首页模板重构：
   - 头部增加磁盘总数与总容量概览（如「2 个磁盘 · 总计 476 GB」）。
   - 磁盘卡片增加使用率数字、按使用率分级变色的容量条（<60% 主题色，60–85% 琥珀，>85% 红色），并显示已用/总容量与剩余空间。
   - 收藏夹为空时显示「暂无收藏」引导空状态；有收藏时保持现有列表。
   - 无磁盘时显示空状态提示。

2. `types/index.ts`：
   - `DiskBrowserI18n` 增加 `totalSpace`、`usedSpace`、`freeSpace`、`disks`、`clickToBrowse`、`noFavorites` 可选字段。

3. `styles/index.scss`：
   - 优化首页头部、磁盘卡片 hover 反馈、容量条颜色分级、总览块、空状态样式。

4. i18n 文件（zh_CN.json / en_US.json）：
   - 为上述新增键补充中英文案。