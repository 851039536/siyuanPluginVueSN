优化 diskBrowser 其他页面 UI，并审查/优化冗余与代码逻辑：

1. `Sidebar.vue`：
   - 磁盘行改为「图标 + 盘符/卷标 + 容量信息」双行结构，显示使用率、已用/总容量，未激活时显示右侧箭头。
   - 收藏夹增加空状态。
   - 引入 `formatSize`。

2. `FolderList.vue`：
   - 地址栏下方列标题改用 CSS grid 与列表项对齐；操作列宽度匹配。
   - 空状态和 loading 保持不变，但调整空状态文案层级。
   - 状态栏显示当前路径和缓存状态更清晰。

3. `FolderListItem.vue`：
   - 操作按钮从 opacity 0/1 改为更平滑的 hover 显示，并加 `title`。
   - 三列布局与列标题对齐（grid）。
   - 收藏按钮只对文件夹显示，打开/复制对所有项显示，浏览仅文件夹。

4. `AddressBar.vue`：
   - 面包屑超出时保持横向滚动，添加 root 图标。
   - 工具栏保持紧凑。

5. `styles/*.scss`：
   - 同步 grid 布局、hover 过渡、箭头、空状态等样式。

6. 逻辑审查：
   - `useDiskBrowser.ts`：`toggleDisk` 切换磁盘时清空当前路径/文件夹状态已可，但增加守卫；`navigateToRoot`/`navigateBack`/`navigateToPath` 统一使用 `setCurrentPath` 减少重复。
   - `utils/index.ts`：`readDirectoryContents` 对每个文件 `statSync` 导致性能问题，改为 `withFileTypes` + 仅在需要时 stat；`getDiskInfo` 保持。
   - 修复 `formatDate` 中未来日期误判为「今天」等边界问题。
   - 消除重复的 `buildPath` 逻辑（保留单一实现）。

7. 保持所有 ESLint 与类型检查通过。