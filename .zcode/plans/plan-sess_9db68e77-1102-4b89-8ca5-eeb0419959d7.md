修复 apiDebugger 功能模块的代码问题：

1. `useApiDebugger.ts`：
   - 修复 `clearRequest` 中 `Object.assign(ref, value)` 错误，改为直接赋值 `.value`。
   - 引入 `isValidHttpMethod` 校验历史记录，过滤脏数据。
   - 让 `ApiDebuggerStorage.addRecord` 返回新历史数组，避免重复加载存储。
   - 调整 `createRecord`：`id` 使用模块级自增计数避免碰撞；移除未使用的 `url` 字段。
   - 删除 `loadHistory` 死代码。
   - 在 `onMounted` 异步加载中增加 disposed 保护。
   - `copyToClipboard` 保留工具函数导入，简化直接转发。

2. `types/index.ts`：
   - 增加 `HTTP_METHODS` 常量、`HttpMethod` 派生类型和 `isValidHttpMethod` 类型守卫。

3. `types/storage.ts`：
   - `addRecord` 返回 `Promise<ApiRequestRecord[]>`（裁剪后的新历史）。
   - 删除未使用的 `loadHistory`。

4. `index.vue`：
   - 修复 XSS：在 `syntaxHighlight` 中对匹配片段先做 HTML 转义。
   - `handleCopyResponse` 使用 `showMessage` 反馈成功/失败。
   - 模板中历史记录 method 类名使用安全方式（历史已过滤）。
   - 删除未使用的 i18n 相关死代码引用（保留 i18n 文件不动，仅清理 JS 中死导入/包装）。

5. `styles/index.scss`：
   - 删除未使用的 `&__copy-btn` 规则。

6. 清理死代码：删除 `composables` 中无意义的 `copyToClipboard` 包装，Vue 组件直接使用 `copyToClipboard` 工具函数。